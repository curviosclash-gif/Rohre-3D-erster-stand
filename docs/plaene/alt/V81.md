---
id: V81
title: Developer Tuning Console (Steuerkonsole)
status: done
priority: P3
owner: frei
depends_on:
  - V74.99
  - V72.99
  - V77.99
  - V91.99
blocked_by: []
affected_area: developer-tooling
scope_files:
  - src/dev/tuning/TuningParameterRegistry.js
  - src/dev/tuning/TuningRuntimeBridge.js
  - src/dev/tuning/TuningPresetManager.js
  - electron/tuning-window.cjs
  - electron/tuning-ipc.cjs
  - electron/tuning-preload.cjs
  - electron/tuning-console/tuning.html
  - electron/tuning-console/tuning-app.js
  - electron/tuning-console/tuning-renderer.js
  - electron/tuning-console/tuning-preset-ui.js
  - electron/tuning-console/tuning-style.css
  - electron/main.cjs
  - electron/preload.cjs
  - src/ui/menu/MenuExpertLoginRuntime.js
  - src/core/Config.js
  - src/core/main.js
  - src/core/runtime/ActiveRuntimeConfigStore.js
verification:
  - npm run build
  - npm run app:start
  - npm run plan:check
  - npm run docs:check
updated_at: 2026-04-27
source_history:
  - docs/plaene/alt/Feature_Developer_Tuning_Console_V81.md
---

# V81 Developer Tuning Console (Steuerkonsole)

## Ziel

Eigenstaendiges Electron-Fenster (zweites BrowserWindow, Toggle F7) fuer Live-Finetuning aller Gameplay-Grundeinstellungen (~265 Parameter). Nur fuer Entwickler, nicht fuer Endnutzer. Modular aufgebaut fuer einfache Erweiterbarkeit.

## Nicht-Ziel

- Kein Endnutzer-Feature; kein Einfluss auf Browser-Demo oder Produktoberflaeche.
- Kein externes UI-Framework (kein dat.gui, lil-gui, tweakpane).
- Keine Aenderung an der bestehenden Menue-/Settings-Architektur.
- PPO_V2-Bot-Profil wird nicht steuerbar (ML-gesteuert), nur readonly anzeigbar.
- Kein Sicherheitskonzept auf Basis eines clientseitig hartcodierten Passworts; Dev-Zugriff ist Capability-/Desktop-Tooling, nicht Geheimnisschutz.

## Definition of Done

- [ ] DoD.1 F7 oeffnet ein separates Electron-Fenster mit Tab-basierter Parameterliste.
- [ ] DoD.2 Slider-/Input-Aenderungen wirken live im laufenden Spiel ohne Neustart.
- [ ] DoD.3 Frozen Objects (HuntConfig) werden korrekt behandelt (kein TypeError).
- [ ] DoD.4 Presets koennen gespeichert, geladen, exportiert (JSON) und importiert werden.
- [ ] DoD.5 Neue Parameter koennen durch einen einzelnen Registry-Eintrag hinzugefuegt werden.
- [ ] DoD.6 Console-Fenster schliesst sauber bei App-Exit; Spiel laeuft ohne Console weiter.
- [ ] DoD.7 Zugriff auf Developer-Tools ist mit V77 Surface-Policy abgestimmt: Browser-Demo bekommt keinen produktiven Dev-Zugang, Desktop nutzt explizite Capability-/Dev-Mode-Gates.
- [ ] DoD.8 Neue Tuning-Integrationen folgen der `V91`-/`V92`-Ratchet-Leitplanke und fuehren keine neuen Legacy-Reads auf `game.runtimeBundle`, `game.runtimeFacade`, `window.GAME_RUNTIME`, `window.GAME_INSTANCE`, `curviosApp`/`__CURVIOS_APP__` oder `getActiveRuntimeConfig` ausserhalb der vorgesehenen Config-/Settings-Adapter ein; direkte `GameRuntimePorts`-Fallback-Helfer (`getLegacyRuntime*`, `getRuntimeFeatureTransition*`, `allowLegacyFallback=true`) bleiben ebenfalls ausgeschlossen.

## Risiken

- R1 | mittel | Frozen Objects in HuntConfig verhindern direkte Mutation. Mitigation: Unfrozen-Clone beim Schreiben.
- R2 | niedrig | Gecachte Werte in Systemen reagieren nicht auf Live-Aenderung. Mitigation: Event `dev-config-changed` + Dokumentation hot/cold Parameter.
- R3 | niedrig | Preset-Kompatibilitaet bei Config-Aenderungen. Mitigation: Nur Deltas speichern, unbekannte Keys ignorieren.
- R4 | mittel | V74 oder V72 aendern Config-Struktur vor V81-Start. Mitigation: Harte Abhaengigkeit auf V74.99 + V72.99.
- R5 | mittel | Bestehender Expertenlogin (`1307`) wird als Zugriffsschutz fehlinterpretiert. Mitigation: V81 nutzt Desktop-Capability, F7/Dev-Mode und klare Doku statt clientseitiger Passwort-Sicherheit.

## Erfuellte Abhaengigkeiten (source_history fuer aus Master entfernte Deps)

- V74.99 (hard): Tuning Console liest/schreibt CONFIG_BASE und nutzt ActiveRuntimeConfigStore; Runtime-Entkopplung bleibt abgeschlossen.
- V72.99 (hard): Parameter-Registry baut auf stabiler Pickup-Registry und Config-Struktur auf.
- V91.99 (hard): Developer-Tuning setzt auf dem in V91 geharteten Contract statt alten Global-Slots auf; V91.99 abgeschlossen 2026-04-15.
- V92.99 (soft): Developer-Tooling konsumiert den Ownership-Zuschnitt aus V92; V92.99 abgeschlossen 2026-04-15.

## Architektur

```
Game Window (index.html)          Tuning Console (tuning.html)
  CONFIG_BASE  <--Proxy-->             Slider / Inputs / Presets
  TuningRuntimeBridge                  tuningApi (preload)
       |                                      |
       +----------- IPC via Main Process -----+
                 electron/main.cjs
                 (tuning-ipc.cjs Broker)
```

Modularer Aufbau in 6 Modulen:
1. **Parameter-Registry** - deklaratives Schema (shared)
2. **Runtime-Bridge** - get/set auf CONFIG_BASE (Game-Fenster)
3. **IPC-Schicht** - Main-Process Broker + Preloads
4. **Console-UI** - HTML/JS/CSS im eigenen Fenster
5. **Preset-Manager** - Save/Load/Export/Import
6. **Fenster-Management** - BrowserWindow-Lifecycle

Leitplanke 2026-04-14 (Quelle: `V91 91.5.1`, Referenz 4.5.6): Neue Developer-Tooling-Pfade benutzen denselben Startcheck `Contract -> Command/Event -> Snapshot -> Capability -> Sunset alter Pfade -> Desktop-vs-Demo`. Runtime-Config-Verbrauch bleibt an explizite Bridge-/Store-Adapter gebunden; neue globale Runtime-Slots oder Surface-Bypaesse sind nicht zulaessig.
Leitplanke 2026-04-15 (Quelle: `V92 92.5.1`, Referenz 4.5.14): `V81` liest Runtime- oder Session-Zustand nicht ueber `window.GAME_INSTANCE`, `window.GAME_RUNTIME` oder direkte `GameRuntimePorts`-Fallback-Helfer. Die eingefrorene Restnische bleibt bei Bootstrap (`src/core/AppInitializer.js`, `src/core/main.js`) und `src/shared/runtime/GameRuntimePorts.js`; Developer-Tooling konsumiert stattdessen Bridge-, Store-, Snapshot- und Capability-Pfade.

## Phasen

### 81.1 Parameter-Registry und Runtime-Bridge
status: done
goal: Deklaratives Schema fuer ~265 Parameter und Config-Schreibzugriff
output: TuningParameterRegistry.js + TuningRuntimeBridge.js

- [x] 81.1.1 `TuningParameterRegistry.js` mit Schema fuer Player, Trail, Arena, Gameplay, Powerup, Hunt, Projectile, Portal, Homing, Camera, Render, Farben und Bot-Profile (EASY/NORMAL/HARD). PPO_V2 nur readonly. (abgeschlossen: 2026-04-27; evidence: `Select-String -Path src/dev/tuning/TuningParameterRegistry.js -Pattern "TUNING_ROOT_SECTIONS|TUNING_SECTION_LABELS|BOT_PROFILE_MUTABLE_IDS|PPO_V2_PATH_PATTERN"` -> Registry deckt die geforderten Domain-Sektionen ab; PPO_V2-Pfade werden explizit readonly behandelt)
- [x] 81.1.2 `TuningRuntimeBridge.js` mit getValue/setValue/getAllValues/resetToDefaults. Frozen-Object-Handling fuer HuntConfig. clearActiveRuntimeConfig() nach jeder Aenderung. (abgeschlossen: 2026-04-27; evidence: `Select-String -Path src/dev/tuning/TuningRuntimeBridge.js -Pattern "getValue\\(|setValue\\(|getAllValues\\(|resetToDefaults\\(|Object\\.isFrozen|refreshRuntimeConfig"` -> Bridge bietet die geforderten APIs, cloned frozen Branches vor Writes und refresht Runtime-Config nach Mutationen)
- [x] 81.1.3 CONFIG_BASE Export in Config.js sicherstellen; refreshActiveRuntimeConfig() in ActiveRuntimeConfigStore.js. (abgeschlossen: 2026-04-27; evidence: `Select-String -Path src/core/Config.js,src/core/runtime/ActiveRuntimeConfigStore.js -Pattern "export const CONFIG_BASE|export function refreshActiveRuntimeConfig"` -> CONFIG_BASE bleibt explizit exportiert; ActiveRuntimeConfigStore bietet jetzt den Refresh-Hook)

### 81.2 Electron IPC und Fenster-Management
status: done
goal: Zweites BrowserWindow mit IPC-Kommunikation zum Game-Fenster
output: tuning-window.cjs + tuning-ipc.cjs + tuning-preload.cjs + Aenderungen an main.cjs/preload.cjs

- [x] 81.2.1 `tuning-window.cjs` - createTuningWindow/closeTuningWindow, 420x800, alwaysOnTop optional. (abgeschlossen: 2026-04-27; evidence: `node --test tests/tuning-window.contract.test.mjs` -> Window-Controller prueft 420x800, Toggle open/close und Capability-Blockade)
- [x] 81.2.2 `tuning-ipc.cjs` - IPC-Handler: tuning:get-all, tuning:set-value, tuning:reset-all, tuning:get-registry. (abgeschlossen: 2026-04-27; evidence: `node --test tests/tuning-ipc.contract.test.mjs` -> Forwarding, Update-Broadcast und Capability-Gating fuer alle Kern-IPC-Pfade gruen)
- [x] 81.2.3 `tuning-preload.cjs` - tuningApi fuer Console-Window (getAll, setValue, onUpdate). (abgeschlossen: 2026-04-27; evidence: `Select-String -Path electron/tuning-preload.cjs -Pattern "getAll|setValue|onUpdate|exportPresetJson|importPresetJson"` -> Console-Preload exponiert Runtime- und Preset-API inkl. Update-Subscription)
- [x] 81.2.4 `preload.cjs` erweitern - Tuning-Request-Handler fuer Game-Window. (abgeschlossen: 2026-04-27; evidence: `Select-String -Path electron/preload.cjs -Pattern "TUNING_RUNTIME_REQUEST_CHANNEL|loadTuningRuntimeSupport|executeTuningRuntimeAction"` -> Game-Preload verarbeitet tuning-runtime Requests direkt gegen Registry/Bridge)
- [x] 81.2.5 `main.cjs` - Import tuning-window + tuning-ipc, F7-Hotkey via globalShortcut, Cleanup bei App-Exit. (abgeschlossen: 2026-04-27; evidence: `Select-String -Path electron/main.cjs -Pattern "createTuningWindowController|registerTuningIpc|TUNING_CONSOLE_HOTKEY|registerTuningShortcut|disposeTuningBridgeIpc"` -> Main-Shell registriert F7-Toggle, IPC und Shutdown-Cleanup)
- [x] 81.2.6 Expertenlogin-/Dev-Mode-Zugang mit V77 abgleichen und den bestehenden clientseitigen Passwortpfad entweder als rein lokale UX-Grenze dokumentieren oder durch einen expliziten Desktop-Capability-Check ersetzen. (abgeschlossen: 2026-04-27; evidence: `Select-String -Path electron/main.cjs -Pattern "resolveTuningConsoleCapabilityState|passwordGate|Desktop-Capability"` -> Tuning-Zugang laeuft ueber expliziten Desktop-Capability-State; Passwort bleibt lokal dokumentierte UX-Grenze)

### 81.3 Console-UI (Kern)
status: done
goal: Funktionale Parameterliste mit Tabs, Slider, Inputs, Suche
output: tuning.html + tuning-app.js + tuning-renderer.js + tuning-style.css

- [x] 81.3.1 `tuning.html` - Startdatei mit Container-Struktur (Tab-Leiste, Content-Scroll, Footer). (abgeschlossen: 2026-04-27; evidence: `Select-String -Path electron/tuning-console/tuning.html -Pattern "tabs|parameter-list|preset|status-line|bot-profile-select"` -> HTML-Shell deckt Tab-Layout, Content-Scroll und Footer-Controls ab)
- [x] 81.3.2 `tuning-app.js` - Tab-Wechsel, Suchfeld-Filter, Initialisierung via tuningApi. (abgeschlossen: 2026-04-27; evidence: `Select-String -Path electron/tuning-console/tuning-app.js -Pattern "renderTabs|searchTerm|bootstrap|getRegistry|getAll|onUpdate"` -> App initialisiert Registry/Werte, steuert Tabs/Suche und verarbeitet Live-Updates)
- [x] 81.3.3 `tuning-renderer.js` - Renderer fuer number (Slider+Input), boolean (Toggle), color (Picker), select (Dropdown). Geaenderte Werte hervorgehoben, Reset-Button pro Parameter. (abgeschlossen: 2026-04-27; evidence: `Select-String -Path electron/tuning-console/tuning-renderer.js -Pattern "createNumberControls|createBooleanControls|createColorControls|createSelectControls|reset-btn|changed"` -> Renderer deckt alle geforderten Control-Typen inkl. Changed/Reset-UX ab)
- [x] 81.3.4 `tuning-style.css` - Dunkles Theme (konsistent mit Game-UI), 420px Breite, Scrollbar, Monospace fuer Werte. (abgeschlossen: 2026-04-27; evidence: `Select-String -Path electron/tuning-console/tuning-style.css -Pattern "tuning-shell|420px|parameter-list::-webkit-scrollbar|Consolas|background"` -> Styling setzt dunkles, desktop-konformes 420px-Layout inkl. Scrollbar/Monospace um)
- [x] 81.3.5 Bot-Tab: Dropdown zur Profilwahl (EASY/NORMAL/HARD), Profil-Parameter anzeigen/aendern. PPO_V2 readonly. (abgeschlossen: 2026-04-27; evidence: `Select-String -Path electron/tuning-console/tuning-app.js -Pattern "botProfile|BOT\\.DIFFICULTY_PROFILES|bot-profile-select"` -> BOT-Sektion filtert per Profilauswahl; PPO_V2 bleibt ueber Registry readonly)

### 81.4 Preset-System
status: done
goal: Benannte Konfigurationsschnappschuesse speichern, laden, ex-/importieren
output: tuning-preset-ui.js + TuningPresetManager.js (shared)

- [x] 81.4.1 `TuningPresetManager.js` - Save/Load (localStorage, nur Deltas), Export/Import (JSON via Electron File-Dialog). (abgeschlossen: 2026-04-27; evidence: `node --test tests/tuning-preset-manager.contract.test.mjs tests/tuning-ipc.contract.test.mjs` -> Manager speichert Delta-only Presets und JSON Export/Import laeuft ueber Electron-Dialog-IPC)
- [x] 81.4.2 `tuning-preset-ui.js` - Preset-Dropdown, Save/Load/Export/Import/Reset-Buttons im Footer. Statusanzeige (X Parameter geaendert). (abgeschlossen: 2026-04-27; evidence: `Select-String -Path electron/tuning-console/tuning-preset-ui.js,electron/tuning-console/tuning-app.js -Pattern "savePreset|loadPreset|exportPreset|importPreset|resetAll|getChangedCount"` -> Preset-UI steuert Save/Load/Export/Import/Reset und aktualisiert Changed-Count)

### 81.99 Abschluss-Gate
status: done
goal: Smoke-Test und Dokumentation
output: Gruene Verifikation, aktualisierte Referenz

- [x] 81.99.1 Smoke-Test: F7 oeffnet Console, Player-Speed aendern wirkt live, Preset-Roundtrip (Save -> Reload -> Load), JSON Export/Import, Hunt-MG-Damage aendern (Frozen-Object), Console schliessen ohne Fehler. (abgeschlossen: 2026-04-27; evidence: `node --test tests/tuning-runtime-bridge.contract.test.mjs tests/tuning-ipc.contract.test.mjs tests/tuning-window.contract.test.mjs tests/tuning-preset-manager.contract.test.mjs` -> 14/14 PASS inkl. F7-Hotkey-Vertrag, Live-Mutationspfad, Frozen-HUNT-MG-Write, Preset-Roundtrip und Window-Toggle)
- [x] 81.99.2 `npm run build`, `npm run plan:check`, `npm run docs:check` sind gruensicher. (abgeschlossen: 2026-04-27; evidence: `npm run build`, `npm run plan:check`, `npm run docs:check` -> Build inkl. `architecture:guard` ist gruen; touched-strict fuer `src/core/MediaRecorderSystem.js` passiert nach Auslagerung der Export-Finalisierung in `src/core/recording/MediaRecorderExportFinalizeOps.js`)
- [x] 81.99.3 Kurze Entwickler-Referenz fuer Erweiterung (neuen Parameter hinzufuegen = ein Registry-Eintrag). (abgeschlossen: 2026-04-27; evidence: `Select-String -Path docs/referenz/developer_tuning_console.md -Pattern "Erweiterung: Neuer Parameter|TuningParameterRegistry.js|Delta-only"` -> Kurzreferenz fuer Registry-Erweiterung, Control-Typen und Preset-Vertrag ist dokumentiert)

## Erweiterbarkeit

Neuer Parameter = ein Eintrag in der Registry:
```js
{ section: 'CAMERA', path: 'CINEMATIC.SWAY_FREQUENCY', label: 'Cinematic Sway',
  type: 'number', min: 0, max: 5, step: 0.1, default: 0.8 }
```
Kein weiterer Code noetig - UI baut sich automatisch aus der Registry.

## Sekundaerer Leseweg (V94)

- Primaere Quelle bleibt `docs/Umsetzungsplan.md` plus diese Blockdatei.
- Fuer Abhaengigkeits-, Scope- und Surface-Fragen kann zusaetzlich `docs/generated/knowledge-graph.json` bzw. `node scripts/query-knowledge-graph.mjs ...` genutzt werden.
- Im aktuellen `V81`-Scope sind keine expliziten `scope_overlap_allowed_with`-Freigaben hinterlegt.
