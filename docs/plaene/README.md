# Plaene

Plan-Lifecycle:
1. Neue/ueberarbeitete Plaene liegen in `docs/plaene/neu/`.
2. Die manuelle Uebernahme in den Masterplan erfolgt durch den User.
3. Danach werden Plaene nach `docs/plaene/alt/` verschoben.
