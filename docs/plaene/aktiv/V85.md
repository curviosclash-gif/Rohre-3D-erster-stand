---
id: V85
title: Persistence-, Content-Contracts und Schema-Migrationen
status: active
priority: P2
owner: Agent-Codex
depends_on:
  - V83.99
blocked_by: []
affected_area: data-contracts
scope_files:
  - src/core/Config.js
  - src/core/config/maps/**
  - src/core/settings/**
  - src/ui/menu/**
  - src/ui/SettingsStore.js
  - src/ui/Profile*
  - src/core/replay/**
  - src/core/recording/**
  - src/state/arcade/**
  - src/entities/MapSchema.js
  - src/entities/CustomMapLoader.js
  - editor/js/EditorMapSerializer.js
  - editor/js/ui/EditorBuildCatalog.js
  - editor/templates/**
  - src/shared/contracts/**
  - src/shared/utils/**
  - tests/persistence-version-migration.contract.test.mjs
  - docs/referenz/ai_architecture_context.md
verification:
  - npm run architecture:report
  - npm run check:architecture:boundaries
  - npm run check:architecture:metrics
  - npm run check:architecture:ratchet
  - npm run typecheck:architecture
  - npm run plan:check
  - npm run docs:sync
  - npm run docs:check
updated_at: 2026-04-13
source_history:
  - docs/plaene/alt/Feature_Persistence_Content_Contracts_Schema_Migration_V85.md
---

# V85 Persistence-, Content-Contracts und Schema-Migrationen

## Ziel

Nach `V83` Persistenz- und Content-Pfade so haerten, dass kuenftige Features ueber versionierte Vertraege und kontrollierte Migrationen erweitert werden koennen.

## Nicht-Ziel

- Kein Datenbank- oder Backend-Zwang als Teil dieses Blocks.
- Kein kompletter inhaltlicher Rebalance- oder Content-Rewrite.
- Kein einmaliger Massenimport aller historischen Artefakte ohne Migrationsstrategie.
- Kein stilles Weitertragen schema-loser JSON-Pfade als neue Norm.
- Kein vollstaendiger Editor-UX- oder Dock-Redesign-Block; editornahe Arbeit bleibt hier auf Content-, Descriptor- und Serialisierungsvertraege begrenzt.

## Definition of Done

- [ ] DoD.1 Settings, Profile, Presets, Replay, Arcade-Progress und weitere persistierte Artefakte tragen explizite Schema-Versionen.
- [ ] DoD.2 Fuer versionierte Artefakte existieren dokumentierte und technisch verankerte Migrationspfade.
- [ ] DoD.3 Content-Registries fuer Maps, Rewards, Missionen, Fahrzeuge und Modifiers laufen ueber stabile Descriptor-Vertraege.
- [ ] DoD.4 Import/Export- und Persistence-Pfade validieren Version, Capability und Fallback bewusst statt implizit.
- [ ] DoD.5 Folgefeatures koennen neue Datenfelder und Content-Descriptoren additiv einfuehren, ohne alte Staende unkontrolliert zu brechen.

## Risiken

- R1 | hoch | Historische JSON-Staende koennen bei harter Versionsanhebung brechen, wenn Migrationspfade unvollstaendig sind.
- R2 | hoch | Content-Registries koennen zu generisch werden, wenn reale Descriptor-Faelle nicht zuerst inventarisiert werden.
- R3 | mittel | Browser-/Desktop-Capability-Grenzen werden inkonsistent, wenn Persistenzlogik nicht zentral gelesen wird.
- R4 | mittel | Import/Export-UX wird verwirrend, wenn Fehler-, Warn- und Fallback-Pfade nicht bewusst gestaltet werden.
- R5 | mittel | Runtime-Presetkatalog, Editor-Build-Katalog und Custom-Map-Serialisierung driften auseinander, wenn dieselben Content-Descriptoren nicht aus einer gemeinsamen Quelle gelesen werden.

## Phasen

### 85.1 Dateninventar und Versionsmatrix erstellen
status: done
goal: Persistierte Artefakte und schema-lose Stellen systematisch erfassen
output: Dokumentierte Versionsmatrix und Inventar

Hinweis 2026-04-04 (V84-Folgeverbrauch): Replay- und Snapshot-Artefakte setzen auf dem serialisierbaren `snapshot_envelope` des `MatchKernel` auf. `V85` versioniert deshalb `gameStateSnapshot`, `simStateSnapshot`, `runtimeProjection` und zugehoerige Replay-Metadaten, statt neue ad-hoc JSON-Shapes neben dem Kernelvertrag einzufuehren.

Hinweis 2026-04-04 (Content-Registry-Scope): Runtime-Map-Presets, editornahe Build-Kataloge, Vorlagen/Snippets und Custom-Map-Import gelten als derselbe Content-Familienbaum. `V85` inventarisiert und versioniert diese Quellen gemeinsam, statt getrennte Sonderkataloge fuer Runtime, Editor und Import/Export zu pflegen.

- [x] 85.1.1 Persistierte Artefakte, Import/Export-Pfade und schema-lose JSON-Stellen inventarisieren, inklusive Runtime-Map-Presets, Editor-Templates, Build-Katalog-Metadaten und Custom-Map-JSON. (abgeschlossen: 2026-04-12; evidence: Get-Content src/ui/StorageKeys.js,src/ui/SettingsStore.js,src/ui/ProfileTransferOps.js,src/ui/menu/MenuPresetStore.js,src/ui/menu/MenuDraftStore.js,src/ui/menu/MenuTextOverrideStore.js,src/ui/menu/MenuTelemetryStore.js,src/state/arcade/ArcadeRunState.js,src/state/arcade/ArcadeVehicleProfile.js,src/ui/arcade/vehicle-manager/VehicleManagerLoadoutPresets.js,src/core/replay/ReplayRecorder.js,src/entities/mapSchema/MapSchemaMigrationOps.js,src/entities/CustomMapLoader.js,editor/js/EditorMapSerializer.js,src/core/config/maps/MapPresetCatalogBaseData.js -> Inventar 85.1.1 in V85)
- [x] 85.1.2 Eine Versionsmatrix fuer Settings, Profile, Presets, Replay, Arcade-Progress, Map-/Template-Descriptoren und kuenftige Meta-Artefakte definieren. (abgeschlossen: 2026-04-13; evidence: Get-Content src/ui/SettingsStore.js,src/shared/contracts/SettingsProfileContract.js,src/ui/ProfileTransferOps.js,src/ui/menu/MenuPresetStore.js,src/ui/menu/MenuDraftStore.js,src/ui/menu/MenuTextOverrideStore.js,src/ui/menu/MenuTelemetryStore.js,src/state/arcade/ArcadeRunState.js,src/state/arcade/ArcadeVehicleProfile.js,src/ui/arcade/vehicle-manager/VehicleManagerLoadoutPresets.js,src/core/replay/ReplayRecorder.js,src/entities/mapSchema/MapSchemaMigrationOps.js,src/core/config/maps/MapPresetCatalogBaseData.js,editor/js/ui/EditorBuildCatalog.js -> Versionsmatrix 85.1.2 in V85 plus Leitplanke in ai_architecture_context)

#### Inventar 85.1.1 (2026-04-12)

| Artefaktfamilie | Persistenz-/Transferpfad | Versionierungsstand | Hinweise |
| --- | --- | --- | --- |
| Settings-Snapshot | `cuviosclash.settings.v1` via `SettingsStore` | storage key `*.v1` + Feld `settingsVersion` (Migration auf Zielversion) | Legacy-Keys werden beim Lesen mitgefuehrt (`aero-arena-3d.*`, `mini-curve-fever-3d.settings.v3/v4`) |
| Settings-Profile | `cuviosclash.settings-profiles.v1` via `SettingsStore.loadProfiles/saveProfiles` | key-versioniert, aber Profileintraege ohne explizites `schemaVersion`-Feld | Normalisierung ueber `SettingsProfileContract`, kein expliziter Payload-Vertrag |
| Profil-Import/-Export | `ProfileTransferOps` JSON-Transfer | Export mit `contractVersion: profile-export.v1`; Import akzeptiert auch Legacy ohne Contract-Huelle | Rueckwaertskompatibel, aber kein harter Reject fuer unbekannte Contract-Version |
| Menu-Presets | `cuviosclash.menu-presets.v1` via `MenuPresetStore` | payload `schemaVersion: menu-preset-store.v1` | Zusammenfuehrung mit fixed preset catalog |
| Menu-Drafts | `cuviosclash.menu-drafts.v1` via `MenuDraftStore` | payload `schemaVersion: menu-draft-store.v1` | Session-typisierte Drafts |
| Menu-Text-Overrides | `cuviosclash.menu-text-overrides.v1` via `MenuTextOverrideStore` | key-versioniert, payload schema-los | Rohes Key/Value-Objekt ohne `schemaVersion` |
| Menu-Telemetry | `cuviosclash.menu-telemetry.v1` via `MenuTelemetryStore` | key-versioniert, payload schema-los | Umfangreiche Aggregatstruktur ohne explizites Versionfeld |
| Arcade-Run-Records | `cuviosclash.arcade-run-profile.v1` via `ArcadeRunRuntime` + `createArcadeRunRecords()` | key-versioniert + payload `schemaVersion: arcade-run-profile.v1` | Persistierung im Runtime-Flow, nicht direkt in `src/state/arcade/**` |
| Arcade-Vehicle-Profile & Loadout-Presets | `cuviosclash.arcade-vehicle-profile.v1` (`ArcadeVehicleProfile`) und `cuviosclash.arcade-vehicle-loadouts.v1` (`VehicleManagerLoadoutPresets`) | beide payloads mit expliziter Schema-Version | Vereinheitlichte Store-Nutzung plus LocalStorage-Fallback |
| Replay-Export | `ReplayRecorder.exportReplayJSON()/persistReplay()` | Replay-Daten tragen `version: replay.v1` | Export vorhanden; Import-/Migrationspfad fuer Replay nicht im aktuellen Scope verankert |
| Map-/Editor-JSON (Custom Maps) | `custom_map_test` via `CustomMapLoader` + `MapSchemaMigrationOps` + `EditorMapSerializer` | `MAP_SCHEMA_VERSION = 4`, Migration fuer legacy/schema 1-3 | Einheitlicher Parse-/Migrationspfad inkl. sichtbarer Warnungen |
| Runtime-Map-Presets & Build-Katalog | `src/core/config/maps/**`, `editor/js/ui/EditorBuildCatalog.js` | derzeit schema-los (statische JS-Objekte, kein `schemaVersion`) | Kandidat fuer Descriptor-/Registry-Vertrag in `85.3.x` |

Schema-/Scope-Luecken aus dem Inventar:
- `editor/templates/**` ist im Repo aktuell nicht vorhanden (Scope-Pfad ohne Dateien).
- `settings-profiles`, `menu-text-overrides` und `menu-telemetry` tragen keine explizite Payload-Schema-Version.
- Runtime-Map-Presetdaten in `src/core/config/maps/**` liegen weiterhin als schema-lose Objektstrukturen vor.

#### Versionsmatrix 85.1.2 (2026-04-13)

Namensregel fuer Folgearbeit:
- Persistierte Store-Payloads verwenden `schemaVersion`.
- Import-/Export-, Snapshot- und Transfer-Huellen verwenden `contractVersion`; bestehende Replay-Huellen mit `version` bleiben bis zum Migrationsrahmen kompatibel, gelten aber als derselbe Vertrags-Typ.
- Runtime-/Editor-/Template-Kataloge verwenden kuenftig `descriptorVersion`.
- Authored Map-Dokumente bleiben die bewusste Ausnahme: dort ist das numerische `schemaVersion` aus `MAP_SCHEMA_VERSION` weiterhin autoritativ.

| Artefaktfamilie | Autoritatives Versionssignal | V85-Entscheid fuer die Matrix | Folgearbeit |
| --- | --- | --- | --- |
| Settings-Snapshot | Storage-Familie `cuviosclash.settings.v1` plus Payload-Feld `settingsVersion` | Der Key-Suffix identifiziert nur die Store-Familie; fachlich fuehrt das innere `settingsVersion` die Migration. Ein Key-Rollover ist erst fuer einen echten Store-Schnitt zulaessig, nicht fuer additive Feldupdates. | `85.2.1` zieht den Load-/Upgrade-Pfad auf dieselbe Leseregel. |
| Settings-Profile | Storage-Familie `cuviosclash.settings-profiles.v1`; Payload heute nacktes Array | Zielvertrag ist eine versionierte Huelle `schemaVersion: settings-profiles.v1` mit `profiles[]`; die Profileintraege bleiben ueber `SettingsProfileContract` normalisiert und konsumieren denselben Settings-Migrationspfad wie der Snapshot. | `85.2.2` migriert Legacy-Arrays in die neue Huelle. |
| Profil-Import/-Export | `contractVersion: profile-export.v1` | Transferartefakte bleiben strikt von lokalen Store-Schemata getrennt. Legacy-Import ohne Huelle ist nur ein Uebergangspfad; unbekannte kuenftige `contractVersion`s duerfen spaeter nicht still akzeptiert werden. | `85.2.1` definiert Reject-/Fallback-Regeln, `85.5.1` den Sunset fuer huellenlose Legacy-Imports. |
| Menu-Presets | `schemaVersion: menu-preset-store.v1` | Nutzer-Presets bleiben ein versionierter Store; der feste Runtime-Katalog ist kein Teil dieses Payload-Vertrags, sondern spaeter Registry-Input fuer `85.3`. | `85.2.2` haengt den Store an den gemeinsamen Migrationsrahmen. |
| Menu-Drafts | `schemaVersion: menu-draft-store.v1` | Session-typisierte Entwurfsdaten bleiben ein eigener Store-Vertrag und werden getrennt von Presets migriert. | `85.2.2` vereinheitlicht Load-/Upgrade-/Fallback-Verhalten. |
| Menu-Text-Overrides | Storage-Familie `cuviosclash.menu-text-overrides.v1`; Payload heute schema-los | Zielvertrag ist eine Huelle `schemaVersion: menu-text-overrides.v1` mit explizitem Override-Container statt rohem Key/Value-Objekt. | `85.2.2` migriert den heutigen Raw-Store in die versionierte Huelle. |
| Menu-Telemetry | Storage-Familie `cuviosclash.menu-telemetry.v1`; Payload heute schema-los | Zielvertrag ist eine Huelle `schemaVersion: menu-telemetry.v1`; Event-, Balance- und Recent-Round-Modelle bleiben additive Unterstrukturen innerhalb derselben Huelle. | `85.2.2` legt die erste versionierte Telemetry-Huelle und ihre Fallback-Regeln fest. |
| Arcade-Run-Records | `schemaVersion: arcade-run-profile.v1`; fachliches Teilmodell `scoreModel: arcade-score.v1` | Artefakt-Schema und Score-Modell bleiben absichtlich getrennte Versionsebenen, damit ein Score-Rebalance nicht automatisch einen Store-Break erzwingt. | `85.2.1` dokumentiert, wann Score-Modell-Bumps additive Migration vs. harten Contract-Wechsel verlangen. |
| Arcade-Vehicle-Profile | `schemaVersion: arcade-vehicle-profile.v1` | Profil-Progression bleibt ein eigener Store-Vertrag mit expliziter Payload-Version. | `85.2.2` fuehrt denselben Migrationsrahmen wie bei den anderen Stores ein. |
| Arcade-Loadout-Presets | `schemaVersion: arcade-vehicle-loadouts.v1` | Loadouts bleiben ein eigener Preset-Store und werden nicht im Vehicle-Profile-Schema versteckt. | `85.2.2` vereinheitlicht Store-Loading und Legacy-Fallbacks. |
| Replay-Export und Snapshot-Familie | Top-Level heute `version: replay.v1`; darunter `snapshot_envelope`-/`gameStateSnapshot`-/`simStateSnapshot`-/`runtimeProjection`-Familien | Replay ist der Transport-/Persistenzumschlag; die verschachtelten Snapshot-Familien bleiben eigenstaendige Contracts aus dem MatchKernel-Verbrauch. `V85` fuehrt deshalb keinen parallelen Replay-Sondershape neben diesen Snapshot-Vertraegen ein. | `85.2.1` definiert, wie `version` und kuenftige `contractVersion` kompatibel gelesen werden; `85.4.x` haertet Export-/Import-Warnungen. |
| Map-/Editor-JSON (Custom Maps) | Numerisches `schemaVersion` aus `MAP_SCHEMA_VERSION` (aktuell `4`) | Authored Maps bleiben auf der bestehenden numerischen Schema-Linie; Parser und Migration liegen zentral in `MapSchemaMigrationOps`. Kuenftige Descriptor-Registries referenzieren die migrierte Map, statt eine konkurrierende String-Version fuer dasselbe Dokument einzufuehren. | `85.2.1` verankert Reject fuer unbekannte Zukunftsversionen; `85.3.x` baut Descriptoren um diese Linie herum. |
| Runtime-Map-Presets, Build-Katalog, Templates | Heute schema-los; Zielsignal `descriptorVersion: content-descriptor.v1` | Runtime-, Editor- und kuenftige Template-Quellen werden als eine Content-Familie betrachtet. Die heutigen statischen JS-Objekte sind nur Migrationsquelle; der spaetere Zielvertrag ist ein gemeinsamer Descriptor-Envelope statt separater Sonderlisten. | `85.3.1` fuehrt die Descriptor-Familie produktiv ein. |
| Kuenftige Meta-Artefakte | Reserviert: `artifactType` plus familie-spezifisches `contractVersion` | Meta-Dateien wie Export-Manifeste, Audit- oder Migrationsevidence bekommen keine schema-lose Catch-all-JSON-Huelle. Jede neue Meta-Familie benoetigt ein explizites `artifactType` und eine eigene `contractVersion` nach dem Muster `<family>.v1`. | `85.2.1` uebernimmt diese Regel in den Migrationsrahmen; `85.5.x` definiert Sunset-/Shadow-Write-Strategien pro Familie. |

### 85.2 Migrationsrahmen verankern
status: done
goal: Gemeinsamen Upgrade- und Fallback-Rahmen fuer versionierte Artefakte schaffen
output: Kleiner Migrationsrahmen fuer Load-, Upgrade- und Reject-Pfade

- [x] 85.2.1 Einen kleinen Migrationsrahmen fuer Load-, Upgrade-, Fallback- und Reject-Pfade definieren. (abgeschlossen: 2026-04-13; evidence: Get-Content src/shared/contracts/ArtifactVersionMigrationContract.js,src/ui/ProfileTransferOps.js,src/entities/mapSchema/MapSchemaMigrationOps.js,src/core/replay/ReplayRecorder.js,tests/persistence-version-migration.contract.test.mjs,package.json -> Migrationsrahmen 85.2.1 inkl. Profile-/Map-/Replay-Verbrauch und Contract-Testpfad)
- [x] 85.2.2 Bestehende Persistence-, Preset- und authoring-nahe Serialisierungspfade schrittweise auf denselben Versions- und Migrationsrahmen ziehen. (abgeschlossen: 2026-04-13; evidence: Get-Content src/ui/SettingsStore.js,src/ui/menu/MenuPresetStore.js,src/ui/menu/MenuDraftStore.js,src/ui/menu/MenuTextOverrideStore.js,src/ui/menu/MenuTelemetryStore.js,src/state/arcade/ArcadeVehicleProfile.js,src/ui/arcade/vehicle-manager/VehicleManagerLoadoutPresets.js,src/ui/ProfileTransferOps.js,tests/persistence-version-migration.contract.test.mjs -> Store-/Preset-Migrationsrahmen inkl. Legacy-Fallback, Reject und Envelope-Rewrites)

#### Migrationsrahmen 85.2.1 (2026-04-13)

Gemeinsamer Rahmen:
- `resolveArtifactVersionState()` in `src/shared/contracts/ArtifactVersionMigrationContract.js` liefert den kleinen, wiederverwendbaren Entscheidungsvertrag `current|upgrade|fallback|reject` fuer Versionsfelder (`schemaVersion`, `contractVersion`, `version`) inklusive Missing-Version- und Future-Reject-Handling.
- `parseProfileImport()` konsumiert denselben Rahmen fuer Profil-Transfers: unbekannte `contractVersion` wird jetzt explizit abgelehnt, huellenlose Legacy-Imports bleiben als bewusstes Fallback zulaessig.
- `migrateMapDocument()` nutzt denselben Rahmen fuer authored Map-Schemas (`schemaVersion`): Legacy-/Missing-Version bleibt Fallback, Versionen `< v4` laufen ueber denselben Upgrade-Pfad, Zukunftsversionen werden weiterhin hart rejected.
- Replay-Export schreibt jetzt parallel `contractVersion` und die bestehende Legacy-Schreibweise `version` (`replay.v1`), damit kuenftige Leser denselben Migrationsrahmen ohne Shape-Bruch konsumieren koennen.

Test-/Verifikationsvorbereitung:
- `tests/persistence-version-migration.contract.test.mjs` deckt den neuen Rahmen sowie Profile-, Map- und Replay-Verbrauchspfad als `node-contract`-Testfall ab.
- `package.json` fuehrt den neuen Contract-Test explizit im `test:contract`-Einstieg.

#### Migrationsrahmen 85.2.2 (2026-04-13)

Schrittweise Store-/Preset-Migration:
- `SettingsStore.loadProfiles()/saveProfiles()` liest Legacy-Arrays weiter, validiert aber ueber `resolveArtifactVersionState()` und schreibt auf den versionierten Envelope `schemaVersion: settings-profiles.v1` zurueck.
- `MenuPresetStore` und `MenuDraftStore` klassifizieren Persistenzdaten jetzt ueber denselben Entscheidungsvertrag (`current|upgrade|fallback|reject`) statt impliziter Shape-Annahmen; Legacy-Shapes werden auf die kanonischen Store-Envelopes normalisiert.
- `MenuTextOverrideStore` und `MenuTelemetryStore` fuehren erstmals explizite Payload-Huellen (`menu-text-overrides.v1`, `menu-telemetry.v1`) ein, behalten Legacy-Raw-Reads als Fallback und migrieren beim Laden automatisch.
- `ArcadeVehicleProfile.loadVehicleProfiles()` rejectet Zukunftsschemata sichtbar, normalisiert legacy/missing Versionen auf `arcade-vehicle-profile.v1` und rewritet bereinigte Profile.
- `VehicleManagerLoadoutPresets` validiert Persistenzdaten ueber denselben Resolver und migriert schema-lose Legacy-Payloads auf `arcade-vehicle-loadouts.v1`.
- `parseProfileImport()` behandelt jetzt auch explizit gesetzte, aber ungueltige `contractVersion` als Reject statt stillem Missing-Fallback.

Contract-Abdeckung:
- `tests/persistence-version-migration.contract.test.mjs` prueft zusaetzlich die 85.2.2-Migrationspfade fuer Settings-Profile, Menu-Drafts/Text/Telemetry und Arcade-Profile/Loadout-Presets auf Envelope-Rewrite und Reject-Verhalten.

### 85.3 Content-Contracts und Registries schneiden
status: done
goal: Content ueber stabile Descriptoren statt verstreute Sonderlogik modellieren
output: Content-Registries fuer Maps, Presets, Rewards, Missionen, Fahrzeuge und Modifiers

- [x] 85.3.1 Maps, Runtime-Presets, Editor-Templates, Rewards, Missionen, Fahrzeuge und Modifiers ueber stabile Descriptor- oder Registry-Vertraege modellieren. (abgeschlossen: 2026-04-13; evidence: Get-Content src/shared/contracts/ContentDescriptorContract.js,src/shared/contracts/RuntimeMapCatalogContract.js,editor/js/ui/EditorBuildCatalog.js,src/shared/contracts/ArcadeMissionContract.js,src/shared/contracts/ArcadeRewardContract.js,src/shared/contracts/ArcadeModifierContract.js,src/entities/vehicle-registry.js,src/core/config/maps/presets/expert_maps.js,tests/content-descriptor-registries.contract.test.mjs,package.json -> Descriptor-Registries content-descriptor.v1 fuer Runtime/Editor/Arcade/Fahrzeuge inkl. Contract-Abdeckung)
- [x] 85.3.2 Verstreute Content-Sonderlogik in Runtime, Editor-Build-Katalog, Custom-Map-Import und Folgeverbrauchern auf dieselben Registries und Contracts zurueckfuehren. (abgeschlossen: 2026-04-13; evidence: Get-Content src/core/arcade/ArcadeRunRuntime.js,editor/js/ui/EditorBuildCatalog.js,src/entities/CustomMapLoader.js,src/entities/mapSchema/CustomMapSelectionResolver.js,src/state/arcade/ArcadeMissionState.js,src/entities/vehicle-registry.js,src/shared/contracts/RuntimeMapCatalogContract.js,tests/content-descriptor-registries.contract.test.mjs -> Runtime-/Editor-/Import-Verbrauch liest Descriptor-Registries als kanonische Quelle)

#### Content-Descriptor-Registries 85.3.1 (2026-04-13)

Gemeinsamer Descriptor-Rahmen:
- `src/shared/contracts/ContentDescriptorContract.js` fuehrt `descriptorVersion: content-descriptor.v1` plus einheitlichen Registry-Envelope (`descriptorType`, `source`, `status`, `entries`, `metadata`) fuer Content-Familien ein.
- `RuntimeMapCatalogContract` exportiert `listRuntimeMapPresetDescriptors()` und `getRuntimeMapPresetRegistryDescriptor()` fuer Runtime-Map-Presets inklusive stabiler Feature-Flags (Portale, Gates, Items, Missionen, GLB, Parcours).
- `EditorBuildCatalog` exportiert denselben Envelope fuer Build-Katalog und den Template-Scope: `getEditorTemplateRegistryDescriptor()` markiert `editor/templates/**` bewusst als `status: missing`, solange der Pfad nicht existiert.

Arcade-/Vehicle-Registries:
- `ArcadeMissionContract`, `ArcadeRewardContract` und `ArcadeModifierContract` stellen descriptorbasierte Registry-Ausgaben bereit, damit Mission-, Reward- und Modifier-Kataloge nicht mehr nur implizite Laufzeitstrukturen bleiben.
- `vehicle-registry.js` stellt mit `listVehicleDescriptors()` und `getVehicleRegistryDescriptor()` einen stabilen Fahrzeug-Descriptorvertrag bereit.
- `expert_gauntlet` ist als expliziter Runtime-Map-Key in den Expert-Presets verankert, damit Arcade-Sektorpools und Runtime-Map-Registry denselben Key-Raum teilen.

Contract-Abdeckung:
- `tests/content-descriptor-registries.contract.test.mjs` prueft Registry-Envelope, Descriptor-Version und Cross-Referenzen zwischen Arcade-Sektorpools und den map-/reward-/modifier-Registries.
- `package.json` fuehrt den neuen Test im `test:contract`-Einstieg.

#### Content-Descriptor-Verbrauch 85.3.2 (2026-04-13)

Verbrauchspfad auf denselben Registry-Vertrag zurueckgefuehrt:
- `ArcadeRunRuntime` konsumiert Runtime-Map-Keyspace/Labels jetzt ueber `RuntimeMapCatalogContract` statt ueber einen separaten `MapPresetCatalog`-Import; Reward- und Modifier-Auswahl im Intermission-Flow lesen denselben Descriptor-Registry-Vertrag wie die Shared Contracts.
- `EditorBuildCatalog` nutzt den eigenen Descriptor-Envelope als kanonische Leseflaeche fuer `get/find/list/default`-Getter; Kategorie-Metadaten (`accentColor`, Beschreibung) bleiben im Registry-Metadatenbereich statt in separaten Consumer-Sonderlisten.
- `CustomMapLoader` und `CustomMapSelectionResolver` validieren bekannte Runtime-Map-Keys ueber den Runtime-Descriptor-Satz und waehlen den Fallback-Key (`standard` bevorzugt) auf derselben Registry-Basis.
- Missions- und Vehicle-Folgeverbrauch (`ArcadeMissionState`, `vehicle-registry`) liest zulaessige IDs jetzt aus den Descriptor-Registries statt aus verstreuten Objekt-Keylisten.

Contract-Abdeckung 85.3.2:
- `tests/content-descriptor-registries.contract.test.mjs` prueft zusaetzlich die Consumer-Lesewege fuer Runtime-Map-Labels/-Keys, Editor-Build-Getter und descriptorbasierte Custom-Map-Fallbacks.

### 85.4 Import/Export- und Capability-Grenzen haerten
status: done
goal: Persistenz- und Dateipfade bewusst an Version und Capability koppeln
output: Validierte Import/Export- und Persistence-Wege

- [x] 85.4.1 Datei-, Browser-, Desktop-, Editor-Import- und Template-Pfade ueber Capability- und Versionspruefungen absichern. (abgeschlossen: 2026-04-13; evidence: Get-Content src/ui/menu/MenuConfigShareOps.js,src/core/runtime/MenuRuntimePresetConfigService.js,editor/js/ui/EditorSessionControls.js,src/shared/contracts/EditorPathContract.js,vite.config.js,src/entities/CustomMapLoader.js,src/core/recording/DownloadService.js,editor/js/ui/EditorBuildCatalog.js,tests/persistence-version-migration.contract.test.mjs,tests/content-descriptor-registries.contract.test.mjs -> capability-/versionsgesicherte Import-/Export-Pfade fuer menu-config, editor disk i/o, custom-map storage, desktop save und templates)
- [x] 85.4.2 Fehler-, Warn- und Migrationsmeldungen fuer inkompatible oder veraltete Artefakte bewusst gestalten. (abgeschlossen: 2026-04-13; evidence: Get-Content src/ui/menu/MenuConfigShareOps.js,src/core/runtime/MenuRuntimePresetConfigService.js,src/entities/CustomMapLoader.js,src/entities/mapSchema/CustomMapSelectionResolver.js,src/state/match-session/MatchSessionFeedbackPlan.js,editor/js/ui/EditorSessionControls.js,editor/js/ui/EditorBuildCatalog.js,src/core/recording/DownloadService.js,tests/persistence-version-migration.contract.test.mjs,tests/content-descriptor-registries.contract.test.mjs,tests/core-targeted-runtime.spec.js -> bewusste Nutzer-/Fallback-Meldungen fuer menu-config, custom-map, editor-disk, template-capability und recording-export)

#### Import-/Capability-Hardening 85.4.1 (2026-04-13)

Datei-/Editor-Disk-Pfad:
- `editor/js/ui/EditorSessionControls.js` sendet bei `SAVE_MAP_DISK` jetzt explizit `contractVersion: editor-disk-io.v1`, prueft Fetch-Verfuegbarkeit vor dem Request und rejectet ungueltige Response-Contract-Versionen sichtbar.
- `vite.config.js` validiert denselben Disk-I/O-Vertrag fuer eingehende API-Requests und antwortet fuer alle Editor-Disk-API-Pfade mit derselben Contract-Version.
- `src/shared/contracts/EditorPathContract.js` fuehrt `EDITOR_DISK_IO_CONTRACT_VERSION` als gemeinsame Source of Truth fuer Renderer/API ein.

Browser-/Custom-Map-Pfad:
- `src/entities/CustomMapLoader.js` exponiert `resolveCustomMapStorageCapability()` (`custom-map-storage-capability.v1`) und liefert Capability-Status (`available`, `providerKind`, `degradedReason`) in allen Import-Resultaten mit.
- Custom-Map-Import meldet fehlende Browser-Storage-Verfuegbarkeit jetzt explizit als Capability-Fehler statt implizitem Nullpfad.

Desktop-Save-/Recording-Pfad:
- `src/core/recording/DownloadService.js` validiert die Contract-Version des Electron-Save-Adapters vor App-Save-Versuchen; unbekannte Adapter-Vertraege werden mit Warnsignal uebersprungen und sauber auf Browser-Fallback geleitet.

Menu-Config-Transfer und Template-Pfade:
- `src/ui/menu/MenuConfigShareOps.js` verwendet jetzt einen versionierten Envelope (`menu-config-share.v1`) mit Legacy-Fallback fuer alte payload-only Imports und explizitem Reject fuer unbekannte Zukunftsversionen.
- `editor/js/ui/EditorBuildCatalog.js` ergaenzt `resolveEditorTemplateImportCapability()` (`editor-template-import-capability.v1`) als expliziten Capability-Status fuer den weiterhin fehlenden `editor/templates/**`-Pfad.
- `tests/persistence-version-migration.contract.test.mjs` und `tests/content-descriptor-registries.contract.test.mjs` decken die neuen Version-/Capability-Vertraege ab.

#### Import-/Feedback-Meldungen 85.4.2 (2026-04-13)

Recheck von `85.4.1` gegen DoD zeigte keine blockerhafte Vertragsluecke; offen war vor allem die bewusste Rueckmeldung an Nutzer und Folge-Consumer.

Bewusste Fehler-, Warn- und Migrationsmeldungen:
- `src/ui/menu/MenuConfigShareOps.js` liefert fuer Config-Import jetzt strukturierte Rueckmeldungen (`message`, `tone`, `warnings`, `migration`) statt nur `success/error`; `MenuRuntimePresetConfigService` zeigt Legacy-Fallbacks sichtbar als Warnpfad statt sie still als normalen Erfolg zu behandeln.
- `src/entities/CustomMapLoader.js` und `src/entities/mapSchema/CustomMapSelectionResolver.js` geben fuer Browser-Storage-Ausfall, fehlende Custom-Maps, defekte Artefakte und erfolgreiche Legacy-Migrationen bewusst formulierte `reason`-/`message`-/`details`-Pfade aus; `MatchSessionFeedbackPlan` spiegelt denselben Vertrag als Toast- und Konsolenfeedback im Matchstart.
- `editor/js/ui/EditorSessionControls.js` unterscheidet bei Export, Import, Playtest und Disk-Save jetzt explizit normale Hinweise vs. Migrationshinweise; inkompatible `editor-disk-io.v1`-Antworten verweisen sichtbar auf einen Stand-Mismatch zwischen Renderer und Dev-Server.
- `src/core/recording/DownloadService.js` kapselt Recording-Export-Fallbacks in bewusste Statusmeldungen (`message`, `warnings`) fuer App-Save, Disk-API und Browser-Download; veraltete Desktop-Save-Adapter werden nicht nur geloggt, sondern als expliziter Warnhinweis an Folge-Consumer markiert.
- `editor/js/ui/EditorBuildCatalog.js` fuehrt fuer den fehlenden Template-Pfad neben `degradedReason` jetzt auch eine lesbare Capability-Meldung, damit spaetere Authoring-Consumer `editor/templates/**` nicht nur technisch, sondern auch UX-seitig sauber kommunizieren koennen.

Contract-Abdeckung 85.4.2:
- `tests/persistence-version-migration.contract.test.mjs` prueft die neuen Feedback-Felder fuer Menu-Config- und Custom-Map-Import.
- `tests/content-descriptor-registries.contract.test.mjs` verankert die lesbare Template-Capability-Meldung.
- `tests/core-targeted-runtime.spec.js` prueft Recording-Export-Statusmeldungen fuer Desktop-App und Browser-Fallback.

### 85.5 Rollout und Sunset fuer Altformate vorbereiten
status: done
goal: Altformate und Kompatibilitaetsadapter kontrolliert auslaufen lassen
output: Sunset-Regeln und Folgeabgleich fuer neue Features

- [x] 85.5.1 Altformate, Shadow-Writes oder Kompatibilitaetsadapter mit Sunset-Regeln dokumentieren. (abgeschlossen: 2026-04-13; evidence: Get-Content docs/plaene/aktiv/V85.md,docs/referenz/ai_architecture_context.md,src/ui/ProfileTransferOps.js,src/core/replay/ReplayRecorder.js,src/ui/SettingsStore.js,src/ui/menu/MenuConfigShareOps.js,editor/js/ui/EditorSessionControls.js,vite.config.js,src/core/recording/DownloadService.js,src/entities/mapSchema/MapSchemaMigrationOps.js -> Sunset-Matrix und Exit-Kriterien fuer Legacy-/Shadow-Write-/Adapterpfade dokumentiert)
- [x] 85.5.2 Folgeblocks, Editor-Authoring-Verbraucher und Referenzdoku auf den neuen Daten- und Content-Leseweg ausrichten. (abgeschlossen: 2026-04-13; evidence: Get-Content src/ui/ProfileTransferOps.js,src/core/ProfileManager.js,src/ui/ProfileUiController.js,src/ui/arcade/ArcadeMenuSurface.js,editor/js/main.js,editor/js/ui/EditorSessionControls.js,docs/plaene/aktiv/V85.md,docs/plaene/aktiv/V86.md,docs/referenz/ai_architecture_context.md,docs/referenz/ai_project_onboarding.md -> Profil-/Arcade-/Editor-Folgeverbrauch auf kanonische Contracts gespiegelt und Folgeblock V86 daran ausgerichtet)

#### Sunset-Regeln 85.5.1 (2026-04-13)

Recheck von `85.4.2` gegen DoD und Ist-Code zeigte keine blockerhafte Vertragsluecke; die naechste offene Arbeit ist der kontrollierte Sunset fuer verbleibende Legacy-/Kompatibilitaetspfade.

Verbindliche Leitplanken fuer den Rest von V85:
- Kompatibilitaetspfade sind nur noch read-/import-seitig zulaessig; alle erstparteilichen Writer bleiben auf dem kanonischen Zielvertrag (`schemaVersion`/`contractVersion`/`descriptorVersion`) und fuehren keine neuen Legacy-Schreibformen ein.
- Jeder verbleibende Fallback braucht eine benannte Exit-Bedingung (kein "dauerhaftes Legacy"), einen benannten Besitzerpfad und einen dokumentierten Folgeabgleich.
- Neue Fallbacks duerfen in V85 nur noch aufgenommen werden, wenn sie denselben strukturierten Feedback-Vertrag (`reason`, `message`, `warnings`, optional `migration`) liefern und im Plan explizit inklusive Sunset-Trigger nachgetragen werden.

| Pfad/Familie | Aktueller Kompatibilitaetsmodus | Sunset-Regel / Exit-Kriterium | Folgeabgleich |
| --- | --- | --- | --- |
| `ProfileTransferOps` (`profile-export`) | Import akzeptiert Legacy-Payload ohne `contractVersion`; Export schreibt `profile-export.v1`. | Legacy-Import bleibt nur bis alle erstparteilichen Transferpfade (UI, Dev-Tools, Doku-Beispiele) ausschliesslich Envelope-Exports referenzieren. Danach `allowMissingVersion` fuer Profile-Import auf `false`. | `85.5.2`: Verbraucher und Referenzdoku auf Envelope-only trimmen; kein Legacy-Beispielpfad offen lassen. |
| `ReplayRecorder` (`replay.v1`) | Shadow-Write: `contractVersion` plus Legacy-Feld `version`. | Legacy-Feld `version` bleibt nur, solange mindestens ein dokumentierter Verbraucher noch darauf basiert. Entfernen erst nach Verbraucherabgleich und Contract-Testnachweis fuer `contractVersion`-Only-Leser. | `85.5.2`: Replay-/Import-Verbraucher und Referenztexte auf `contractVersion` als primaeres Signal ausrichten. |
| `SettingsStore.loadProfiles()` (`settings-profiles.v1`) | Legacy-Array ohne Envelope wird gelesen und beim Laden auf Envelope rewritet. | Legacy-Array-Fallback bleibt als Migrationspfad, aber ohne neue Writer. Entfernen erst, wenn Follow-up-Datenquellen keine nackten Arrays mehr liefern und mindestens ein Gate-Abgleich ohne Legacy-Rewrite gefahren wurde. | `85.5.2`: Authoring-/Tooling-Verbraucher auf Envelope-Shape festziehen. |
| `MenuConfigShareOps` (`menu-config-share.v1`) | Payload-only Import ohne Envelope bleibt als Legacy-Fallback lesbar; Export bleibt envelope-only. | Legacy-Payload-Fallback endet, sobald alte Copy/Paste-Snippets aus Doku/Workflows entfernt und alle UI-Importpfade auf versionierte Huelle festgelegt sind. | `85.5.2`: Menu-/Authoring-Verbraucher auf strict envelope input vorbereiten. |
| `EditorSessionControls` + `vite.config.js` (`editor-disk-io.v1`) | Response ohne `contractVersion` wird aktuell toleriert (`allowMissingVersion=true`). | Missing-Version-Fallback endet, sobald Renderer und Dev-Server durchgaengig `editor-disk-io.v1` senden/antworten; danach missing-Version als Stand-Mismatch rejecten. | `85.5.2`: Editor-Authoring- und Tooling-Verbraucher auf strict response contract heben. |
| `DownloadService` (`preload.save.v1`) | Adapter ohne `contractVersion` gilt als zulaessig; explizit ungueltige Versionen werden verworfen. | Missing-Version-Fallback fuer Desktop-Save endet, sobald Preload-Bridge in allen gepflegten Desktop-Builds `contractVersion` garantiert. Danach Missing-Version als inkompatibel behandeln. | `85.5.2`: Desktop-Verbraucher und Referenzdoku auf verbindliches Save-Adapter-Contract-Feld spiegeln. |
| `MapSchemaMigrationOps` (`MAP_SCHEMA_VERSION=4`) | Legacy-Mapformate/schema v1-v3 werden migriert; Zukunftsversionen werden rejected. | Upgradepfad fuer v1-v3 bleibt als bewusstes Importfenster bestehen, wird aber nicht um weitere Sondershapes erweitert. Entfernen/Haertung erst nach Authoring-Rollout mit dokumentierter Sunset-Entscheidung fuer Alt-Maps. | `85.5.2`: Editor-Authoring-Verbraucher und Runtime-Leseweg auf denselben v4-/Descriptor-Vertrag koppeln. |

#### Folgeabgleich 85.5.2 (2026-04-13)

Recheck von `85.5.1` zeigte eine echte Verbrauchsluecke: Der Profil-Import dokumentierte zwar den Legacy-Sunset, spiegelte ihn aber noch nicht als strukturiertes Feedback in die UI. Parallel hing `ArcadeMenuSurface` beim Vehicle-Mastery-Read weiterhin direkt an rohem `localStorage`, und der Editor exponierte den Descriptor-/Template-Vertrag noch nicht sichtbar fuer Folgearbeit.

Folgeverbrauch und Folgeblock-Anker jetzt auf denselben Leseweg gehoben:
- `ProfileTransferOps`, `ProfileManager` und `ProfileUiController` liefern fuer Profil-Import jetzt denselben strukturierten Envelope-/Legacy-Vertrag wie die uebrigen V85-Importpfade (`reason`, `message`, `tone`, `warnings`, `usedLegacyFallback`, `migration`). Legacy-Profile ohne `contractVersion` bleiben bewusst lesbar, werden aber fuer UI und Folge-Tools explizit als normalisierter Fallback sichtbar.
- `ArcadeMenuSurface` liest Vehicle-Mastery nicht mehr roh aus `localStorage`, sondern ueber `SettingsStore.loadJsonRecord()` plus `ArcadeVehicleProfileContract` (`readArcadeVehicleProfileRecord()` / `getArcadeVehicleProfileRecord()`). Damit konsumiert die Arcade-Menueoberflaeche denselben schemaVersion-/Migrationspfad wie Runtime und Persistence-Writer, ohne den State-Layer direkt zu ziehen.
- Der Editor spiegelt den Content-Leseweg jetzt ueber `editor/js/main.js` und `EditorSessionControls`: Runtime-Snapshots und DOM-Datasets exponieren `descriptorVersion`, Entry-Count und Template-Capability, waehrend Session-Aktionen (`Neu`, `Import`, `Disk-Save`, `Playtest`) denselben Descriptor-/Template-/Disk-I/O-Vertrag als Hint transportieren.
- `docs/plaene/aktiv/V86.md` baut kuenftige Authoring-Arbeit jetzt explizit auf diese bereits vorhandenen V85-Contract-Signale auf, statt neue Parallelpfade fuer Templates, Build-Katalog oder Runtime-/Import-Lesewege zu erfinden.

Damit ist `85.5` fachlich abgeschlossen; offene Restarbeit fuer `V85` liegt nur noch im Abschluss-Gate `85.99`.

### 85.99 Abschluss-Gate
status: open
goal: Architektur- und Doku-Gates fuer den Persistence-/Content-Scope schliessen
output: Gruene Gates und dokumentierte Versionsstrategie

- [ ] 85.99.1 Architektur-, Doku- und Governance-Gates fuer den migrierten Persistence-/Content-Scope sind gruensicher.
- [ ] 85.99.2 Versionsmatrix, Migrationsregeln und Capability-Grenzen sind klar genug fuer additive Folgefeatures dokumentiert.
