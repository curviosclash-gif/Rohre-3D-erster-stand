---
id: V92
title: Runtime-Application-Ownership-Entkopplung und Orchestrator-Zuschnitt
status: planned
priority: P2
owner: frei
depends_on:
  - V91.99
blocked_by: []
affected_area: runtime-application-ownership
scope_files:
  - src/application/session-runtime/SessionRuntimeCommandExecutor.js
  - src/application/session-runtime/MenuLobbyServiceFactory.js
  - src/application/session-runtime/NetworkLobbyService.js
  - src/application/session-runtime/StorageLobbyService.js
  - src/core/GameRuntimeFacade.js
  - src/core/runtime/GameRuntimeCoordinator.js
  - src/shared/runtime/GameRuntimePorts.js
  - src/core/AppInitializer.js
  - src/ui/MatchFlowUiController.js
  - src/ui/PauseOverlayController.js
  - src/ui/HudRuntimeSystem.js
  - src/state/RoundStateTickSystem.js
  - src/platform/**
  - src/shared/contracts/PlatformCapabilityRegistry.js
  - src/shared/contracts/SessionRuntimeCommandContract.js
  - src/shared/contracts/SessionRuntimeSnapshotContract.js
  - scripts/architecture-report.mjs
  - scripts/check-architecture-boundaries.mjs
  - scripts/check-architecture-ratchet.mjs
  - tests/lifecycle-capability.contract.test.mjs
  - tests/runtime-regressions.contract.test.mjs
  - docs/referenz/ai_architecture_context.md
  - .agents/test_mapping.md
verification:
  - npm run architecture:report
  - npm run check:architecture:boundaries
  - npm run check:architecture:ratchet
  - npm run typecheck:architecture
  - npm run plan:check
  - npm run docs:sync
  - npm run docs:check
updated_at: 2026-04-15
source_history:
  - docs/plaene/alt/Feature_Runtime_Application_Ownership_Entkopplung_V92_2026-04-15.md
---

# V92 Runtime-Application-Ownership-Entkopplung und Orchestrator-Zuschnitt

## Ziel

Die nach `V91` verbleibenden Architektur-Hotspots gezielt abbauen, damit neue Produkt- und Gameplay-Arbeit nicht wieder in `GameRuntimeFacade`, `MatchFlowUiController`, breite `GameRuntimePorts`-Fallbacks oder globale Runtime-Surfaces hineinwaechst.

- `src/application/**` soll vom reinen Dispatcher-Vorhof zu einer echten Use-Case-Schicht fuer Runtime-Commands werden.
- `GameRuntimePorts` soll im migrierten Scope Commands, Snapshots und Capability-Pfade ohne Legacy-Fallbacks auf `game.runtimeFacade` oder `game.runtimeCoordinator` bereitstellen.
- `window.GAME_RUNTIME` und aehnliche globale Runtime-Surfaces sollen aus produktiven Pfaden verschwinden oder auf read-only Diagnostics reduziert werden.
- Folgeblocks `V64`, `V81`, `V82` und `V86` sollen dieselben Ownership-Grenzen konsumieren, statt die bekannten Orchestrator-Hotspots weiter aufzublasen.

## Nicht-Ziel

- Kein Multiplayer-Produktisierungsblock; LAN-/Online-Hauptpfade bleiben in `V64`.
- Kein Arcade-/Parcours-Featureblock; Progression, Leaderboard, Ghost und HUD-Ausbau bleiben in `V82`.
- Kein Developer-Tooling-Featureblock; Tuning-Window, Presets und F7-Flow bleiben in `V81`.
- Kein kosmetischer Rewrite grosser Dateien nur wegen Zeilenanzahl; Splits muessen Ownership-, Contract- oder Snapshot-Ziele direkt verbessern.
- Kein Endnutzer-Redesign fuer Menues, HUD oder Browser-Demo.

## Definition of Done

- [ ] DoD.1 `src/application/**` besitzt fuer den migrierten Scope die Runtime-Use-Cases hinter `start_match`, `return_to_menu`, `host_lobby`, `join_lobby` und `apply_settings`; der Executor bleibt ein schmaler Normalisierungs-, Observability- und Dispatch-Adapter.
- [ ] DoD.2 `GameRuntimePorts` liefert im migrierten Scope Commands, Snapshots und Projektionen ohne produktive Fallbacks auf `game.runtimeFacade`, `game.runtimeCoordinator` oder andere breite Legacy-Surfaces.
- [ ] DoD.3 `window.GAME_RUNTIME`, `GAME_INSTANCE` und aehnliche globale Runtime-Handles sind kein produktiver Zugriffspfad mehr; verbleibende Debug-Surfaces sind explizit read-only oder blockerfest dokumentiert.
- [ ] DoD.4 `GameRuntimeFacade` und `MatchFlowUiController` sind im betroffenen Scope sichtbar schmaler geschnitten; neue Fachlogik landet nicht mehr in diesen Sammelmodulen.
- [ ] DoD.5 Architektur-Guards, Referenzdoku und Folgeblock-Leitplanken (`V64`, `V81`, `V82`, `V86`) spiegeln denselben Ownership- und Sunset-Stand.

## Risiken

- R1 | hoch | Ein zu frueher Sunset von Runtime- oder Debug-Surfaces bricht bestehende Tooling-, Test- oder Diagnosepfade.
- R2 | hoch | Der Block vermischt sich mit `V64` oder `V82`, wenn Produkt- oder Gameplay-Featurearbeit nicht sauber von Ownership-Schnitten getrennt bleibt.
- R3 | mittel | Ein halbherziger Split erzeugt nur neue Dateigrenzen, aber keine echte Ownership-Verbesserung.
- R4 | mittel | `V81` oder spaetere Runtime-Config-Arbeit reaktivieren globale Config- oder Runtime-Slots, wenn Bridge-/Store-Grenzen nicht frueh genug festgezogen werden.

## Einplanungslogik fuer Folgeblocks

- `V64` soll `V92` vor produktiver Erweiterung von Multiplayer-Use-Cases konsumieren, damit Host-/Join-/Disconnect-Arbeit nicht erneut in Fassade, UI-Hotspots oder Legacy-Ports landet.
- `V81` soll `V92` vor Runtime-Bridge-/IPC-Integration konsumieren, damit Developer-Tooling keine neuen globalen Runtime- oder Config-Backdoors reaktiviert.
- `V82` wird nicht voll blockiert: daten- und regelnahe Phasen (`82.1`, `82.2`, `82.10`) koennen parallel vorbereitet werden; UI-/HUD-/Overlay-nahe Ausbauten sollen den Zuschnitt aus `92.4` jedoch konsumieren, bevor neue Last in `MatchFlowUiController` oder HUD-Hotspots landet.
- `V86` konsumiert `V92` vor allem als Leitplanke fuer Runtime- und Capability-Zugriffe; ein harter Blocker ergibt sich nur fuer runtime-nahe Editor-Glue-Pfade.

Leitplanke 2026-04-15 (Quelle: Architektur-Review nach `V91`): `V92` fuehrt keinen zweiten Runtime- oder Application-Begriffsraum neben den bereits eingefuehrten Shared Contracts ein. Neue Ownership wird ueber bestehende Command-, Snapshot-, Capability- und Guard-Pfade geschnitten, nicht ueber Paralleladapter.
Leitplanke 2026-04-15 (Quelle: `V91 91.5.1`, Referenz 4.5.6): Neue Architekturarbeit benutzt denselben Startcheck `Contract -> Command/Event -> Snapshot -> Capability -> Sunset alter Pfade -> Desktop-vs-Demo`; neue globale Runtime-Slots, direkte `curviosApp`-Reads ausserhalb dedizierter Plattformadapter oder neue `runtimeFacade`-Durchgriffe sind nicht zulaessig.

## Phasen

### 92.1 Ownership-Schnitt und Zielpfade fixieren
status: done
goal: Hotspots, Besitzgrenzen und Zielpfade verbindlich schneiden
output: Inventar plus Zielbild fuer Application-Layer, Ports und Debug-Surfaces

- [x] 92.1.1 Die verbleibenden Hotspots (`GameRuntimeFacade`, `MatchFlowUiController`, `GameRuntimePorts`, `window.GAME_RUNTIME`) gegen reale Aufrufer, Besitzgrenzen und Folgeblock-Risiken inventarisieren. (abgeschlossen: 2026-04-15; evidence: `Select-String -Path src/core/AppInitializer.js,src/core/main.js,src/core/GameBootstrap.js,src/core/runtime/GameRuntimeCoordinator.js,src/core/GameRuntimeFacade.js,src/ui/MatchFlowUiController.js,src/ui/MatchFlowTransitionHotspots.js,src/shared/runtime/GameRuntimePorts.js,src/shared/runtime/GameRuntimeFeaturePorts.js,src/core/runtime/GameRuntimeBundle.js -Pattern 'GAME_RUNTIME|GAME_INSTANCE|runtimeFacade|runtimeCoordinator|MatchFlowUiController|GameRuntimeFacade|createRuntimePorts'` -> produktive Aufrufer, Legacy-Adapter und Global-Publishes fuer alle vier Hotspots inventarisiert)
- [x] 92.1.2 Fuer jeden Hotspot den Zielpfad ueber Application-Service, Snapshot, Port oder Diagnostics-only-Surface festlegen und im Architekturkontext spiegeln. (abgeschlossen: 2026-04-15; evidence: `Select-String -Path docs/plaene/aktiv/V92.md,docs/referenz/ai_architecture_context.md -Pattern 'Zielpfad 2026-04-15|Hotspot-Zielpfad-Matrix \\(V92 92\\.1\\.2\\)'` -> Hotspot-Zielpfad-Matrix im Blockplan und Architekturkontext gespiegelt)

Inventar 2026-04-15 (`V92 92.1.1`):

| Hotspot | Reale produktive Aufrufer (Ist) | Aktuelle Besitzgrenze / Drift | Folgeblock-Risiko |
| --- | --- | --- | --- |
| `GameRuntimeFacade` | `src/core/runtime/GameRuntimeCoordinator.js` delegiert fast den gesamten Runtime-Intent-Katalog weiter; `src/shared/runtime/GameRuntimePorts.js` und `src/state/MatchLifecycleSessionPort.js` nutzen `runtimeFacade` im Intent-/Projection-Resolver; `src/ui/HudRuntimeSystem.js`, `src/ui/TouchInputSource.js` und `src/ui/MatchFlowTransitionHotspots.js` lesen weiterhin direkte Facade-Pfade | Die Fassade ist trotz frueherer Splits weiter Sammeladapter fuer Session-, Menu-, Lobby-, Arcade-, Recording- und Telemetrie-Pfade; sie bleibt damit faktisch die bequemste Querschnittssurface | `V64` und `V81` wuerden Host-/Join-, Diagnostics- oder Tooling-Logik sehr leicht erneut in die Fassade schieben statt in Application-Services oder kleine Ports |
| `MatchFlowUiController` | `src/core/GameBootstrap.js` erzeugt den Controller als Runtime-Handle; `src/core/main.js` nutzt ihn direkt fuer `onRoundEnd()` und `resumeFromPause()`; `src/shared/runtime/GameRuntimePorts.js` exponiert ihn ueber `matchUiPort`; `src/core/InteractiveMatchRuntimeGuard.js` greift fuer den Rueckweg auf `applyReturnToMenuUi()` zu | Der Controller kombiniert weiter Match-Lifecycle-Intents, Pause-/Return-to-Menu-Flow, Arcade-Overlay-Rendering, Hunt-Feedback, Recording-/Telemetry-Helfer und UI-State-Anwendung in einer Klasse | `V82` und spaetere HUD-/Overlay-Arbeit wuerden denselben Hotspot weiter aufblasen; auch `V64`-nahe Lifecycle-Feedbacks koennten hier statt in getrennten UI-/Application-Pfaden landen |
| `GameRuntimePorts` | `src/core/GameBootstrap.js` erstellt die Port-Schicht einmal fuer Runtime/UI; `src/ui/**` und `src/state/**` konsumieren `lifecyclePort`, `runtimeIntentPort`, `matchUiPort`, `arcadePort` und `recordingPort`; `src/ui/MatchFlowTransitionHotspots.js` und `src/shared/runtime/GameRuntimeFeaturePorts.js` erweitern die Ports noch ueber Legacy-Fallbacks auf `runtimeFacade` und `runtimeCoordinator` | Die Port-Schicht ist bereits die kanonische Naht, enthaelt aber fuer den migrierten Scope noch produktive Restadapter auf Bundle- und Legacy-Slots (`runtimeFacade`, `runtimeCoordinator`, `game.*`) | Ohne Zuschnitt in `92.3`/`92.5` wachsen neue Feature-Pfade wieder indirekt auf Legacy-Surfaces, obwohl sie formal "nur" Ports konsumieren; das betrifft besonders `V64`, `V81` und runtime-nahe Teile von `V86` |
| `window.GAME_RUNTIME` plus `GAME_INSTANCE` | `src/core/AppInitializer.js` publiziert beide Globals beim Start; `src/core/main.js` raeumt sie in `dispose()` wieder auf; `src/ui/arcade/ArcadeMenuSurface.js` liest `window.GAME_INSTANCE?.runtimeFacade`, `window.GAME_INSTANCE?._showStatusToast` und `window.GAME_INSTANCE?.settingsManager?.store` direkt | Die Globals bleiben sichtbare, beschreibbare Runtime-Surface und halten damit nicht nur externe Debug-Pfade, sondern auch UI-nahe Laufzeitleser auf einem Legacy-Einstieg | Solange produktive UI-Pfade an den Globals haengen, bleibt der Sunset in `92.3.2` riskant und `V82`-nahe UI-Arbeit bekommt weiter einen impliziten Global-Bypass statt Command-/Snapshot-Verbrauch |

Zielpfad 2026-04-15 (`V92 92.1.2`):

| Hotspot | Zielpfad ab `92.2+` | Explizite Ownership | Sunset-/Guard-Kriterium |
| --- | --- | --- | --- |
| `GameRuntimeFacade` | `SessionRuntimeCommandExecutor` delegiert nur noch auf kleine Application-Use-Cases (`start_match`, `return_to_menu`, `host_lobby`, `join_lobby`, `apply_settings`); UI liest Runtime-Status nur ueber Snapshots/Ports statt direkter Facade-Aufrufe | Runtime-Fachlogik liegt im Application-Layer und in dedizierten Runtime-Services, die Fassade bleibt nur Legacy-Forwarder | Neue Facade-Aufrufer ausserhalb erlaubter Adapter sind in `92.5.1` Guard-violation; bestehende Aufrufer werden pro Subphase auf Commands/Snapshots migriert |
| `MatchFlowUiController` | Aufteilung in Controller-Port plus spezialisierte Adapter (`LifecycleIntent`, `ArcadeOverlayProjection`, `TelemetryFeedback`), konsumiert ueber `UiControllerRuntimePorts` | UI-Orchestrierung liegt im UI-Layer; Runtime-Entscheidungen laufen ueber Command- und Snapshot-Vertraege | Keine neuen Featurezweige mehr in `MatchFlowUiController`; Guard- und Review-Kriterium: neue Methoden muessen einem bestehenden Teiladapter zuordenbar sein |
| `GameRuntimePorts` | Ports bleiben schmale Naht (`runtimeIntentPort`, `lifecyclePort`, `matchUiPort`, `runtimeProjectionPort`) ohne produktive Fallbacks auf `game.runtimeFacade`/`game.runtimeCoordinator` fuer migrierte Commands | Ownership fuer Command-/Snapshot-Semantik liegt in Shared Contracts + Application-Layer, nicht in Port-Fallback-Logik | Port-Fallback-Zaehler fuer `runtimeFacade`/`runtimeCoordinator` werden in `92.5.1` als Ratchet markiert und duerfen fuer neue Arbeit nicht steigen |
| `window.GAME_RUNTIME` + `GAME_INSTANCE` | Nur noch read-only Diagnostics-Surface (expliziter `debugRuntimeSnapshot`) oder kompletter Publish-Stopp; produktive UI-/Runtime-Leser laufen ueber Ports/Snapshots | Runtime-Laufzeitbesitz liegt bei `SessionRuntime`/Application-Layer, Globals sind kein Fach-API mehr | Guard-Matrix erlaubt nur Diagnoseadapter als Caller; produktive Reads in `src/ui/**`, `src/state/**` oder `src/core/**` schlagen als Legacy-Surface-Verstoss fehl |

Verifikationshinweis 2026-04-15: Fuer die non-`*.99`-Subphase bleibt der volle Architektur-Scope (`architecture:report`, `check:architecture:boundaries`, `check:architecture:ratchet`, `typecheck:architecture`) bis `92.99` bewusst offen; in dieser Subphase werden nur Plan-/Doku-Gates direkt nachgezogen.

### 92.2 Application-Layer fuer Runtime-Commands materialisieren
status: done
goal: Runtime-Use-Cases aus Dispatcher- und Core-Mischpfaden ziehen
output: Kleine Application-Services hinter den zentralen Runtime-Commands

- [x] 92.2.1 Runtime-Use-Cases fuer `apply_settings`, `start_match`, `return_to_menu`, `host_lobby` und `join_lobby` in kleine Application-Services schneiden, sodass `SessionRuntimeCommandExecutor` nicht mehr direkt an breite Core-Runtime-Implementierungen gekoppelt ist. (abgeschlossen: 2026-04-15; evidence: `Select-String -Path src/application/session-runtime/SessionRuntimeCommandExecutor.js,src/application/session-runtime/SessionRuntimeCommandUseCases.js -Pattern 'createSessionRuntimeCommandUseCases|applySettings|startMatch|returnToMenu|hostLobby|joinLobby'` -> Executor delegiert die fuenf Runtime-Commands an kleine Application-Use-Cases)
- [x] 92.2.2 Observability-, Result- und Failure-Vertraege (`executeSessionRuntimeCommandResult`) auf denselben Use-Case-Schnitt heben, ohne neue parallele Command-Pfade einzufuehren. (abgeschlossen: 2026-04-15; evidence: `Select-String -Path src/application/session-runtime/SessionRuntimeCommandExecutor.js,src/application/session-runtime/SessionRuntimeCommandUseCases.js,tests/runtime-regressions.contract.test.mjs -Pattern 'session_runtime_command_use_case|executeResult\\(|invalid_command|resultStatus'` -> Executor waehlt nur noch den Use-Case; Observability-, Settled-Result- und Failure-Vertrag liegen im Use-Case-Schnitt und sind im Runtime-Regression-Contract gespiegelt)

### 92.3 Legacy-Port- und Global-Surface-Sunset
status: done
goal: Produktive Runtime-Pfade von breiten Legacy-Einstiegen loesen
output: Schmalere Runtime-Ports und reduzierte globale Debug-Surfaces

- [x] 92.3.1 `GameRuntimePorts` fuer den migrierten Scope von Legacy-Fallbacks auf `game.runtimeFacade` und `game.runtimeCoordinator` befreien oder diese Restadapter klar auf nicht-produktive Migrationsnischen begrenzen. (abgeschlossen: 2026-04-15; evidence: `Select-String -Path src/shared/runtime/GameRuntimePorts.js,tests/runtime-regressions.contract.test.mjs,docs/referenz/ai_architecture_context.md -Pattern 'resolveSessionRuntimeAccess|getRuntimeFeatureTransitionFacade|no longer falls back to legacy runtimeFacade|V92 92\\.3\\.1'` -> produktive `runtimeIntentPort`-/`lifecyclePort`-Fallbacks entfernt, Session-Snapshot ohne Legacy-Facade gespiegelt, Restadapter nur noch fuer Arcade/Recording-Uebergang markiert)
- [x] 92.3.2 `window.GAME_RUNTIME`, `GAME_INSTANCE` und vergleichbare globale Runtime-Publishes auf read-only Diagnostics zurueckschneiden oder aus produktiven Pfaden entfernen. (abgeschlossen: 2026-04-15; evidence: `Select-String -Path src/core/GameRuntimeFacade.js,src/ui/MenuController.js,src/ui/arcade/ArcadeMenuSurface.js,tests/runtime-regressions.contract.test.mjs -Pattern 'runtimeAccess|getArcadeMenuSurfaceState|requestArcadeReplayPlayback|GAME_INSTANCE|GAME_RUNTIME'` -> Arcade-Menue konsumiert Runtime-/Toast-/Store-Zugriffe ueber injizierten Runtime-Access statt produktiver Global-Reads; Contract-Ratchet verhindert die Rueckkehr direkter `GAME_INSTANCE`/`GAME_RUNTIME`-Reads)

### 92.4 Orchestrator-Hotspots aufteilen
status: done
goal: Sammelmodule entlang echter Verantwortung schneiden
output: Sichtbar schmalere Fassade und UI-Orchestrierung

- [x] 92.4.1 `MatchFlowUiController` entlang von Verantwortung schneiden: Lifecycle-Intents, Arcade-/Overlay-Rendering und Telemetrie-/Feedback-Pfade sollen nicht weiter in einer Sammelklasse wachsen. (abgeschlossen: 2026-04-15; evidence: `Select-String -Path src/ui/MatchFlowUiController.js,src/ui/MatchFlowLifecycleController.js,src/ui/MatchFlowArcadeOverlayController.js,src/ui/MatchFlowTelemetryController.js,tests/runtime-regressions.contract.test.mjs -Pattern 'MatchFlowLifecycleController|MatchFlowArcadeOverlayController|MatchFlowTelemetryController|runtime-port-return|Sector clear'` -> `MatchFlowUiController` delegiert Lifecycle-, Overlay- und Telemetrie-Verantwortung an spezialisierte Helfer; Contract-Tests spiegeln den Split)
- [x] 92.4.2 `GameRuntimeFacade` auf schmale Forwarding-, Composition- oder Legacy-Adapter-Rollen reduzieren; Arcade-, Recording-, Session- oder Multiplayer-Unterfluesse landen in expliziten Diensten statt erneut in der Fassade. (abgeschlossen: 2026-04-15; evidence: `Select-String -Path src/core/GameRuntimeFacade.js,src/core/runtime/GameRuntimeArcadeSupport.js,src/core/runtime/GameRuntimeRecordingSupport.js,tests/runtime-regressions.contract.test.mjs -Pattern 'GameRuntimeArcadeSupport|createGameRuntimeRecordingFacadeSupport|_arcadeSupport|_recordingSupport|recordMatchEndTelemetry'` -> `GameRuntimeFacade` delegiert Arcade-/Recording-Unterfluesse an explizite Support-Dienste; der Runtime-Regression-Contract spiegelt die Telemetrie-Delegation)

### 92.5 Ratchet, Doku und Folgeblock-Verbrauch absichern
status: done
goal: Ownership-Schnitt fuer Folgearbeit stabil verankern
output: Nachgeschaerfte Guards, Referenzdoku und Leitplanken fuer Folgeblocks

- [x] 92.5.1 Architektur-Checks, Report oder Guard-Matrix so nachschaerfen, dass neue `runtimeFacade`-, `GAME_RUNTIME`-, Port-Fallback- oder App-Global-Bypaesse im migrierten Scope frueh auffallen. (abgeschlossen: 2026-04-15; evidence: `Select-String -Path scripts/architecture/legacy-surface-guard-matrix.json,scripts/architecture/architecture-budget-ratchet.json -Pattern 'window.GAME_INSTANCE|GameRuntimePorts-fallbacks|allowLegacyFallback|getRuntimeFeatureTransition'` -> Guard-Matrix und Budget-Ratchet verankern `window.GAME_INSTANCE` sowie explizite GameRuntimePorts-Fallback-Helfer als Legacy-Surfaces)
- [x] 92.5.2 Referenzdoku, Test-Mapping und Folgeblock-Leitplanken fuer `V64`, `V81`, `V82` und `V86` auf denselben Ownership- und Sunset-Stand spiegeln. (abgeschlossen: 2026-04-15; evidence: `Select-String -Path docs/referenz/ai_architecture_context.md,.agents/test_mapping.md,docs/plaene/aktiv/V64.md,docs/plaene/aktiv/V81.md,docs/plaene/aktiv/V82.md,docs/plaene/aktiv/V86.md -Pattern '4\\.5\\.14|window\\.GAME_INSTANCE|GameRuntimePorts|getLegacyRuntime|getRuntimeFeatureTransition|allowLegacyFallback'` -> Referenzkontext, Test-Mapping und Folgeblock-Leitplanken spiegeln denselben Ratchet fuer Globals- und Fallback-Restnischen)

### 92.99 Abschluss-Gate
status: open
goal: Ownership-Folgeblock ohne neue Legacy-Backdoors schliessen
output: Gruene Architektur-, Plan- und Doku-Gates plus klarer Verbrauchsstand fuer Folgeblocks

- [ ] 92.99.1 `npm run architecture:report`, `npm run check:architecture:boundaries`, `npm run check:architecture:ratchet`, `npm run typecheck:architecture`, `npm run plan:check`, `npm run docs:sync` und `npm run docs:check` sind fuer den betroffenen Scope gruensicher.
- [ ] 92.99.2 Die dokumentierten Hotspots sind fuer den migrierten Scope reduziert oder klar als Restadapter markiert; neue Produktarbeit auf `V64`, `V81`, `V82` und `V86` braucht keine neuen Legacy-Backdoors.

Abgleich 2026-04-15 (Subphase `V92 92.2.1`): `src/application/session-runtime/SessionRuntimeCommandUseCases.js` kapselt jetzt die fuenf migrierten Runtime-Commands `apply_settings`, `start_match`, `return_to_menu`, `host_lobby` und `join_lobby` als kleine Application-Services. `src/application/session-runtime/SessionRuntimeCommandExecutor.js` behaelt damit nur noch Command-Normalisierung, Observability und Dispatch; die direkte Kopplung an Settings-, Session- und Multiplayer-Implementierungen ist aus dem Executor herausgezogen. Non-`*.99`-Verifikation bleibt bewusst beim Block-End-Gate; naechste offene V92-Subphase ist `92.2.2`.
Abgleich 2026-04-15 (Subphase `V92 92.2.2`): `src/application/session-runtime/SessionRuntimeCommandUseCases.js` traegt jetzt fuer den gesamten Runtime-Command-Katalog die gemeinsame Execution-Huelle fuer `received`/`completed`/`failed`-Observability, Settled-Results und Failure-Vertraege. `src/application/session-runtime/SessionRuntimeCommandExecutor.js` ist dadurch auf Command-Normalisierung plus Use-Case-Auswahl reduziert; `executeSessionRuntimeCommandResult()` fuehrt keinen parallelen Pfad mehr neben den Use-Cases. `tests/runtime-regressions.contract.test.mjs` verankert den neuen Use-Case-Observability-Schnitt fuer asynchrone Fehler und `invalid_command`; naechste offene V92-Subphase ist `92.3.1`.
Abgleich 2026-04-15 (Subphase `V92 92.3.1`): `src/shared/runtime/GameRuntimePorts.js` liest migrierte Runtime-Intents und Lifecycle-Pfade jetzt nur noch ueber Bundle-Handles (`runtimeCoordinator`, `runtimeFacade`) und nicht mehr produktiv ueber `game.runtimeCoordinator`/`game.runtimeFacade`. Fuer den noch nicht migrierten Arcade-/Recording-Scope bleiben explizit benannte Transition-Adapter erhalten (`getRuntimeFeatureTransitionCoordinator()` / `getRuntimeFeatureTransitionFacade()`), sodass die Restnische klar begrenzt ist. Der Session-Snapshot nutzt denselben Ownership-Schnitt ueber `RuntimeSessionContract`, `game.session` und den Bundle-State statt Legacy-Facade-Reads; `tests/runtime-regressions.contract.test.mjs` ratcheted den Sunset mit Negativtests fuer alte Fallbacks. Naechste offene V92-Subphase ist `92.3.2`.
Abgleich 2026-04-15 (Subphase `V92 92.3.2`): Recheck der zuletzt bearbeiteten Subphase `92.3.1` zeigte keine neue Legacy-Fallback-Luecke im migrierten `runtimeIntentPort`-/`lifecyclePort`-Scope; der offene Rest war der produktive Global-Read im Arcade-Menue. `src/core/GameRuntimeFacade.js` exponiert den Arcade-/Toast-/Store-Zugriff fuer das Menue jetzt als expliziten `runtimeAccess`-Schnitt, den `src/ui/MenuController.js` in den Menu-Binding-Context durchreicht. `src/ui/arcade/ArcadeMenuSurface.js` liest Runtime-State, Replay-Fallback und Settings-Store dadurch nicht mehr aus `window.GAME_INSTANCE` oder `window.GAME_RUNTIME`, sondern ueber denselben injizierten Runtime-Access-Pfad. `tests/runtime-regressions.contract.test.mjs` verankert den Ratchet gegen direkte produktive `GAME_INSTANCE`/`GAME_RUNTIME`-Reads; naechste offene V92-Subphase ist `92.4.1`.
Abgleich 2026-04-15 (Subphase `V92 92.4.1`): `src/ui/MatchFlowUiController.js` bleibt die stabile UI-Fassade, delegiert Lifecycle-Intents (`startRound`, `onRoundEnd`, `returnToMenu`) jetzt aber an `src/ui/MatchFlowLifecycleController.js`, Arcade-/Message-Overlay-Rendering an `src/ui/MatchFlowArcadeOverlayController.js` und Hunt-/Round-End-Telemetrie an `src/ui/MatchFlowTelemetryController.js`. Damit wachsen Round-End-, Return-to-Menu-, Intermission-/Post-Run-Overlay- und Feedback-Pfade nicht weiter in derselben Sammelklasse, waehrend `PauseOverlayController` und bestehende Call-Sites unveraendert konsumieren. `tests/runtime-regressions.contract.test.mjs` spiegelt den Split mit kleinen Contract-Checks fuer Lifecycle-Port-Delegation und den extrahierten Hunt-Feedback-Seam. Gating: `npm run plan:check`, `npm run docs:sync` und `npm run docs:check` gruen; `npm run build` bleibt in dieser Session an `check:eslint:touched-strict` fuer die bereits erweiterte Fremd-/Folgescope-Datei `src/core/GameRuntimeFacade.js` blockiert (`docs/Fehlerberichte/2026-04-15_v92-92.4.1-build-touched-strict-gameruntimefacade.md`). Naechste offene V92-Subphase ist `92.4.2`.
Abgleich 2026-04-15 (Subphase `V92 92.4.2`): Recheck der zuletzt bearbeiteten Subphase `92.4.1` zeigte eine Guard-Luecke (`ui -> state`) in `src/ui/MatchFlowLifecycleController.js`; der Lifecycle-Controller konsumiert Round-End- und Transition-Strategien jetzt injiziert ueber `src/ui/MatchFlowUiController.js` statt direkter State-Imports. `src/core/GameRuntimeFacade.js` haelt Arcade- und Recording-Unterfluesse im migrierten Scope nur noch als schmale Composition-/Forwarding-Seams. `src/core/runtime/GameRuntimeArcadeSupport.js` uebernimmt Arcade-Run-State, Controller-Aktivierung, Sudden-Death-/Replay-/Reward-Pfade und Round-/Match-End-Telemetrie, waehrend `src/core/runtime/GameRuntimeRecordingSupport.js` mit `createGameRuntimeRecordingFacadeSupport()` den Cinematic-Recording-, Dump-, Metrics- und Ghost-Clip-Zugriff als expliziten Dienst kapselt. `tests/runtime-regressions.contract.test.mjs` ratcheted Match-End-, Arcade- und Recording-Delegation; `npm run plan:check`, `npm run docs:sync`, `npm run docs:check` und `npm run build` sind gruen. Naechste offene V92-Subphase ist `92.5.1`.
Abgleich 2026-04-15 (Subphase `V92 92.5.1`): Die Guard-Matrix `scripts/architecture/legacy-surface-guard-matrix.json` fuehrt `window.GAME_INSTANCE` jetzt als eigene Diagnostics-only-Legacy-Surface und ratcheted `GameRuntimePorts-fallbacks` ueber explizite Helfer (`getLegacyRuntime*`, `getRuntimeFeatureTransition*`, `allowLegacyFallback=true`) statt ueber den bloessen Datei-/Factory-Namen. `scripts/architecture/architecture-budget-ratchet.json` friert denselben Ist-Stand fuer `window.GAME_INSTANCE` (nur `src/core/AppInitializer.js`, `src/core/main.js`) und `GameRuntimePorts-fallbacks` (nur `src/shared/runtime/GameRuntimePorts.js`, dokumentierter Restcaller `src/ui/MatchFlowTransitionHotspots.js`) als Budget ein, sodass neue Bypaesse im migrierten Scope frueh auffallen. `node scripts/check-architecture-boundaries.mjs`, `node scripts/check-architecture-ratchet.mjs` und `node scripts/check-architecture-metrics.mjs` sind gruensicher; naechste offene V92-Subphase ist `92.5.2`.
Abgleich 2026-04-15 (Subphase `V92 92.5.2`): `docs/referenz/ai_architecture_context.md` fuehrt mit Abschnitt `4.5.14` jetzt dieselbe Ratchet-Baseline fuer Folgeblocks: `window.GAME_INSTANCE` bleibt auf Bootstrap-Diagnostics begrenzt und `GameRuntimePorts`-Fallback-Helfer bleiben auf `src/shared/runtime/GameRuntimePorts.js` plus den dokumentierten Restcaller `src/ui/MatchFlowTransitionHotspots.js` beschraenkt. `.agents/test_mapping.md` spiegelt das als konkrete `architecture-guard`-Budgetregel und neue Pfad-zu-Guard-Kommandos fuer `src/shared/runtime/GameRuntimePorts.js`, `src/core/AppInitializer.js`, `src/core/main.js` und `src/ui/MatchFlowTransitionHotspots.js`. `docs/plaene/aktiv/V64.md`, `docs/plaene/aktiv/V81.md`, `docs/plaene/aktiv/V82.md` und `docs/plaene/aktiv/V86.md` konsumieren denselben Ownership-/Sunset-Stand jetzt explizit in DoD- oder Leitplankenform, sodass Folgearbeit keine neuen Runtime-Globals oder direkten Fallback-Helfer aufzieht. Naechste offene V92-Subphase ist `92.99.1`.
