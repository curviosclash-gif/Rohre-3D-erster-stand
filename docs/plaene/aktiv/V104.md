---
id: V104
title: Runtime- und UI-God-Object-Sunset mit Port-Zuschnitt
status: planned
priority: P2
owner: frei
depends_on:
  - V92.99
  - V103.99
blocked_by: []
affected_area: runtime-ui-god-object-sunset
scope_files:
  - src/core/runtime/GameRuntimeBundle.js
  - src/shared/runtime/GameRuntimePorts.js
  - src/shared/runtime/UiControllerRuntimePorts.js
  - src/core/GameRuntimeFacade.js
  - src/ui/UIManager.js
  - src/ui/UIStartSyncController.js
  - src/ui/UINavigationLifecycleController.js
  - src/ui/arcade/ArcadeVehicleManager.js
  - src/ui/MatchFlowUiController.js
  - src/shared/contracts/PlatformCapabilityRegistry.js
  - src/shared/contracts/SettingsRuntimeLimitsContract.js
  - src/platform/electron/ElectronPlatformBridge.js
  - src/platform/electron/ElectronShellLifecycleBridge.js
  - scripts/architecture/legacy-surface-guard-matrix.json
  - scripts/architecture/architecture-budget-ratchet.json
  - scripts/architecture-report.mjs
  - scripts/check-architecture-boundaries.mjs
  - scripts/check-architecture-metrics.mjs
  - scripts/check-architecture-ratchet.mjs
  - tests/runtime-regressions.contract.test.mjs
  - tests/platform-capabilities.contract.test.mjs
  - docs/referenz/ai_architecture_context.md
  - .agents/test_mapping.md
verification:
  - npm run architecture:report
  - npm run check:architecture:boundaries
  - npm run check:architecture:metrics
  - npm run check:architecture:ratchet
  - npm run typecheck:architecture
  - npm run graph:check
  - npm run plan:check
  - npm run docs:sync
  - npm run docs:check
updated_at: 2026-04-28
source_history: []
---

# V104 Runtime- und UI-God-Object-Sunset mit Port-Zuschnitt

## Ziel

Die nach `V91`, `V92` und `V103` sichtbar verbliebenen God-Object-Reste nicht nur weiter zu kapseln, sondern strukturell so weit zurueckzuschneiden, dass neue Produktarbeit nicht erneut in breite Runtime- und UI-Sammelmodule hineinwaechst.

Der Block fokussiert dazu vier bleibende Restflaechen:

- `GameRuntimeBundle` und `GameRuntimePorts` sollen keine bequeme oeffentliche Alias- und Fallback-Oberflaeche fuer neue Arbeit mehr sein.
- `UIManager`, `UIStartSyncController` und `UINavigationLifecycleController` sollen denselben Snapshot-/Intent-Pfad konsumieren statt ueber `this.game`, direkte Settings-Mutationen oder Reach-Through-Ketten zu wachsen.
- `MatchFlowUiController` und `ArcadeVehicleManager` sollen verbleibende breite Runtime-/State-Zugriffe auf kleine Feature-Ports und dedizierte Adapter reduzieren.
- `PlatformCapabilityRegistry` und angrenzende Plattformvertraege sollen Resolver bleiben; direkte `curviosApp`-/`__CURVIOS_APP__`-Reads gehoeren langfristig nur noch in dedizierte Plattformadapter.

Der nachhaltige Zielzustand ist erreicht, wenn Fachlogik zuerst ueber `Contract -> Snapshot -> Intent-Port -> Feature-Adapter` waechst und nicht mehr ueber `game.*`, Bundle-Aliasse, Global-Slots oder zentrale God-Objects.

## Nicht-Ziel

- Kein Arena-Rebuild-, Remount- oder Teardown-Stabilisierungsblock; dieser Scope bleibt in `V100`.
- Kein Security-/XSS-/Path-Traversal-Hardening-Block; diese Arbeit bleibt in `V102`.
- Kein Big-Bang-Rewrite grosser Dateien nur wegen Zeilenanzahl.
- Kein zweiter Runtime-Container, kein paralleles UI-Framework und kein neues Global-Slot-System.
- Kein Produkt- oder Visual-Redesign fuer Menues, HUD oder Browser-Demo.

## Definition of Done

- [ ] DoD.1 Die migrierten UI-Controller (`UIManager`, `UIStartSyncController`, `UINavigationLifecycleController`) enthalten im Blockscope keine produktiven `this.game`-Reach-Throughs mehr; Lesen und Schreiben laufen ueber Snapshots, Ports oder klar benannte Feature-Adapter.
- [ ] DoD.2 Die oeffentliche Alias- und Wrapper-Flaeche von `GameRuntimeBundle` ist reduziert; neue oder migrierte Aufrufer konsumieren keine breiten Bundle-Aliasse mehr als Fach-API.
- [ ] DoD.3 `MatchFlowUiController` und `ArcadeVehicleManager` nutzen im migrierten Scope schmale Controller-/Runtime-Ports statt breiter Runtime- oder State-Zugriffe.
- [ ] DoD.4 `PlatformCapabilityRegistry` und `SettingsRuntimeLimitsContract` lesen produktive Plattformzustandsdaten nicht mehr direkt aus `curviosApp`-/`__CURVIOS_APP__`-nahen Globals; diese Reads liegen in einem dedizierten Plattformadapterpfad.
- [ ] DoD.5 Architektur-Ratchets, Contract-Tests, Referenzdoku und Test-Mapping erzwingen denselben Sunset-Stand fuer `constructor(game)`, `this.game = game`, Bundle-Aliasse, `GameRuntimePorts`-Fallbacks und direkte Plattform-Globals.
- [ ] DoD.6 Die Scope-Abgrenzung gegen `V100` und `V102` ist dokumentiert und im migrierten Pfad werden weder Stabilitaets- noch Security-Themen erneut in God-Objects zurueckgeschoben.

## Risiken

- R1 | hoch | Versteckte Reach-Throughs in Menu-, Settings-, Overlay- oder Replay-Pfaden koennen den Alias-Sunset blockieren, wenn nicht alle realen Leser frueh inventarisiert werden.
- R2 | hoch | Ein zu aggressiver Alias- oder Port-Sunset bricht bestehende Diagnostics-, Developer- oder Plattformpfade, wenn Restadapter nicht bewusst markiert bleiben.
- R3 | mittel | Zu feinkoernige Ports erzeugen Port-Sprawl ohne Ownership-Gewinn, wenn der Schnitt nicht entlang echter Use-Cases erfolgt.
- R4 | mittel | `V100` und `V102` koennen dieselben Dateien tangieren; ohne saubere Blockgrenzen drohen Merge-Konflikte oder doppelte Planarbeit.
- R5 | mittel | Ein reiner Dateisplit ohne Guard- und Snapshot-Ratchet verschiebt das Problem nur und tarnt God-Objects hinter mehr Dateinamen.

## Erfuellte Abhaengigkeiten

- `V92.99` (hard): Ownership-, Port- und Legacy-Surface-Ratchet sind die bindende Baseline fuer weiteren Alias- und Fallback-Abbau.
- `V103.99` (hard): Der Settings-Persistenz- und Mutationsvertrag liefert den schmalen Store-/Result-Pfad, damit UI-Splits keine neuen `settingsManager.store`-Bypaesse reaktivieren.

## Scope-Abgrenzung zu Nachbarbloecken

- `V100` behaelt Arena-Rebuild, Remount und deduplizierte StartSync-Stabilisierung. `V104` uebernimmt nur den nachhaltigen Ownership-, Port- und Alias-Zuschnitt derselben Problemzone.
- `V102` behaelt Security-, Path-, XSS-, Body-Limit- und Tooling-Abdeckung. `V104` uebernimmt nur die Architektur- und Erweiterbarkeitssicht auf dieselben Hotspots.
- Produkt- oder Gameplay-Folgearbeit soll nach `V104` dieselben Ports und Snapshots konsumieren, statt neue Spezialpfade direkt an `UIManager`, `GameRuntimeBundle` oder `PlatformCapabilityRegistry` anzudocken.

## Offene Review-Follow-ups im Scope

- `P14`: Event-Listener-Duplikation in `UIStartSyncController.js` soll im neuen Port-/Lifecycle-Schnitt verschwinden statt nur lokal gepatcht zu werden.
- Architektureller Zuschnitt aus `P45`: Die Hotspot-Komplexitaet von `UIStartSyncController.js`, `UIManager`-nahen Flows und `ArcadeVehicleManager.js` wird hier aus Ownership-Sicht adressiert; Security-/Tooling-Anteile von `P45` bleiben bei `V102`.

## Phasen

### 104.1 Rest-Hotspots und Zielvertraege inventarisieren
status: open
goal: Verbleibende God-Object-Nutzung gegen reale Aufrufer und Zielpfade schneiden
output: Hotspot-Matrix fuer Runtime-Bundle, UI-Controller und Plattform-Resolver

- [ ] 104.1.1 Reale breite Konsumenten von `GameRuntimeBundle`, `GameRuntimePorts`, `UIManager`, `UIStartSyncController`, `UINavigationLifecycleController`, `ArcadeVehicleManager`, `MatchFlowUiController` und `PlatformCapabilityRegistry` inventarisieren: Alias-Reads, direkte Mutationen, Global-Zugriffe, Fallback-Helfer und implizite Reach-Throughs.
- [ ] 104.1.2 Zielmatrix fuer jeden Rest-Hotspot festlegen und in Blockdatei plus Architekturkontext spiegeln: welcher Snapshot, welcher Intent-Port, welcher Feature-Adapter und welcher Restadapter bis zum Sunset erlaubt bleibt; Scope-Grenzen zu `V100` und `V102` explizit mitschneiden.

### 104.2 Runtime-Bundle- und Port-Oeffentlichkeit verengen
status: open
goal: Bundle und Runtime-Ports verlieren ihre Rolle als breite Fach-API
output: Reduzierte Alias-/Wrapper-Flaeche und kleinere Fallback-Budgets

- [ ] 104.2.1 Migrierte Leser und Schreiber von Bundle-Aliassen oder Runtime-Wrappers auf explizite Read-/Write-Ports, Snapshots oder kleine Runtime-Services umstellen; neue Featurearbeit bekommt keinen Grund mehr, `GameRuntimeBundle` direkt als Sammelobjekt zu lesen.
- [ ] 104.2.2 `GAME_RUNTIME_LEGACY_ALIAS_SPECS`, `GAME_RUNTIME_LEGACY_WRAPPER_SPECS` und dokumentierte `GameRuntimePorts`-Transition-Fallbacks im migrierten Scope sichtbar verkleinern; verbleibende Reste bleiben nur als benannte Diagnostics- oder Migrationsadapter erlaubt.

### 104.3 Menu- und Start-Controller auf Snapshot-/Intent-Pfade heben
status: open
goal: UI-God-Objects entlang echter Bedien- und Rendering-Verantwortung schneiden
output: Schmale Menu-/Start-Seams statt `this.game`-Ketten

- [ ] 104.3.1 `UIManager`, `UIStartSyncController` und `UINavigationLifecycleController` auf einen gemeinsamen Menu-/Start-Snapshot sowie explizite Intent-Ports zuschneiden; direkte `this.game`-Reads, lokale Settings-Reach-Throughs und versteckte Bidirektionalitaet werden fuer den migrierten Scope entfernt.
- [ ] 104.3.2 Listener-, Toast-, Menu-Chrome-, Start-Setup- und Surface-Policy-Pfade so ordnen, dass neue UI-Funktionen zuerst in kleinen Ops-/Adapter-Modulen oder Ports landen statt die drei Controller wieder aufzublasen.

### 104.4 Sekundaere Hotspots und Plattform-Resolver fertigziehen
status: open
goal: Restbreite in Match-, Arcade- und Plattformpfaden beseitigen
output: Kleine Feature-Adapter und pure Resolver

- [ ] 104.4.1 `MatchFlowUiController` und `ArcadeVehicleManager` fuer den migrierten Scope auf kleine Controller- oder Runtime-Ports sowie Feature-Adapter reduzieren; direkte breite Runtime-/State-Zugriffe werden entfernt oder klar als Restadapter begrenzt.
- [ ] 104.4.2 `PlatformCapabilityRegistry`, `SettingsRuntimeLimitsContract` und die beteiligten Electron-Bridges so schneiden, dass direkte `curviosApp`-/`__CURVIOS_APP__`-Reads nur noch im dedizierten Plattformadapterpfad leben und Resolverfunktionen mit reinen Inputs/Snapshots arbeiten.

### 104.5 Ratchet, Contract-Tests und Erweiterungspfad absichern
status: open
goal: God-Object-Rueckfall fuer Folgearbeit technisch verhindern
output: Kleinere Budgets, klarere Tests und dokumentierter Erweiterungspfad

- [ ] 104.5.1 Guard-Matrix, Metrics und Ratchet fuer `constructor(game)`, `this.game = game`, Bundle-Aliasse, `GameRuntimePorts`-Fallbacks und direkte Plattform-Globals nachschaerfen, sodass der erlaubte Restbestand sinkt statt nur eingefroren bleibt.
- [ ] 104.5.2 Contract-Tests, Referenzdoku und Test-Mapping auf denselben bevorzugten Erweiterungspfad heben: `Contract -> Snapshot -> Intent-Port -> Feature-Adapter`, ohne neue God-Objects oder Global-Bypaesse.

### 104.99 Abschluss-Gate
status: open
goal: Nachhaltigen God-Object-Abbau gruensicher abschliessen
output: Reproduzierbare Evidence fuer Ownership-, Port- und Resolver-Sunset

- [ ] 104.99.1 `npm run architecture:report`, `npm run check:architecture:boundaries`, `npm run check:architecture:metrics`, `npm run check:architecture:ratchet`, `npm run typecheck:architecture`, `npm run graph:check`, `npm run plan:check`, `npm run docs:sync` und `npm run docs:check` sind gruen; erforderliche targeted-/contract-Tests sind gruen oder blockerfest dokumentiert.
- [ ] 104.99.2 Im migrierten Scope gibt es keine neuen produktiven `constructor(game)`-, `this.game = game`-, Bundle-Alias-, `GameRuntimePorts`-Fallback- oder direkten `curviosApp`-Reads; verbleibende Legacy-Surfaces sind namentlich als Diagnostics- oder eng begrenzte Migrationsadapter markiert.

## Sekundaerer Leseweg (V94)

- Primaere Quelle bleibt `docs/Umsetzungsplan.md` plus diese Blockdatei.
- Fuer Abhaengigkeits-, Scope- und Surface-Fragen kann zusaetzlich `docs/generated/knowledge-graph.json` bzw. `node scripts/query-knowledge-graph.mjs ...` genutzt werden.
- Der Block ist bewusst als Architektur-Follow-up zu `V91`, `V92` und `V103` zu lesen und trennt sich von `V100` (Stabilisierung) sowie `V102` (Security-Hardening) entlang derselben Graph- und Guard-Leitplanken.
