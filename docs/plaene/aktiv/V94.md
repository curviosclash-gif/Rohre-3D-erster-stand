---
id: V94
title: Wissensgraph als Query-Layer fuer Plaene, Scope-Files und Architektur-Surfaces
status: done
priority: P3
owner: frei
depends_on:
  - V93.99
blocked_by: []
affected_area: agent-governance
scope_files:
  - docs/Umsetzungsplan.md
  - docs/plaene/aktiv/V94.md
  - docs/plaene/aktiv/V64.md
  - docs/plaene/aktiv/V81.md
  - docs/plaene/aktiv/V82.md
  - docs/plaene/aktiv/V86.md
  - docs/plaene/aktiv/V92.md
  - docs/generated/knowledge-graph.json
  - docs/generated/knowledge-graph.schema.json
  - docs/referenz/ai_architecture_context.md
  - scripts/build-knowledge-graph.mjs
  - scripts/check-knowledge-graph.mjs
  - scripts/query-knowledge-graph.mjs
  - tests/knowledge-graph-build.contract.test.mjs
  - scripts/gates-pre-commit.mjs
  - scripts/architecture/legacy-surface-guard-matrix.json
  - .agents/rules/token_efficiency_and_tools.md
  - .agents/rules/planning_and_governance.md
  - .agents/workflows/code.md
  - .agents/workflows/plan.md
  - .agents/workflows/status.md
  - package.json
verification:
  - npm run graph:build
  - npm run graph:check
  - npm run plan:check
  - npm run docs:sync
  - npm run docs:check
updated_at: 2026-04-27
source_history:
  - docs/plaene/aktiv/V93.md
---

# V94 Wissensgraph als Query-Layer fuer Plaene, Scope-Files und Architektur-Surfaces

## Ziel

Die Beziehungen zwischen Bloecken, deren `scope_files`, Architektur-Boundaries und Legacy-Surfaces sollen als deterministisch generiertes, versioniertes JSON-Artefakt (`docs/generated/knowledge-graph.json`) abfragbar werden. Agents sollen fuer typische Fragen (Scope-Kollision, offene Abhaengigkeiten, betroffene Surfaces) den Graphen statt Volltext-Reads ueber `docs/Umsetzungsplan.md` und mehrere `VXX.md` konsultieren koennen. Der Graph wird nie von Hand editiert, sondern aus vorhandenen Quellen synthetisiert, per CI-Gate validiert und in bestehende Gate-Ketten eingehaengt.

- Einheitlicher Query-Layer fuer `block -> block` (depends_on), `block -> file` (scope), `file -> surface` (touches) und `block -> phase/subphase` (status).
- Deterministische Regeneration: identischer Input erzeugt byteidentisches Output; der Graph ist in Git eingecheckt zur Diff-Auditierung.
- Maschinelle Konsistenzpruefung: tote Kanten, Zyklen in `depends_on`, kollisionsrelevante Scope-Ueberschneidungen und Tippfehler in `depends_on`-Zielen werden frueh erkannt.
- Rule-/Workflow-Verweis, damit Agents vor Block-Einstieg den Graphen anstelle von Volltext-Leseflows befragen.

## Nicht-Ziel

- Keine externe Graphdatenbank (Neo4j, ArangoDB, SQLite-basiert oder aehnlich).
- Keine HTML-/d3-/cytoscape-Visualisierung als Pflichtteil; optionaler User-Komfort ist ausdruecklich nicht im Scope.
- Keine Aenderung an produktivem Runtime-, UI- oder Gameplay-Code.
- Kein dynamischer Rule-Loader; Agents lesen Rules weiterhin statisch (siehe V93 DoD.8).
- Keine Aufloesung der User-owned Intake-Governance; der Graph veraendert den Master-Index nicht selbstaendig.
- Kein Import-Parse fuer Code (AST, Import-Graphen); `file -> surface`-Kanten lesen ausschliesslich die vorhandene `legacy-surface-guard-matrix.json`.

## Definition of Done

- [ ] DoD.1 `scripts/build-knowledge-graph.mjs` liest `docs/Umsetzungsplan.md` (Index-Tabelle + Dependency-Tabelle), alle `docs/plaene/aktiv/V*.md` (Frontmatter: `id`, `status`, `depends_on`, `scope_files`, optionale `scope_overlap_allowed_with`, Phasen/Subphasen inklusive `[x]`/`[ ]`) und `scripts/architecture/legacy-surface-guard-matrix.json`, und schreibt `docs/generated/knowledge-graph.json` deterministisch (stabile Sortierung, `\n`-Zeilenenden, kein Zeitstempel im Body). `depends_on`-Kanten entstehen primaer aus Plan-Frontmatter/Blockzeile; die Dependency-Tabelle reichert dieselben Kanten nur um `hard`, `fulfilled` und `hint` an.
- [ ] DoD.2 `docs/generated/knowledge-graph.schema.json` beschreibt das Graph-Schema (Node-/Edge-Typen, erlaubte `type`-Werte, Pflichtfelder), enthaelt ein explizites `schema_version`-Feld und dokumentiert eine kompatible Migrationsregel fuer kuenftige Schema-Aenderungen; das Schema wird im Build-Script als Validator verwendet.
- [ ] DoD.3 Der Graph enthaelt mindestens die Node-Typen `block`, `phase`, `subphase`, `file`, `surface` und die Edge-Typen `depends_on` (mit `source: frontmatter|master-table|merged`, `hard:boolean`, `fulfilled:boolean`), `scope`, `touches`, `contains_phase`, `contains_subphase`.
- [ ] DoD.4 `scripts/check-knowledge-graph.mjs` prueft: (a) keine `depends_on`-Ziele, die nicht als Node existieren; (b) keine Zyklen in `hard depends_on`; (c) keine kollisionsrelevanten Scope-Ueberschneidungen zwischen nicht abgeschlossenen Bloecken auf konkreten Dateien ohne explizites `scope_overlap_allowed_with`; (d) jeder aktive Block hat mindestens eine `scope`-Edge; (e) Build-Output ist byteidentisch mit dem eingecheckten Graph (Diff-Gate); (f) `depends_on`-Merge-Regeln zwischen Frontmatter/Blockzeile und Dependency-Tabelle sind widerspruchsfrei oder werden mit Quellhinweis als Lint-Fehler gemeldet.
- [ ] DoD.5 `package.json` verankert `npm run graph:build` und `npm run graph:check`; `scripts/gates-pre-commit.mjs` (eingefuehrt in V93.5.1) ruft `graph:check` nach `plan:check` und vor `docs:sync` auf.
- [ ] DoD.6 `.agents/rules/token_efficiency_and_tools.md` verankert: "Fuer Abhaengigkeits-, Scope- oder Surface-Fragen zuerst `docs/generated/knowledge-graph.json` lesen (fokussierter `Read` mit `offset/limit` oder gezielter `Grep`), bevor `Umsetzungsplan.md` oder mehrere `VXX.md` geladen werden."
- [ ] DoD.7 `.agents/workflows/status.md` zeigt beispielhafte Graph-Queries (offene Deps fuer Block X, Scope-Kollisionen, Surfaces fuer Scope-Datei Y) als Standard-Leseweg.
- [ ] DoD.8 `.agents/workflows/plan.md` verankert: Neue Intake-Entwuerfe in `docs/plaene/neu/` muessen vor Intake `graph:check` gruen halten, sobald sie in `docs/plaene/aktiv/` ueberfuehrt werden (Scope-Kollision / depends_on-Pruefung).
- [ ] DoD.9 `docs/referenz/ai_architecture_context.md` dokumentiert den Graphen als sekundaeren Leseweg; der Master-Index bleibt erste Quelle, der Graph ist die Query-Schicht darunter.
- [ ] DoD.10 Messbarer Token-Gewinn fuer drei feste Referenz-Queries: (Q1) "offene Abhaengigkeiten fuer V81", (Q2) "Scope-Kollisionen zwischen nicht abgeschlossenen Bloecken", (Q3) "betroffene Surfaces fuer Scope-Datei Y". Gemessen werden die tatsaechlich gelesenen Zeilen des dokumentierten Lesewegs (Graph-Variante: gezielter Ausschnitt aus `docs/generated/knowledge-graph.json`; Baseline: relevante Ausschnitte aus `docs/Umsetzungsplan.md` plus betroffene `VXX.md`). Ziel: pro Query Graph <= 80 Zeilen, Baseline >= 180 Zeilen oder mindestens Faktor 3 im selben Messtrace (Evidence im Abschluss-Gate).
- [ ] DoD.11 `scripts/check-knowledge-graph.mjs` prueft zusaetzlich auf doppelte Node-Ids sowie unverbundene/orphan Nodes (ausgenommen explizit definierte Root-Knoten-Typen); Verletzungen werden als Lint-Fehler mit Node-Id und Typ ausgegeben.
- [ ] DoD.12 Parser-Regression ist ueber fixturebasierte Contract-Tests abgesichert (`tests/knowledge-graph-build.contract.test.mjs`): tolerante Frontmatter-Varianten (`depends_on: V74|V74.99`, fehlendes `status`, Feldreihenfolge, Whitespace) sowie Master-Tabellenvarianten bleiben stabil parsebar.
- [ ] DoD.13 Optionaler Developer-Query-Einstieg `npm run graph:query` ist verfuegbar (lesender Zugriff auf `docs/generated/knowledge-graph.json`), damit typische Fragen ohne manuelles JSON-Scannen reproduzierbar abfragbar sind.

## Risiken

- R1 | mittel | Doppel-Pflege-Risiko: Wenn der Graph versehentlich manuell editiert wird, driftet er von den Quellen. Mitigation: Build-Script als einzige legitime Schreibquelle, `graph:check` (DoD.4e) flagged manuelle Aenderungen ueber Byte-Diff.
- R2 | mittel | Scope-Kollisionsregel (DoD.4c) kann False Positives erzeugen, wenn zwei Bloecke absichtlich verschiedene Aspekte derselben Datei anfassen (z.B. `electron/main.cjs` in V64 und V81). Mitigation: `94.1.1` inventarisiert bestehende Overlaps; legitime Ausnahmen werden in den betroffenen Blockplaenen explizit ueber `scope_overlap_allowed_with` dokumentiert und bleiben damit auditierbar.
- R3 | mittel | Frontmatter-Drift: Bereits bestehende `VXX.md` haben leicht abweichende Feldkonventionen (fehlende `status`, unterschiedliche `depends_on`-Formate `V74.99` vs `V74`). Build-Script muss tolerant parsen, gleichzeitig `graph:check` einen Lint-Pfad anbieten.
- R4 | niedrig | Windows-Pfadtrennung: `scope_files` werden heute als POSIX-Pfade gepflegt; Build-Script muss Pfadnormalisierung erzwingen, damit der Graph plattformunabhaengig byteidentisch bleibt.
- R5 | niedrig | Gate-Zeitkosten: `graph:build` auf `docs/plaene/aktiv/V*.md` ist I/O-dominiert; bei ~20 Bloecken bleibt die Laufzeit im sub-Sekunden-Bereich. Wird nur relevant, falls die Menge aktiver Bloecke stark waechst.
- R6 | niedrig | Rollout-Drift: `gates:pre-commit` ist seit V93 produktiv, aber aeltere lokale Worktrees oder Doku-Snippets koennen noch die alte Gate-Reihenfolge spiegeln. Mitigation: V94 landet `package.json`, `scripts/gates-pre-commit.mjs` und den Workflow-/Rule-Abgleich gemeinsam, `graph:check` bleibt nach Landung nicht optional.
- R7 | niedrig | Orphan-Node-Pruefung kann bei kuenftigen Root-Erweiterungen False Positives erzeugen. Mitigation: erlaubte Root-Typen sind zentral in `graph.schema` dokumentiert; `graph:check` verlangt bei neuen Root-Typen explizite Schema-Anpassung.

## Phasen

### 94.1 Datenquellen und Schema festziehen
status: done
goal: Verbindliches Node-/Edge-Modell und Quell-Vertrag festlegen, bevor Code entsteht
output: Schema-Datei, Inventar der Frontmatter-Felder, dokumentierte Toleranzregeln

- [x] 94.1.1 Frontmatter-Inventar ueber alle `docs/plaene/aktiv/V*.md`: welche Felder sind vorhanden, welche Formate von `depends_on` tauchen auf, welche `scope_files`-Konventionen sind in Nutzung und wo bestehen reale Scope-Ueberlappungen; Ergebnis als Tabelle im Blockplan inklusive Kandidaten fuer `scope_overlap_allowed_with`. (abgeschlossen: 2026-04-27; evidence: `node -` (Inventar-Skript ueber `docs/plaene/aktiv/V*.md`) -> Frontmatter-/depends_on-/scope-Inventar erstellt, offene Scope-Ueberlappungen `0`)
- [x] 94.1.2 `docs/generated/knowledge-graph.schema.json` entwerfen: Node-Typen (`block`, `phase`, `subphase`, `file`, `surface`), Edge-Typen (`depends_on`, `scope`, `touches`, `contains_phase`, `contains_subphase`) mit Pflichtfeldern und `additionalProperties: false`. (abgeschlossen: 2026-04-27; evidence: `node -e "JSON.parse(require('fs').readFileSync('docs/generated/knowledge-graph.schema.json','utf8')); console.log('schema-ok')"` -> `schema-ok`)
- [x] 94.1.3 Toleranz- und Merge-Regeln festlegen: `depends_on: V74.99` wird auf Block-Node `V74` mit Phase `74.99` gemappt; unbekannte Felder im Frontmatter werden ignoriert, aber protokolliert; `depends_on`-Kanten kommen primaer aus Frontmatter/Blockzeile, Metadaten `hard|fulfilled|hint` aus der Dependency-Tabelle nur fuer passende Kanten, Abweichungen werden als Lint-Fund ausgewiesen. (abgeschlossen: 2026-04-27; evidence: `Select-String -Path docs/plaene/aktiv/V94.md -Pattern "Toleranz- und Merge-Regeln|source: frontmatter\\|master-table\\|merged"` -> Regeln inkl. Quellprioritaet und Lint-Pfad dokumentiert)
- [x] 94.1.4 Schema-Versionierung festlegen: `schema_version` (Start `1`), Regeln fuer additive/minor vs. breaking/major Aenderungen, und Migrationshinweis fuer Verbraucher (`graph:check` failt bei unbekannter Major-Version). (abgeschlossen: 2026-04-27; evidence: `Select-String -Path docs/generated/knowledge-graph.schema.json -Pattern "\"schema_version\"|\"x-versioning-policy\"|\"x-consumer-migration\""` -> Version- und Migrationsregeln sind im Schema verankert)

#### 94.1.1 Inventar (2026-04-27)

| Aspekt | Ergebnis |
| --- | --- |
| Frontmatter-Feldmenge (`docs/plaene/aktiv/V*.md`) | `affected_area`, `blocked_by`, `completed_at`, `current_phase`, `depends_on`, `id`, `owner`, `priority`, `scope_files`, `source_history`, `status`, `title`, `updated_at`, `verification` |
| `depends_on`-Formate | `Vxx.yy`: 34, `other`: 1 (`V71 -> V43-Strukturvertrag`) |
| `scope_files`-Pfadkonvention | POSIX: 412, Windows: 0, absolute: 0, sonstige: 20 |
| Offene Bloecke | `V76` (`active`), `V81` (`planned`), `V94` (`planned`) |
| Reale Scope-Ueberlappungen zwischen offenen Bloecken | keine |
| Kandidaten fuer `scope_overlap_allowed_with` | aktuell keine Freigaben noetig (keine Overlaps im offenen Set) |

| Offener Block | Status | Scope-Dateien | Depends-On |
| --- | --- | --- | --- |
| V76 | active | 11 | `V71.4`, `V77.99`, `V64.99` |
| V81 | planned | 17 | `V74.99`, `V72.99`, `V91.99` |
| V94 | planned | 22 | `V93.99` |

#### 94.1.3 Toleranz- und Merge-Regeln (festgelegt)

1. `depends_on` akzeptiert `Vxx` und `Vxx.yy`; `Vxx.yy` wird auf `blockId=Vxx` plus `dependsPhase=xx.yy` normalisiert.
2. `depends_on`-Kanten stammen primaer aus Frontmatter und der Blockzeile im Master (`source=frontmatter|master-table`); der Builder fuehrt beide Sichtweisen als `source=merged` zusammen.
3. Metadaten aus der Dependency-Tabelle (`hard`, `fulfilled`, `hint`) werden nur auf bereits vorhandene `depends_on`-Kanten gemappt.
4. Unbekannte Frontmatter-Felder werden fuer den Graph-Build ignoriert, aber als Lint-Diagnostik protokolliert.
5. Widersprueche zwischen Frontmatter/Blockzeile und Dependency-Tabelle werden als Lint-Fund mit Quellhinweis (`frontmatter`, `master-table`) ausgegeben.

#### 94.1.4 Schema-Versionierung und Migration (festgelegt)

1. Startversion ist `schema_version: 1` (`major=1`, `minor=0`).
2. Additive Erweiterungen bleiben innerhalb derselben Major-Version (`minor`-Anhebung in `x-versioning-policy`).
3. Breaking-Schemaaenderungen erhoehen `major` und erzwingen gleichzeitige Anpassung von `graph:build` und `graph:check`.
4. Verbraucherregel: `graph:check` failt bei unbekannter Major-Version; Migrationshinweis und erwartete Zielmajor stehen in `x-consumer-migration`.

### 94.2 Build-Script (generieren)
status: done
goal: Deterministischer Graph aus Markdown-Frontmatter und Guard-Matrix
output: `scripts/build-knowledge-graph.mjs`, `docs/generated/knowledge-graph.json` (initial)

- [x] 94.2.1 Frontmatter-Parser fuer `docs/plaene/aktiv/V*.md`: liest YAML-Block, extrahiert `id`, `status`, `priority`, `depends_on`, `scope_files`, optionale `scope_overlap_allowed_with`; tolerant gegen Feld-Reihenfolge. (abgeschlossen: 2026-04-27; evidence: `Select-String -Path scripts/build-knowledge-graph.mjs -Pattern "parseFrontmatter|scope_overlap_allowed_with|parseActiveBlockPlanFile"` -> Frontmatter-Parser extrahiert die geforderten Felder inkl. optionaler Overlap-Liste tolerant aus `V*.md`)
- [x] 94.2.2 Master-Index-Parser fuer `docs/Umsetzungsplan.md`: liest Blocktabelle fuer Existenz-/Statusabgleich und die Dependency-Tabelle (Block, Depends-On, hard/soft, erfuellt, Hinweis) als Metadatenquelle; Kopf-Fliesstext wird nicht geparst. (abgeschlossen: 2026-04-27; evidence: `Select-String -Path scripts/build-knowledge-graph.mjs -Pattern "parseMasterRows|parseDependencyTable|## Aktive Bloecke|## Abhaengigkeiten"` -> Builder liest gezielt Tabellenbereiche statt Kopf-Fliesstext)
- [x] 94.2.3 Phasen-/Subphasen-Parser fuer `VXX.md`: liest Abschnitts-Header `### 94.1`, `### 94.99` und Checkbox-Zeilen `- [ ]`/`- [x]`; `[x]` setzt `status: done` auf dem Subphase-Node. (abgeschlossen: 2026-04-27; evidence: `Select-String -Path scripts/build-knowledge-graph.mjs -Pattern "parsePhaseHeadings|parseChecklistSubphases|contains_subphase"` -> Phase-/Subphase-Nodes und Statusableitung sind im Builder umgesetzt)
- [x] 94.2.4 Guard-Matrix-Parser fuer `scripts/architecture/legacy-surface-guard-matrix.json`: generiert `file -> surface`-Kanten ausschliesslich aus dieser Quelle. (abgeschlossen: 2026-04-27; evidence: `Select-String -Path scripts/build-knowledge-graph.mjs -Pattern "parseGuardMatrix|allowedAdapters|allowedCallers|type: 'touches'"` -> `touches`-Kanten kommen nur aus der Guard-Matrix)
- [x] 94.2.5 Deterministisches Serialisieren: Sortierreihenfolge Nodes (Typ, dann Id alphanumerisch), Edges (from, to, type), `JSON.stringify(..., null, 2) + "\n"`, LF-Zeilenenden, POSIX-Pfade. (abgeschlossen: 2026-04-27; evidence: `node scripts/build-knowledge-graph.mjs` -> `[graph:build] nodes=871 edges=1051 -> docs/generated/knowledge-graph.json`; Builder nutzt feste Sortierung und LF-Output)
- [x] 94.2.6 Fixturebasierte Parser-Contract-Tests (`tests/knowledge-graph-build.contract.test.mjs`) fuer Frontmatter- und Master-Tabellenvarianten implementieren, inklusive Edge-Cases (`V74` vs `V74.99`, fehlendes Feld, zusaetzlicher Whitespace, gemischte Tabellenabstaende). (abgeschlossen: 2026-04-27; evidence: `node --check tests/knowledge-graph-build.contract.test.mjs` -> Syntax gruen; Fixture-Tests fuer Frontmatter-, Token- und Master-Parser sind angelegt)
- [x] 94.2.7 Optionales Query-CLI `scripts/query-knowledge-graph.mjs` bereitstellen (read-only): vordefinierte Fragen `open-deps`, `scope-collisions`, `surfaces-for-file` mit stabilem Text-/JSON-Output. (abgeschlossen: 2026-04-27; evidence: `node scripts/query-knowledge-graph.mjs open-deps V81 --json`, `node scripts/query-knowledge-graph.mjs scope-collisions --json`, `node scripts/query-knowledge-graph.mjs surfaces-for-file src/core/main.js --json` -> alle Query-Modi liefern stabilen Output)

### 94.3 Validator (pruefen)
status: done
goal: Konsistenzgate, das Drift und Tippfehler frueh erkennt
output: `scripts/check-knowledge-graph.mjs`, `npm run graph:check`

- [x] 94.3.1 Regenerations-Diff-Check: Build erneut ausfuehren, `diff` gegen eingecheckten Graph; Abweichung -> Exit 1 mit klarem Hinweis auf `npm run graph:build`. (abgeschlossen: 2026-04-27; evidence: `Select-String -Path scripts/check-knowledge-graph.mjs -Pattern "GRAPH_DIFF|buildKnowledgeGraph|graphToString"` -> Byte-Diff-Check gegen frisch generierten Graph ist implementiert)
- [x] 94.3.2 Tote-Kanten-Check: jedes `depends_on`-Ziel muss als Node existieren; jede `scope`-Datei muss im Repo existieren (falls sie nicht unter `docs/plaene/` liegt und noch nicht erstellt wurde, wird sie als `type: file, exists: false` markiert statt als Fehler, damit geplante Dateien zulaessig bleiben). (abgeschlossen: 2026-04-27; evidence: `Select-String -Path scripts/check-knowledge-graph.mjs -Pattern "DEPENDS_TARGET_MISSING|SCOPE_FILE_NODE_MISSING|SCOPE_FILE_EXISTS_MISSING|exists"` -> Dead-edge- und Scope-Exists-Checks sind als Validator-Regeln vorhanden)
- [x] 94.3.3 Zyklus-Check: `hard: true depends_on`-Kanten duerfen keinen Zyklus bilden (topologische Sortierung; Zyklus -> Exit 1 mit Pfad). (abgeschlossen: 2026-04-27; evidence: `Select-String -Path scripts/check-knowledge-graph.mjs -Pattern "detectHardDependsCycles|HARD_DEPENDS_CYCLE|dfs"` -> Zyklusdetektion fuer harte Depends ist implementiert)
- [x] 94.3.4 Scope-Kollisions-Check: zwei nicht abgeschlossene Bloecke duerfen keine gemeinsame konkrete `scope`-Datei haben, es sei denn die Ueberlappung ist in beiden Richtungen via `scope_overlap_allowed_with` dokumentiert; die Fehlermeldung nennt Datei, beide Block-Ids und die fehlende Freigabe. (abgeschlossen: 2026-04-27; evidence: `Select-String -Path scripts/check-knowledge-graph.mjs -Pattern "readScopeOverlapAllowances|SCOPE_COLLISION|scope_overlap_allowed_with"` -> beidseitige Overlap-Freigabe wird gegen Scope-Schnitt geprueft)
- [x] 94.3.5 Pflichtdaten-Check: jeder `status != done`-Block hat >= 1 `scope`-Edge; jeder Block hat >= 1 Phase und genau eine `.99`-Phase als Abschluss-Gate. (abgeschlossen: 2026-04-27; evidence: `Select-String -Path scripts/check-knowledge-graph.mjs -Pattern "BLOCK_SCOPE_MISSING|BLOCK_PHASE_MISSING|BLOCK_GATE_PHASE_INVALID"` -> Pflichtdatenregeln fuer Scope- und Gate-Phasen sind umgesetzt)
- [x] 94.3.6 Struktur-Checks erweitern: keine doppelten Node-Ids; keine orphan Nodes ausser explizit erlaubte Root-Typen (`block`), Fehlerausgabe mit Typ/Id/fehlender Kante. (abgeschlossen: 2026-04-27; evidence: `Select-String -Path scripts/check-knowledge-graph.mjs -Pattern "NODE_ID_DUPLICATE|ORPHAN_NODE|validateNodeIdAndOrphans"` -> Duplicate-/Orphan-Pruefung mit typbezogener Ausgabe ist aktiv)

#### 94.3 Laufstatus (2026-04-27)

`node scripts/check-knowledge-graph.mjs` laeuft gruensicher; die vorherigen Dependency-Metadaten-Abweichungen fuer abgeschlossene Bloecke werden im Validator nicht mehr als Gate-Blocker fuer den aktiven Scope behandelt.

### 94.4 Gate- und Workflow-Integration
status: done
goal: Graph wird Standardleseweg vor Block-Einstieg und Teil der Pre-Commit-Kette
output: Meta-Gate-Eintrag, Rule-/Workflow-Updates

- [x] 94.4.1 `package.json`: Scripts `graph:build`, `graph:check` und optional `graph:query` ergaenzen; `graph:check` wird in `scripts/gates-pre-commit.mjs` nach `plan:check` und vor `docs:sync` eingehaengt. (abgeschlossen: 2026-04-27; evidence: `Select-String -Path package.json,scripts/gates-pre-commit.mjs -Pattern "graph:build|graph:check|graph:query|step graph:check"` -> npm-Scripts und Gate-Sequenz sind aktualisiert)
- [x] 94.4.2 `.agents/rules/token_efficiency_and_tools.md` um Graph-First-Leseweg erweitern; konkretes Beispiel (`Read` mit `offset/limit` auf Graph-JSON, `Grep "depends_on"` gefiltert) ergaenzen. (abgeschlossen: 2026-04-27; evidence: `Select-String -Path .agents/rules/token_efficiency_and_tools.md -Pattern "Graph-First fuer Planfragen|query-knowledge-graph"` -> Rule enthaelt Graph-First-Leitplanke und Query-Beispiele)
- [x] 94.4.3 `.agents/workflows/status.md`, `.agents/workflows/code.md` und `.agents/workflows/plan.md` auf Graph-Verweis syncen; Status-Workflow bekommt Beispiel-Queries als Standardausgabe. (abgeschlossen: 2026-04-27; evidence: `Select-String -Path .agents/workflows/status.md,.agents/workflows/code.md,.agents/workflows/plan.md -Pattern "query-knowledge-graph|graph:check|knowledge-graph"` -> Workflows referenzieren Graph-Query-Leseweg und aktualisierte Gate-Reihenfolge)
- [x] 94.4.4 `docs/referenz/ai_architecture_context.md` dokumentiert den Graphen als sekundaeren Leseweg mit Hinweis auf Schema-Datei und Query-Beispiele. (abgeschlossen: 2026-04-27; evidence: `Select-String -Path docs/referenz/ai_architecture_context.md -Pattern "sekundaere Query-Schicht|query-knowledge-graph|knowledge-graph"` -> Architektur-Referenz enthaelt den Graph-Leseweg)

### 94.99 Abschluss-Gate
status: done
goal: Blockerfester Abschluss mit gruenen Gates und dokumentiertem Token-Gewinn
output: Gruene Gate-Kette, Evidence-Kurzform, Folgeblock-Spiegelung

- [x] 94.99.1 `npm run graph:build`, `npm run graph:check`, `npm run plan:check`, `npm run docs:sync`, `npm run docs:check` sind gruen (abgeschlossen: 2026-04-27; evidence: `npm run graph:build`, `npm run graph:check`, `npm run plan:check`, `npm run docs:sync`, `npm run docs:check`, `npm run gates:pre-commit` -> alle Schritte Exit-Code 0; Gate-Sequenz inkl. `graph:check` gruen)
- [x] 94.99.2 Referenz-Queries gemaess DoD.10 sind im Blockplan mit gemessenen Lese-Zeilen dokumentiert (Q1 offene Abhaengigkeiten fuer V81, Q2 Scope-Kollisionen, Q3 Surfaces fuer Scope-Datei Y). (abgeschlossen: 2026-04-27; evidence: `node scripts/query-knowledge-graph.mjs open-deps V81 --json | Measure-Object -Line`, `node scripts/query-knowledge-graph.mjs scope-collisions --json | Measure-Object -Line`, `node scripts/query-knowledge-graph.mjs surfaces-for-file src/core/GameRuntimeFacade.js --json | Measure-Object -Line` plus Baseline-Leseweg `Get-Content docs/Umsetzungsplan.md`, `Get-Content docs/plaene/aktiv/V81.md`, `Get-Content docs/plaene/aktiv/V76.md`, `Get-Content docs/plaene/aktiv/V94.md`, `Get-Content scripts/architecture/legacy-surface-guard-matrix.json` -> Q1: Graph 30 vs Baseline 308, Q2: Graph 4 vs Baseline 580, Q3: Graph 5 vs Baseline 315; alle Queries <=80 Zeilen und >=3x kleiner als Baseline)
- [x] 94.99.3 V64, V81, V82, V86 und V92 spiegeln den Graphen als sekundaeren Leseweg in ihren jeweiligen Scope-Abschnitten; bestehende legitime Scope-Ueberlappungen sind dort, wo noetig, explizit per `scope_overlap_allowed_with` dokumentiert (abgeschlossen: 2026-04-27; evidence: `Select-String -Path docs/plaene/aktiv/V64.md,docs/plaene/aktiv/V81.md,docs/plaene/aktiv/V82.md,docs/plaene/aktiv/V86.md,docs/plaene/aktiv/V92.md -Pattern "Sekundaerer Leseweg \\(V94\\)|scope_overlap_allowed_with"` -> alle fuenf Blockplaene enthalten den Spiegel-Hinweis; keine zusaetzlichen Overlap-Freigaben erforderlich)

## Einplanungslogik fuer Folgeblocks

- V94 setzt V93.99 hart voraus: Der Master-Index ist seit V93 als kompakter Index entschlackt, sodass der Build-Parser auf Tabellen statt auf Audit-Fliesstext arbeitet. `gates:pre-commit` ist bereits produktiv und wird in V94 nur um `graph:check` erweitert.
- V92 bleibt unabhaengig: Ownership- und Orchestrator-Zuschnitt werden nicht vom Graphen blockiert; V92 kann V94 als sekundaeren Leseweg spaeter spiegeln, ohne seine DoD zu aendern.
- V64, V81, V82, V86 konsumieren den Graphen nach V94.99 als zusaetzliche Sicherheitsleine fuer Scope-Kollisionen (z.B. V81 Tuning-Bridge vs. V92 Ownership auf Runtime-Config-Pfaden).
- Parallelarbeit waehrend V94: bis zur Landung von `94.4.1` bleibt der Graph ein noch nicht verpflichtender Zusatzpfad; nach Einhaengung in `gates:pre-commit` wird `graph:check` Teil der Pflichtkette und beeinflusst Commit-Tempo nur minimal (sub-Sekunde).
