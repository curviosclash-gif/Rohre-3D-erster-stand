---
id: V64
title: Desktop Hauptprodukt Multiplayer Online Offline Kompatibilitaet
status: planned
priority: P2
owner: frei
depends_on:
  - V74.99
  - V77.99
blocked_by: []
affected_area: desktop-multiplayer
scope_files:
  - electron/main.cjs
  - electron/static-server.cjs
  - .env.app
  - src/shared/contracts/RuntimeSessionContract.js
  - src/shared/contracts/MultiplayerSessionContract.js
  - src/shared/contracts/SignalingSessionContract.js
  - src/core/runtime/MenuRuntimeMultiplayerService.js
  - src/core/runtime/MenuRuntimeSessionService.js
  - src/core/runtime/RuntimeSessionLifecycleService.js
  - src/core/runtime/MatchStartValidationService.js
  - src/network/**
  - src/ui/UIManager.js
  - src/ui/UIStartSyncController.js
  - src/ui/menu/**
  - server/lan-signaling.js
  - server/signaling-server.js
verification:
  - npm run build:app
  - npm run plan:check
  - npm run docs:sync
  - npm run docs:check
updated_at: 2026-04-15
source_history:
  - docs/plaene/alt/Feature_Desktop_Hauptprodukt_Multiplayer_Online_Offline_Kompatibilitaet_V64.md
---

# V64 Desktop Hauptprodukt Multiplayer Online Offline Kompatibilitaet

## Ziel
Die Desktop-App als fuehrendes Produkt fuer Curvios Clash etablieren und den verbindlichen Multiplayer-Hauptpfad fuer `single`, `splitscreen`, `lan` und `online` stellen.

## Nicht-Ziel
- Kein globales Matchmaking, Presence-Backend oder Anti-Cheat-Scope.
- Kein Rueckfall auf `storage-bridge` als produktiven Multiplayer-Standard.
- Keine Browser-Host-Paritaet gegen das V77-Produktbild.

## Definition of Done
- [ ] DoD.1 Desktop ist der produktive Hauptpfad fuer `single`, `splitscreen`, `lan` und `online`.
- [ ] DoD.2 Der produktive Multiplayer-Menuefluss verwendet echte `lan`- oder `online`-Transporte.
- [ ] DoD.3 LAN und Online nutzen einen gemeinsamen fachlichen Session-, Lobby- und Match-Vertrag.
- [ ] DoD.4 Offline-Pfade teilen dieselben Match-, Loadout- und Lifecycle-Grundvertraege.
- [ ] DoD.5 App-Build, Signaling-Konfiguration und Diagnostik sind fuer Desktop reproduzierbar dokumentiert.
- [ ] DoD.6 Reconnect, Disconnect, Host-Rolle, Ready-Gates, `playerLoaded`, `stateUpdate` und Return-to-Menu sind kompatibel charakterisiert.
- [ ] DoD.7 Neue Multiplayer-Arbeit folgt der `V91`-/`V92`-Ratchet-Leitplanke: Commands/Snapshots/Capability-Resolver statt neuer Legacy-Surface-Reads (`game.runtimeBundle`, `game.runtimeFacade`, `window.GAME_RUNTIME`, `window.GAME_INSTANCE`, `curviosApp`/`__CURVIOS_APP__`, `getActiveRuntimeConfig` ausserhalb Config/Settings) oder direkter `GameRuntimePorts`-Fallback-Helfer (`getLegacyRuntime*`, `getRuntimeFeatureTransition*`, `allowLegacyFallback=true`).

## Risiken
- R1 | hoch | Menue und Runtime geraten in einen Transport-Mischzustand aus Alt- und Echttransport.
- R2 | hoch | Offline-Pfade regressieren waehrend des Multiplayer-Refactors.
- R3 | hoch | Lifecycle-Drift zwischen LAN und Online erzeugt schwer reproduzierbare Match-Fehler.
- R4 | mittel | App-Konfigurationsluecken brechen produktive Online-Nutzung trotz lokaler Entwicklung.

## Erfuellte Abhaengigkeiten (source_history fuer aus Master entfernte Deps)

- V74.99 (hard): Runtime-Entkopplung, Lifecycle-/Capability-/Composition-Grenzen; keine neuen `game.*`- oder private-Facade-Backdoors.
- V50/V52 Netzwerk-Baseline (hard): SessionAdapter-, Lobby-, Signaling- und `stateUpdate`-Grundvertraege bleiben verbindlich.
- V77.99 (hard): Surface-Policy `Vollversion hostet, Demo joint` verbindlich; V77.99 abgeschlossen 2026-04-15.
- V91.99 (soft): Legacy-Sunset und Guard-Ratchet aus V91 konsumieren, bevor neue Runtime-Surfaces wachsen; V91.99 abgeschlossen 2026-04-15.
- V92.99 (soft): Ownership-Zuschnitt aus V92 konsumieren, bevor neue Runtime-/UI-/Port-Hotspots wachsen; V92.99 abgeschlossen 2026-04-15.

## Vorabstand vor Blockstart
- Der Block bleibt trotz vorgezogener LAN-Arbeit `planned`; die harten Abhaengigkeiten `V74.99` und `V77.99` bleiben verbindlich.
- Vorgezogen umgesetzt in Commit `b029cb5`:
  - Desktop-Multiplayer kann im Menue bereits ueber echte LAN-Lobbyobjekte laufen statt nur ueber `storage-bridge` (`src/core/runtime/MenuRuntimeMultiplayerService.js`, `src/composition/core-ui/LanMenuMultiplayerBridge.js`).
  - `RuntimeSessionContract` und `RuntimeSessionLifecycleService` bilden `multiplayer + lan` bereits auf echte Runtime-Adapter mit Connection-Context ab (`src/shared/contracts/RuntimeSessionContract.js`, `src/core/runtime/RuntimeSessionLifecycleService.js`).
  - LAN-Signaling und Lobbyfluss decken bereits `create`, `ready`, `leave`, `invalidate-ready`, `match-start` und robusteren Statusabgleich ab (`src/shared/contracts/SignalingSessionContract.js`, `server/lan-signaling.js`, `src/network/LANMatchLobby.js`).
  - Der LAN-Client reused seine bestehende Lobby-Identitaet beim Runtime-Start; Match-Start-Orchestrierung wurde modular aus der Fassade geschnitten (`src/network/LANSessionAdapter.js`, `src/core/runtime/RuntimeMultiplayerFlowService.js`, `src/core/GameRuntimeFacade.js`).
- Noch offen trotz Vorabstand:
  - Produktmatrix und Capability-Modell fuer `Desktop Vollversion / Browser Demo`
  - bewusste `LAN`- oder `Online`-Wahl im produktiven Menuefluss
  - Online-Signaling/App-Konfiguration, `build:app`, Mehrinstanzen-Smokes und Abschluss-Gate

## Phasen

### 64.1 Produktentscheid und Transportmatrix festziehen
status: done
goal: Desktop-first-Multiplayer als klaren Produktvertrag formulieren
output: Host-, Join- und Offline-Matrix fuer Desktop und Browser

Leitplanke 2026-04-13 (Quelle: `V77 77.4.1`, `src/shared/contracts/PlatformCapabilityRegistry.js`): Die Surface-Policy exportiert jetzt die kanonische Transportmatrix fuer Folgearbeit: `desktop-app` bleibt produktisch auf echten `lan|online`-Transporten (`defaultMultiplayerTransport = lan`, Host und Join auf beiden), `browser-demo` fuehrt Multiplayer nur als explizite `join-only`-Rolle ueber `lan`, und `storage-bridge` bleibt dort ausschliesslich als dokumentierter Legacy-Pfad sichtbar. `V64` fuehrt keine zweite Produkt- oder Transport-Terminologie neben diesem Shared-Contract-Layer ein.
Leitplanke 2026-04-14 (Quelle: `V77 77.6.1`): V64 konsolidiert dieselbe Surface-Lesekette fuer Host-/Join- und CTA-Entscheide end-to-end: Produktrollen kommen ausschliesslich aus `resolveSurfacePolicy`/`resolveSurfaceMultiplayerGateAccess`, Browser-Demo bleibt `join-only`, und `host`-/`discover`-Signale duerfen in Browser-Flows weder als degradierte Host-Paritaet noch als impliziter Unlock erscheinen.
Leitplanke 2026-04-14 (Quelle: `V91 91.5.1`, Referenz 4.5.6): Folgearbeit nutzt fuer neue Runtime-Pfade den Startcheck `Contract -> Command/Event -> Snapshot -> Capability -> Sunset alter Pfade -> Desktop-vs-Demo`; neue Direktaufrufer auf Legacy-Surfaces sind im Multiplayer-Scope nicht zulaessig.
Leitplanke 2026-04-15 (Quelle: `V92 92.5.1`, Referenz 4.5.14): Multiplayer-Folgearbeit weitet weder die `window.GAME_INSTANCE`-Budgetnische (nur `src/core/AppInitializer.js`, `src/core/main.js`) noch die `GameRuntimePorts`-Fallback-Nische (nur `src/shared/runtime/GameRuntimePorts.js`, dokumentierter Restcaller `src/ui/MatchFlowTransitionHotspots.js`) aus. Neue Host-/Join-/Lifecycle-Pfade konsumieren Commands, Snapshots und Capability-Resolver statt Runtime-Globals oder direkter `getLegacyRuntime*`-/`getRuntimeFeatureTransition*`-Helfer.

- [x] 64.1.1 Den Produktentscheid `Desktop first fuer Multiplayer` in Rollen, Pflichten und Einschraenkungen fuer Desktop und Browser uebersetzen. (abgeschlossen: 2026-04-15; evidence: `src/shared/contracts/DesktopMultiplayerRoleContract.js` neu angelegt; `resolveDesktopMultiplayerRoleSurface()` liest `resolveSurfacePolicy`/`resolveSurfaceCapabilityAccess` und gibt `canHost`, `canJoin`, `hostTransports`, `joinTransports`, `offlineSessionTypes` zurueck; `resolveDesktopMultiplayerRoleMatrix()` deckt alle `PLATFORM_PRODUCT_SURFACE_IDS`-Eintraege ab; `npm run build` gruen)
- [x] 64.1.2 Eine transportneutrale Kompatibilitaetsmatrix fuer `single`, `splitscreen`, `lan` und `online` definieren. (abgeschlossen: 2026-04-15; evidence: `DESKTOP_MULTIPLAYER_COMPATIBILITY_MATRIX` und `DESKTOP_SESSION_TRANSPORT_REQUIREMENT`/`DESKTOP_TRANSPORT_NETWORK_REQUIREMENT` in `DesktopMultiplayerRoleContract.js`; deckt `single`/`splitscreen` (offlineCompatible=true) und `lan`/`online` (offlineCompatible=false, multiplayerTransportRequired) ab)

### 64.2 Session-Contract und Capability-Modell vereinheitlichen
status: done
goal: Session-, Transport- und Capability-Begriffe zentralisieren
output: Klarer Vertrag fuer Runtime, Menue und App-Shell

Hinweis 2026-04-01: `RuntimeSessionContract` bildet `multiplayer + lan` bereits auf `adapterSessionType = lan` ab; der eigentliche Capability- und Produktvertrag ist aber noch nicht abgeschlossen.
Leitplanke 2026-04-03 (Quelle: `docs/referenz/ai_architecture_context.md`, Abschnitt `4. Runtime-Vertraege`): `RuntimeSessionContract`, `MultiplayerSessionContract` und `SignalingSessionContract` bleiben der autoritative Shared-Contract-Layer fuer Session-, Transport- und Host- oder Join-Begriffe; Desktop-Shell-Faehigkeiten laufen ausschliesslich ueber kleine Preload- oder Shell-Capabilities statt ueber direkte Electron-, IPC- oder BrowserWindow-Zugriffe im Menue- oder Runtime-Code.

- [x] 64.2.1 `RuntimeSessionContract`, `MultiplayerSessionContract` und `SignalingSessionContract` fuer `sessionType`, `adapterSessionType`, `multiplayerTransport` und Host- oder Join-Capabilities als gemeinsamen Shared-Contract-Layer schaerfen. (abgeschlossen: 2026-04-15; evidence: `MULTIPLAYER_SESSION_ROLES` (HOST/CLIENT) in `RuntimeSessionContract.js` + `resolveRuntimeSessionCapabilities()` (canHost/canJoin transport-level); re-export in `MultiplayerSessionContract.js` + `SignalingSessionContract.js`; `SIGNALING_COMMAND_ROLE_MAP` + `resolveSignalingCommandRole()` in SignalingSessionContract; `MULTIPLAYER_HOST_AUTHORITATIVE_MESSAGE_TYPES` + helpers in MultiplayerSessionContract; `npm run build` gruen)
- [x] 64.2.2 `storage-bridge` fachlich auf Legacy- oder Fallback-Status zurueckstufen und hart markieren. (abgeschlossen: 2026-04-15; evidence: `git show --stat --oneline 604f536 -> fix(V64 64.2.2): hard-mark storage-bridge as legacy fallback`; `npm run build -> ok`; `npm run plan:check -> ok`; `npm run docs:sync -> updated=0`; `npm run docs:check -> ok`)

### 64.3 Menue- und Lobby-Pfad auf echte Transporte umstellen
status: done
goal: Produktiven Multiplayer aus dem Altpfad herausziehen
output: Echte LAN- und Online-Lobbyerzeugung im Menuefluss

Hinweis 2026-04-01: Der Desktop-Pfad nutzt bereits eine modulare LAN-Bridge mit echter Lobby-, Rollen-, Ready- und Match-Start-Anbindung; die bewusste Transportwahl im UI und der Online-Pfad fehlen noch.

- [x] 64.3.1 Den aktiven Multiplayer-Menuefluss so umbauen, dass Desktop-Nutzer bewusst `LAN` oder `Online` waehlen. (abgeschlossen: 2026-04-15; evidence: `index.html`, `MenuRuntimeSessionService.js`, `UIStartSyncController.js`, `MenuRuntimeMultiplayerService.js` -> explizite LAN/Online-Auswahl im Start-Setup, produktiver Default statt Legacy-Restwert, Online-Auswahl ehrlich als noch nicht verdrahteter Menu-Lobbypfad markiert; `npm run build` gruen; `npm run gates:pre-commit` gruen)
- [x] 64.3.2 Start-Validierung, Ready-Logik und Fehlermeldungen auf einen transportneutralen Vertrag heben. (abgeschlossen: 2026-04-15; evidence: `MatchStartValidationService.js` -> legacy-Transport-Meldung nutzt `sessionContract.transportAudienceLabel`, "nicht verbunden"-Meldung traegt aktiven Transport-Label; `MatchLobbySessionState.js` -> `MULTIPLAYER_SESSION_ROLES.HOST/CLIENT` statt hartcodierter Strings; `npm run build` gruen; `npm run gates:pre-commit` gruen)
- [x] 64.3.3 Lobby-Code-, Rollen- und Statusfluss an echte Lobby- oder Session-Objekte anbinden. (abgeschlossen: 2026-04-15; evidence: `MultiplayerMenuEventHandlers.js`, `MenuMultiplayerActionBindings.js`, `GameRuntimeFacade.js`, `UIManager.js`, `UIStartSyncController.js`, `OnlineMatchLobby.js` -> Leave-/Ready-Events korrekt verdrahtet, Lobby-Code-Feld waehrend aktiver Session read-only aus echter Session synchronisiert, Rollen-/Statusanzeigen lesen `joined|connected|pendingMatchCommandId` aus `getSessionState()`, `OnlineMatchLobby.getLocalPeerId()` schliesst den lokalen Peer-Kontext fuer echte Session-Objekte; `npm run build` gruen; `npm run gates:pre-commit` gruen)

### 64.4 Desktop-LAN-Host und Discovery produktisieren
status: done
goal: Eingebetteten LAN-Host robust und degradierbar machen
output: Entkoppelter Host-Start mit klarer Discovery- und Fehlerdiagnostik

Hinweis 2026-04-01: Discovery-gekoppelter Join sowie die Signaling-Routen fuer `create`, `ready`, `leave`, `invalidate-ready` und `match-start` sind vorgezogen; Start-/Stop-/Portdiagnostik und die explizite Entkopplung vom Fensterstart bleiben offen.

- [x] 64.4.1 Den eingebetteten LAN-Signaling-Host fuer Start, Stop, Portwahl und Fehlerdiagnostik haerten. (abgeschlossen: 2026-04-15; evidence: `electron/main.cjs`, `server/lan-signaling.js` -> Start/Stop serialisiert, Start-Timeout + Close-Timeout gehaertet, Portwahl faellt nach festen Ports auf ephemeren Fallback zurueck, `get-lan-server-status` liefert Diagnose-Snapshot inkl. `attemptedPorts`/`lastError`, `/discovery/info` exponiert `hostIp` + `diagnostics`; `node --check electron/main.cjs` gruen; Inline-Node-Smoke gegen `/discovery/info` gruen (`hostIp=127.0.0.1`, `diagnostics.selectedPortMode=ephemeral-fallback`); `npm run build` gruen; `npm run plan:check` gruen; Block-End offen: `npm run build:app`, `npm run docs:sync`, `npm run docs:check`)
- [x] 64.4.2 Discovery-, Host-IP- und Join-Flows robust gegen stale Broadcasts und Port-Konflikte machen. (abgeschlossen: 2026-04-15; evidence: `electron/main.cjs`, `src/application/session-runtime/NetworkLobbyService.js`, `src/application/session-runtime/NetworkLobbyDiscoveryResolver.js`, `src/application/session-runtime/NetworkLobbyServiceSupport.js`, `src/ui/menu/multiplayer/MenuMultiplayerHostIpResolver.js` -> Discovery-Cache dedupliziert Hosts jetzt auf `ip+lobbyCode` statt stale `ip+port`-Eintraege zu konservieren; Join-Aufloesung validiert Discovery-Treffer aktiv ueber `/discovery/info`, korrigiert Portwechsel ueber den Diagnose-Snapshot und liefert gezielte Fehlerbilder fuer stale Broadcasts oder mehrere gefundene Hosts; Host-IP-Resolver konsumiert `get-lan-server-status` direkt inkl. `hostIp`/`selectedPort`; `node --check electron/main.cjs` gruen; `npm run build` gruen; `npm run gates:pre-commit` gruen)
- [x] 64.4.3 Desktop-Fensterstart und LAN-Host-Start explizit ueber kleine Shell- oder Preload-Capabilities entkoppeln. (abgeschlossen: 2026-04-15; evidence: `electron/main.cjs` -> `createDesktopWindowShellCapability()` und `createLanHostShellCapability()` schneiden Fenster- und LAN-Host-Lifecycle explizit; `startDesktopShell()`, `second-instance`, Save-Dialoge und Discovery-Publish lesen nur noch die Window-Capability, waehrend `get-lan-server-status`/`start-lan-server`/`stop-lan-server` weiter ueber die kleine Host-Capability laufen; `node --check electron/main.cjs` gruen; `npm run build` gruen; `npm run gates:pre-commit` gruen)

### 64.5 Online-Signaling und App-Konfiguration abschliessen
status: done
goal: Online-Desktopbetrieb reproduzierbar machen
output: Klare Endpoint-, TURN- und Packaging-Strategie fuer die App

- [x] 64.5.1 `VITE_SIGNALING_URL`, TURN-Optionen, Overrides und Packaging-Dokumentation fuer Desktop definieren. (abgeschlossen: 2026-04-15; evidence: `.env.app`, `docs/referenz/desktop_online_signaling.md`, `README.md` -> Desktop-Online-Vertrag, Shell-Override-Strategie und Packaging-Regeln fuer `build:app`/`app:package` definiert; `npm run build:app` gruen; `npm run plan:check` gruen; `npm run docs:sync` -> updated=0)
- [x] 64.5.2 `OnlineMatchLobby` und `OnlineSessionAdapter` gegen Timeout, Retry, Reconnect und Endpoint-Fehlerbilder haerten. (abgeschlossen: 2026-04-15; evidence: `src/network/OnlineSignalingSupport.js` neu (typed errors, URL-Validierung, resolveRetryDelays, resolveConnectTimeoutMs, createSocketLifecycleError, createServerSignalingError, isRetryableSignalingError, createResumeSignalingEnvelope); `src/network/OnlineMatchLobby.js` + `src/network/OnlineSessionAdapter.js` auf OnlineSignalingSupport umgestellt: lokale Helpers entfernt, resolveOnlineSignalingUrl in create()/join()/connect(), isRetryableSignalingError im Retry-Loop, typed socket-/server-/timeout-Errors, SIGNALING_EVENT_TYPES-Konstanten statt Literale, CONNECTION_RESUMED und PLAYER_RECONNECTED behandelt, reconnect()-Methode mit RESUME_CONNECTION hinzugefuegt; `src/shared/contracts/SignalingSessionContract.js` um RESUME_CONNECTION/CONNECTION_RESUMED/PLAYER_RECONNECTED erweiterert; `npm run build` gruen; `npm run gates:pre-commit` gruen)

### 64.6 Match-Lifecycle und State-Sync angleichen
status: done
goal: LAN und Online ueber denselben Lifecycle-Kern fuehren
output: Einheitliche Gates fuer Start, Sync und Return-to-Menu

Hinweis 2026-04-01: Connection-Context, Runtime-Join-Reuse und Match-Start-Orchestrierung sind fuer den LAN-Pfad bereits modularisiert; Online-Angleichung, Characterization und Vollabdeckung der Lifecycle-Matrix fehlen noch.
Leitplanke 2026-04-03 (Quelle: `docs/referenz/ai_architecture_context.md`, Abschnitt `4. Runtime-Vertraege`): Match-Start, Pause, Disconnect und Return-to-Menu folgen auch fuer `lan` und `online` einem einzigen oeffentlichen Lifecycle-Kern; weder UI noch Shell fuehren parallele Exit-, Dispose- oder Session-Finalize-Pfade ausserhalb der definierten Lifecycle- und Capability-Ports ein.
Leitplanke 2026-04-07 (Quelle: `docs/referenz/ai_architecture_context.md`, Abschnitt `4.6.4.2 Verbrauchsleitplanken fuer V64 und V75`): Der seit `V87` gehaertete FINALIZING-/MENU-Vertrag bleibt auch fuer `lan` und `online` verbindlich. Multiplayer-Folgearbeit behandelt `return_to_menu` nur als Lifecycle-Command, wartet den expliziten `match_finalized`-/`menu_opened`-Abschluss ueber Runtime-Snapshots oder Events ab und fuehrt kein transport-, shell- oder lobby-spezifisches Session-Dispose, Menu-Reopen oder Cleanup ausserhalb dieses Pfads ein. Diagnostik konsumiert bounded Observability ueber den Shared-Store statt ueber eigene unbounded History- oder Legacy-Bypass-Puffer.

Hinweis 2026-04-04 (V84-Folgeverbrauch): Der kuenftige Netzwerklayer uebersetzt Host-/Client-Input in dieselben `run_profile`-, `seed_envelope`- und `input_frame`-Huellen wie Replay und Training und broadcastet nur serialisierbare `snapshot_envelope`-/`gameStateSnapshot`-Daten. `SessionRuntime` bleibt Besitzer von Transport, Lifecycle und Capability-Gates; direkte LAN-/Online-Sonderpfade im `MatchKernel`, Renderer oder UI sind ausgeschlossen.

- [x] 64.6.1 Session-Initialisierung, `playerLoaded`, `arena_start`, Pause, Disconnect und Return-to-Menu auf einen gemeinsamen Lifecycle-Kern verdichten, inklusive explizitem `finalizing -> match_finalized -> menu_opened`-Abschluss fuer `lan` und `online`. (abgeschlossen: 2026-04-16; evidence: `src/core/runtime/MultiplayerMatchLifecycleKernel.js` neu (attachMultiplayerLifecycleKernel/detachMultiplayerLifecycleKernel); `MULTIPLAYER_MESSAGE_TYPES.MATCH_LIFECYCLE_SIGNAL` + `MULTIPLAYER_LIFECYCLE_SIGNAL_TYPES` in `MultiplayerSessionContract.js`; `SessionAdapterBase.broadcastLifecycleSignal()`; `LANSessionAdapter` + `OnlineSessionAdapter` emittieren `matchLifecycleSignal`-Event; `RuntimeSessionLifecycleService` haengt Kernel fuer Client-Netzwerk-Sessions ein; `MatchFinalizeFlowService` broadcastet `MATCH_FINALIZED` vor Teardown (Host-only); `GameRuntimeFacade._lifecycleKernelHandlers` deklariert; `npm run build` gruen; `npm run gates:pre-commit` gruen)
- [x] 64.6.2 State-Reconciliation, Round-Start-Gates und Host-Autoritaet transportbedingt statt fachlich unterscheiden. (abgeschlossen: 2026-04-16; evidence: `MULTIPLAYER_MESSAGE_TYPES.PLAYER_ARENA_LOADED` + `ROUND_START_GATE` in `MultiplayerSessionContract.js`; `ROUND_START_GATE` in `MULTIPLAYER_HOST_AUTHORITATIVE_MESSAGE_TYPES`; `SessionAdapterBase._resolveHostPeerId()` + `notifyArenaLoaded()` + `broadcastRoundStartGate()`; `LANSessionAdapter._resolveHostPeerId()` returns `'host'`, `OnlineSessionAdapter._resolveHostPeerId()` delegates to `_findHostPeerId()`; both adapters handle `PLAYER_ARENA_LOADED` -> emit `playerLoaded`, `ROUND_START_GATE` -> emit `roundStartGate`; `waitForRuntimePlayersLoaded()` client path uses `notifyArenaLoaded()` + `roundStartGate` event, host path uses `broadcastRoundStartGate()`; `setupRuntimeHostFullStateSyncHandler()` reacts to `fullStateSyncNeeded` with immediate `sendStateToPeer()` snapshot; `npm run build` gruen; `npm run gates:pre-commit` gruen)
- [x] 64.6.3 Produktisierung ausschliesslich ueber oeffentliche Lifecycle- und Capability-Ports anbinden; keine parallelen Exit-, Dispose-, Cleanup- oder Shell-Sonderpfade zulassen. (abgeschlossen: 2026-04-16; evidence: `electron/main.cjs` -> graceful-close handshake (`request-graceful-close` / `graceful-close-ready`); `electron/preload.cjs` -> `createLifecycleContract()` mit `onGracefulClose`/`confirmGracefulClose` im `curviosApp`-Bundle; `src/platform/electron/ElectronPlatformBridge.js` -> `createElectronPreloadLifecycleAdapter()`; `src/platform/electron/ElectronShellLifecycleBridge.js` neu (duenner Adapter, nur Lifecycle-Sub-Vertrag, kein PlatformCapability-Kette); `src/core/AppInitializer.js` -> `attachShellLifecycleBridge()` abonniert `onGracefulClose` -> `game.dispose()` -> Lifecycle-Kern (GAME_DISPOSE) -> MATCH_FINALIZED-Broadcast -> `confirmGracefulClose()`; guard-matrix allowedAdapters + ratchet fuer ElectronShellLifecycleBridge aktualisiert; `npm run build` gruen; `npm run gates:pre-commit` gruen)

### 64.7 Offline-Kompatibilitaet schuetzen
status: done
goal: Lokale Modi gegen Netzwerk-Refactors absichern
output: Expliziter Schutz fuer Single- und Splitscreen-Kompatibilitaet

- [x] 64.7.1 `single` und `splitscreen` gegen Multiplayer-Refactor regressionssicher halten. (abgeschlossen: 2026-04-16; evidence: `tests/offline-session-compatibility.contract.test.mjs` neu (21 Tests, alle gruen); deckt RuntimeSessionContract-Aufloesung, resolveRuntimeSessionCapabilities (canHost/canJoin=false), DESKTOP_MULTIPLAYER_COMPATIBILITY_MATRIX (offlineCompatible=true), createRuntimeSessionAdapter->LocalSessionAdapter, MatchStartValidationService (kein Multiplayer-Gate fuer offline), waitForRuntimePlayersLoaded (sofortiger Return fuer LocalSessionAdapter) ab; in `test:contract` eingehaengt; `npm run test:contract` 190/191 gruen (T97 pre-existing); `npm run gates:pre-commit` gruen)
- [x] 64.7.2 Desktop ohne Internet als gueltigen Produktzustand fuer lokale Modi, LAN und sinnvolle Online-Fehlerbilder behandeln. (abgeschlossen: 2026-04-16; evidence: `src/shared/contracts/DesktopMultiplayerRoleContract.js` erweitert um `DESKTOP_CONNECTIVITY_STATES` (NO_NETWORK/LOCAL_NETWORK_ONLY/INTERNET) und `resolveDesktopConnectivityProfile()` (characterizes noNetworkSessionTypes, localNetworkOnlySessionTypes, internetRequiredSessionTypes je Konnektivitaetszustand plus deutschsprachige onlineUnavailableHint/lanOfflineHint); `src/network/OnlineSignalingSupport.js` erweitert um `signaling_network_unavailable` in NON_RETRYABLE_SIGNALING_ERROR_CODES und `createNetworkUnavailableSignalingError()` mit deutschem Fehlertext; `npm run build` gruen; `npm run gates:pre-commit` gruen)

### 64.8 Verifikationsmatrix und Rollout-Schutz
status: done
goal: Leichtgewichtige Characterization-Checks fuer die Transportmatrix vorbereiten
output: Fokussierter Desktop-Mehrinstanzen- und Contract-Scope

- [x] 64.8.1 Eine Verifikationsmatrix fuer `single`, `splitscreen`, `lan-host`, `lan-client`, `online-host` und `online-client` ableiten. (abgeschlossen: 2026-04-16; evidence: `tests/transport-verification-matrix.contract.test.mjs` neu (23 Tests, alle gruen); deckt DESKTOP_MULTIPLAYER_COMPATIBILITY_MATRIX (networkRequirement, offlineCompatible, splitscreenCompatible), resolveRuntimeSessionCapabilities (canHost/canJoin/isNetworkSession fuer alle 6 Szenarien), resolveDesktopConnectivityProfile (noNetwork/localNetworkOnly/internet-Zuordnung), DESKTOP_CONNECTIVITY_STATES, resolveDesktopMultiplayerRoleSurface fuer DESKTOP_APP und Cross-Contract-Konsistenzpruefungen ab; in `test:contract` eingehaengt; `npm run test:contract` 214/214 gruen; `npm run gates:pre-commit` gruen)
- [x] 64.8.2 Einen fokussierten Desktop-Mehrinstanzen-Smoke fuer Host, Join, Start und Disconnect pflegen. (abgeschlossen: 2026-04-17; evidence: `tests/desktop-multiinstance-smoke.contract.test.mjs` neu (29 Tests, alle gruen); deckt Host-Signaling-Kommandos (CREATE_LOBBY=host, ANSWER=host), Host-autoritative Message-Types (MATCH_LIFECYCLE_SIGNAL, ROUND_START_GATE, FULL_STATE_SYNC, HOST_LEAVING), Client-only Message-Types (JOIN, RECONNECT), Join-Signaling (JOIN_LOBBY=client, OFFER=client), Match-Start-Sequenz (PLAYER_ARENA_LOADED nicht host-autorit., ROUND_START_GATE host-autorit., MATCH_LIFECYCLE_SIGNAL-Envelope), Lifecycle-Kernel (attach/detach, hostDisconnected->returnToMenu, match_finalized->returnToMenu, Kein Fire bei MENU-State), Cross-Instance-Konsistenz (LEAVE=both, ICE=both, READY=both, LAN- und Online-Host symmetrisch); in `test:contract` eingehaengt; `node --test tests/desktop-multiinstance-smoke.contract.test.mjs` -> 29/29 gruen; Hinweis: Echter Electron-Mehrinstanzen-Smoke via Playwright bleibt durch P24 (spawn EPERM Windows) blockiert und ist fuer 64.99 vermerkt)

### 64.99 Abschluss-Gate
status: open
goal: Desktop-Multiplayer ohne Produkt- oder Surface-Widerspruch schliessen
output: Gruene App-, Plan- und Doku-Gates plus dokumentierte Infra-Restpunkte

- [ ] 64.99.1 Kein produktiver Standardfluss fuehrt mehr ueber `storage-bridge`; Browser- und Desktop-Rollen sind widerspruchsfrei.
- [ ] 64.99.2 `npm run build:app`, direkt betroffene Netzwerk- oder Desktop-Smokes sowie `npm run plan:check`, `npm run docs:sync` und `npm run docs:check` sind abgeschlossen.
