---
id: V91
title: Architektur-Ratchet und Legacy-Surface-Sunset
status: planned
priority: P2
owner: frei
depends_on:
  - V87.99
  - V77.99
blocked_by: []
affected_area: architecture-ratchet-and-legacy-sunset
scope_files:
  - src/core/runtime/GameRuntimeBundle.js
  - src/core/GameBootstrap.js
  - src/core/main.js
  - src/core/runtime/GameRuntimeCoordinator.js
  - src/core/GameRuntimeFacade.js
  - src/shared/runtime/GameRuntimePorts.js
  - src/core/runtime/ActiveRuntimeConfigStore.js
  - src/core/Config.js
  - src/core/settings/**
  - src/ui/**
  - src/state/**
  - src/platform/**
  - src/shared/contracts/**
  - scripts/architecture-report.mjs
  - scripts/check-architecture-boundaries.mjs
  - scripts/check-architecture-touched-strict.mjs
  - scripts/check-architecture-ratchet.mjs
  - scripts/check-architecture-metrics.mjs
  - tests/platform-capabilities.contract.test.mjs
  - tests/runtime-regressions.contract.test.mjs
  - docs/referenz/ai_architecture_context.md
  - docs/referenz/ai_project_onboarding.md
  - .agents/test_mapping.md
verification:
  - npm run architecture:report
  - npm run check:architecture:boundaries
  - npm run check:architecture:touched-strict
  - npm run check:architecture:metrics
  - npm run check:architecture:ratchet
  - npm run typecheck:architecture
  - npm run plan:check
  - npm run docs:sync
  - npm run docs:check
updated_at: 2026-04-14
source_history:
  - docs/plaene/neu/Feature_Legacy_Runtime_Surface_Sunset_V91.md
---

# V91 Architektur-Ratchet und Legacy-Surface-Sunset

## Ziel

Die nach `V74`, `V83`, `V84`, `V87` und `V89` bewusst verbliebenen Legacy-Runtime-Surfaces sowie nur dokumentarisch abgesicherte Ownership-Regeln so weit haerten, dass neue Feature-Arbeit die Zielarchitektur nicht mehr ueber `game.*`, `runtimeFacade`, globale Runtime-Slots oder rohe Preload-Globals umgeht.

Der Block verbindet dazu vier Themen in einem gemeinsamen Ratchet:

- maschinenlesbare Guard-Matrix fuer Ownership, Importkanten und verbotene Legacy-Surfaces
- haertere Sunset-Regeln fuer `game.runtimeBundle`, `game.runtimeFacade`, `window.GAME_RUNTIME`, breite `GameRuntimePorts`-Fallbacks sowie `curviosApp`-/`__CURVIOS_APP__`-Reach-Throughs ausserhalb definierter Adapter
- kleine Contract-Tests fuer Lifecycle-, Capability- und Snapshot-Verbrauch statt rein dokumentarischer Leitplanken
- eine kurze Start-Checkliste fuer Folgefeatures, damit `V64`, `V81`, `V86` und spaetere Blocks denselben Architekturpfad benutzen
- Zuschnitt grosser Sammelmodule wie `PlatformCapabilityRegistry` und `UIManager`, wenn sie Ownership, Surface-Policy oder Architektur-Ratchet sichtbar erschweren
- Vereinheitlichung kleiner Shared-Contract-Helfer wie `normalizeString()` statt mehrfacher lokaler Kopien in Contract-Dateien

## Nicht-Ziel

- Kein Multiplayer-Produktisierungsblock; echte `lan`-/`online`-Produktpfade bleiben in `V64`.
- Kein Surface-Policy-Block; die Produktrollen `desktop-app` vs `browser-demo` bleiben primaer in `V77`.
- Kein pauschaler Rewrite aller grossen Dateien nur fuer Stil- oder Groessenfragen.
- Kein Browser-demo-first-Refactor; Desktop und gemeinsame Shared-Contracts bleiben die primaere Zielflaeche.

## Definition of Done

- [ ] DoD.1 Eine maschinenlesbare Guard-Matrix beschreibt fuer den migrierten Scope erlaubte Ownership, Importkanten und Legacy-Ausnahmen; Architektur-Checks lesen denselben Vertrag statt nur impliziter Heuristiken.
- [ ] DoD.2 Neue oder geaenderte produktive Dateien im migrierten Scope duerfen `game.runtimeBundle`, `game.runtimeFacade`, `window.GAME_RUNTIME`, rohe `curviosApp`-/`__CURVIOS_APP__`-Globals und breite `GameRuntimePorts`-Fallbacks nicht mehr als Fachpfad nutzen.
- [ ] DoD.3 UI-, State- und Runtime-Verbrauch fuer Lifecycle, Capability-Gating und Runtime-Config laeuft im migrierten Scope ueber Commands, Snapshots, benannte Capability-Adapter oder explizite Config-Injection.
- [ ] DoD.4 Kleine Contract- oder Modul-Tests decken die kritischen Architekturbegriffe `return_to_menu`, `match_finalized -> menu_opened`, Capability-Fallbacks und snapshotgetriebenes UI-Gating sichtbar ab.
- [ ] DoD.5 Referenzdoku, Onboarding, Test-Mapping und Folgeblocks `V64`, `V81` und `V86` konsumieren dieselbe Start-Checkliste und Sunset-Matrix fuer neue Feature-Arbeit.

## Risiken

- R1 | hoch | Ein zu frueher Sunset von Alias- oder Wrapper-Surfaces bricht versteckte Produkt-, Tooling- oder Diagnosepfade.
- R2 | hoch | Der Block kollidiert mit `V77` oder `V64`, wenn Surface-Policy und Multiplayer-Produktisierung nicht sauber abgegrenzt bleiben.
- R3 | mittel | Runtime-Config-Ownership kollidiert mit `V81`, falls Developer-Tuning weiterhin globale Active-Runtime-Slots voraussetzt.
- R4 | mittel | Zu grobe Verbotsregeln erzeugen Guard-Rauschen statt gezielter Architekturverbesserung.
- R5 | mittel | Ein zu grosser Architekturblock mischt sinnvolle Modul-Splits mit fachlicher Produktarbeit und erzeugt dadurch unnötige Merge-Konflikte.

## Phasen

### 91.1 Guard-Matrix und Sunset-Zielbild fixieren
status: done
goal: Dokumentierte Architekturregeln in pruefbare Guard-Daten ueberfuehren
output: Kanonische Matrix fuer Ownership, Legacy-Ausnahmen und Sunset-Ziele

- [x] 91.1.1 Die aktiven Legacy-Surfaces (`game.runtimeBundle`, `game.runtimeFacade`, `window.GAME_RUNTIME`, `GameRuntimePorts`-Fallbacks, `curviosApp`-/`__CURVIOS_APP__`-Reads, globale Runtime-Config-Slots) gegen reale Aufrufer und erlaubte Uebergangsadapter inventarisieren. (abgeschlossen: 2026-04-14; evidence: `git grep -n "game\\.runtimeBundle|game\\.runtimeFacade|window\\.GAME_RUNTIME|curviosApp|__CURVIOS_APP__|getActiveRuntimeConfig" -- src electron` und `git grep -n "GameRuntimePorts\\.js" -- src` -> produktive Aufrufer und Uebergangsadapter fuer alle sechs Legacy-Surfaces dokumentiert)
- [x] 91.1.2 Eine kleine, maschinenlesbare Guard-Matrix fuer Ownership, Importkanten, verbotene Globals und erlaubte Uebergangsadapter definieren und in Referenzdoku plus Guard-Konfiguration verankern. (abgeschlossen: 2026-04-14; evidence: `scripts/architecture/legacy-surface-guard-matrix.json` angelegt mit 5 Surfaces, je patterns/status/forbiddenForNewWork/allowedAdapters/allowedCallers/sunsetPhase/sunsetCriteria; Abschnitt 4.5.2 mit Kurzregeln in `docs/referenz/ai_architecture_context.md` verankert)

Inventar 2026-04-14 (`V91 91.1.1`):

| Legacy-Surface | Reale produktive Aufrufer (Ist) | Erlaubter Uebergangsadapter bis 91.2 |
| --- | --- | --- |
| `game.runtimeBundle` | `src/core/MatchSessionRuntimeBridge.js` (Fallback `this.game.runtimeBundle`), `src/state/RoundStateTickSystem.js` (`ports.lifecyclePort.returnToMenu`) | `GameRuntimeBundle` als Legacy-Container bleibt vorerst erlaubt; neue Aufrufer ausserhalb Runtime-/Bridge-Schicht sind nicht zulaessig |
| `game.runtimeFacade` + `window.GAME_RUNTIME` | `src/core/PlayingStateSystem.js`, `src/state/RoundStateTickSystem.js`, `src/ui/HudRuntimeSystem.js`, `src/ui/UINavigationLifecycleController.js`; Publish/Cleanup in `src/core/AppInitializer.js` und `src/core/main.js` | `GameRuntimeCoordinator`/`AppInitializer` duerfen den Debug-/Legacy-Handle noch publizieren, aber keine neuen Fachaufrufer mehr anbinden |
| `GameRuntimePorts`-Fallbacks | Port-Erzeugung aktuell nur in `src/core/GameBootstrap.js`; Fallbacks auf `game`/`runtimeFacade`/`runtimeCoordinator` liegen intern in `src/shared/runtime/GameRuntimePorts.js` | `createRuntimePorts()` bleibt Uebergangsadapter; neue Feature-Arbeit soll stattdessen Commands/Snapshots nutzen |
| `curviosApp` + `__CURVIOS_APP__` | Direkte Reads in `src/entities/ai/ObservationBridgePolicy.js` und `src/shared/contracts/PlatformCapabilityRegistry.js`; Adapterzugriff in `src/platform/electron/ElectronPlatformBridge.js` | Nur der Plattformadapter (`src/platform/electron/**`) ist langfristig erlaubt; direkte Reads bleiben bis 91.3 als explizite Migrationsschuld markiert |
| Globale Runtime-Config-Slots (`ActiveRuntimeConfigStore`) | `src/core/Config.js` und `src/core/settings/SettingsSanitizerOps.js` lesen `getActiveRuntimeConfig`; `src/core/runtime/GameRuntimeBundle.js` markiert den Adapter in `runtimeConfigAdapter` | `ActiveRuntimeConfigStore` bleibt nur als dokumentierter Uebergangsadapter fuer Runtime-/Settings-Scope erlaubt, bis Snapshot/Injection-Pfad in 91.3 gezogen ist |

Gate-Hinweis 2026-04-14: `npm run plan:check`, `npm run docs:sync` und `npm run docs:check` sind gruen; `npm run build` bleibt in dieser Umgebung mit `Error: spawn EPERM` blockiert (siehe `docs/Fehlerberichte/2026-04-14_v91-build-vite-spawn-eperm.md`).

### 91.2 Harte Verbotsregeln und Ratchet erweitern
status: done
goal: Neue Backdoors technisch frueh blockieren statt nur dokumentieren
output: Boundary-, touched-strict-, metrics- und ratchet-Regeln fuer Legacy-Surfaces

- [x] 91.2.1 `check:architecture:boundaries` und `check:architecture:touched-strict` so erweitern, dass neue produktive Reads auf verbotene Legacy-Surfaces im migrierten Scope direkt fehlschlagen. (abgeschlossen: 2026-04-15; evidence: `collectLegacySurfaceFindings()` in `ArchitectureAnalysis.mjs` liest `legacy-surface-guard-matrix.json`, alle vier Surfaces mit `forbiddenForNewWork:true` werden in boundaries und touched-strict geprueft; aktuell 0 Violations)
- [x] 91.2.2 `architecture:report`, `check:architecture:metrics` und `check:architecture:ratchet` um sichtbare Zaehler fuer Legacy-Surface- und Global-Slot-Verbrauch erweitern, damit Altlasten nicht wieder anwachsen. (abgeschlossen: 2026-04-15; evidence: `architecture-budget-ratchet.json` enthaelt 4 neue Legacy-Surface-Baseline-Eintraege; `check:architecture:metrics` und `check:architecture:ratchet` zeigen alle 4 Surfaces mit `OK: at-baseline`; `architecture:report` zeigt eigenen Abschnitt `Legacy-surface usage`)
- [x] 91.2.3 Die aktuelle `PlatformCapabilityRegistry` in Daten-, Resolver- und Policy-Verbrauch schneiden, sodass neue Surface- oder Capability-Arbeit keinen zentralen God-Object-Hotspot weiter vergroessert. (abgeschlossen: 2026-04-15; evidence: `PlatformCapabilityData.js` als reine Datenschicht angelegt mit allen PLATFORM_*-Konstanten und PLATFORM_CAPABILITY_REGISTRY; `PlatformCapabilityRegistry.js` importiert aus Data und behaelt alle Resolver; alle bestehenden Import-Sites unveraendert)

Recheck 2026-04-14: `91.2.3` bleibt gegen DoD konsistent (Datenschicht und Resolver-Layer sind sauber getrennt, keine neue Guard-Luecke). Verbleibende Unsauberkeit war ein zu breiter impliziter Legacy-Fallback in `GameRuntimePorts`/`GameRuntimeCoordinator`/`GameRuntimeFacade`; dieser Restpfad wird in `91.3.1` auf explizite Adapterrollen reduziert.

### 91.3 Verbrauchspfade auf Commands, Snapshots und Injection ziehen
status: open
goal: Reach-Throughs in Runtime-, UI- und State-Pfaden abbauen
output: Schlankerer Fachverbrauch ohne breite Legacy-Surfaces

- [x] 91.3.1 `GameRuntimeFacade`, `GameRuntimeCoordinator` und `GameRuntimePorts` fuer den migrierten Scope auf explizite Forwarding-, Diagnostics- oder Restadapterrollen reduzieren. (abgeschlossen: 2026-04-14; evidence: `Select-String -Path src/shared/runtime/GameRuntimePorts.js,src/core/runtime/GameRuntimeCoordinator.js,src/core/GameRuntimeFacade.js,tests/runtime-regressions.contract.test.mjs -Pattern 'resolveRuntimeIntentAdapter|legacy-game-slot|getUiManager\\(\\) \\{ return this.getRuntimeHandle\\('` -> explizite Adapter-Reihenfolge und neue Regression-Tests fuer Restadapter-Vertrag verankert)
- [x] 91.3.2 UI-, State- und Config-Consumer auf `session_runtime_snapshot`, `match_flow_snapshot`, `platform_capability_snapshot` oder explizite Runtime-Config-Injection umstellen, statt globale `game.*`- oder `ActiveRuntimeConfigStore`-Reads zu vertiefen. (abgeschlossen: 2026-04-14; evidence: `game.runtimeFacade`-Reads aus PlayingStateSystem/RoundStateTickSystem/HudRuntimeSystem/UINavigationLifecycleController auf `arcadePort.tickSuddenDeath`/`lifecyclePort.restartRound+returnToMenu`/`runtimeIntentPort.handleMenuPanelChanged` migriert; `getActiveRuntimeConfig`-Import aus SettingsSanitizerOps entfernt und durch direkten CONFIG-Proxy-Read ersetzt; Ratchet-Baselines runtimeFacade=2 und ActiveRuntimeConfigStore=3 gesetzt; alle 4 Architecture-Gates gruen)
- [x] 91.3.3 Doppelte Contract-Helfer wie `normalizeString()` bzw. `normalizeText()` in eine gemeinsame Shared-Contract-Utility ziehen, damit Normalisierung nicht in vielen Contract-Dateien driftet. (abgeschlossen: 2026-04-14; evidence: `src/shared/contracts/ContractNormalizeUtils.js` angelegt; 12 Contract-Dateien importieren jetzt `normalizeString`/`normalizeText` aus dem Utility statt lokale Kopien; CameraPerspectiveContract und RecordingCaptureContract behalten ihre Lowercase-Variante als explizit abweichende Semantik; alle Architecture-Gates gruen)
- [ ] 91.3.4 `UIManager`, `UINavigationLifecycleController` und angrenzende Sync-Controller so schneiden, dass Access-Context, Developer-State und Surface-Policy nicht mehr ueber bidirektionale Reach-Throughs oder versteckte Ripple-Ketten laufen.
- [ ] 91.3.5 Wiederkehrendes Boilerplate in den 7 Persistence-Stores (`SettingsStore`, `MenuDraftStore`, `MenuPresetStore`, `MenuTelemetryStore`, `MenuTextOverrideStore`, `VehicleManagerLoadoutPresets`, `ArcadeVehicleProfile`) in eine gemeinsame Factory oder Hilfsfunktion extrahieren (Version-Resolution, Reject-Early-Return, Fallback-Persistence — aktuell ~93 Zeilen Duplikat ueber 7 Dateien).
- [ ] 91.3.6 Stille `catch {}`-Bloecke in denselben 7 Persistence-Stores durch strukturiertes Logging ersetzen (mindestens Store-Name, Fehlerursache, Datenkontext); den vorhandenen `StoragePlatform.onMigrationResult`-Callback anbinden, damit Datenverlust bei korruptem LocalStorage nicht mehr unsichtbar bleibt.

Abgleich 2026-04-14 (`91.3.1`):

- `src/shared/runtime/GameRuntimePorts.js` fuehrt eine explizite Adapter-Aufloesung fuer Runtime-Intents ein: zuerst Runtime-Bundle-Handles (`runtimeCoordinator`, `runtimeFacade`), erst danach markierte Legacy-Restadapter (`game.runtimeCoordinator`, `game.runtimeFacade`).
- `createLifecyclePort()` nutzt denselben Intent-Resolver statt verteilter ad-hoc-Fallback-Ketten; der Restadapterpfad bleibt damit zentral und kontrollierbar.
- `src/core/runtime/GameRuntimeCoordinator.js` konsumiert fuer `getPorts()`, `getRuntimeFacade()` und `getUiManager()` keine rohen Runtime-Slots (`runtime.runtimePorts`, `runtime.runtimeFacade`, `runtime.uiManager`) mehr.
- `src/core/GameRuntimeFacade.js` liest `uiManager` nur noch ueber den Runtime-Bundle-Handle statt ueber direkte `game.uiManager`-Reach-Throughs.
- `tests/runtime-regressions.contract.test.mjs` deckt die neue Adapter-Leitplanke mit drei neuen Regression-Tests ab.
- Testausfuehrung fuer `node --test tests/runtime-regressions.contract.test.mjs` ist in dieser Umgebung mit `spawn EPERM` blockiert; non-`*.99`-Policy bleibt unveraendert (Test-Run deferred bis Block-Gate/geeignete Umgebung).

### 91.4 Lifecycle- und Capability-Contract-Tests nachziehen
status: open
goal: Kritische Architekturbegriffe ueber kleine Tests absichern
output: Schmaler Contract-Scope fuer Lifecycle-, Capability- und Gating-Vertraege

- [ ] 91.4.1 Kleine Contract- oder Modultests fuer `return_to_menu`, `match_finalized -> menu_opened`, Finalize-Blocking und Capability-Fallbacks auf denselben Shared-Contract-Vertrag ziehen.
- [ ] 91.4.2 Snapshotgetriebenes UI-Gating, Surface-Blocked-Feedback und degradierte Capability-Pfade so absichern, dass neue Feature-Arbeit keinen Reach-Through auf rohe Plattformobjekte braucht.

### 91.5 Folgeblock-Governance und Start-Checkliste verankern
status: open
goal: Architekturregeln fuer neue Features als wiederverwendbare Leitplanke sichtbar machen
output: Kurze Start-Checkliste plus synchronisierte Folgeblocks

- [ ] 91.5.1 Eine kurze Feature-Start-Checkliste fuer `Contract`, `Command/Event`, `Snapshot`, `Capability`, `Sunset alter Pfade` und `Desktop-vs-Demo` in Referenzdoku und Onboarding verankern.
- [ ] 91.5.2 `V64`, `V81`, `V86`, Test-Mapping und angrenzende Governance-Doku auf dieselbe Ratchet- und Sunset-Leitplanke spiegeln, damit neue Produktarbeit keine alten Runtime-Surfaces reaktiviert.

### 91.99 Abschluss-Gate
status: open
goal: Guard-, Doku- und Test-Ratchet fuer Folgearbeit gruensicher abschliessen
output: Gruene Architektur-Gates plus blockerfester Reststatus fuer verbleibende Restadapter

- [ ] 91.99.1 `npm run architecture:report`, `npm run check:architecture:boundaries`, `npm run check:architecture:touched-strict`, `npm run check:architecture:metrics`, `npm run check:architecture:ratchet`, `npm run typecheck:architecture`, `npm run plan:check`, `npm run docs:sync` und `npm run docs:check` sind fuer den betroffenen Scope gruensicher.
- [ ] 91.99.2 Verbleibende Legacy-Surfaces sind fuer den migrierten Scope entfernt, als Restadapter eng markiert oder blockerfest dokumentiert; neue Runtime- oder Plattform-Backdoors wurden nicht eingefuehrt.
