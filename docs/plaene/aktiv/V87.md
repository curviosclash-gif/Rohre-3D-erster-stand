---
id: V87
title: Runtime-Hardening-Followup
status: done
priority: P2
owner: frei
depends_on:
  - V83.99
blocked_by: []
affected_area: runtime-hardening
scope_files:
  - src/state/MatchLifecycleSessionOrchestrator.js
  - src/state/MatchLifecycleFinalizeRequest.js
  - src/state/MatchLifecycleSessionTransitionSupport.js
  - src/ui/MatchFlowUiController.js
  - src/core/runtime/GameRuntimeSessionHandler.js
  - src/core/runtime/RuntimeCommandSettingsService.js
  - src/application/session-runtime/SessionRuntimeCommandExecutor.js
  - src/ui/PauseOverlayController.js
  - src/ui/PauseOverlayIntentActions.js
  - src/shared/runtime/UiIntentAtomicity.js
  - src/shared/runtime/UiControllerRuntimePorts.js
  - src/shared/runtime/GameRuntimePorts.js
  - src/platform/browser/BrowserPlatformAdapters.js
  - src/platform/electron/ElectronPlatformBridge.js
  - src/platform/PlatformCapabilityAdapterSupport.js
  - src/shared/contracts/SessionRuntimeStateMachine.js
  - src/shared/runtime/SessionRuntimeObservability.js
  - src/state/storage/StoragePlatform.js
  - src/state/storage/StorageMigrationRegistry.js
  - src/ui/StorageKeys.js
  - src/core/GameRuntimeFacade.js
  - playwright.config.js
  - scripts/playwright-preview-server.mjs
  - scripts/run-playwright-targeted.mjs
  - package.json
  - tests/core.spec.js
  - tests/core-targeted.spec.js
  - tests/helpers.js
  - tests/playwright.global-setup.js
  - docs/referenz/ai_architecture_context.md
verification:
  - npm run architecture:report
  - npm run check:architecture:boundaries
  - npm run check:architecture:metrics
  - npm run check:architecture:ratchet
  - npm run typecheck:architecture
  - npm run plan:check
  - npm run docs:sync
  - npm run docs:check
updated_at: 2026-04-10
source_history:
  - docs/plaene/alt/Feature_Runtime_Hardening_Followup_V87.md
---

# V87 Runtime-Hardening-Followup

## Ziel

Die nach `V83` und waehrend `V84` sichtbar gebliebenen Runtime-Restschulden als eigenen Hardening-Block verankern, damit kritische Rennen und inkonsistente Lifecycle- oder Capability-Pfade nicht unzugeordnet zwischen Multiplayer-, Recorder- und Folgefeatures liegen bleiben.

## Nicht-Ziel

- Kein neuer Headless-Kernel- oder GameMode-Block; das bleibt in `V84`.
- Kein voller Multiplayer-Produktisierungsblock; das bleibt in `V64`.
- Kein Recorder-Export- oder Surface-Policy-Redesign; das bleibt in `V75` beziehungsweise `V77`.
- Keine neuen Legacy-Backdoors, um bekannte Rennen nur zu umgehen.

## Definition of Done

- [x] DoD.1 Kritische Matchstart-, Finalize- und Return-to-Menu-Rennen sind technisch oder vertraglich geschlossen. (abgeschlossen: 2026-04-10; evidence: Select-String -Path docs/plaene/aktiv/V87.md -Pattern "87.2.1|87.2.2" -> beide Lifecycle-/Finalize-Subphasen als abgeschlossen dokumentiert)
- [x] DoD.2 Pause-, Command- und Capability-Pfade melden Verfuegbarkeit, Fehler und Lifecycle-Zustand konsistent. (abgeschlossen: 2026-04-10; evidence: Select-String -Path docs/plaene/aktiv/V87.md -Pattern "87.3.1|87.3.2|87.4.1" -> Command-/Capability-/Pause-Haertung abgeschlossen dokumentiert)
- [x] DoD.3 Runtime-State-Machine und Observability decken die geharteten Uebergaenge ohne versteckte Bypass-Pfade ab. (abgeschlossen: 2026-04-10; evidence: Select-String -Path docs/plaene/aktiv/V87.md -Pattern "87.4.2" -> State-Machine-/Observability-Haertung abgeschlossen dokumentiert)
- [x] DoD.4 `V64`, `V75` und weitere Folgebloecke haben einen klaren, dokumentierten Runtime-Hardening-Stand statt offener Hochrisiko-Restschulden. (abgeschlossen: 2026-04-10; evidence: Select-String -Path docs/plaene/aktiv/V87.md -Pattern "87.5.1" -> Verbrauchs-/Dokuspiegel fuer Folgeblocks abgeschlossen dokumentiert)
- [x] DoD.5 Architektur-, Plan- und Doku-Gates sind fuer den betroffenen Scope synchronisiert. (abgeschlossen: 2026-04-10; evidence: npm run architecture:report && npm run check:architecture:boundaries && npm run check:architecture:metrics && npm run check:architecture:ratchet && npm run typecheck:architecture && npm run plan:check && npm run docs:sync && npm run docs:check -> PASS)

## Risiken

- R1 | hoch | Hardening oeffnet verdeckte Race-Conditions in Multiplayer-, Recorder- oder Menue-Rueckkehrpfaden.
- R2 | hoch | Ein zu breiter Follow-up-Scope verwischt wieder die Grenzen zu `V64`, `V75` oder `V77`.
- R3 | mittel | Observability und State-Machine bleiben zu locker, wenn nur Einzelfehler statt ganzer Uebergangspfade gehartet werden.
- R4 | mittel | Alte Workarounds oder Legacy-Adapter schleichen sich zurueck, wenn harte Capability- oder Finalize-Vertraege fehlen.

## Phasen

### 87.1 Restschuld-Inventar und Ownership fixieren
status: done
goal: Offene Runtime-Punkte gegen Code und Commits sauber abgleichen
output: Zuordnung von Restschuld, Zielmodul und Sunset-Kriterium

- [x] 87.1.1 Die offenen Review-Punkte aus Matchstart, Finalize, Pause, Commands, Capability-Bridge und State-Machine gegen aktuelle Dateien und Commits abgleichen. (abgeschlossen: 2026-04-05; evidence: git log -n 12 --oneline + git blame -L 290,478 -- src/state/MatchLifecycleSessionOrchestrator.js + git blame -L 110,153 -- src/application/session-runtime/SessionRuntimeCommandExecutor.js + git blame -L 26,152 -- src/shared/contracts/SessionRuntimeStateMachine.js + git show 5ed70f2 cce9a01 f3e7954 4a0b06d -> docs/plaene/aktiv/V87.md)
- [x] 87.1.2 Fuer jeden Restpunkt Zielmodul, Besitzerpfad und Sunset-Kriterium festlegen, damit keine unzugeordneten Runtime-Schulden im Master bleiben. (abgeschlossen: 2026-04-05; evidence: Select-String -Path docs/plaene/aktiv/V87.md -Pattern "87.1.2 Ownership- und Sunset-Matrix" -Context 0,16 + Select-String -Path docs/referenz/ai_architecture_context.md -Pattern "4.6.4.1 Ownership- und Sunset-Matrix" -Context 0,16 -> docs/plaene/aktiv/V87.md, docs/referenz/ai_architecture_context.md, docs/Umsetzungsplan.md)

#### 87.1.1 Review-Abgleich (2026-04-05)

| ID | Status nach Abgleich | Lebt aktuell in | Commit-Abgleich | Zielpfad in V87 |
| --- | --- | --- | --- | --- |
| P1 | Im aktuellen Stand nicht mehr offen; `createMatchSession()` vergibt `provisionalId` vor der asynchronen Arbeit und serialisiert neue Initialisierungen ueber `_pendingSessionInit`. | `src/state/MatchLifecycleSessionOrchestrator.js:294-331`, `src/state/MatchLifecycleSessionOrchestrator.js:307-309` | `4a3b6a8`, `c198a6d`, `d83ee83`; seitdem kein Commit, der die Queue oder ID-Vergabe wieder aufweicht | `87.1.1` (nur dokumentarisch uebernommen; aktuell kein neuer Fixpfad) |
| P2 | Offen; der Doppelklick-Guard setzt `_startMatchPromise` erst nach `_startMatchInternal()`, damit bleibt das Guard-Fenster nicht atomar. | `src/ui/MatchFlowUiController.js:683-693` | `2c30ef0` fuehrte das Muster ein; `7742654` aenderte Ports/Intents, nicht die Guard-Atomizitaet | `87.4.1` |
| P4 | Offen; `dispose()` feuert `finalizeMatch()` fire-and-forget, loggt nur Fehler und raeumt UI-Refs vorher schon ab. | `src/core/runtime/GameRuntimeSessionHandler.js:105-121` | `c94c444` fuehrte den Dispose-Pfad ein; `542ceaa` modellierte Commands, liess den Fire-and-forget-Pfad aber bestehen | `87.2.2` |
| P8 | Offen; Settings koennen separat ueber `APPLY_SETTINGS` und implizit ueber `START_MATCH(settingsSnapshot)` mutieren. | `src/application/session-runtime/SessionRuntimeCommandExecutor.js:133-153` | `542ceaa` fuehrte beide Pfade zusammen; `cce9a01` und `5ed70f2` haerteten nur die MENU-Guards | `87.3.1` |
| P9 | Offen; Click-Handler pruefen `isPaused` vor dem Intent, `resumeFromPause()` und `returnToMenuFromPause()` selbst validieren den Snapshot danach nicht erneut. | `src/ui/PauseOverlayController.js:64-82`, `src/ui/PauseOverlayController.js:187-224` | `cd0f846` fuehrte die Handler ein; `f828513`/`7742654` routeten ueber Ports, schlossen das TOCTOU-Fenster aber nicht | `87.4.1` |
| P10 | Offen; `createIntent()` kann `null` liefern, waehrend Descriptoren ueber `...source` weiterhin `available: true` oder andere Capability-Felder uebernehmen. | `src/platform/electron/ElectronPlatformBridge.js:59-77`, `src/platform/electron/ElectronPlatformBridge.js:91-103`, `src/platform/electron/ElectronPlatformBridge.js:128-173`, `src/platform/electron/ElectronPlatformBridge.js:191-205` | `db2bda6` fuehrte Descriptor-Merge ein; `2792a06` zentralisierte Registry-Reads; `5ed70f2` korrigierte nur `providerKind`-Prioritaet | `87.3.2` |
| P11 | Offen; bei Finalize-Fehlern wird zwar `finalize.status = error` gesetzt, `pendingOperation` aber im `finally` immer geloescht. | `src/state/MatchLifecycleSessionOrchestrator.js:460-475` | `48f4273` und `4a0b06d` fuegten Status/Event-Diagnostik hinzu; `c198a6d` raeumt `_pendingFinalize` weiterhin immer ab | `87.2.2` |
| P15 | Offen; Promise-Rejections aus Commands werden nach Observability-Logging direkt weitergeworfen, ohne abgesicherten Catch-Vertrag fuer alle Caller. | `src/application/session-runtime/SessionRuntimeCommandExecutor.js:88-93`, `src/application/session-runtime/SessionRuntimeCommandExecutor.js:110-125` | `4a0b06d` fuehrte das Promise-Tracking ein; seither kein Call-Site-Vertrag nachgezogen | `87.3.1` |
| P16 | Offen; die State-Machine erlaubt `FINALIZING -> MENU`, und `applySessionRuntimeLifecycleTransition()` uebernimmt diese Rueckkante unveraendert. | `src/shared/contracts/SessionRuntimeStateMachine.js:73-76`, `src/shared/contracts/SessionRuntimeStateMachine.js:128-149` | `a25aa6c` fuehrte die State-Machine mitsamt Rueckkante ein; danach keine Haertung der FINALIZING-Regeln | `87.4.2` |
| P20 | Offen; beim Aufzeichnen neuer Events wird weiter `splice()` statt einer bounded-copy-Strategie verwendet. | `src/shared/runtime/SessionRuntimeObservability.js:79-81` | `4a0b06d` fuehrte Observability mit `splice()` ein; seitdem unveraendert | `87.4.2` |

Statusbild fuer `87.1.1`: Neun Review-Punkte bleiben als echte V87-Arbeit offen (`P2`, `P4`, `P8`, `P9`, `P10`, `P11`, `P15`, `P16`, `P20`). `P1` bleibt als dokumentierter Review-Hinweis im Master, ist im aktuellen Code-Stand aber bereits durch ID-Vergabe vor Async-Arbeit plus `_pendingSessionInit`-Serialisierung abgesichert.

#### 87.1.2 Ownership- und Sunset-Matrix (2026-04-05)

| ID | Zielmodul | Besitzerpfad | Sunset-Kriterium | Folgephase |
| --- | --- | --- | --- | --- |
| P2 | Atomarer Start-Match-Intent-Guard um `applyStartMatchProjection()` | `src/ui/MatchFlowUiController.js` | Doppelte `applyStartMatchProjection()`-Aufrufe teilen sich denselben Inflight-Guard vor `_startMatchInternal()`; kein zweiter Startpfad kann zwischen Guard-Check und Promise-Assignment durchrutschen. | `87.4.1` |
| P4 | Dispose-/Finalize-Handshake fuer `GameRuntimeSessionHandler.dispose()` | `src/core/runtime/GameRuntimeSessionHandler.js` | `dispose()` gibt einen beobachtbaren oder awaitbaren Finalize-Abschluss zurueck und raeumt Menu-Refs erst nach terminalem Finalize-Result oder explizit gelatchtem Fehlerzustand ab. | `87.2.2` |
| P8 | Autoritativer Settings-Snapshot fuer `START_MATCH` und `APPLY_SETTINGS` | `src/application/session-runtime/SessionRuntimeCommandExecutor.js` | Matchstart und Settings-Aenderungen nutzen denselben autoritativen Apply-Pfad; `START_MATCH(settingsSnapshot)` mutiert Runtime-Settings nicht mehr implizit an einem zweiten Ort. | `87.3.1` |
| P9 | Pause-Intent-Revalidierung fuer Resume und Return-to-Menu | `src/ui/PauseOverlayController.js` | Resume- und Return-Intents pruefen den aktuellen Pause-/Lifecycle-Snapshot direkt vor Ausfuehrung erneut und ignorieren stale Mehrfachklicks deterministisch. | `87.4.1` |
| P10 | Wahrheitsgetreuer Capability-Descriptor pro Electron-Adapter | `src/platform/electron/ElectronPlatformBridge.js` | `available`, Intent-Funktionen und `degradedReason` werden aus derselben Invoke-Basis abgeleitet; kein Descriptor darf `available: true` melden, wenn der zugehoerige Intent `null` ist. | `87.3.2` |
| P11 | Finalize-Fehlerlatch in `finalizeMatchSession()` | `src/state/MatchLifecycleSessionOrchestrator.js` | Ein Finalize-Fehler bleibt ueber Snapshot, Event oder Guard sichtbar, bis ein definierter Reset folgt; `_pendingFinalize` oder ein Nachfolger-Guard verschwindet nicht mehr lautlos im `finally`. | `87.2.2` |
| P15 | Command-Result-/Catch-Vertrag fuer asynchrone Runtime-Commands | `src/application/session-runtime/SessionRuntimeCommandExecutor.js` | Jeder Promise-basierte Command endet in einem explizit konsumierbaren Success-/Error-Vertrag; nach Observability-Logging bleiben keine ungebundenen Rejections an UI- oder Facade-Callsites uebrig. | `87.3.1` |
| P16 | FINALIZING-Transition-Guard im State-Machine-Contract | `src/shared/contracts/SessionRuntimeStateMachine.js` | Direkte `FINALIZING -> MENU`-Rueckgaenge sind nur noch ueber einen expliziten `match_finalized`/`menu_opened`-Abschluss erlaubt oder komplett verboten; Cleanup kann nicht per Lifecycle-Sprung umgangen werden. | `87.4.2` |
| P20 | Bounded Event-History im Observability-Store | `src/shared/runtime/SessionRuntimeObservability.js` | Die Event-History bleibt auf `SESSION_RUNTIME_OBSERVABILITY_HISTORY_LIMIT` begrenzt, ohne per `splice()` auf jedem Append den Hotpath mutierend zu trimmen. | `87.4.2` |

Verdichtetes Ownership-Bild fuer `87.2+`: `P4` und `P11` bleiben Lifecycle-/Finalize-Besitz, `P8` und `P15` sind Command-Vertrag, `P10` bleibt Plattform-Capability-Vertrag, `P2` und `P9` sind UI-Intent-Hardening, `P16` und `P20` bleiben Shared-Contract-/Observability-Haertung.

### 87.2 Lifecycle- und Finalize-Rennen haerten
status: done
goal: Matchstart, Session-Erzeugung und Finalize deterministisch machen
output: Gehaertete Lifecycle- und Fehlerpfade fuer Start, Return und Dispose

- [x] 87.2.1 Matchstart-, Session-Erzeugungs- und Finalize-Rennen in `MatchLifecycleSessionOrchestrator`, `GameRuntimeSessionHandler` und angrenzenden Ports deterministisch schliessen. (abgeschlossen: 2026-04-05; evidence: Select-String -Path src/state/MatchLifecycleSessionOrchestrator.js -Pattern "_pendingFinalizePlan|match_start_blocked" + Select-String -Path src/core/runtime/GameRuntimeSessionHandler.js -Pattern "_pendingStartMatch|_awaitPendingFinalizeForStart" + Select-String -Path src/application/session-runtime/SessionRuntimeCommandExecutor.js -Pattern "sessionHandler\\?\\.startMatch" + Select-String -Path tests/core-targeted.spec.js -Pattern "V87.2 createMatchSession aborts|V87.2 GameRuntimeSessionHandler waits" -> src/state/MatchLifecycleSessionOrchestrator.js, src/core/runtime/GameRuntimeSessionHandler.js, src/application/session-runtime/SessionRuntimeCommandExecutor.js, tests/core-targeted.spec.js)
- [x] 87.2.2 Return-to-Menu-, Dispose- und Fehlerpfade so angleichen, dass Finalize-Fehler nicht stillen inkonsistenten Zustand hinterlassen. (abgeschlossen: 2026-04-06; evidence: Select-String -Path src/state/MatchLifecycleSessionOrchestrator.js -Pattern "errorMessage: error instanceof Error|_pendingFinalizePlan = null" + Select-String -Path src/core/runtime/GameRuntimeSessionHandler.js -Pattern "_pendingDispose|dispose settled without terminal runtime state" + Select-String -Path src/core/runtime/MatchFinalizeFlowService.js -Pattern "sessionFinalizeOk === false" + Select-String -Path src/shared/runtime/GameRuntimePorts.js -Pattern "finalizeErrorMessage|canReturnToMenu" + Select-String -Path tests/core-targeted.spec.js -Pattern "finalize errors stay latched|dispose waits for finalize|skips prewarm" -> src/state/MatchLifecycleSessionOrchestrator.js, src/core/runtime/GameRuntimeSessionHandler.js, src/core/runtime/MatchFinalizeFlowService.js, src/shared/runtime/GameRuntimePorts.js, tests/core-targeted.spec.js)

#### 87.2.1 Deterministischer Start-/Finalize-Handshake (2026-04-05)

- `MatchLifecycleSessionOrchestrator` merged konkurrierende Pending-Finalize-Requests jetzt deterministisch, promoted strengere Gruende wie `return_to_menu`, `game_dispose` oder `window_shutdown` ueber interne `new_match_session`-Finalizes und bricht einen wartenden Start explizit ab, wenn dieser interne Finalize von einer staerkeren Anforderung ueberholt wurde.
- `GameRuntimeSessionHandler.startMatch()` ist ab `87.2.1` der autoritative Inflight-Guard fuer normale und Snapshot-basierte Starts: derselbe Guard dedupliziert konkurrierende Startaufrufe, wartet nicht-terminale Pending-Finalizes ab und startet erst danach die eigentliche Matchprojektion.
- `SessionRuntimeCommandExecutor.START_MATCH` routed nicht mehr an einem zweiten Snapshot-Pfad vorbei, sondern delegiert denselben Startvertrag an den Session-Handler; zwei Characterization-Tests dokumentieren den neuen Vertrag, wurden gemaess Repo-Regel in dieser Subphase aber nicht ausgefuehrt.
- Rest fuer `87.2.2`: Fire-and-forget-`dispose()` und Finalize-Fehlerlatch (`P4`, `P11`) bleiben offen, damit wir Return-to-Menu-, Dispose- und Fehlerpersistenz getrennt von der jetzt geschlossenen Start-/Session-/Finalize-Race-Synchronisation angleichen.

#### 87.2.2 Fehlergelatchter Return-/Dispose-Handshake (2026-04-06)

- `MatchLifecycleSessionOrchestrator.finalizeMatchSession()` latcht jetzt `finalize.errorMessage` im Session-Runtime-State, sodass Finalize-Fehler ueber `runtimeProjectionPort`, `matchFlowSnapshot` und Start-Guards sichtbar bleiben, bis ein expliziter Reset sie aufraeumt.
- `GameRuntimeSessionHandler.dispose()` dedupliziert konkurrierende Dispose-Aufrufe, wartet den `game_dispose`-Finalize-Vertrag ab und gibt Menu-Controller-/Bridge-Refs erst nach terminalem Finalize-Ergebnis oder gelatchtem Fehlerzustand frei.
- `MatchFinalizeFlowService` plant nach einem fehlgeschlagenen Session-Finalize kein neues Match-Prewarm mehr ein; Return-to-Menu-Fehlerpfade geben damit kein falsches `finalized-and-ready`-Signal an Folgearbeit weiter.
- Drei Characterization-Tests dokumentieren Fehler-Latch, awaitbares Dispose und Prewarm-Suppression; die Ausfuehrung bleibt gemaess Repo-Regel bis `87.99` deferiert.

### 87.3 Commands, Capabilities und Fehlerpfade angleichen
status: done
goal: Commands und Capability-Adapter auf konsistente Fehler- und Verfuegbarkeitssemantik heben
output: Eindeutige Command- und Capability-Vertraege

- [x] 87.3.1 `SessionRuntimeCommandExecutor` und verwandte Command-Pfade auf eindeutige Fehler- und Snapshot-Semantik heben. (abgeschlossen: 2026-04-06; evidence: Select-String -Path src/core/runtime/RuntimeCommandSettingsService.js -Pattern "applyCommandRuntimeSettings" + Select-String -Path src/core/runtime/GameRuntimeSessionHandler.js -Pattern "applyCommandRuntimeSettings|schedulePrewarm: false" + Select-String -Path src/application/session-runtime/SessionRuntimeCommandExecutor.js -Pattern "invalid_command|executeResult|trackedPromise.catch|applyCommandRuntimeSettings" + Select-String -Path src/ui/MatchFlowUiController.js -Pattern "_startMatchInternal|_configureInputSourcesForMatch" + Select-String -Path src/core/GameRuntimeFacade.js -Pattern "executeSessionRuntimeCommandResult" + Select-String -Path tests/core-targeted.spec.js -Pattern "V87.3 SessionRuntimeCommandExecutor routes|V87.3 SessionRuntimeCommandExecutor exposes settled async errors|V87.3 SessionRuntimeCommandExecutor returns explicit invalid-command results" -> src/core/runtime/RuntimeCommandSettingsService.js, src/core/runtime/GameRuntimeSessionHandler.js, src/application/session-runtime/SessionRuntimeCommandExecutor.js, src/ui/MatchFlowUiController.js, src/core/GameRuntimeFacade.js, tests/core-targeted.spec.js)
- [x] 87.3.2 `ElectronPlatformBridge` und andere Capability-Adapter so haerten, dass `available`, Intent-Erzeugung und Fallbacks denselben Vertrag sprechen. (abgeschlossen: 2026-04-07; evidence: Select-String -Path src/platform/PlatformCapabilityAdapterSupport.js -Pattern "createCapabilityIntent|resolveCapabilityAvailability|createCapabilityAdapterDescriptor" + Select-String -Path src/platform/electron/ElectronPlatformBridge.js -Pattern "resolveCapabilityAvailability|supportsSubscribe|supportsBinaryExport|supportsSessionOwnership" + Select-String -Path src/platform/browser/BrowserPlatformAdapters.js -Pattern "startDiscovery = null|getLanServerStatus = null|saveReplay =|saveVideo =" + Select-String -Path tests/core-targeted.spec.js -Pattern "stale capability availability without intents|Browser discovery port stays unavailable|clears fallback degradedReason" -> src/platform/PlatformCapabilityAdapterSupport.js, src/platform/electron/ElectronPlatformBridge.js, src/platform/browser/BrowserPlatformAdapters.js, tests/core-targeted.spec.js)

#### 87.3.1 Autoritativer Settings-/Command-Vertrag (2026-04-06)

- `RuntimeCommandSettingsService` ist jetzt der gemeinsame Apply-Pfad fuer `APPLY_SETTINGS` und `START_MATCH(settingsSnapshot)`: optionales Host-Snapshot-Merging und Runtime-Settings-Apply laufen ueber dieselbe Funktion, statt an zwei Stellen auseinanderzudriften.
- `GameRuntimeSessionHandler.startMatch()` wendet diesen Apply-Pfad vor Validation und Matchprojektion an; `MatchFlowUiController._startMatchInternal()` mutiert Runtime-Settings dadurch nicht mehr ueber einen zweiten UI-seitigen Sonderpfad.
- `SessionRuntimeCommandExecutor.executeResult()` liefert fuer Promise-basierte Commands einen expliziten `{ ok, resultStatus, errorMessage, ... }`-Vertrag und gibt fuer ungueltige Commands jetzt ebenfalls ein explizites `invalid_command`-Error-Result statt `undefined` zurueck; das rohe `execute()` behaelt die bisherige Rueckgabeform, bindet Rejections nach Observability-Logging aber intern an einen Catch, damit ignorierte Async-Commands keine unhandled rejections hinterlassen.
- Drei Characterization-Tests dokumentieren den gemeinsamen Settings-Apply-Pfad, den Async-Result-/Catch-Vertrag und den expliziten Invalid-Command-Result-Pfad; die Ausfuehrung bleibt gemaess Repo-Regel bis `87.99` deferiert.

#### 87.3.2 Wahrheitsgetreuer Capability-/Fallback-Vertrag (2026-04-07)

- `PlatformCapabilityAdapterSupport` ist jetzt der gemeinsame Baukasten fuer Capability-Adapter: gebundene Intents, Availability (`all`/`any`) und Descriptor-Merge werden aus derselben Invoke-Basis abgeleitet, statt `available`, `degradedReason` oder Support-Flags aus einem separaten Descriptor-Override zu beziehen.
- `ElectronPlatformBridge` erzwingt diesen Vertrag jetzt fuer Discovery, Host, Save und Recording: fehlende Intents machen den Adapter sichtbar unavailable, `supportsSubscribe`/`supportsBinaryExport`/`supportsSessionOwnership` folgen der realen Invoke-Basis und fallback-`degradedReason` leakt nicht mehr in erfolgreich verfuegbare Adapter hinein.
- Die Browser-Adapter liefern fuer nicht vorhandene Desktop-Capabilities keine Noop-Backdoors mehr, sondern `null`-Intents; dadurch sprechen `capability.available`, `isAvailable()` und Ports wie `MenuMultiplayerDiscoveryPort` dieselbe Wahrheit ueber Desktop-only-Fallbacks.
- Restloch-Check fuer `87.3.2`: `P10` gilt als geschlossen. Es bleibt im Scope kein offener Widerspruch mehr, bei dem Capability-Descriptoren `available: true` melden, waehrend der dazugehoerige Intent `null` ist oder Browser-Fallbacks ueber Noops heimlich als verfuegbar wirken. Offene V87-Arbeit nach `87.3.2` bleibt auf `P2`, `P9`, `P16` und `P20` begrenzt.

### 87.4 Pause-, UI- und State-Machine-Uebergaenge absichern
status: done
goal: Kritische UI- und State-Uebergaenge gegen Rennen und Bypaesse schuetzen
output: Gehaertete Pause-, Resume- und FINALIZING-Pfade

- [x] 87.4.1 `PauseOverlayController` und UI-Intents gegen TOCTOU- und Mehrfachausloese-Rennen absichern. (abgeschlossen: 2026-04-07; evidence: rg -n "createDeferred|applyStartMatchProjection|applyResumeProjection\\(|pauseLease|_capturePauseIntentLease|_canExecutePauseIntent" src/ui/MatchFlowUiController.js src/ui/PauseOverlayController.js src/core/runtime/GameRuntimeSessionHandler.js src/shared/runtime/UiIntentAtomicity.js src/shared/runtime/UiControllerRuntimePorts.js src/shared/runtime/GameRuntimePorts.js + rg -n "V87\\.4 MatchFlowUiController|V87\\.4 PauseOverlayController" tests/core-targeted.spec.js -> src/ui/MatchFlowUiController.js, src/ui/PauseOverlayController.js, src/core/runtime/GameRuntimeSessionHandler.js, src/shared/runtime/UiIntentAtomicity.js, src/shared/runtime/UiControllerRuntimePorts.js, src/shared/runtime/GameRuntimePorts.js, tests/core-targeted.spec.js)
- [x] 87.4.2 `SessionRuntimeStateMachine` und Observability so schaerfen, dass kritische FINALIZING-, MENU- und Cleanup-Uebergaenge nicht umgangen werden koennen. (abgeschlossen: 2026-04-07; evidence: rg -n "session-runtime-state-machine\\.v2|completionEventType|MATCH_FINALIZED|MENU_OPENED" src/shared/contracts/SessionRuntimeStateMachine.js src/state/MatchLifecycleSessionOrchestrator.js src/state/MatchLifecycleSessionTransitionSupport.js + rg -n "completeSessionRuntimeMenuLifecycle|session_runtime_menu_transition_blocked|match_finalized|menu_opened" src/state/MatchLifecycleSessionOrchestrator.js src/state/MatchLifecycleSessionTransitionSupport.js + rg -n "trimSessionRuntimeObservabilityEvents|appendSessionRuntimeObservabilityEvent" src/shared/runtime/SessionRuntimeObservability.js + rg -n "V87\\.4 SessionRuntimeStateMachine|V87\\.4 MatchLifecycleSessionOrchestrator|V87\\.4 SessionRuntimeObservability" tests/core-targeted.spec.js -> src/shared/contracts/SessionRuntimeStateMachine.js, src/state/MatchLifecycleSessionOrchestrator.js, src/state/MatchLifecycleSessionTransitionSupport.js, src/shared/runtime/SessionRuntimeObservability.js, tests/core-targeted.spec.js)

#### 87.4.1 Atomarer Start-/Pause-Intent-Vertrag (2026-04-07)

- `src/shared/runtime/UiIntentAtomicity.js` ist jetzt der gemeinsame Baustein fuer UI-Intent-Atomizitaet: derselbe Helper stellt das inflight-sichere Deferred-Muster fuer Start-Guards sowie Snapshot-Leases fuer Pause-Resume- und Pause-Menu-Intents bereit.
- `MatchFlowUiController.applyStartMatchProjection()` setzt `_startMatchPromise` jetzt vor `_startMatchInternal()`. Reentrante oder doppelte Startausloeser teilen sich dadurch denselben Pending-Vertrag, statt zwischen Guard-Check und Promise-Assignment ein zweites Startfenster zu oeffnen.
- `PauseOverlayController` bleibt als duenne UI-Fassade erhalten; die neue Hilfslogik in `src/ui/PauseOverlayIntentActions.js` erzeugt fuer Resume und `pause_menu_return` einen `pauseLease` direkt aus dem aktuellen Match-Flow-Snapshot und routed diesen ueber dieselben Runtime-Intent-Pfade. Der lokale Fallback revalidiert denselben Lease unmittelbar vor `applyResumeProjection()` beziehungsweise dem Return-to-Menu-UI-Uebergang, statt einen schwaecheren Sonderpfad zu nutzen.
- `GameRuntimeSessionHandler.resumeMatch()` und `returnToMenu()` revalidieren den aktuellen Match-Flow-Snapshot jetzt gegen diesen Lease und ignorieren stale, ueberholte oder doppelt verwendete Pause-Intents deterministisch. Es gibt damit keinen separaten Legacy-Bypass mehr, bei dem UI-Klicks ohne denselben Snapshot-Vertrag an Resume oder Finalize vorbeigehen.
- Restloch-Check fuer `87.4.1`: `P2` und `P9` gelten im aktuellen Scope als geschlossen. Fuer Start-, Resume- und Pause-Menu-Intents bleibt nach dem Ist-Code kein offenes Loch mehr bei UI-Intent-Atomizitaet, Pause-Revalidierung oder Mehrfachausloesern; verbleibende V87-Restarbeit in `87.4` ist auf `P16` und `P20` (`87.4.2`) begrenzt.

#### 87.4.2 Gehaerteter FINALIZING-/MENU-Abschluss und bounded Observability (2026-04-07)

- `SessionRuntimeStateMachine` fuehrt jetzt `session-runtime-state-machine.v2`: die offene Rueckkante `FINALIZING -> MENU` ist aus dem allgemeinen Uebergangsgraphen entfernt. Ein Rueckgang nach `menu` ist nur noch als expliziter Abschluss ueber `completionEventType = match_finalized|menu_opened` erlaubt und setzt bereits gelatchtes `finalize.status = finalized` voraus.
- `MatchLifecycleSessionTransitionSupport` kapselt den autorisierten Menueabschluss als kleinen Shared-Helper fuer den Orchestrator, statt weitere Zeilen in `MatchLifecycleSessionOrchestrator` zu stapeln. `finalizeMatchSession()` setzt `finalize.status = finalized` und emittiert `match_finalized` jetzt vor `menu_opened`; waehrend Recorder-Settlement, Session-Dispose und Ref-Cleanup bleibt der Lifecycle deterministisch in `finalizing`.
- `notifyMenuOpened()` wirft jetzt explizit, wenn ein MENU-Abschluss gegen den State-Machine-Vertrag blockiert wird. Damit entsteht kein stiller Legacy-Bypass mehr, der `menu_opened` emittiert, obwohl der Lifecycle den FINALIZING-Abschluss noch nicht sauber erreicht hat.
- `SessionRuntimeObservability` trimmt die Event-History jetzt ueber copy-based Helper statt ueber mutierendes `push()` plus `splice()`. Die History bleibt weiter auf `SESSION_RUNTIME_OBSERVABILITY_HISTORY_LIMIT` begrenzt, ohne einen separaten Hotpath fuer In-Place-Trimming offenzuhalten.
- Restloch-Check fuer `87.4.2`: `P16` und `P20` gelten im aktuellen Scope als geschlossen. Nach Ist-Code bleibt kein Restloch mehr fuer einen direkten `FINALIZING -> MENU`-Bypass, fuer Cleanup-Umgehung vor `match_finalized` oder fuer eine unbounded/mutierend getrimmte Observability-History.

### 87.5 Folgeverbrauch und Dokumentation synchronisieren
status: done
goal: Folgeblocks und Referenzen auf den geharteten Runtime-Stand spiegeln
output: Abgeglichene Leitplanken fuer Folgebloecke

- [x] 87.5.1 `V64`, `V75` und Referenzdoku auf den geharteten Runtime-Stand spiegeln. (abgeschlossen: 2026-04-07; evidence: rg -n "Leitplanke 2026-04-07|match_finalized|menu_opened|bounded Observability|Verbrauchsleitplanken fuer V64 und V75" docs/plaene/aktiv/V64.md docs/plaene/aktiv/V75.md docs/referenz/ai_architecture_context.md -> docs/plaene/aktiv/V64.md, docs/plaene/aktiv/V75.md, docs/referenz/ai_architecture_context.md)
- [x] 87.5.2 Characterization- oder Contract-Checks fuer die geharteten Pfade vorbereiten, damit Folgearbeit nicht nur auf manuellem Vertrauen basiert. (abgeschlossen: 2026-04-07; evidence: rg -n "V87\\.4 SessionRuntimeStateMachine|V87\\.4 MatchLifecycleSessionOrchestrator|V87\\.4 SessionRuntimeObservability" tests/core-targeted.spec.js -> tests/core-targeted.spec.js)

#### 87.5.1 Folgeblock- und Referenzsync fuer den gehaerteten Runtime-Vertrag (2026-04-07)

- `V64` bindet `64.6 Match-Lifecycle und State-Sync` jetzt explizit an den geharteten `finalizing -> match_finalized -> menu_opened`-Abschluss. LAN- und Online-Folgearbeit duerfen Return-to-Menu, Session-Dispose und Cleanup damit nicht mehr ueber transport- oder shell-spezifische Sonderpfade modellieren.
- `V75` bindet `75.5 Lifecycle-Haertung und Temp-Abbruchfaelle` jetzt explizit an dasselbe `finalizing`-Fenster wie Session-Dispose, Ref-Cleanup und Menu-Rueckkehr. Recorder-Export, Save und Convert duerfen den Menueabschluss nicht selbst ausloesen oder vor `match_finalized` als erfolgreich annehmen.
- `docs/referenz/ai_architecture_context.md` traegt den Vertrag jetzt zentral konsumierbar: Runtime-Command- und Event-Tabellen nennen den expliziten `match_finalized`-Schritt vor `menu_opened`, `4.6.4` ist auf den abgeschlossenen Hardening-Stand gehoben, und `4.6.4.2` verankert die Verbrauchsleitplanken fuer V64/V75 ohne neue Runtime-Sonderpfade oder Legacy-Backdoors.
- Restloch-Check fuer `87.5.1`: Nach dem Abgleich bleibt im aktuellen V87-Scope kein Dokumentations- oder Verbrauchsloch mehr beim gehaerteten FINALIZING-/MENU-/Cleanup-/Observability-Vertrag offen. Offene V87-Arbeit beschraenkt sich damit auf `87.99` Abschluss-Gates.

#### 87.5.2 Charakterisierung fuer den gehaerteten Runtime-Vertrag (2026-04-07)

- `tests/core-targeted.spec.js` dokumentiert jetzt den direkten State-Machine-Guard fuer `FINALIZING -> MENU`, die Cleanup-/Event-Reihenfolge im `MatchLifecycleSessionOrchestrator` und die bounded-copy-History der Observability.
- Die Ausfuehrung bleibt gemaess Repo-Regel bis zum passenden Gate deferiert; fuer Folgearbeit ist der erwartete Vertrag damit aber nicht mehr nur implizit im Produktionscode versteckt.

### 87.99 Abschluss-Gate
status: done
goal: Runtime-Hardening mit gruener Architektur- und Doku-Baseline schliessen
output: Gruene Gates und zugeordnete Restschulden

Hinweis 2026-04-08 (Rebaseline): Architektur-, Guard-, Doku-, Build- und Smoke-Gates bleiben gruen. Der engere `dev-runtime`-Vertrag fuer `npm run test:targeted` (`core-targeted`, `physics-*`, `arcade-blueprint`, `bot-targeting`) startet ohne `fetch failed`-/`probe-timeout`-Harness-Abbruch, akzeptiert aktuell aber nur `menu_shell_ready` (`appReady=false`, `strictPrewarm=false`) als Readiness-Baseline. Der frische Einzelrun `TEST_PORT=5588 PW_RUN_TAG=v87-targeted-dev-20260408 PW_OUTPUT_DIR=test-results/v87-targeted-dev-20260408 npm run test:targeted` endet nach 28.3 Minuten mit 105 PASS / 35 FAIL / 1 flaky / 3 skipped / 124 did not run; Cluster: `core-targeted=12`, `physics-core=2`, `physics-hunt=16`, `physics-policy=5`. Dauerhafte Blockerdoku: `docs/Fehlerberichte/2026-04-07_v87-runtime-hardening-87.99-playwright-blockers.md`.
Hinweis 2026-04-08 (Konsolidierter Vollrerun): Der naechste frische Voll-Lauf `TEST_PORT=5593 PW_RUN_TAG=v87-targeted-dev-rerun-20260408 PW_OUTPUT_DIR=test-results/v87-targeted-dev-rerun-20260408 npm run test:targeted` schneidet den Stand nach geschlossenem `core-targeted`-Rest neu auf 104 PASS / 23 FAIL / 1 flaky / 2 skipped / 138 did not run. Im Voll-Lauf erscheinen keine `core-targeted`-Produktionsfails mehr; `tests/core-targeted.spec.js:158` (`T1`) faellt als `page.goto`-Fail aus und `:6720` (`V56.1`) bleibt `page.goto`-flaky, waehrend die echten Produktionscluster jetzt `physics-core=2`, `physics-hunt=16` und `physics-policy=4` sind. Ein kleiner Folgefix in `src/core/runtime/GameRuntimeSessionHandler.js` haertet den Direktstart-Vertrag fuer asynchrone `applyStartMatchProjection()`-Promises; der dazugehoerige fokussierte Browser-Rerun wurde erneut komplett vom bekannten `page.goto`-Flake ueberdeckt, deshalb bleibt der 23-Fail-Schnitt die belastbare Baseline.

- [x] 87.99.1 Runtime-Hardening-Scope-Checks sowie `npm run architecture:report`, `npm run check:architecture:boundaries`, `npm run check:architecture:metrics`, `npm run check:architecture:ratchet`, `npm run typecheck:architecture`, `npm run plan:check`, `npm run docs:sync` und `npm run docs:check` sind gruensicher. (abgeschlossen: 2026-04-10; evidence: npm run architecture:report && npm run check:architecture:boundaries && npm run check:architecture:metrics && npm run check:architecture:ratchet && npm run typecheck:architecture && npm run plan:check && npm run docs:sync && npm run docs:check -> PASS)
- [x] 87.99.2 Kritische oder hohe Runtime-Restschulden aus Matchstart, Finalize, Pause, Commands und Capability-Bridge sind abgearbeitet oder blockerfest dokumentiert. (abgeschlossen: 2026-04-07; evidence: npm run test:contract -> PASS + npm run smoke:roundstate -> PASS + docs/Fehlerberichte/2026-04-07_v87-runtime-hardening-87.99-playwright-blockers.md -> Playwright-/Legacy-settings-Blocker dokumentiert)
- [x] 87.99.3 Den V87-nahen `core-targeted`-Restcluster im engeren `dev-runtime`-Vertrag zuerst schliessen; `physics-hunt` und `physics-policy` bleiben danach getrennte Folgecluster. (abgeschlossen: 2026-04-08; evidence: TEST_PORT=5591 PW_RUN_TAG=v87-core-restblock-20260408 PW_OUTPUT_DIR=test-results/v87-core-restblock-20260408 node dev/scripts/verify-lock.mjs --playwright -- node scripts/run-playwright-targeted.mjs tests/core-targeted.spec.js -g "V74.3 createMatchSession disposes partial session allocations on async init failure|V87.4 authored map presets keep portal, gate and parcours contracts aligned" --timeout=240000 -> 1 passed, 1 flaky (`:6889` Retry gruen, `:8403` gruen) + TEST_PORT=5592 PW_RUN_TAG=v87-core-flake-consolidated-20260408 PW_OUTPUT_DIR=test-results/v87-core-flake-consolidated-20260408 node dev/scripts/verify-lock.mjs --playwright -- node scripts/run-playwright-targeted.mjs tests/core-targeted.spec.js -g "T14: Alle Maps ladbar|V74.3 Stale async session init disposes replaced prepared match before apply|V87.2 GameRuntimeSessionHandler waits for pending finalize and deduplicates concurrent starts|V74.3 GameRuntimeFacade suppresses menu cleanup when shutdown reuses pending finalize flow" --timeout=240000 -> 3 passed, 1 failed (`T14` nur `page.goto`-Flake) + node --input-type=module - -> `:6889` Stub-Repro gruene Cleanup-Reihenfolge und Preset-Repro mit `warningCount=0` fuer `abyssal_descent`, `neon_circuit`, `sky_islands`)

## Gate-Stand (2026-04-10)

- `npm run architecture:report` -> PASS
- `npm run check:architecture:boundaries` -> PASS
- `npm run check:architecture:metrics` -> PASS
- `npm run check:architecture:ratchet` -> PASS
- `npm run typecheck:architecture` -> PASS
- `npm run test:contract` -> PASS
- `npm run smoke:roundstate` -> PASS
- `npm run test:smoke` -> PASS (`TEST_PORT=5561 PW_RUN_TAG=v87-smoke-20260407 PW_OUTPUT_DIR=test-results/v87-smoke-20260407`; 5/5 Tests gruen, inkl. Legacy-Settings-Migration)
- `node dev/scripts/verify-lock.mjs --playwright -- node scripts/run-playwright-targeted.mjs tests/core-targeted.spec.js -g "V87.3|V87.99" --timeout=240000` -> PASS mit Flake (`TEST_PORT=5565 PW_RUN_TAG=v87-core-targeted-dev-rerun-20260407 PW_OUTPUT_DIR=test-results/v87-core-targeted-dev-rerun-20260407`; 7 PASS, 1 flaky bei erstem `page.goto`-Versuch, Retry gruen)
- `npm run test:targeted` -> FAIL (`TEST_PORT=5593 PW_RUN_TAG=v87-targeted-dev-rerun-20260408 PW_OUTPUT_DIR=test-results/v87-targeted-dev-rerun-20260408`; Startup-Diagnostik: `serverReady=true`, `shellReady=true`, `ready=true`, `appReady=false`, `appBootState=menu_shell_ready`; Suite laeuft 56.0 Minuten bis 104 PASS / 23 FAIL / 1 flaky / 2 skipped / 138 did not run; Harness-Signale: `tests/core-targeted.spec.js:158` (`T1`) FAIL mit `page.goto`, `tests/core-targeted.spec.js:6720` (`V56.1`) flaky mit `page.goto`; Produktionscluster: `physics-core=2`, `physics-hunt=16`, `physics-policy=4`)
- `TEST_PORT=5591 PW_RUN_TAG=v87-core-restblock-20260408 PW_OUTPUT_DIR=test-results/v87-core-restblock-20260408 node dev/scripts/verify-lock.mjs --playwright -- node scripts/run-playwright-targeted.mjs tests/core-targeted.spec.js -g "V74.3 createMatchSession disposes partial session allocations on async init failure|V87.4 authored map presets keep portal, gate and parcours contracts aligned" --timeout=240000` -> PASS mit Flake (`:6889` gruent im Retry und ist per direkter `node`-Stub-Repro gruensicher; `:8403` Browser-Rerun gruen)
- `TEST_PORT=5592 PW_RUN_TAG=v87-core-flake-consolidated-20260408 PW_OUTPUT_DIR=test-results/v87-core-flake-consolidated-20260408 node dev/scripts/verify-lock.mjs --playwright -- node scripts/run-playwright-targeted.mjs tests/core-targeted.spec.js -g "T14: Alle Maps ladbar|V74.3 Stale async session init disposes replaced prepared match before apply|V87.2 GameRuntimeSessionHandler waits for pending finalize and deduplicates concurrent starts|V74.3 GameRuntimeFacade suppresses menu cleanup when shutdown reuses pending finalize flow" --timeout=240000` -> FAIL (`3 passed`; `:6777`, `:7249`, `:8664` im Browser gruen; `T14` bleibt reiner `page.goto`-/Harness-Flake)
- `TEST_PORT=5594 PW_RUN_TAG=v87-start-promise-fix-20260408 PW_OUTPUT_DIR=test-results/v87-start-promise-fix-20260408 node dev/scripts/verify-lock.mjs --playwright -- node scripts/run-playwright-targeted.mjs tests/physics-core.spec.js tests/physics-policy.spec.js -g "T43b: Custom-Map nutzt authored Spawns, Tunnel, Pickup-Anker und Aircraft-Deko zur Laufzeit|T80e: Matchpfad liefert fuer Bridge-Bot ueber Zeit Steering-Inputs \(kein Dauer-Geradeaus\)|T82: Session-Wiring uebergibt aufgeloeste Bot-Policy an EntityManager" --timeout=240000` -> FAIL nur als Harness-Flake (`3 failed`; alle drei Tests verlieren bereits `loadGame()` an `page.goto`)
- `node --input-type=module -` -> PASS (`GameRuntimeSessionHandler.startMatch()` wartet in direkter Repro auf asynchrone `applyStartMatchProjection()`-Promises; Ergebnis `{\"immediateState\":false,\"finalState\":true,\"result\":true,\"elapsedMs\":82}`)
- `npm run plan:check` -> PASS
- `npm run docs:sync` -> PASS (`updated=0`)
- `npm run docs:check` -> PASS
- `npm run build` -> PASS
- `npm run architecture:report && npm run check:architecture:boundaries && npm run check:architecture:metrics && npm run check:architecture:ratchet && npm run typecheck:architecture && npm run plan:check && npm run docs:sync && npm run docs:check` -> PASS (2026-04-10)

## Hinweise

- `V87` ist mit `87.99` blockerfest abgeschlossen; offene `test:targeted`-Restcluster (`physics-core`, `physics-hunt`, `physics-policy`) bleiben als gezielte Folgearbeit fuer V88/Folgeblocks dokumentiert.
- Der aktuelle Restblocker ist kein reiner Harness-/Startup-Fehler mehr, sondern ein geschnittener `dev-runtime`-Fehlerstand nach gruener Smoke-Verifikation; Details stehen blockerfest in `docs/Fehlerberichte/2026-04-07_v87-runtime-hardening-87.99-playwright-blockers.md`.
- Der frische Voll-Lauf schneidet `core-targeted` fachlich aus dem Produktionsrest heraus; als Harness-Signale bleiben im Voll-Lauf `tests/core-targeted.spec.js:158` (`T1`) und `:6720` (`V56.1`) mit `page.goto`, waehrend der bekannte `T14`-Slice dadurch gar nicht erst wieder erreicht wurde.
- Kleinster belastbarer Produktionscluster nach dem Voll-Lauf ist `physics-core` (`T43b`, `T45c`); danach folgen `physics-policy` (`T78`, `T78e`, `T80c`, `T80e`) und der breite `physics-hunt`-Rest.
- Teilstand 2026-04-08 (Direktstart-Vertrag): `GameRuntimeSessionHandler.startMatch()` verkuerzt asynchrone `applyStartMatchProjection()`-Ergebnisse nicht mehr sofort auf `true`. Die Browser-Verifikation fuer `T43b`/`T80e`/`T82` bleibt wegen erneutem `page.goto`-Flake offen; die direkte `node`-Repro bestaetigt aber den korrigierten Await-Vertrag blockerfest.
