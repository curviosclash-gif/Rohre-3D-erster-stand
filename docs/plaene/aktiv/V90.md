---
id: V90
title: Toolchain-Security und Dependency-Upgrade
status: done
priority: P1
owner: frei
depends_on:[]
blocked_by: []
affected_area: toolchain-security-dependency-upgrade
scope_files:
  - package.json
  - package-lock.json
  - vite.config.js
  - playwright.config.js
  - electron/package.json
  - electron/package-lock.json
  - server/package.json
  - .github/workflows/ci.yml
  - .agents/test_mapping.md
  - docs/referenz/ai_architecture_context.md
  - docs/prozess/Open_Findings.md
  - docs/prozess/Dokumentationsstatus.md
  - docs/generated/knowledge-graph.json
  - docs/generated/knowledge-graph.coverage.json
  - docs/generated/knowledge-graph.scorecard.json
  - docs/plaene/aktiv/V120.md
  - docs/plaene/aktiv/V90.md
scope_overlap_allowed_with:
  - V116
  - V96
  - V102
  - V105
  - V106
  - V107
  - V110
  - V111
  - V112
  - V113
  - V114
  - V120
  - V123
  - V125
  - V126
verification:
  - npm audit --audit-level=low
  - npm --prefix electron audit --audit-level=low
  - npm run architecture:guard
  - npm run build
  - npm run docs:check
  - npm run gates:pre-commit
current_phase: 90.99
updated_at: 2026-06-11
completed_at: 2026-05-18
source_history:
  - docs/plaene/neu/Feature_Toolchain_Security_Dependency_Upgrade_2026-04-10.md
---

# Feature: Toolchain-Security und Dependency-Upgrade (Vorschlag V90)

Stand: 2026-06-11
Status: abgeschlossen; verbleibende Audit-Reste als dokumentierte Security-Ausnahmen mit Wiedervorlage
Owner: frei
Risiko: hoch
plan_file: `docs/plaene/aktiv/V90.md`

## Ziel

Die aktuellen `npm audit`- und `npm outdated`-Befunde kontrolliert abbauen, ohne Build-, Electron-, Playwright- oder Vite-Vertraege zu destabilisieren.

Der Audit vom 2026-04-10 meldet 5 Security-Befunde:

- `flatted` - high, Prototype Pollution.
- `rollup` - high, Arbitrary File Write via Path Traversal.
- `brace-expansion` - moderate, Zero-step sequence DoS.
- `esbuild`/`vite` - moderate, Dev-Server-Request-Isolation.

Zusaetzlich sind mehrere Tooling-Abhaengigkeiten veraltet, darunter `@playwright/test`, `eslint`, `ws`, `@types/node`, `@commitlint/*`, sowie Major-Kandidaten `vite`, `three` und `typescript`.

## Nicht-Ziel

- Kein unkontrolliertes `npm audit fix --force` ohne Kompatibilitaetsmatrix.
- Kein gleichzeitiger Major-Upgrade von Vite, Three und TypeScript ohne getrennte Smoke-/Build-Gates.
- Keine Playwright-Vollsuites ausser an expliziten Block-Gates oder auf User-Anforderung.

## Betroffene Dateien und Bereiche

- `package.json`
- `package-lock.json`
- `vite.config.js`
- `playwright.config.js`
- `electron/package.json`
- `electron/package-lock.json`
- `server/package.json`
- `.github/workflows/ci.yml`
- `.agents/test_mapping.md`
- `docs/referenz/ai_architecture_context.md`
- `docs/prozess/Open_Findings.md`
- `docs/prozess/Dokumentationsstatus.md`
- `docs/generated/knowledge-graph.json`
- `docs/generated/knowledge-graph.coverage.json`
- `docs/generated/knowledge-graph.scorecard.json`
- `docs/plaene/aktiv/V120.md`

## Definition of Done

- [x] DoD.1 `npm audit --audit-level=low` ist gruen oder jeder verbleibende Befund ist mit Risiko, Scope und Upgrade-Blocker dokumentiert. (abgeschlossen: 2026-05-18; evidence: `npm audit --audit-level=low --json` -> Root 2 moderate Vite/esbuild-Reste, `npm --prefix electron audit --audit-level=low --json` -> Electron 12 force-only Reste, `npm --prefix server audit --audit-level=low --json` -> 0; Ausnahmen und Wiedervorlage unten dokumentiert)
- [x] DoD.2 Nicht-breaking Security-Fixes sind separat von Major-Upgrades umgesetzt. (abgeschlossen: 2026-05-18; evidence: 90.2.3 -> `fast-uri@3.1.2`, `ip-address@10.2.0` und `@tootallnate/once@2.0.1` ohne `--force`; Major-Pfade bleiben nur dokumentierte Folge-Slices)
- [x] DoD.3 Major-Upgrades fuer Vite, Three und TypeScript haben eine Kompatibilitaetsmatrix mit Rollback-Pfad. (abgeschlossen: 2026-05-18; evidence: 90.3.1, 90.3.2, 90.3.3 -> Vite-, Three- und TypeScript-/Tooling-Matrix mit Gate- und Rollback-Pfad dokumentiert)
- [x] DoD.4 Build-, Architektur- und Doku-Gates bleiben gruen. (abgeschlossen: 2026-05-18; evidence: `npm run architecture:guard`, `npm run build`, `npm run docs:check` -> PASS)
- [x] DoD.5 CI- und lokale Skripte nutzen dieselben Toolchain-Versionen und dokumentierten Node-/npm-Annahmen. (abgeschlossen: 2026-05-18; evidence: 90.3.1, 90.3.3 und `npm ls vite esbuild rollup postcss --depth=3`, `npm --prefix electron ls electron electron-builder tar @tootallnate/once ip-address --depth=5`, `npm --prefix server ls ws --depth=2` -> lokale Toolchain, CI-`lts/*`-Annahmen und getrennte Upgrade-Leiter dokumentiert)

## Intake-Hinweis fuer den User

- Ziel-Masterplan: `docs/Umsetzungsplan.md`
- Block-ID: `V90`, im Master aufgenommen.
- kanonische Blockdatei: `docs/plaene/aktiv/V90.md`
- hard dependencies: keine
- soft dependencies: `V88.99`, weil Testarchitektur und Failure-Taxonomie Upgrade-Risiken besser eingrenzen
- Hinweis: Intake ist abgeschlossen; `docs/plaene/neu/*` bleibt nur Source-History.

## Phasen

### 90.1 Audit- und Versionenmatrix
status: done
goal: Audit-, Outdated- und CI-Matrix erfassen
output: Klassifizierte Befund- und Versionenbasis


- [x] 90.1.1 `npm audit`, `npm outdated`, Electron-/Server-Unterpakete und CI-Versionen erfassen. (abgeschlossen: 2026-04-29; evidence: `npm audit --audit-level=low`, `npm outdated`, `npm --prefix electron audit --audit-level=low`, `npm --prefix electron outdated`, `npm --prefix server audit --audit-level=low`, `npm --prefix server outdated`, `Select-String -Path .github/workflows/ci.yml -Pattern 'setup-node|node-version|npm ci|npm --prefix electron ci'` -> Root-, Electron-, Server- und CI-Matrix fuer V90 erfasst)
- [x] 90.1.2 Befunde in `runtime`, `dev-server`, `build-tooling`, `test-tooling`, `electron-shell` und `server` klassifizieren. (abgeschlossen: 2026-04-29; evidence: `npm ls flatted rollup postcss esbuild brace-expansion`, `npm --prefix electron ls @tootallnate/once tar electron electron-builder`, `npm --prefix server ls ws` -> Befunde auf Root-Guard-/Build-Tooling, Dev-Server, Electron-Shell und Server-Pfade verdichtet)

### 90.2 Nicht-breaking Security-Fixes
status: done
goal: Kompatible Security-Fixes ohne Major-Zwang uebernehmen
output: Reduzierter Audit-Report mit dokumentierten Gate- und Fix-Resten


- [x] 90.2.1 `npm audit fix` ohne `--force` trocken bewerten und nur kompatible Lockfile-Updates uebernehmen. (abgeschlossen: 2026-04-29; evidence: `npm audit fix --dry-run`, `npm --prefix electron audit fix --dry-run`, `npm audit fix`, `npm --prefix electron audit fix`, `npm audit --audit-level=low`, `npm --prefix electron audit --audit-level=low` -> Root auf 2 moderate Restbefunde reduziert; Electron-Tree auf 12 verbleibende High/Low-Befunde ohne `vite@8`- oder `electron@41`-/`electron-builder@26`-Sprung verdichtet)
- [x] 90.2.2 Build-, Architektur- und Doku-Gates nachziehen. (abgeschlossen: 2026-05-18; evidence: `npm run architecture:guard`, `npm run build`, `npm run docs:check` -> PASS; der historische `typecheck:architecture`-Blocker aus `docs/Fehlerberichte/2026-04-29_v90-90.2.2-build-typecheck-architecture-blocked.md` ist fuer diesen Gate-Slice nicht mehr reproduzierbar)
- [x] 90.2.3 Nachgezogene non-force Audit-Fix-Kandidaten separat entscheiden und nur mit eigenem Gate-Scope anwenden (`fast-uri` im Root-Tooling, `ip-address` im Electron-Tree). (abgeschlossen: 2026-05-18; evidence: `npm audit fix`, `npm --prefix electron audit fix`, `npm ls fast-uri`, `npm --prefix electron ls ip-address @tootallnate/once`, `npm audit --audit-level=low`, `npm --prefix electron audit --audit-level=low`, `npm --prefix server audit --audit-level=low`, `npm run architecture:guard`, `npm run build`, `npm run docs:check`, `npm run graph:build`, `npm run graph:check` -> `fast-uri@3.1.2`, `ip-address@10.2.0` und `@tootallnate/once@2.0.1` per non-force Lockfile-Scope uebernommen; Root bleibt bei 2 moderaten Vite/esbuild-Major-Resten, Electron bei 12 Electron-/Electron-Builder-/tar-/@tootallnate/once-Resten, Server-Audit und Gates PASS)

### 90.3 Major-Upgrade-Entscheidungen
status: done
goal: Breaking-Upgrades getrennt bewerten und absichern
output: Kompatibilitaetsmatrix mit Rollback-Pfad


- [x] 90.3.1 Vite/esbuild/Rollup-Upgradepfad isoliert planen, inklusive Dev-Server-, Preview- und Electron-App-Build. (abgeschlossen: 2026-05-18; evidence: `npm audit --audit-level=low --json`, `npm outdated --json`, `npm ls vite esbuild rollup postcss --depth=3`, `npm view vite@6.4.2 version engines dependencies peerDependencies --json`, `npm view vite@7.3.3 version engines dependencies peerDependencies --json`, `npm view vite@8.0.13 version engines dependencies peerDependencies optionalDependencies --json`, `npm view rolldown-vite@latest version engines dependencies peerDependencies --json`, `npm view vite dist-tags --json`, `npm view rollup@latest version engines optionalDependencies --json`, `npm view esbuild@latest version engines optionalDependencies --json`, `node -v`, `npm -v`, `rg -n "rollupOptions|optimizeDeps|esbuild|build\\.target|target:|configureServer|configurePreviewServer|transformIndexHtml|splitVendorChunk|import\\.meta\\.hot\\.accept|manualChunks" vite.config.js dev src scripts tests electron .github package.json` -> Matrix unten: kleinster Security-Sprung ist `vite@6.4.2`; Vite 7/8 erfordern Node `20.19+`/`22.12+`, Vite 8 wechselt auf Rolldown/Oxc und braucht separaten Dev-/Preview-/Electron-Gate)
- [x] 90.3.2 Three-Upgrade separat bewerten, weil Rendering-, Loader- und Asset-Pfade betroffen sein koennen. (abgeschlossen: 2026-05-18; evidence: `npm ls three --depth=3`, `npm outdated --json`, `npm audit --audit-level=low --json`, `npm view three@0.160.1 version engines dependencies peerDependencies optionalDependencies type sideEffects exports --json`, `npm view three@latest version engines dependencies peerDependencies optionalDependencies type sideEffects exports --json`, `rg -l "from 'three'|from \"three\"|three/addons|three/examples|THREE\\." -- src editor tests dev scripts`, `rg -n "three|OrbitControls|TransformControls|GLTFLoader|OBJLoader|MTLLoader|BufferGeometryUtils|outputColorSpace|toneMapping|WebGLRenderer" src editor tests dev .agents/test_mapping.md` -> Matrix unten: Three hat keinen Audit-Befund, ist aber `0.160.1` -> `0.184.0` mit breitem Renderer-/Loader-/Editor-Scope)
- [x] 90.3.3 TypeScript-/ESLint-/Playwright-Upgrades getrennt von Runtime-Dependencies schneiden. (abgeschlossen: 2026-05-18; evidence: `npm outdated --json`, `npm ls typescript eslint @playwright/test @commitlint/cli @commitlint/config-conventional @types/node ws --depth=1`, `npm audit --audit-level=low --json`, `npm view typescript@5.9.3 version engines bin dependencies peerDependencies optionalDependencies --json`, `npm view typescript@latest version engines bin dependencies peerDependencies optionalDependencies --json`, `npm view eslint@10.0.3 version engines bin dependencies peerDependencies optionalDependencies --json`, `npm view eslint@latest version engines bin dependencies peerDependencies optionalDependencies --json`, `npm view eslint-plugin-boundaries@6.0.2 version engines peerDependencies dependencies --json`, `npm view @playwright/test@1.58.2 version engines bin dependencies peerDependencies optionalDependencies --json`, `npm view @playwright/test@latest version engines bin dependencies peerDependencies optionalDependencies --json`, `npm view @commitlint/cli@latest version engines dependencies peerDependencies optionalDependencies --json`, `rg -n "typescript|tsc|eslint|playwright|@playwright|commitlint|@types/node|\\bws\\b" package.json package-lock.json playwright.config.js electron server scripts tests .agents/test_mapping.md` -> Matrix unten: TypeScript 6 ist separater Architecture-Typecheck-Slice; ESLint/Commitlint, Playwright und `ws` duerfen nicht mit Runtime-Upgrades vermischt werden)

### 90.99 Abschluss-Gate
status: done
goal: Toolchain-Hardening reproduzierbar abschliessen
output: Gruene Gates oder dokumentierte Security-Ausnahmen


- [x] 90.99.1 `npm run architecture:guard`, `npm run build`, `npm run docs:check` und der kleinste relevante Dependency-Sanity-Check sind gruen. (abgeschlossen: 2026-05-18; evidence: `npm run architecture:guard` -> PASS mit bestehenden 29 `innerHTML`-Warnungen im Ratchet, `npm run build` -> PASS, `npm run docs:check` -> PASS, `npm ls vite esbuild rollup postcss --depth=3`, `npm --prefix electron ls electron electron-builder tar @tootallnate/once ip-address --depth=5`, `npm --prefix server ls ws --depth=2` -> erwartete Versionen)
- [x] 90.99.2 Verbleibende Security-Ausnahmen sind dokumentiert und haben ein Wiedervorlagedatum. (abgeschlossen: 2026-05-18; evidence: `npm audit --audit-level=low --json`, `npm --prefix electron audit --audit-level=low --json`, `npm --prefix server audit --audit-level=low --json`, `npm run graph:build`, `npm run gates:pre-commit` -> Root/Electron-Reste sind force-only Major-Pfade; Wiedervorlage 2026-06-17 oder frueher bei neuer non-force Fix-Verfuegbarkeit)

## Audit-Snapshot 2026-04-29

- `runtime`: Kein direkter Root-Runtime- oder Server-Runtime-Befund offen; `npm --prefix server audit --audit-level=low` ist gruen.
- `dev-server`: `vite@5.4.21` haengt an `esbuild@0.21.5`; nach dem sicheren Fix-Slice bleiben genau 2 moderate Befunde offen, deren Fix `vite@8.0.10` erzwingt.
- `build-tooling`: Root-`npm audit fix` hat den Guard-/Build-Pfad ueber `eslint -> flat-cache -> flatted@3.4.2`, `eslint -> minimatch -> brace-expansion@5.0.5`, `vite -> postcss@8.5.12` und `vite -> rollup@4.60.2` bereinigt.
- `test-tooling`: `npm outdated` meldet nur noch Patch-/Minor-Kandidaten fuer `@playwright/test`, `@types/node`, `@commitlint/*`, `eslint` und `ws`; kein eigener Audit-Befund bleibt in diesem Ast offen.
- `electron-shell`: `npm --prefix electron audit fix` hat `@xmldom/xmldom`, `lodash` und `brace-expansion` aus dem verbleibenden Report entfernt; offen bleiben `electron@33.4.11` sowie `electron-builder@25.1.8`-abgeleitete `tar`-/`@tootallnate/once`-Befunde, deren Fix `electron@41.3.0` bzw. `electron-builder@26.8.1` verlangt.
- `server`: `npm --prefix server outdated` ist nach dem Audit-Refresh leer; `ws` ist damit kein offener V90-Blocker mehr.
- `ci`: `.github/workflows/ci.yml` bleibt auf `actions/setup-node@v4` mit `node-version: 'lts/*'` sowie `npm ci` fuer Root und Electron gepinnt; jeder spaetere Major-Sprung muss diese Matrix mitziehen.

## Audit-Snapshot 2026-05-18

- `root`: `npm audit --audit-level=low` meldet 3 Befunde: `vite@5.4.21` -> `esbuild@0.21.5` bleibt der bekannte moderate Dev-Server-/Major-Upgrade-Pfad (`vite@8.0.13` via `--force`), neu hinzu kommt `fast-uri@3.1.0` ueber `@commitlint/cli -> ajv`, high, mit non-force `npm audit fix`-Kandidat.
- `electron-shell`: `npm --prefix electron audit --audit-level=low` meldet 13 Befunde: `electron@33.4.11` und `electron-builder@25.1.8`-abgeleitete `tar`-/`@tootallnate/once`-Befunde bleiben Major-gebunden (`electron@42.1.0`, `electron-builder@26.8.1` via `--force`), neu erfasst ist `ip-address@10.1.0` ueber `electron-builder -> socks`, moderate, mit non-force `npm audit fix`-Kandidat.
- `server`: `npm --prefix server audit --audit-level=low` ist weiterhin gruen.
- `gate-status`: `npm run architecture:guard`, `npm run build` und `npm run docs:check` sind gruen; damit ist der alte 90.2.2 Build-/Typecheck-Blocker nicht mehr der V90-Stopper.
- `scope-entscheidung`: Kein `npm audit fix`, kein `npm audit fix --force`, keine Package-/Lockfile-Aenderung in diesem Audit-Baseline-Slice; `fast-uri` und `ip-address` brauchen einen separaten non-force Fix-Gate, die Vite/Electron/Electron-Builder-Pfade bleiben 90.3-/90.99-Risiko.

## Audit-Fix-Snapshot 2026-05-18 (90.2.3)

- `root`: `npm audit fix` ohne `--force` aktualisierte nur `fast-uri` auf `3.1.2`; `npm audit --audit-level=low` meldet danach nur noch 2 moderate Vite/esbuild-Befunde, deren Fix weiterhin `vite@8.0.13` via `--force` erzwingt.
- `electron-shell`: `npm --prefix electron audit fix` ohne `--force` aktualisierte `ip-address` auf `10.2.0` und den lockfile-kompatiblen `@tootallnate/once`-Pfad auf `2.0.1`; `npm --prefix electron audit --audit-level=low` meldet danach 12 verbleibende Electron-/Electron-Builder-/tar-/@tootallnate/once-Befunde, deren Fix weiterhin Major-Upgrades auf `electron@42.1.0` oder `electron-builder@26.8.1` erzwingt.
- `server`: `npm --prefix server audit --audit-level=low` bleibt gruen.
- `gate-status`: `npm run architecture:guard`, `npm run build`, `npm run docs:check`, `npm run graph:build` und `npm run graph:check` bleiben gruen; der V90/V120-Overlap ist fuer die generierten Graph-Artefakte beidseitig freigegeben; `90.3` ist damit der naechste offene V90-Scope fuer isolierte Major-Upgrade-Entscheidungen.

## Abschluss-Snapshot 2026-05-18 (90.99)

- `root`: `npm audit --audit-level=low --json` bleibt mit 2 moderaten Befunden rot, beide im bekannten `vite@5.4.21`/`esbuild@0.21.5`-Dev-Server-Pfad. `fixAvailable` erzwingt weiter `vite@8.0.13` via `--force`; kleinster kontrollierter Folge-Slice bleibt erst `vite@6.4.2` gemaess 90.3.1-Gate-Leiter.
- `electron-shell`: `npm --prefix electron audit --audit-level=low --json` bleibt mit 12 Befunden rot, weil `electron@33.4.11` und `electron-builder@25.1.8` nur ueber Major-Upgrades (`electron@42.1.0`, `electron-builder@26.8.1`) repariert werden. Kein neuer non-force Kandidat bleibt offen.
- `server`: `npm --prefix server audit --audit-level=low --json` ist gruen; `npm --prefix server ls ws --depth=2` zeigt `ws@8.20.0`.
- `wiedervorlage`: 2026-06-17 oder frueher, wenn `npm audit` fuer Root oder Electron einen non-force Fix ohne Vite-/Electron-/Electron-Builder-Major-Sprung meldet. Bis dahin ist P21 eine akzeptierte dokumentierte Security-Ausnahme, kein Package-/Lockfile-Aenderungsauftrag.
- `abschluss-gates`: `npm run architecture:guard`, `npm run build`, `npm run docs:check`, `npm run graph:build`, `npm run gates:pre-commit`, `node scripts/query-knowledge-graph.mjs open-deps V90 --json` und `node scripts/query-knowledge-graph.mjs scope-collisions --json` sind gruen beziehungsweise ohne V90-Blocker; `scope-collisions` zeigt nur bekannte, freigegebene Overlaps.

## Audit-Snapshot 2026-06-11 (P21-Wiedervorlage)

- `ausloeser`: User-Auftrag Security-Wiedervorlage V90/P21 (vorgezogen vom 2026-06-17) mit expliziter D3-Freigabe fuer Package-/Lockfile-Aenderungen.
- `github-triage`: Die Dependabot-API (`gh api repos/curviosclash-gif/Rohre-3D-erster-stand/dependabot/alerts`) meldet 21 Alerts gesamt, davon 14 open (1 high, 5 medium, 8 low) und keine criticals in keinem State. Die gemeldeten "46 Findings / 2 critical" sind via API nicht reproduzierbar; kein P1-Runtime-Befund. 12 der 14 offenen Alerts betreffen `torch` in `python/requirements.txt`/`python/requirements-ppo.txt` und liegen ausserhalb des npm-/P21-Scopes (eigener Intake noetig).
- `vorher` (npm audit 2026-06-11): Root 4 moderate (`brace-expansion`, `esbuild`, `vite`, `ws`), Electron 12 = 11 high + 1 moderate (`tmp` GHSA-ph9p-34f9-6g65 high, `brace-expansion` moderate, Rest `electron`-/`electron-builder`-/`tar`-Kette), Server 1 moderate (`ws` GHSA-58qx-3vcg-4xpx).
- `non-force-fixes`: `npm audit fix` ohne `--force` pro Manifest: Root 4 -> 2 (`brace-expansion`, `ws` bereinigt), Electron 12 -> 10 (`tmp`, `brace-expansion` bereinigt), Server 1 -> 0 (`ws@8.21.0`, Runtime-Pfad gruen). Der Commit-Titel `e2e2d5c7` nennt irrtuemlich `8.20.1`; Lockfile und installierter Tree weisen `8.21.0` aus.
- `vite-slice`: `vite@5.4.21` -> `vite@6.4.2` gemaess 90.3.1 Option A (Range `^6.4.2`, `esbuild@0.25.12`); Root-Audit danach 0 Befunde. Gates: `npm run build` inkl. prebuild `architecture:guard` PASS, Dev-Server-Smoke `npm run dev` -> VITE v6.4.2 ready, HTTP 200 auf `127.0.0.1:4173`. Beobachtung: mehrminuetiger Warmup-CPU-Spin nach dem ersten Request, danach stabile Antworten; bei Wiederauftreten als eigener Befund aufnehmen.
- `electron-rest` (10 Befunde, alle Major-gebunden, kein non-force-Kandidat): `electron@33.4.11` braucht `electron@42.4.0`, `electron-builder@25.1.8` braucht `electron-builder@26.15.2`. Runtime-Anteil (Desktop-Shell): 18 Electron-Advisories, davon high GHSA-532v-xpq5-8h95, GHSA-8337-3p73-46f4, GHSA-jjp3-mq3x-295m, GHSA-9wfr-w7mm-pc7f (Use-after-free/Switch-Injection), Rest moderate/low; Exploit-Kontext ist die lokale Desktop-Shell ohne Remote-Content-Laden, daher kein P1. Build-time-Anteil (Packaging-Toolchain, nicht ausgeliefert): `tar` mit 6 high GHSAs (u. a. GHSA-34x7-hfp2-rc4v, GHSA-8qq5-rm4j-mr97) ueber `app-builder-lib`/`dmg-builder`/`node-gyp`/`cacache`/`make-fetch-happen`. Kleinster Upgrade-Pfad bleibt der getrennte Doppel-Major `electron@42` + `electron-builder@26.15` als eigener Gate-Slice (Build/Package/Smoke), in diesem Lauf bewusst nicht ausgefuehrt.
- `wiedervorlage`: 2026-07-11 oder frueher, wenn `npm --prefix electron audit` einen non-force Fix ohne Electron-/Electron-Builder-Major meldet oder der Electron-Major-Slice eingeplant wird. P21-Scope reduziert sich auf den Electron-Tree; Root und Server sind gruen.

## Vite-/esbuild-/Rollup-Matrix 2026-05-18 (90.3.1)

- `aktueller Stand`: Root nutzt `vite@5.4.21` mit `esbuild@0.21.5`, `rollup@4.60.2` und `postcss@8.5.12`; lokal laufen Node `v24.13.1` und npm `11.8.0`, CI nutzt `actions/setup-node@v4` mit `node-version: 'lts/*'`.
- `Audit-Rest`: `npm audit --audit-level=low --json` meldet 2 moderate Root-Befunde (`vite <=6.4.1`, `esbuild <=0.24.2` ueber Vite); `npm audit fix --force` wuerde direkt `vite@8.0.13` ziehen, ist aber nicht der kleinste kontrollierte Security-Sprung.
- `Option A - minimaler Security-Major`: `vite@6.4.2` liegt ausserhalb der gemeldeten Vite-Range und bringt `esbuild ^0.25.0`, `rollup ^4.34.9`, `postcss ^8.5.3`; Node-Engine bleibt `^18.0.0 || ^20.0.0 || >=22.0.0`. Das ist der bevorzugte naechste Code-Slice fuer `package.json`/`package-lock.json`, weil er die Security-Reste voraussichtlich ohne Node-Baseline- und Rolldown-Wechsel abbaut.
- `Option B - Vite 7 aktuell`: `vite@7.3.3` bringt `esbuild ^0.27.0`, `rollup ^4.43.0`, `postcss ^8.5.6`, verlangt aber Node `^20.19.0 || >=22.12.0`; offizielle Migration nennt ausserdem geaenderte Default-Browser-Targets und entfernte/deprecated Vite-APIs. Diese Option ist erst sinnvoll, wenn CI die Node-Baseline explizit absichert.
- `Option C - Vite 8 aktuell`: `vite@8.0.13` ersetzt den produktiven Rollup-/esbuild-Kern durch Rolldown/Oxc, nutzt `rolldown 1.0.1`, `lightningcss ^1.32.0`, `postcss ^8.5.14` und laesst `esbuild` nur noch als Peer fuer kompatible Pfade zu; offizielle Vite-8-Doku empfiehlt fuer groessere Projekte den Zwischenschritt ueber `rolldown-vite` auf Vite 7, um Bundler-Risiko von Vite-8-Risiko zu trennen.
- `lokaler Config-Treffer`: Der produktive Vite-Scope hat keine `optimizeDeps`-/`esbuild`-Overrides und keinen `build.target`; relevant sind `build.rollupOptions.output.manualChunks` in `dev/vite/rendererShellConfig.js` sowie eigene Plugins mit `configureServer`/`configurePreviewServer` in `vite.config.js`. Bei Vite 8 waere `build.rollupOptions` zunaechst Kompatibilitaetspfad, spaeter aber auf `build.rolldownOptions` zu pruefen.
- `Gate-Leiter fuer den naechsten Code-Slice`: erst `vite@6.4.2` isoliert installieren und `npm audit --audit-level=low`, `npm run build`, `npm run build:app`, `npm run check:electron-renderer-drift`, einen Dev-Server-Smoke (`npm run test:playwright:desktop-e2e -- core-shell core-runtime` mit eigenem `TEST_PORT`/`PW_RUN_TAG`) und einen Preview-Smoke (`npm run test:desktop:smoke`) fahren. Erst wenn Option A rot bleibt oder Produkt-/CI-Signale stabil sind, Vite 7/`rolldown-vite`/Vite 8 als getrennte Folge-Slices planen.
- `Rollback`: Fuer jeden Upgrade-Slice bleiben `package.json` und `package-lock.json` die primaere Rueckrollflaeche (`git revert <commit>` nach Commit; vor Commit scoped restore der beiden Dateien und danach `npm ci`). Vite-Config-Aenderungen duerfen erst in einem eigenen Commit erfolgen, wenn der reine Dependency-Sprung ein konkretes Gate-Problem zeigt.

## Three-Matrix 2026-05-18 (90.3.2)

- `aktueller Stand`: Root nutzt `three@0.160.1`; `npm outdated --json` meldet `latest: 0.184.0`, aber `wanted: 0.160.1`, weil `package.json` ueber `^0.160.0` innerhalb der 0.x-Minor-Grenze bleibt. `npm audit --audit-level=low --json` meldet weiterhin nur die 2 moderaten Vite/esbuild-Befunde und keinen Three-Befund.
- `Upgrade-Distanz`: `0.160.1` -> `0.184.0` ueberspringt 24 Three-Revisionen. Die offizielle Three-Migration empfiehlt Versionswechsel revisionsweise zu verfolgen (`https://github.com/mrdoob/three.js/wiki/Migration-Guide`); fuer V90 ist Three deshalb kein Security-Hotfix, sondern ein eigener Rendering-Risiko-Slice.
- `Paket-/Export-Abgleich`: Beide Versionen bleiben ESM-Pakete und exportieren `./addons/*` sowie `./examples/jsm/*`; die lokalen `three/addons/...`-Imports sind damit formal weiter aufgeloest. Geaendert ist aber der Node-/Side-Effects-Pfad (`./examples/jsm/nodes/**/*` -> `./src/nodes/**/*`) und `0.184.0` ergaenzt `./webgpu`/`./tsl`, weshalb Tree-Shaking, Nodes und manuelle Chunk-Erkennung nicht blind als unveraendert gelten duerfen.
- `lokaler Scope`: `rg -l "from 'three'|from \"three\"|three/addons|three/examples|THREE\\." -- src editor tests dev scripts` findet 102 Dateien. Heisse Flaechen sind `src/core/Renderer.js`, `src/core/renderer/RecordingCapturePipeline.js`, `src/core/renderer/RenderQualityController.js`, `src/entities/**`, `editor/js/EditorCore.js`, `editor/js/EditorAssetLoader.js`, `src/ui/arcade/vehicle-manager/VehicleManagerPreview3d.js`, `tests/gpu.spec.js` und `tests/editor-vehicle.spec.js`.
- `Addon-/Loader-Risiko`: Orbit-/TransformControls, OBJ-/MTL-/GLTFLoader und `mergeGeometries` laufen ueber `three/addons`. `dev/vite/rendererShellConfig.js` chunked resolved `node_modules/three/examples/jsm/loaders/OBJLoader.js`/`MTLLoader.js`; das bleibt wahrscheinlich kompatibel, muss aber im Build-Output geprueft werden, sobald die Three-Version tatsaechlich steigt.
- `Renderer-Risiko`: Produktpfade setzen `WebGLRenderer`, `BasicShadowMap`, `PCFSoftShadowMap`, `ACESFilmicToneMapping`, `toneMappingExposure` und teils `outputColorSpace = THREE.SRGBColorSpace`. Besonders empfindlich sind Screenshot-/Recording-Pfade mit `preserveDrawingBuffer`, das Editor-Viewport-Verhalten und `tests/gpu.spec.js`, das Tone-Mapping-Zustaende direkt ausliest.
- `Empfohlener Slice`: Kein Package-Sprung in 90.3.2. Der naechste Three-Code-Slice darf `package.json`/`package-lock.json` isoliert auf eine Zielversion heben, soll aber bei Rot-Signalen auf Zwischenrevisionen zurueckfallen statt `0.160.1` -> `0.184.0` mit Konfig- und Code-Fixes zu vermischen.
- `Gate-Leiter fuer den naechsten Code-Slice`: nach dem Dependency-Sprung zuerst `npm run build`, `npm run build:app`, `npm run test:desktop:smoke`, `npm run test:playwright:browser-compat -- tests/editor-vehicle.spec.js`, `npm run test:editor-ui` und bei Renderer-/Tone-/Capture-Auffaelligkeiten `npm run test:heavy` beziehungsweise `tests/gpu.spec.js` laufen lassen. Visuale Produktkontrolle braucht mindestens Desktop-Smoke plus Editor-/Vehicle-Preview-Signal.
- `Rollback`: Primaere Rueckrollflaeche bleiben `package.json` und `package-lock.json`; Renderer-, Loader- oder Vite-Chunk-Anpassungen erst nach einem konkret reproduzierten Gate-Fehler und dann in getrennten Commits. Vor Commit kann der Slice scoped restored und mit `npm ci` auf `three@0.160.1` zurueckgesetzt werden.

## TypeScript-/ESLint-/Playwright-Matrix 2026-05-18 (90.3.3)

- `aktueller Stand`: `npm outdated --json` meldet `typescript 5.9.3 -> latest 6.0.3` als echten Major ausserhalb der Range, `eslint 10.0.3 -> wanted/latest 10.4.0`, `@playwright/test 1.58.2 -> wanted/latest 1.60.0`, `@types/node 25.5.0 -> 25.8.0`, `@commitlint/* 20.4.4 -> wanted 20.5.3/latest 21.0.1` und `ws 8.19.0 -> 8.20.1`. `npm audit --audit-level=low --json` meldet weiterhin nur die 2 moderaten Vite/esbuild-Befunde; keiner dieser Tooling-Kandidaten ist aktuell Audit-getrieben.
- `TypeScript 6`: `typescript@6.0.3` behaelt Node `>=14.17`, ist aber laut offizieller Release-Note ein Uebergangsrelease mit Breaking-Changes und Deprecations Richtung TypeScript 7 (`https://www.typescriptlang.org/docs/handbook/release-notes/typescript-6-0.html`). Lokale Entschaerfung: `tsconfig.json` setzt bereits `strict: false`, `types: []`, `noEmit: true`; `tsconfig.architecture.json` setzt explizit `types: ["node"]`. Risiko bleibt bei `rootDir`-/Dateilisten-, JS-Check- und Node-Typ-Aenderungen.
- `ESLint 10.x`: Root ist bereits auf `eslint@10.0.3`, nutzt Flat Config (`eslint.config.js`) und `eslint-plugin-boundaries@6.0.2` mit Peer `eslint >=6.0.0`; `eslint@10.4.0` behaelt dieselbe Node-Engine `^20.19.0 || ^22.13.0 || >=24`. Die offizielle v10-Migration (`https://eslint.org/docs/latest/use/migrate-to-10.0.0`) bleibt relevant fuer Rule-/Plugin-API-Aenderungen, lokal aber eher als Minor-Regressionsrisiko in `lint:architecture` und `check:eslint:touched-strict`.
- `Playwright 1.60`: `@playwright/test@1.60.0` bleibt Node `>=18`, bringt aber neue Browser-Bundles und laut Release-Notes entfernte lange deprecated APIs sowie Reporter-/Trace-/HAR-Aenderungen (`https://playwright.dev/docs/release-notes`). Lokale Treffer sind `playwright.config.js`, `playwright.editor.config.mjs`, `playwright.local.repro.config.mjs`, `scripts/playwright-*`, `dev/scripts/verify-lock.mjs` und alle `tests/*.spec.js`; damit ist Playwright ein Test-Harness-Slice, kein Runtime-Fix.
- `Commitlint/@types/ws`: `@commitlint/*@21.0.1` verlangt Node `>=22.12.0` und betrifft Hook-/Commit-Governance; erst `20.5.3` ist ein kleinerer Kandidat. `@types/node@25.8.0` gehoert zum TypeScript-Slice, weil es `typecheck:architecture` beeinflussen kann. `ws@8.20.1` ist Runtime-/Server-/Training-Code und bleibt getrennt von 90.3.3, solange kein eigener Server-Gate-Scope freigegeben ist.
- `Empfohlene Reihenfolge`: (1) ESLint/Commitlint-20.x als Tooling-Minor-Slice mit `npm run lint:architecture`, `npm run check:eslint:touched-strict`, `npm run gates:pre-commit`; (2) TypeScript 6 plus `@types/node` als Architecture-Typecheck-Slice mit `npm run typecheck:architecture`, `npm run architecture:guard`, `npm run build`; (3) Playwright 1.60 als isolierter Browser-Harness-Slice mit eigenem `TEST_PORT`/`PW_RUN_TAG`, `npx playwright --version`, `npm run test:desktop:smoke`, `npm run test:playwright:browser-compat -- tests/runtime-facade.spec.js` und bei Editor-Signal `npm run test:editor-ui`.
- `Rollback`: Jeder Slice bleibt auf `package.json`/`package-lock.json` begrenzt, bis ein konkretes Gate rot wird. Vor Commit scoped restore der Paketdateien und `npm ci`; nach Commit `git revert <commit>` plus bei Playwright ggf. Browser-Cache/Installationsstand ueber `npx playwright install` erneut synchronisieren. Config-Aenderungen an ESLint, TS oder Playwright erst in separaten Folgecommits, wenn die Dependency-Aenderung den Fehler reproduzierbar zeigt.

## Risiken

- R1 | hoch | `npm audit fix --force` hebt Vite auf eine Breaking-Version und destabilisiert Build/Preview/Electron.
- R2 | mittel | Three-Major-Upgrade veraendert Loader-, Material- oder Kamera-Verhalten.
- R3 | mittel | Playwright-Upgrade veraendert bestehende Harness-Flakes und erschwert V87/V88-Abschluss.
- R4 | mittel | Lockfile-only-Fixes werden ohne CI-/Build-Abgleich committed und verstecken echte Kompatibilitaetsfehler.
- R5 | hoch | Neue Advisory-Daten koennen non-force Transitiv-Fixes sichtbar machen; alte Audit-Finding-Texte duerfen diese Fix-Kandidaten nicht als erledigt kaschieren.
- R6 | mittel | Vite 8 kann `build.rollupOptions` zunaechst kompatibel abbilden, fuehrt aber Rolldown/Oxc als neue Bundler-/Transform-Basis ein; Custom-Plugin-Middleware und `manualChunks` brauchen deshalb eigene Dev-/Preview-/Electron-Smokes.
- R7 | hoch | Ein direkter Three-Sprung `0.160.1` -> `0.184.0` kann Rendering-, Loader-, Editor- und Recording-Aenderungen koppeln; Zwischenrevisionen und getrennte Commits sind der bevorzugte Rueckfallpfad.
- R8 | mittel | TypeScript 6 und `@types/node` koennen JS-Check-/Architecture-Typecheck-Fehler sichtbar machen, obwohl der Produktcode unveraendert bleibt.
