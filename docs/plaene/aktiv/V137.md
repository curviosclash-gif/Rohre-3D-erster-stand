---
id: V137
title: CodeGraph Read-only Installationsspike fuer Graph-RAG
status: planned
priority: P2
owner: frei
depends_on:
  - V107.99
  - V110.99
  - V111.99
  - V120.1
soft_depends_on:
  - V122.5
  - V124.1
blocked_by: []
affected_area: codegraph-readonly-installation-spike
scope_files:
  - docs/Umsetzungsplan.md
  - docs/plaene/aktiv/V137.md
  - docs/plaene/neu/Feature_CodeGraph_Readonly_Installation_Spike.md
  - docs/plaene/CHANGELOG.md
  - .gitignore
scope_reference_files:
  - AGENTS.md
  - .agents/rules/planning_and_governance.md
  - .agents/rules/token_efficiency_and_tools.md
  - docs/plaene/aktiv/V94.md
  - docs/plaene/aktiv/V107.md
  - docs/plaene/aktiv/V110.md
  - docs/plaene/aktiv/V111.md
  - docs/plaene/aktiv/V120.md
  - docs/plaene/aktiv/V122.md
  - docs/plaene/aktiv/V124.md
scope_overlap_allowed_with:
  - V120
  - V123
  - V122
  - V124
  - V138
updated_at: 2026-05-28
verification:
  - npm run plan:check
  - npm run plan:index:check
  - npm run graph:check
  - npm run docs:sync
  - npm run docs:check
source_history:
  - docs/plaene/neu/Feature_CodeGraph_Readonly_Installation_Spike.md
---

# CodeGraph Read-only Installationsspike fuer Graph-RAG

## Kurzfassung

V137 prueft CodeGraph als optionalen, read-only Hilfsindex fuer generische Code-Navigation. Der bestehende Curvios-Wissensgraph bleibt kanonisch fuer Planstatus, Dependencies, Locks, Scope-Dateien, Critical Paths, Produktsemantik, Testauswahl und Evidence.

CodeGraph darf nur Kandidaten fuer Symbol-, Caller-/Callee-, Trace- und Implementierungsfragen liefern. Jede Aussage mit Plan-, Runtime-, Test- oder Governance-Wirkung muss gegen Curvios-Graph, Quelle oder Test-Evidence gegengeprueft werden.

## Ziel

- CodeGraph sicher und reversibel als lokalen read-only Spike einplanen.
- Vor jeder Installation klaeren, welche Dateien, User-Configs und lokalen Caches entstehen.
- Den Nutzen gegen bestehende Curvios-Queries messen, statt eine zweite Wahrheit aufzubauen.
- Eine klare Go/No-Go-Entscheidung fuer V120/V122 vorbereiten:
  - behalten als optionaler Candidate Provider,
  - nur manuell/CLI nutzen,
  - oder sauber verwerfen.
- V120 nicht mit einer stillen Tool-Installation aufblaehen; V137 bleibt ein eigener Gate- und Recovery-Scope.

## Nicht-Ziel

- Kein Ersatz von `scripts/query-knowledge-graph.mjs`.
- Keine automatische MCP-Registrierung ohne explizites User-Gate.
- Keine globale Installation oder PowerShell-Pipe-Installation als Default.
- Keine Schreibtools, keine Agent-Orchestrierung und keine Rueckschreibung in Curvios-Plaene, Memory oder Graph.
- Keine Aufnahme von `.codegraph/`, lokalen SQLite-Dateien, generierten CodeGraph-Reports oder Agent-Config-Dumps in Git.
- Kein Anspruch, dass CodeGraph Curvios-Produktsemantik, Locks oder User-Gates kennt.
- Keine produktiven Spiel-, Runtime-, UI-, Multiplayer-, Recording-, Physik- oder Bot-Training-Aenderungen.

## Einordnung

Primaere Andockstelle ist `V120`: CodeGraph kann dort spaeter als zusaetzliche Kandidatenquelle fuer Code-Symbole und Callpfade dienen, sofern der Spike nach `V120.1` genug Nutzen zeigt.

Sekundaere Andockstelle ist `V122`, falls ein read-only MCP-Tool fuer Agent-Kontext sinnvoll wird. Dann gilt weiter: erster MCP nur read-only, Registrierung nur nach User-Gate, Ergebnisse bleiben Hinweise.

`V124` bleibt der bessere Zielblock fuer produktsemantische Verbesserungen im Curvios-Graph. CodeGraph darf dort nur technische Code-Naehe liefern, keine Produktbedeutung.

## Installation Strategy

Die erste Evaluation laeuft ohne dauerhafte Agent-Registrierung. Bevor `codegraph init` im Hauptrepo ausgefuehrt wird, muss `.codegraph/` als lokaler Indexpfad in `.gitignore` abgesichert sein oder ein gleichwertiger No-Commit-Schutz dokumentiert werden.

Bevor ein Installer mit Schreibwirkung laeuft, wird ein aktueller Freshness-Check der offiziellen CodeGraph-Doku gemacht und die konkrete Version festgehalten. MCP-Registrierung, globale Installation und Agent-Config-Aenderungen sind immer separate D4/User-Gates.

## Decision-Klasse und Gates

| Schritt | Klasse | Gate | Begruendung |
| --- | --- | --- | --- |
| Doku-/Freshness-Check | D0 | AUTO | Read-only Web-/Doku-Abgleich. |
| Vergleichsplanung und lokale Reports unter `tmp/` | D1/D2 | REVIEW | Begrenzter Spike ohne aktive Source-of-Truth-Aenderung. |
| `.gitignore`-Absicherung fuer `.codegraph/` | D2 | REVIEW | Kleiner Repo-Schutz, bevor lokale Indexdateien entstehen. |
| `codegraph init` im Hauptrepo | D4 | USER-GATE | Externes Tool erzeugt lokale Daten im Arbeitsbaum. |
| Globale Installation oder Agent-Config | D4 | USER-GATE | Aendert User-Umgebung und MCP-/Agent-Oberflaeche. |
| MCP-Registrierung | D4 | USER-GATE | Neue Tool-Oberflaeche fuer Agents. |
| Uebernahme als V120-Candidate-Provider | D3 | USER-GATE | Aendert aktive Plan-/Workflow-Semantik. |

## Definition of Done

- [x] DoD.1 Aktuelle CodeGraph-Version, Installationsweg, Lizenz, Datei-/Config-Wirkung und MCP-Tools sind dokumentiert. (abgeschlossen: 2026-05-28; evidence: `137.1` dokumentiert offizielle Doku, Changelog, npm-Metadaten `@colbymchenry/codegraph` `0.9.6`/MIT/latest, MCP-Tools und Datei-/Config-Wirkung)
- [x] DoD.2 `.codegraph/` oder der gewaehlte lokale Indexpfad kann nicht versehentlich in Git landen. (abgeschlossen: 2026-05-28; evidence: `137.2` ergaenzt `.gitignore` um `.codegraph/`, bevor ein CodeGraph-Init laeuft)
- [x] DoD.3 Kein Installer mit Schreibwirkung wurde ohne User-Gate ausgefuehrt. (abgeschlossen: 2026-05-28; evidence: bis einschliesslich `137.2` kein Installer, kein `npx @colbymchenry/codegraph`, kein `codegraph init`, keine MCP-Registrierung und keine Agent-/User-Config-Aenderung ausgefuehrt)
- [x] DoD.4 Ein No-write-/Dry-run-Pfad fuer Codex-MCP-Konfiguration wurde geprueft oder als nicht verfuegbar dokumentiert. (abgeschlossen: 2026-05-28; evidence: `npx --yes @colbymchenry/codegraph@0.9.6 install --print-config codex` -> PASS mit Config-Snippet fuer `C:\Users\gunda\.codex\config.toml`, keine Datei-Schreibwirkung)
- [x] DoD.5 CodeGraph wurde gegen mindestens drei Curvios-Referenzfragen verglichen. (abgeschlossen: 2026-05-28; evidence: `137.4` vergleicht SettingsManager-Impact, Spawn-Eventflow und Scope-Collisions gegen CodeGraph `query`/`impact`/`context`-Signale)
- [x] DoD.6 Vergleich trennt klar: Curvios-Wahrheit, CodeGraph-Hinweis, offene Unsicherheit. (abgeschlossen: 2026-05-28; evidence: `137.4` dokumentiert Curvios als kanonisch fuer Plan/Owner/Test/Scope und CodeGraph nur als technische Symbol-/Lesepfad-Hilfe mit Archiv-/False-positive-Risiko)
- [x] DoD.7 Eine Go/No-Go-Entscheidung fuer V120/V122 liegt vor. (abgeschlossen: 2026-05-28; evidence: `137.6` entscheidet `manual-only`: CodeGraph bleibt lokales CLI-Hilfsmittel ohne MCP-/Agent-Registrierung; V120/V122 duerfen hoechstens CLI-Ausgaben als Hinweise aufnehmen)
- [x] DoD.8 Recovery-Pfad wurde trocken dokumentiert oder praktisch ausgefuehrt. (abgeschlossen: 2026-05-28; evidence: `137.6` dokumentiert trocken `npx --yes @colbymchenry/codegraph@0.9.6 uninit` fuer `.codegraph/` und Entfernen des nicht angewendeten Codex-MCP-Stanzas als Recovery; keine D4-Removal-Aktion ausgefuehrt)
- [ ] DoD.9 Abschluss-Gates fuer Plan, Graph, Index und Docs sind gruen oder blockerfest dokumentiert.

## Phasen

### 137.1 Freshness und Installationswirkung
status: done
goal: Externes Tool vor jeder lokalen Wirkung verstehen.
output: Version, Installationsweg, MCP-Wirkung und Dateieffekte sind belegt.

- [x] 137.1.1 Offizielle CodeGraph-Doku erneut pruefen: Version, unterstuetzte Agents, MCP-Tools, `init`, `uninit`, `install --print-config`, lokale Indexpfade. (abgeschlossen: 2026-05-28; evidence: offizielle Doku `https://colbymchenry.github.io/codegraph/getting-started/installation/`, `https://colbymchenry.github.io/codegraph/reference/cli/`, `https://colbymchenry.github.io/codegraph/reference/mcp-server/`, `https://colbymchenry.github.io/codegraph/getting-started/configuration/` und Changelog `https://github.com/colbymchenry/codegraph/blob/main/CHANGELOG.md` geprueft; `npm view @colbymchenry/codegraph version license dist-tags.latest --json` -> PASS mit `0.9.6`/MIT/latest)
- [x] 137.1.2 Datei- und Config-Wirkung klassifizieren: repo-lokal, global user config, MCP registry, Cache, Netzwerkdownload. (abgeschlossen: 2026-05-28; evidence: Doku belegt `.codegraph/` mit SQLite-DB im Projektroot, Agent-MCP-/Instructions-Schreibpfade wie `CLAUDE.md`, `.cursor/rules/codegraph.mdc`, `~/.codex/AGENTS.md`, PATH-/global-vs-local-Installationsfragen, `codegraph uninstall` fuer Agent-Config und `codegraph uninit` fuer Projektindex; `Get-Command codegraph -ErrorAction SilentlyContinue` -> not installed; `.gitignore` enthaelt noch keine `.codegraph/`-Regel und bleibt Aufgabe fuer `137.2`)
- [x] 137.1.3 Sicherheitsentscheidung festhalten: kein Pipe-Installer als Default; bevorzugt versionierter npm-/npx- oder bereits installierter Binary-Pfad. (abgeschlossen: 2026-05-28; evidence: kein Installer, kein `npx @colbymchenry/codegraph`, kein `codegraph init`, keine MCP-Registrierung und keine User-/Agent-Config-Aenderung ausgefuehrt; bevorzugter Folgepfad ist erst `.gitignore`-Schutz in `137.2`, danach nur nach separatem D4/User-Gate versioniert oder bereits installiert weiter)

Evidence-Notiz 2026-05-28: V137.1 klaert die CodeGraph-Freshness ohne lokale Tool-Wirkung. Offizielle Doku und npm-Metadaten zeigen `@colbymchenry/codegraph` `0.9.6` unter MIT, MCP-Tools fuer Suche/Kontext/Trace/Callers/Callees/Impact/Node/Explore/Files/Status und einen Projektindex unter `.codegraph/` mit lokaler SQLite-DB. Installations- und Agentenwirkung bleibt nicht-harmlos: der Installer kann PATH, Agent-MCP-Configs und Instructions-Dateien schreiben; `install --print-config codex` ist der dokumentierte No-write-Config-Pfad, setzt aber eine verfuegbare CLI voraus. `Not-checked:` kein Installer-Download, kein Pipe-Installer, kein `npx`-Run, kein `codegraph init`/`uninit`, keine MCP-Registrierung, keine globale oder lokale Agent-Config-Aenderung, kein Vergleich gegen Curvios-Referenzfragen und keine Vollsuite.

### 137.2 Repo-Schutz vor Init
status: done
goal: Kein lokaler CodeGraph-Index landet in Git.
output: `.gitignore` oder externer Indexpfad schuetzt den Arbeitsbaum.

- [x] 137.2.1 `.gitignore`-Status pruefen und `.codegraph/` absichern, bevor `codegraph init` im Hauptrepo laeuft. (abgeschlossen: 2026-05-28; evidence: `.gitignore` enthielt vor dem Slice keine `.codegraph/`-Regel und ignoriert jetzt `.codegraph/` unter lokalen Laufzeit-/Screenshot-Artefakten)
- [x] 137.2.2 `git status --short` vor Init dokumentieren. (abgeschlossen: 2026-05-28; evidence: `git status --short --branch` vor jeder Init-Wirkung -> `## main...origin/main [ahead 5]` und bereits fremd gestaged `A docs/plaene/neu/Feature_Graph_RAG_Followup_Roadmap_V139.md`; keine `.codegraph/`- oder CodeGraph-Init-Dateien vorhanden)
- [x] 137.2.3 Wenn ein separater Sandbox-Pfad genutzt wird, den Pfad unter `tmp/` halten und nicht als dauerhafte Doku verwenden. (abgeschlossen: 2026-05-28; evidence: kein separater Sandbox-Pfad angelegt; falls spaeter transienter Vergleich noetig wird, bleibt er unter `tmp/graph-rag/codegraph-spike/` und wird nicht zur dauerhaften Quelle)

Evidence-Notiz 2026-05-28: V137.2 schliesst den Repo-Schutz vor dem D4-Init-Gate. `.codegraph/` ist jetzt lokal ignoriert, der Pre-Init-Status ist dokumentiert, und der einzige bekannte Fremdstatus bleibt der bereits gestagte V139-Intake ausserhalb dieses Slices. `Not-checked:` kein CodeGraph-Installer, kein `npx`, kein `codegraph init`/`uninit`, keine MCP-Registrierung, keine Agent-/User-Config-Aenderung, keine CodeGraph-Vergleichsfragen und keine Vollsuite.

### 137.3 Projekt-Init und Baseline
status: done
goal: CodeGraph lokal erzeugen, aber noch nicht als Agent-Tool verdrahten.
output: Lokaler Index und Statussignal ohne getrackten Tool-Churn.

- [x] 137.3.1 User-Gate einholen fuer `codegraph init` oder `codegraph init -i`. (abgeschlossen: 2026-05-28; evidence: User-Freigabe im Thread fuer V137 137.3; `npm run agent:preflight -- --workflow=fix-planung --decision=D4 ...` mit temporaerem Index wegen fremdem staged V139 -> PASS; `npx --yes @colbymchenry/codegraph@0.9.6 init` -> PASS)
- [x] 137.3.2 Nach Init `git status --short` pruefen; getrackte Aenderungen duerfen nur vorher erlaubte Schutz-/Plan-Dateien sein. (abgeschlossen: 2026-05-28; evidence: `.codegraph/` enthaelt nur ignorierte lokale Dateien `.gitignore` und `codegraph.db`; `git status --short --branch` zeigt keine getrackten CodeGraph-Dateien, nur V137-Lock-/Plan-Scope plus bekannte fremde Mobile-/V139-Aenderungen)
- [x] 137.3.3 CodeGraph-Status-/Files-Signal erfassen und grob mit Curvios-Dateiabdeckung vergleichen. (abgeschlossen: 2026-05-28; evidence: `npx --yes @colbymchenry/codegraph@0.9.6 sync` -> PASS mit 1057 geaenderten Dateien und 18321 Nodes; `npx --yes @colbymchenry/codegraph@0.9.6 status` -> PASS mit 1057 Files, 18321 Nodes, 50499 Edges, 50.73 MB DB, Index up to date; `node scripts/query-knowledge-graph.mjs coverage-report --json` -> PASS mit Curvios 2094 tracked Files, 1412 adjusted tracked Files, Coverage-Gate pass)

Evidence-Notiz 2026-05-28: V137.3 hat den lokal ignorierten CodeGraph-Projektindex im Hauptrepo aufgebaut, aber keine MCP-Registrierung, globale Installation oder Agent-/User-Config-Aenderung vorgenommen. Das erste Files-Signal zeigt eine technische Code-/Symbol-nahe Abdeckung: CodeGraph indexiert 1057 Dateien (933 JavaScript, 91 Python, 20 XML, 6 Java, 4 Properties, 3 YAML) und 18321 Nodes. Der Curvios-Graph bleibt breiter und kanonisch fuer Plan, Produktsemantik, Dependencies, Locks und Tests: `coverage-report` meldet 2094 tracked Files, 1412 adjusted tracked Files und ein gruenes Coverage-Gate. `Not-checked:` keine MCP-Registrierung, kein `install --print-config`, keine globale Installation, keine Agent-/User-Config-Schreibpfade, noch kein Vergleich der drei Referenzfragen aus `137.4`.

### 137.4 Vergleich mit Curvios-Graph
status: done
goal: Nutzen an echten Referenzfragen messen.
output: Vergleich trennt harte Curvios-Fakten von CodeGraph-Hinweisen.

- [x] 137.4.1 Settings-Referenz: `SettingsManager` Curvios-Impact gegen CodeGraph-Symbol-/Caller-/Callee-Sicht vergleichen. (abgeschlossen: 2026-05-28; evidence: `node scripts/query-knowledge-graph.mjs impact-for-file src/core/SettingsManager.js --json` -> PASS; CodeGraph `query SettingsManager`, `callers SettingsManager`, `callees SettingsManager`, `impact SettingsManager --depth 2` -> PASS)
- [x] 137.4.2 Spawn-Referenz: Curvios `event-flow spawn` gegen CodeGraph-Trace/Context vergleichen. (abgeschlossen: 2026-05-28; evidence: `node scripts/query-knowledge-graph.mjs event-flow spawn --json` -> PASS; CodeGraph `query spawn`, `query EntitySpawnOps`, `impact EntitySpawnOps --depth 2`, `context "spawn lifecycle EntitySpawnOps SpawnPlacementSystem" --format json --no-code --max-nodes 20` -> PASS)
- [x] 137.4.3 Scope-Referenz: `scope-collisions` bleibt Curvios-only Wahrheit; pruefen, ob CodeGraph trotzdem relevante Code-Lesepfade kuerzt. (abgeschlossen: 2026-05-28; evidence: `node scripts/query-knowledge-graph.mjs scope-collisions --json` -> PASS; CodeGraph `query scope-collisions`, `query scopeCollisions`, `context "scope-collisions query-knowledge-graph active block scope files" --format json --no-code --max-nodes 20` -> PASS)
- [x] 137.4.4 Ergebnis als kurzen transienten Report oder aktive V137-/V120-Evidence zusammenfassen. (abgeschlossen: 2026-05-28; evidence: aktive V137-Evidence-Notiz in diesem Abschnitt)

Evidence-Notiz 2026-05-28: V137.4 bestaetigt CodeGraph als nuetzlichen technischen Candidate Provider, aber nicht als Curvios-Wahrheit. Settings: Der Curvios-Graph liefert fuer `src/core/SettingsManager.js` den kritischen Pfad `settings`, Ownership `settings-runtime`/`settings-domain`, Guardrail gegen `config:settings-runtime-limits`, State-/Config-Beziehungen und Testanker `tests/runtime-settings-live-apply.contract.test.mjs` sowie `tests/settings-manager.contract.test.mjs`. CodeGraph findet die Klasse, Methoden und breite Impact-Lesepfade wie `SettingsStore`, `MenuRuntimeSessionService`, `GameRuntimeFacade`, `ArcadeRunRuntime` und Settings-Facades; `callers`/`callees` auf der Klasse bleiben leer und der Impact nimmt auch archivierte Legacy-JS-Dateien auf. Spawn: Curvios liefert `event:spawn`, `runtime:entity-spawn-ops`, `runtime:spawn-placement-system`, `state:spawn-context`, `config:gameplay-config-contract` und Testanker `tests/physics-core.spec.js` mit Provenance. CodeGraph findet bei breiter `spawn`-Suche relevante Symbole, aber auch gleichnamige Pfade wie `Player::spawn`, Partikel-Spawn, Dev-Server-Spawn und Archiv-JS; gezieltes `EntitySpawnOps`/Context kuerzt dagegen den Code-Lesepfad auf `EntitySpawnOps`, `SpawnPlacementSystem`, `EntityRuntimeContext` und `EntityRuntimeSystemAssembly`. Scope: Curvios `scope-collisions` meldet kanonisch `V106`/`V113`, `V113`/`V96` und `V121`/`V132`; CodeGraph findet keine Plan-/Lock-Wahrheit, sondern nur Test-Referenz `REFERENCE_QUESTIONS`, Repo-Map-Hotspot-Code und Plan-Map-Viewer-Funktionen wie `collisionsForBlock`, plus False Positives aus Runtime-Collision-Systemen. Ergebnis: CodeGraph spart technische Code-Navigation bei gut benannten Symbolen, muss aber immer gegen Curvios-Graph, Quellen oder Tests gegengeprueft werden. `Not-checked:` keine MCP-Registrierung, kein `install --print-config`, keine globale Installation, keine Agent-/User-Config-Aenderung, kein `codegraph sync` in diesem Slice, keine Vollsuite.

### 137.5 MCP-Entscheidung
status: done
goal: Tool-Oberflaeche nur oeffnen, wenn der Nutzen den Governance-Preis rechtfertigt.
output: No-MCP, read-only MCP oder Verzicht ist entscheidbar.

- [x] 137.5.1 No-MCP-Variante bewerten: CLI/Report reicht fuer V120 Candidate Provider? (abgeschlossen: 2026-05-28; evidence: `137.4` zeigt genuegend Nutzen ueber CLI `query`/`impact`/`context`; `npx --yes @colbymchenry/codegraph@0.9.6 status` -> PASS mit 1057 Files/18322 Nodes/50492 Edges, Index up to date)
- [x] 137.5.2 Falls MCP sinnvoll: konkrete read-only Tools, Config-Pfad, Recovery und User-visible Risk nennen. (abgeschlossen: 2026-05-28; evidence: `npx --yes @colbymchenry/codegraph@0.9.6 install --print-config codex` -> PASS mit `C:\Users\gunda\.codex\config.toml`, `[mcp_servers.codegraph]`, `command = "codegraph"`, `args = ["serve", "--mcp"]`; Recovery waere Stanza entfernen und optional `codegraph uninit`; Risiko bleibt neue Agent-Tool-Oberflaeche/PATH-Abhaengigkeit/Index-Staleness)
- [x] 137.5.3 MCP-Registrierung nur nach separatem D4/User-Gate ausfuehren. (abgeschlossen: 2026-05-28; evidence: keine MCP-Registrierung, kein `codegraph install`, keine Aenderung an `C:\Users\gunda\.codex\config.toml`, `C:\Users\gunda\.claude.json` oder Agent-Instructions; nur `install --print-config` no-write ausgefuehrt)

Evidence-Notiz 2026-05-28: V137.5 entscheidet gegen eine sofortige MCP-Oeffnung. Die No-MCP-Variante reicht fuer V120 als manueller Candidate Provider, weil die drei Referenzfragen bereits per CLI genuegend Code-Lesepfade liefern und Curvios-Graph/Quelle/Test ohnehin gewinnen muessen. Ein spaeterer read-only MCP waere technisch klar: Codex-Config-Pfad `C:\Users\gunda\.codex\config.toml`, Server `codegraph`, Command `codegraph`, Args `["serve", "--mcp"]`; relevante Tools bleiben Suche, Context, Callers/Callees, Impact, Files und Status. Governance-Preis: neue Agent-Oberflaeche, PATH-/lokale-Index-Abhaengigkeit und hohes Risiko, technische Naehe als Plan-/Produktwahrheit fehlzulesen. Entscheidung: kein MCP jetzt; V122 kann spaeter mit eigenem D4/User-Gate erneut fragen. `Not-checked:` keine MCP-Registrierung, kein Agent-/User-Config-Schreibzugriff, kein globaler Install, kein `codegraph install`, keine Vollsuite.

### 137.6 Go/No-Go
status: done
goal: Klare Folgeentscheidung statt dauerhafter Probeinstallation.
output: CodeGraph wird behalten, manuell begrenzt oder entfernt.

- [x] 137.6.1 `keep`: CodeGraph bleibt optionaler read-only Candidate Provider fuer V120. (abgeschlossen: 2026-05-28; evidence: bewertet, aber nicht als Agent-/MCP-Keep gewaehlt; CodeGraph-Ausgaben duerfen nur manuelle Hinweise fuer Code-Symbole/Callpfade bleiben)
- [x] 137.6.2 `manual-only`: CodeGraph bleibt lokales CLI-Hilfsmittel ohne Agent-/MCP-Registrierung. (abgeschlossen: 2026-05-28; evidence: Go/No-Go = `manual-only`; lokaler `.codegraph/`-Index bleibt ignoriert, keine MCP- oder Agent-Config-Wirkung)
- [x] 137.6.3 `remove`: `codegraph uninit`/Cleanup ausfuehren und Ergebnis dokumentieren. (abgeschlossen: 2026-05-28; evidence: Removal bewertet, aber nicht ausgefuehrt; Recovery trocken dokumentiert: `npx --yes @colbymchenry/codegraph@0.9.6 uninit` entfernt `.codegraph/`, nicht angewendete MCP-Config haette als Stanza-Entfernung aus `C:\Users\gunda\.codex\config.toml` zu erfolgen)

Evidence-Notiz 2026-05-28: V137.6 setzt die Folgeentscheidung auf `manual-only`. Fuer V120/V122 bedeutet das: CodeGraph darf als lokales CLI-Hilfsmittel technische Kandidaten fuer Symbole, Callpfade und Implementierungsnaehe liefern, aber keine Plan-, Scope-, Lock-, Test- oder Produktsemantik entscheiden. V120/V122-Uebergabe muss CodeGraph-Ausgaben als Hinweis markieren und Curvios-Graph, Quellen oder Test-Evidence als Gewinner benennen. Recovery bleibt reversibel: `.codegraph/` ist ignoriert; `codegraph uninit` waere der lokale Index-Remove; MCP-Stanza wurde nicht geschrieben und braucht daher keine praktische Ruecknahme. `Not-checked:` kein `codegraph uninit`, keine MCP-Registrierung, keine Agent-/User-Config-Aenderung, keine Vollsuite.

### 137.99 Abschluss-Gate
status: open
goal: Der Installationsspike ist governance-klar entschieden.
output: Abschlussnotiz mit Nutzen, Grenzen, Recovery und Folgeentscheidung.

- [x] 137.99.1 `npm run plan:check` ist gruen. (abgeschlossen: 2026-05-28; evidence: `npm run plan:check` -> PASS)
- [x] 137.99.2 `npm run plan:index:check` ist gruen. (abgeschlossen: 2026-05-28; evidence: `npm run plan:index:build` -> PASS; `npm run plan:index:check` -> PASS)
- [x] 137.99.3 `npm run graph:check` ist gruen oder blockerfest dokumentiert. (abgeschlossen: 2026-05-28; evidence: `npm run graph:check` -> FAIL mit `COVERAGE_GATE_FAILED` fuer `update_android_phone.bat`; blockerfest dokumentiert in `docs/Fehlerberichte/2026-05-28_v137-abschluss-blocked-by-android-update-coverage.md`)
- [x] 137.99.4 `npm run docs:sync` und `npm run docs:check` sind gruen oder blockerfest dokumentiert. (abgeschlossen: 2026-05-28; evidence: `npm run docs:sync` -> PASS; `npm run docs:check` -> PASS)
- [ ] 137.99.5 Abschlussnotiz nennt: CodeGraph-Version, Datei-/Config-Wirkung, Vergleichsfragen, MCP-Entscheidung, Recovery und Not-checked.
- [ ] 137.99.6 Falls V120/V122 Folgearbeit aufnehmen: Uebergabe nennt exakt, welche CodeGraph-Ausgaben Hinweise bleiben und welche Curvios-Evidence weiterhin gewinnen muss.

Blocker-Notiz 2026-05-28: V137.99 ist vorbereitet, bleibt aber offen, weil `graph:check` ausserhalb des V137-Scope rot ist. `coverage-report` meldet `update_android_phone.bat` als neue uncovered aktive Datei (`classification: other`, keine `scopeBlocks`). Der V137-Slice absorbiert diesen Android-/Repo-Ops-Drift nicht; Graph-Artefakt-Diffs aus dem Diagnose-Build wurden nicht in den V137-Scope uebernommen. Naechster Schritt: externen Coverage-Scope fuer `update_android_phone.bat` klaeren, danach `137.99.5`/`137.99.6` und Blockabschluss erneut pruefen.

## Risiken

| Risiko | Stufe | Mitigation |
| --- | --- | --- |
| Zweite Wahrheit neben Curvios-Graph | hoch | CodeGraph-Ausgaben nur als Hinweise; Curvios-Graph/Plan/Test bleibt kanonisch. |
| Lokale Indexdateien landen in Git | hoch | `.codegraph/` vor Init ignorieren oder Index ausserhalb des Repos halten. |
| MCP erweitert Agent-Rechte unbemerkt | hoch | MCP nur D4/User-Gate, read-only, keine Schreibtools. |
| Externes Tool veraendert User-Config | mittel | Erst Print-config/Dry-run, danach explizites Gate fuer Config-Schreibzugriff. |
| Technischer Callgraph wird mit Produktwirkung verwechselt | mittel | Vergleichsreport trennt Code-Naehe von Produkt-/Plan-/Test-Evidence. |
| Pflegeaufwand uebersteigt Nutzen | mittel | Go/No-Go nach drei Referenzfragen; kein Dauerbetrieb ohne messbaren Nutzen. |

## Dependencies

- hard: `V107.99` (Core-Graph und Query-Layer)
- hard: `V110.99` (Graph Ops-/Guard-Haertung)
- hard: `V111.99` (Adaptive Diagnose- und Entscheidungsintelligenz)
- hard: `V120.1` (SLO- und Ausgangssignal fuer Graph-RAG, damit der CodeGraph-Vergleich nicht vor der Curvios-Baseline laeuft)
- soft: `V122.5` (nur relevant, falls MCP wirklich in Agent-Kontext uebergeht)
- soft: `V124.1` (Produktsemantik bleibt Curvios-Graph-Scope; CodeGraph liefert hoechstens technische Naehe)
