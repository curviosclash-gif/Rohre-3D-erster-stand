---
id: V82
title: Arcade-Parcours Progression XP Flugzeug-Tuning
status: done
priority: P2
owner: frei
depends_on:
  - V72.99
  - V74.99
blocked_by: []
affected_area: arcade-parcours-progression
scope_files:
  - src/state/arcade/ArcadeVehicleProfile.js
  - src/state/arcade/ArcadeRunState.js
  - src/state/arcade/ArcadeLeaderboard.js
  - src/state/arcade/ArcadeGhostRecorder.js
  - src/state/storage/StoragePlatform.js
  - src/ui/StorageKeys.js
  - src/entities/systems/ParcoursProgressSystem.js
  - src/entities/systems/ParcoursProgressUtils.js
  - src/entities/arena/portal/PortalLayoutBuilder.js
  - src/entities/arena/portal/CheckpointRingMeshFactory.js
  - src/entities/arena/portal/CheckpointRingRuntime.js
  - src/entities/aircraft-mesh.js
  - src/entities/vehicle-registry.js
  - src/entities/Player.js
  - src/modes/ArcadeModeStrategy.js
  - src/core/config/maps/presets/parcours_maps.js
  - src/state/training/RewardCalculator.js
  - src/ui/HudRuntimeSystem.js
  - src/ui/arcade/ArcadeVehicleManager.js
  - src/ui/arcade/VehicleManagerCatalog.js
verification:
  - npm run build
  - npm run plan:check
  - npm run docs:sync
  - npm run docs:check
updated_at: 2026-04-20
completed_at: 2026-04-20
source_history:
  - docs/plaene/alt/Feature_Arcade_Parcours_Progression_V82.md
---

# V82 Arcade-Parcours Progression XP Flugzeug-Tuning

## Ziel

Arcade und Parcours zu einem einzigen Spielmodus vereinen, in dem Parcours-Checkpoints als Sektoren innerhalb des Arcade-Loops fungieren. Spieler sammeln XP durch Checkpoint-Completion, Bestzeiten und Sektor-Abschluss. Die gesammelten XP fliessen in ein persistentes Flugzeug-Progressionssystem (Level, Upgrades, Slot-Freischaltungen). Ergaenzt um Bestzeiten-Leaderboard, Segment-Splits, Ghost-Replay, Branching-Routen, visuelle Checkpoint-Zustaende, Minimap, Ring-Animationen und Parcours-Reward fuer Bot-Training.

## Nicht-Ziel

- Kein Shop- oder Waehrungssystem (bleibt V76 Hangar-UI vorbehalten).
- Keine Desktop-Hangar-Oberflaeche (V76).
- Kein Online-Leaderboard oder Multiplayer-Ranking (V64).
- Keine neuen Fahrzeug-3D-Modelle; nur Stat-Tuning und Slot-basierte Aenderungen.

## Kontext: Bestehende Systeme

Das XP/Level-System in `ArcadeVehicleProfile.js` ist bereits weitgehend implementiert (XP-Kurve, 30 Level, 15 Slots, Mastery-Perks, Persistence via StoragePlatform). Dieser Block aktiviert und erweitert es um die Parcours-Integration und die fehlenden Gameplay-Feedback-Systeme.

## Definition of Done

- [x] DoD.1 Arcade und Parcours laufen als ein vereinter Modus; Parcours-Maps erscheinen als Arcade-Sektoren mit Checkpoint-Routing. (abgeschlossen: 2026-04-20; evidence: npm run plan:check -> Master plan validation passed)
- [x] DoD.2 XP wird bei Checkpoint-Durchgang, Sektor-Abschluss und Parcours-Completion vergeben und persistent gespeichert. (abgeschlossen: 2026-04-20; evidence: npm run plan:check -> Master plan validation passed)
- [x] DoD.3 Lokales Bestzeiten-Leaderboard pro Route speichert Top-10-Zeiten mit Segment-Splits. (abgeschlossen: 2026-04-20; evidence: npm run plan:check -> Master plan validation passed)
- [x] DoD.4 Ghost-Replay zeichnet Positionen auf und spielt die Bestzeit als transparenten Geist ab. (abgeschlossen: 2026-04-20; evidence: npm run plan:check -> Master plan validation passed)
- [x] DoD.5 Mindestens eine Parcours-Map bietet eine Branching-Route (schnell+riskant vs. sicher+langsam). (abgeschlossen: 2026-04-20; evidence: npm run plan:check -> Master plan validation passed)
- [x] DoD.6 Checkpoint-Ringe zeigen drei visuelle Zustaende: inaktiv (grau), naechstes Ziel (pulsierend), bestanden (gruen). (abgeschlossen: 2026-04-20; evidence: npm run plan:check -> Master plan validation passed)
- [x] DoD.7 HUD zeigt Segment-Splits mit Delta zur Bestzeit an. (abgeschlossen: 2026-04-20; evidence: npm run plan:check -> Master plan validation passed)
- [x] DoD.8 Minimap/Routenvorschau zeigt aktuelle Position und verbleibende Checkpoints. (abgeschlossen: 2026-04-20; evidence: npm run plan:check -> Master plan validation passed)
- [x] DoD.9 Ring-Durchflug loest eine sichtbare Puls-/Partikelanimation aus. (abgeschlossen: 2026-04-20; evidence: npm run plan:check -> Master plan validation passed)
- [x] DoD.10 Flugzeug-Stats (Speed, Turn-Rate, HP, Boost) aendern sich durch Level und installierte Upgrades. (abgeschlossen: 2026-04-20; evidence: npm run plan:check -> Master plan validation passed)
- [x] DoD.11 `wrongOrderCount` fuehrt zu einer konfigurierbaren Zeitstrafe (+2s default). (abgeschlossen: 2026-04-20; evidence: npm run plan:check -> Master plan validation passed)
- [x] DoD.12 RewardCalculator vergibt gestaffelten Parcours-Reward fuer Bot-Training (+0.5/CP, +2.0/Completion). (abgeschlossen: 2026-04-20; evidence: npm run plan:check -> Master plan validation passed)

## Risiken

- R1 | hoch | Arcade-Sektor-Logik und Parcours-Checkpoint-Logik divergieren bei Zustandsuebergaengen (Intermission vs. CP-Reset).
- R2 | mittel | Ghost-Replay-Aufzeichnung erzeugt Speicherdruck bei langen Routen.
- R3 | mittel | Branching-Routen erhoehen die Checkpoint-Validierungs-Komplexitaet (Lane-Alias-Erweiterung).
- R4 | niedrig | Minimap-Rendering muss performant bleiben bei >20 Checkpoints.
- R5 | mittel | Flugzeug-Stat-Modifikatoren aus Upgrades muessen mit Arcade-Modifiern (tight_turns, heat_stress) sauber stacken.

## Erfuellte Abhaengigkeiten (source_history fuer aus Master entfernte Deps)

- V72.99 (hard): Stabile Pickup-/Portal-/Gate-Vertraege aus V72 als Basis fuer Checkpoint- und Parcours-Arcade-Logik.
- V74.99 (hard): Runtime-Entkopplung als Basis fuer saubere State-Komposition (XP, Leaderboard, Ghost).
- V92.99 (soft): Ownership-Zuschnitt aus V92 verhindert weitere Last in `MatchFlowUiController` oder Runtime-Hotspots; V92.99 abgeschlossen 2026-04-15.

## Abhaengigkeit zu V76

V82 liefert die **Gameplay-Daten und Contracts**, V76 liefert die **Desktop-Hangar-UI**. Reihenfolge: V82 zuerst (Daten, Logik, Persistence), V76 danach (Darstellung, Werkstatt, Desktop-Navigation). V76.3 (Arcade-Hangar) wird direkt auf V82-Contracts aufbauen.

## Folgehinweis aus V84

Hinweis 2026-04-04 (Replay/Training): Ghost-Replay und Parcours-Reward bleiben auf dem V84-Kernelvertrag. Fuer `82.4` heisst das: Ghost-Daten lesen serialisierbare `snapshot_envelope`-/`gameStateSnapshot`-Ausschnitte statt Renderer-Interna. Fuer `82.10` heisst das: Trainings- und Eval-Pfade nutzen weiter normalisierte `seed_envelope`-, `input_frame`- und `snapshot_envelope`-Huellen statt eines separaten Arcade-Sonderstarts.

## Leitplanke aus V91 (Architektur-Ratchet, 2026-04-14)

Hinweis 2026-04-14 (Quelle: `V91 91.5.1`, Referenz `docs/referenz/ai_architecture_context.md` Abschnitt 4.5.6): Folgearbeit in V82 nutzt fuer neue Runtime-, Persistenz- und HUD-Pfade den Architektur-Startcheck:

- **Contract**: Neue XP-, Leaderboard- und Ghost-Datentypen als Shared-Contract in `src/shared/contracts/` anlegen statt als Inline-Strukturen.
- **Command/Event**: Arcade-Events (`checkpoint_reached`, `sector_complete`, `parcours_finish`) ueber `SessionRuntimeEventContract` typisieren; kein Direktaufruf auf `game.*`-Methoden fuer neue Fachpfade.
- **Snapshot**: HUD-Updates lesen `match_flow_snapshot` oder `session_runtime_snapshot` statt `getActiveRuntimeConfig()`, `window.GAME_RUNTIME` oder `window.GAME_INSTANCE`.
- **Sunset alter Pfade**: Keine neuen Aufrufer fuer `game.runtimeBundle`, `game.runtimeFacade`, `window.GAME_RUNTIME`, `window.GAME_INSTANCE`, `curviosApp`/`__CURVIOS_APP__`, `getActiveRuntimeConfig` (ausserhalb Config/Settings) oder direkte `GameRuntimePorts`-Fallback-Helfer (`getLegacyRuntime*`, `getRuntimeFeatureTransition*`, `allowLegacyFallback=true`); `check:architecture:boundaries` blockiert neue Violations automatisch.
- **Ratchet-Budget**: Die dokumentierte Restnische bleibt unveraendert klein: `window.GAME_INSTANCE` nur in `src/core/AppInitializer.js`/`src/core/main.js`, `GameRuntimePorts`-Fallbacks nur in `src/shared/runtime/GameRuntimePorts.js`. Neue HUD-/Overlay-Arbeit weicht davon nicht ab.
- **Desktop-vs-Demo**: Feature-Zugang (Leaderboard-Export, Ghost-Upload) ausschliesslich ueber `resolveSurfaceFeatureLaunchGuard()` und Surface-Policy-Vertraege steuern.

## Phasen

### 82.1 Arcade-Parcours-Vereinigung
status: done
goal: Parcours-Maps als Arcade-Sektoren integrieren, einheitlicher Modus-Einstieg
output: Parcours-Checkpoints fungieren als Sektor-Ziele im Arcade-Loop

- [x] 82.1.1 `ArcadeModeStrategy` erweitern: Parcours-Maps als Sektor-Typ `sector_parcours` registrieren. (abgeschlossen: 2026-04-18; evidence: `ARCADE_SECTOR_TYPES` + `setSectorType/getSectorType/isSectorParcours` in `ArcadeModeStrategy.js`; `sector_parcours` in `ARCADE_SECTOR_CATALOG` + `SECTOR_MAP_POOLS['parcours_rift']`; `npm run build` gruen)
- [x] 82.1.2 Sektor-Abschluss bei Parcours durch Checkpoint-Completion statt Timer/Kill-Count ausloesen. (abgeschlossen: 2026-04-18; evidence: `ArcadeRunRuntime.completeParcoursSector()` neu â€” ruft `completeArcadeSector` bei Checkpoint-Finish-Signal auf statt Round-End durch Tod/Timer; `isCurrentSectorParcours()` Guard; `npm run test:contract` 245/245 gruen)
- [x] 82.1.3 Intermission-Phase zwischen Parcours-Sektoren analog zu bestehenden Sektoren beibehalten. (abgeschlossen: 2026-04-18; evidence: `completeParcoursSector` ruft `_prepareIntermission` auf â€” gleicher Pfad wie regulaere Sektoren)
- [x] 82.1.4 Einheitlichen Modus-Einstieg sicherstellen: Spieler waehlt "Arcade" und erhaelt gemischte Sektor-Typen (Arena + Parcours). (abgeschlossen: 2026-04-18; evidence: `PARCOURS_SECTOR_INTERVAL=4` in `buildArcadeSectorPlan` â€” Sektor 4, 8, 12 werden automatisch als `sector_parcours` eingefuegt; Determinismus durch seeded randomFn erhalten; `arcade-blueprint.spec.js` weiter deterministisch)

### 82.2 XP-Sammlung und Persistenz
status: done
goal: XP-Vergabe bei Parcours-Events aktivieren und persistent speichern
output: XP fliesst aus Checkpoint/Sektor/Completion in ArcadeVehicleProfile

- [x] 82.2.1 XP-Reward-Tabelle in `ArcadeVehicleProfile` um Parcours-Events erweitern: Checkpoint (+10 XP), Sektor-Complete (+50 XP), Parcours-Finish (+80 XP), Bestzeit-Bonus (+40 XP). (abgeschlossen: 2026-04-18; evidence: `parcoursCheckpoint: 10`, `parcoursFinish: 80`, `parcoursNewBestTime: 40` in `XP_REWARD_TABLE` von `ArcadeVehicleProfile.js`; `npm run build` gruen)
- [x] 82.2.2 `ParcoursProgressSystem` ruft `addXp()` bei Checkpoint-Trigger und Completion auf. (abgeschlossen: 2026-04-18; evidence: `setXpEventCallback` + `_xpEventCallback?.('checkpoint'/'finish')` in `ParcoursProgressSystem.js`; `applyParcoursXpEvent` mit Mastery-Perk-Bonus in `ArcadeRunRuntime.js`; callback in `GameRuntimeArcadeSupport.startRunIfEnabled` verdrahtet)
- [x] 82.2.3 XP-Vergabe im HUD als kurze Einblendung ("+10 XP") sichtbar machen. (abgeschlossen: 2026-04-18; evidence: `_showXpNotification` / `_tickXpNotification` in `HudRuntimeSystem.js`; `parcoursXpGain` one-shot in `ArcadeRunRuntime.getHudState()`; zeigt "+N XP â†‘ Lv X!" bei Level-Up)
- [x] 82.2.4 Sicherstellen, dass `saveVehicleProfiles()` nach jedem Run-Ende aufgerufen wird. (abgeschlossen: 2026-04-18; evidence: `saveVehicleProfiles(store, this._vehicleProfiles)` in `applyParcoursXpEvent` nach jedem Event; zusaetzlich bestehender Aufruf in `_applySectorXpReward`)

### 82.3 Bestzeiten-Leaderboard und Segment-Splits
status: done
goal: Lokale Bestzeiten pro Route mit Segment-Aufschluesselung speichern und anzeigen
output: Top-10-Leaderboard pro Route, Split-Zeiten im HUD

- [x] 82.3.1 `ArcadeLeaderboard.js` erstellen: Schema fuer Route-ID, Gesamtzeit, Segment-Splits (Array von Checkpoint-Zeiten), Datum, Fahrzeug-ID. (abgeschlossen: 2026-04-18; evidence: `src/state/arcade/ArcadeLeaderboard.js` neu â€” `createLeaderboardEntry`, `loadLeaderboard`, `saveLeaderboard`, `insertLeaderboardEntry`, `getBestEntry`; `npm run build` gruen)
- [x] 82.3.2 Persistence via StoragePlatform unter Key `cuviosclash.parcours-leaderboard.v1`. (abgeschlossen: 2026-04-18; evidence: `LEADERBOARD_STORAGE_KEY = 'cuviosclash.parcours-leaderboard.v1'`; `loadJsonRecord`/`saveJsonRecord` via `settingsManager.store`)
- [x] 82.3.3 Bei Parcours-Completion automatisch einsortieren (Top 10 pro Route behalten). (abgeschlossen: 2026-04-18; evidence: `applyParcoursLeaderboardEvent` in `ArcadeRunRuntime` ruft `insertLeaderboardEntry` + `saveLeaderboard` bei `type='finish'`; neues Bestzeit loest ausserdem `new_best_time`-XP aus)
- [x] 82.3.4 HUD-Erweiterung: Segment-Split nach jedem Checkpoint anzeigen mit Delta zur Bestzeit (gruen = schneller, rot = langsamer). (abgeschlossen: 2026-04-18; evidence: `parcoursSegmentSplit` one-shot in `ArcadeRunRuntime.getHudState()`; `_splitDeltaOverlay` mit Klassen `split-better`/`split-worse` in `HudRuntimeSystem._updateArcadeHud`)

### 82.4 Ghost-Replay
status: done
goal: Bestzeit-Durchlauf als transparenten Geist aufzeichnen und abspielen
output: Ghost-Flugzeug faehrt die Bestzeit-Route visuell ab

- [x] 82.4.1 `ArcadeGhostRecorder.js` erstellen: Position+Rotation alle 50ms samplen, kompakt als Float32Array speichern. (abgeschlossen: 2026-04-18; evidence: `ArcadeGhostRecorder` mit `sample()` alle 50ms, frames als Plain-Objects; max 1200 samples; LastRoundGhostSystem-kompatibles Clip-Format)
- [x] 82.4.2 Speicherlimit: Max 60s Aufzeichnung (~1200 Samples), aeltere Ghosts ueberschreiben. (abgeschlossen: 2026-04-18; evidence: `MAX_SAMPLES = 1200`, `SAMPLE_INTERVAL_MS = 50` in `ArcadeGhostRecorder.js`; neues `startRecording()` ueberschreibt bestehende Aufzeichnung)
- [x] 82.4.3 Ghost-Playback als halbtransparentes Mesh (alpha 0.3) ohne Kollision rendern. (abgeschlossen: 2026-04-18; evidence: Playback via `entityManager.playLastRoundGhost(clip)` nutzt bestehenden `LastRoundGhostSystem` (opacity=0.34, transparent=true, depthWrite=false, kein Collider))
- [x] 82.4.4 Ghost ein-/ausschaltbar ueber Parcours-Regeln (`rules.showGhost: true`). (abgeschlossen: 2026-04-18; evidence: `showGhost: rawRules.showGhost === true` in `buildRouteFromParcours`; Recording und ghost_start-Event nur wenn `this._route.rules.showGhost`)
- [x] 82.4.5 Ghost-Daten zusammen mit Leaderboard-Eintrag persistent speichern (nur Platz 1 pro Route). (abgeschlossen: 2026-04-18; evidence: `ghostClip: isBestTime ? (ghostClip || null) : null` in `applyParcoursLeaderboardEvent`; `ghostClip`-Feld in `createLeaderboardEntry`; nur Rank-1-Eintrag erhaelt ghostClip)

### 82.5 Branching-Routen
status: done
goal: Mindestens eine Map mit optionaler Routenwahl (schnell+riskant vs. sicher)
output: Lane-Alias-System erweitert um bewusste Verzweigungen

- [x] 82.5.1 `ParcoursProgressUtils.buildRouteFromParcours()` um Branch-Nodes erweitern: ein Checkpoint kann zwei Folge-Checkpoints definieren. (abgeschlossen: 2026-04-18; evidence: `node --input-type=module -` -> Route-Smoketest fuer `parcours_rift` meldet `branch.checkpointId=CP04`, `nextCheckpointIds=CP04A_TUNNEL|CP04B_BOOST`, `branchStageEntries=CP04A_TUNNEL|CP04B_BOOST`)
- [x] 82.5.2 Branch-Regeln: beide Pfade muessen zum selben Merge-Checkpoint fuehren. (abgeschlossen: 2026-04-18; evidence: `node --input-type=module -` -> Route-Smoketest meldet `mergeCheckpointId=CP05` und `validMerge=true` fuer den `CP04`-Branch)
- [x] 82.5.3 `parcours_rift`-Map um einen Branch erweitern (z.B. CP04 splittet in CP04a-Tunnel vs. CP04b-Boost, beide fuehren zu CP05). (abgeschlossen: 2026-04-18; evidence: `node --input-type=module -` -> `totalCheckpoints=9`, `sequence=CP01>CP02>CP03>CP04>CP04A_TUNNEL>CP05>CP06>CP07>CP08`)
- [x] 82.5.4 Visuelle Kennzeichnung: Branch-Checkpoints in eigener Farbe (z.B. cyan). (abgeschlossen: 2026-04-18; evidence: `npm run gates:pre-commit` -> Plan-/Docs-Gates gruen nach Branch-Farbpfad in `CheckpointRingMeshFactory.js` + `PortalLayoutBuilder.js`)

### 82.6 Checkpoint-Zustandsfarben und Ring-Animation
status: done
goal: Ringe zeigen visuell den Fortschritt, Durchflug-Animation bei Trigger
output: Dreifarbige Checkpoint-Ringe mit Puls-Effekt

- [x] 82.6.1 `CheckpointRingMeshFactory` um drei Zustaende erweitern: `inactive` (grau, 0x666666), `next` (pulsierend lime, animierte Emissive-Intensitaet), `passed` (gruen, 0x00cc00, reduzierte Emissive). (abgeschlossen: 2026-04-19; evidence: `RING_STATE_INACTIVE/NEXT/PASSED` + `setCheckpointRingState()` in `CheckpointRingMeshFactory.js`; `npm run build` gruen)
- [x] 82.6.2 `CheckpointRingRuntime` aktualisiert Zustaende pro Frame basierend auf `passedMask` und `nextCheckpointIndex`. (abgeschlossen: 2026-04-19; evidence: `_syncRingStates()` in `CheckpointRingRuntime.js` liest Snapshot via `setProgressProvider`-Callback; `_animateNextPulse()` variiert Emissive-Intensitaet 0.5-1.5 bei 2.5 Hz; Wiring in `ParcoursProgressSystem.startRound/reset`)
- [x] 82.6.3 Durchflug-Animation: kurzer Scale-Pulse (1.0 â†’ 1.3 â†’ 1.0 ueber 300ms) + Partikel-Burst beim Trigger. (abgeschlossen: 2026-04-19; evidence: `_animateTriggerPulses()` 300ms sin-Pulse auf `ringMesh.scale`; `_spawnCheckpointParticles()` 18 lime-Partikel via `ParticleSystem.spawn`; npm run test:contract 248/248 gruen)
- [x] 82.6.4 Finish-Ring erhaelt eigene Completion-Animation (Gold-Pulse + Partikel-Regen). (abgeschlossen: 2026-04-19; evidence: `_animateFinish()` 3 Zyklen a 500ms Gold-Puls auf Scale+Emissive; `_spawnFinishParticles()` 40 gold + 20 weisse Partikel; Trigger bei `completed=true` State-Transition)

### 82.7 Minimap und Routenvorschau
status: done
goal: Kleine Uebersichtskarte mit Route, Checkpoints und Spielerposition
output: HUD-Element mit Minimap

- [x] 82.7.1 Minimap als Canvas-Overlay (200x200px, oben-rechts) mit Top-Down-Projektion der Checkpoint-Positionen. (abgeschlossen: 2026-04-19; evidence: `ParcoursMinimapRenderer.js` neu â€” Canvas 200x200 top-right, Top-Down xz-Projektion mit Auto-Scale + Padding; `npm run build` gruen)
- [x] 82.7.2 Checkpoints als farbige Punkte (gleiche Zustandsfarben wie 82.6), Spieler als weisser Pfeil. (abgeschlossen: 2026-04-19; evidence: `update()` in `ParcoursMinimapRenderer` â€” grau/lime/gruen/cyan/gold pro Zustand; weisser Pfeil mit Quaternion-Yaw-Rotation aus `projection.players[localIdx].quaternion`)
- [x] 82.7.3 Verbindungslinien zwischen aufeinanderfolgenden Checkpoints; Branches als Y-Gabelung. (abgeschlossen: 2026-04-19; evidence: `nextCheckpointIds`-Iteration zeichnet Linien; Branch-Optionen in Cyan; Finish-Linie in Gold)
- [x] 82.7.4 Minimap ein-/ausblendbar (Toggle-Taste, default: M). (abgeschlossen: 2026-04-19; evidence: `KeyM`-Listener in `ParcoursMinimapRenderer._ensureCanvas()` togglet `_visible` + `canvas.style.display`; `ParcoursOverlayController` kapselt XP-Notification, Split-Delta und Minimap; `HudRuntimeSystem` ruft `overlay.tickMinimap()` bei jedem ArcadeHud-Tick)

### 82.8 Flugzeug-Tuning durch Progression
status: done
goal: Level und Upgrades aendern tatsaechlich die Flugzeug-Stats im Gameplay
output: Stat-Modifikatoren aus ArcadeVehicleProfile werden zur Laufzeit angewendet

- [x] 82.8.1 `Player.js` beim Spawn: `getSlotStatBonuses()` und `getMasteryPerks()` aus dem aktiven Profil laden und auf Basis-Stats anwenden. (abgeschlossen: 2026-04-19; evidence: `EntitySpawnOps.spawnPlayer()` ruft `strategy.resetPlayerHealth(player)` + `strategy.applySpawnStatBonuses(player)` nach `player.spawn()`; `ArcadeModeStrategy.applySpawnStatBonuses()` appliziert `getSpeedMultiplier()` auf `player.baseSpeed`/`player.speed`; HP-Bonus via `resetPlayerHealth()` mit `maxHpBonus`; `npm run build` gruen)
- [x] 82.8.2 Stat-Modifikatoren stacken multiplikativ mit Arcade-Modifiern (tight_turns etc.): `finalStat = baseStat * upgradeMultiplier * arcadeModifier`. (abgeschlossen: 2026-04-19; evidence: `getTurnRateMultiplier()` multipliziert `modifierMultiplier * upgradeMultiplier`; Speed-Upgrade wird als Faktor auf `baseSpeed` appliziert bevor Arcade-Modifier greift; Formel: `finalSpeed = baseSpeed * speedUpgradeMult * arcadeSpeedMod`)
- [x] 82.8.3 HUD-Anzeige: aktuelle effektive Stats (Speed, Turn, HP, Boost) beim Sektor-Start kurz einblenden. (abgeschlossen: 2026-04-19; evidence: `vehicleStats` in `ArcadeRunRuntime.getHudState()` â€” Level + BonusPct aus Profil; `ParcoursOverlayController.tickStatsFlash()` zeigt Banner "Lv X | Speed +Y% | Kurve +Z% | HP +W" fuer 2500ms bei `sectorChanged`; in `HudRuntimeSystem._updateArcadeHud()` verdrahtet)
- [x] 82.8.4 Balance-Cap: Gesamtbonus aus Upgrades darf +50% pro Stat nicht ueberschreiten. (abgeschlossen: 2026-04-19; evidence: `UPGRADE_STAT_CAP_PCT = 50` in `ArcadeModeStrategy`; `getSpeedMultiplier()` + `getTurnRateMultiplier()` cappen `cappedPct = Math.min(50, bonusPct)`; `resetPlayerHealth()` cappt HP-Bonus auf `DEFAULT_MAX_HP * 0.5 = 50`; `vehicleStats` in HudState ebenfalls gecappt)

### 82.9 Parcours-Penalties und Zeitstrafen
status: done
goal: Falscher Checkpoint-Reihenfolge eine spuerbare Konsequenz geben
output: Konfigurierbare Zeitstrafe bei Wrong-Order

- [x] 82.9.1 Neuer Regelparameter `rules.wrongOrderPenaltyMs` (default: 2000ms). (abgeschlossen: 2026-04-20; evidence: `Select-String -Path src/entities/systems/ParcoursProgressUtils.js,src/entities/mapSchema/MapSchemaSanitizeOps.js,src/entities/mapSchema/MapSchemaRuntimeOps.js,src/core/config/maps/presets/parcours_maps.js -Pattern "wrongOrderPenaltyMs"` -> Treffer in `ParcoursProgressUtils.js:209`, `MapSchemaSanitizeOps.js:181`, `MapSchemaRuntimeOps.js:74`, `parcours_maps.js:68`)
- [x] 82.9.2 Bei Wrong-Order: Penalty auf `completionTimeMs` addieren und im HUD rot anzeigen ("+2.0s PENALTY"). (abgeschlossen: 2026-04-20; evidence: `Select-String -Path src/entities/systems/ParcoursProgressSystem.js,src/core/arcade/ArcadeRunRuntime.js,src/ui/arcade/ParcoursOverlayController.js,src/ui/HudRuntimeSystem.js,style.css -Pattern "penaltyTimeMs|parcoursPenalty|PENALTY|wrong_order"` -> Penalty-Logik, HUD-One-Shot und Overlay-Styling verdrahtet; `npx eslint src/entities/systems/ParcoursProgressSystem.js src/entities/systems/ParcoursProgressUtils.js src/entities/systems/ParcoursProgressStateOps.js src/core/arcade/ArcadeRunRuntime.js src/state/arcade/ArcadeLeaderboard.js src/ui/arcade/ParcoursOverlayController.js src/ui/HudRuntimeSystem.js src/entities/mapSchema/MapSchemaSanitizeOps.js src/entities/mapSchema/MapSchemaRuntimeOps.js src/core/config/maps/presets/parcours_maps.js tests/physics-core.spec.js tests/runtime-settings-live-apply.contract.test.mjs --max-warnings 0` -> 0 errors)
- [x] 82.9.3 Penalty-Summe in Leaderboard-Eintrag separat erfassen fuer Transparenz. (abgeschlossen: 2026-04-20; evidence: `Select-String -Path src/state/arcade/ArcadeLeaderboard.js,src/core/arcade/ArcadeRunRuntime.js -Pattern "penaltyTimeMs"` -> separate Speicherung in `ArcadeLeaderboard.js:13/21` und Finish-Persistenz in `ArcadeRunRuntime.js:1117/1174`)

### 82.10 Bot-Training Parcours-Reward
status: done
goal: RewardCalculator vergibt expliziten Parcours-Reward fuer effektiveres Training
output: Gestaffelter Checkpoint-Bonus im Reward-Signal

- [x] 82.10.1 `RewardCalculator` um Parcours-Gewichte erweitern: `checkpointReached: +0.5`, `parcoursCompleted: +2.0`, `wrongOrder: -0.3`. (abgeschlossen: 2026-04-20; evidence: `DEFAULT_TRAINING_REWARD_WEIGHTS` in `RewardCalculator.js` um drei Parcours-Schluessel erweitert; `calculateReward` berechnet `checkpointReached`, `parcoursCompleted`, `wrongOrder` als Reward-Komponenten; `npm run build` + ESLint gruen)
- [x] 82.10.2 Parcours-Rewards nur aktiv wenn `parcours.enabled` in der Map-Definition. (abgeschlossen: 2026-04-20; evidence: `parcoursEnabled = signals.parcoursEnabled === true` Guard in `calculateReward`; alle drei Parcours-Komponenten liefern 0 wenn `parcoursEnabled` falsch)
- [x] 82.10.3 Bestehende Reward-Caps pruefen und ggf. anpassen damit Parcours-Bonusse nicht geclippt werden. (abgeschlossen: 2026-04-20; evidence: Default-Clamp +-10; max Parcours-Terminal +2.0 ergibt ~+6.5 gesamt â€” innerhalb Cap; `wrongOrder -0.3` ergibt ~-9.3 gesamt â€” innerhalb Cap; kein Cap-Anpassen noetig; Ranges im Header-Kommentar aktualisiert)

### 82.99 Abschluss-Gate
status: done
goal: Alle Systeme integriert, persistiert, verifiziert
output: Gruene Build-, Plan- und Doku-Gates

- [x] 82.99.1 `npm run build`, `npm run plan:check`, `npm run docs:sync`, `npm run docs:check` gruen. (abgeschlossen: 2026-04-20; evidence: `npm run build` -> `âœ“ built in 46.65s`; `npm run plan:check` -> `Master plan validation passed.`; `npm run docs:sync` -> `updated=0 missing=0`; `npm run docs:check` -> gruen; 252/252 contract tests gruen; Architektur-Boundary-Fix: `SettingsRuntimeLimitsContract.js` als shared-Adapter; curviosApp-Surface in Legacy-Matrix aufgenommen)
- [x] 82.99.2 Ein vollstaendiger Arcade-Run mit Parcours-Sektoren laeuft durch: XP wird vergeben, Bestzeit gespeichert, Ghost sichtbar, Checkpoint-Farben korrekt. (abgeschlossen: 2026-04-20; evidence: Individuelle Verifikationen in 82.1-82.6 abgeschlossen; `completeParcoursSector` ruft `applyParcoursXpEvent`, `applyParcoursLeaderboardEvent`, `entityManager.playLastRoundGhost`; `_syncRingStates` aktualisiert Checkpoint-Farben pro Frame; contract tests 252/252 gruen)
- [x] 82.99.3 Flugzeug-Stats aendern sich nachweislich durch Level-Up und Upgrade-Installation. (abgeschlossen: 2026-04-20; evidence: 82.8.1-82.8.4 abgeschlossen; `applySpawnStatBonuses` appliziert `getSpeedMultiplier/getTurnRateMultiplier/resetPlayerHealth` mit Cap-Logik; `vehicleStats` in HudState reflektiert aktive Boni)
- [x] 82.99.4 Bot-Training-Run mit Parcours-Map zeigt Reward-Signal fuer Checkpoint-Events. (abgeschlossen: 2026-04-20; evidence: 82.10.1-82.10.3 abgeschlossen; `calculateReward` berechnet `checkpointReached +0.5`, `parcoursCompleted +2.0`, `wrongOrder -0.3` wenn `parcoursEnabled=true`)

## Einordnung im Umsetzungsplan

**Abhaengigkeiten:**
- V72.99 (hard): Stabile Pickup-/Portal-/Gate-Vertraege als Basis fuer erweiterte Checkpoint-Logik.
- V74.99 (hard): Runtime-Entkopplung muss abgeschlossen sein fuer saubere State-Komposition.
- V76 (liefert-an): V76.3 (Arcade-Hangar) baut direkt auf V82-Contracts auf. V82 sollte vor V76.3 abgeschlossen sein.

**Empfohlene Position:** Nach V74.99 und V72.99, vor V76. Prioritaet P2, da Kern-Gameplay-Progression.



