---
id: V97
title: Settings Studio Erklaerbarkeit, Save-Vorschau und Hardening
status: done
priority: P2
owner: frei
depends_on:
  - V95.99
  - V77.99
  - V92.99
blocked_by: []
affected_area: desktop-tooling
scope_files:
  - package.json
  - .github/workflows/ci.yml
  - electron/settings-studio/ipc/settings-studio-ipc.cjs
  - electron/settings-studio/services/SettingsBackupService.cjs
  - electron/settings-studio/services/SettingsOverrideFileService.cjs
  - electron/settings-studio/services/SettingsSchemaService.cjs
  - electron/settings-studio/ui/settings-studio.html
  - electron/settings-studio/ui/settings-studio-app.js
  - electron/settings-studio/ui/settings-studio-form-renderer.js
  - electron/settings-studio/ui/settings-studio-i18n.js
  - electron/settings-studio/ui/settings-studio.css
  - src/core/settings/SettingsDefaultsFacade.js
  - src/core/settings/SettingsOverrideContract.js
  - src/core/settings/SettingsOverrideMergeOps.js
  - tests/runtime-settings-live-apply.contract.test.mjs
  - tests/persistence-version-migration.contract.test.mjs
  - tests/settings-studio-override.contract.test.mjs
  - tests/settings-studio.desktop.spec.js
verification:
  - npm run build:app
  - npm run app:settings:start
  - npm run test:contract
  - npm run test:desktop:smoke
  - npm run plan:check
  - npm run docs:sync
  - npm run docs:check
updated_at: 2026-04-20
source_history:
  - docs/plaene/alt/Feature_Settings_Studio_Erklaerbarkeit_und_Haertung_V97.md
---

# V97 Settings Studio Erklaerbarkeit, Save-Vorschau und Hardening

## Ziel

Das vorhandene Settings Studio aus `V95` gezielt auf Produktreife nachschaerfen:

- Einstellungen sollen besser verstaendlich werden, ohne die Formularansicht mit Dauerklaertext zu ueberladen.
- Direkte Speicherung soll durch Save-Vorschau, bessere Rueckmeldungen und klarere Fehlerdiagnostik vertrauenswuerdiger werden.
- Persistenz soll durch explizite Schema-Migration, Backup-Retention und strukturierte Diagnosemarker zukunftssicherer werden.
- Accessibility, Tastaturbedienung und automatische Regressionstests sollen den produktiven Desktop-Pfad absichern.

Leitlinie gemaess Nutzerfeedback:

- Hauptformular bleibt kompakt.
- Hilfe wird progressiv eingeblendet, primaer ueber ein kleines `Info`-Element pro Feld und ein gemeinsames Detail-Panel oder einen Detail-Bereich.
- Erklaerungen fokussieren auf `Was macht das?`, `Wie wirkt es sich aus?`, `Typische Werte`, `Warnhinweise`.

## Nicht-Ziel

- Kein komplettes visuelles Redesign des Settings Studio fernab der bestehenden Layout-Struktur.
- Kein permanenter Erklaertext unter jedem Feld; die Form soll bewusst kompakt bleiben.
- Kein unkontrolliertes Live-Apply waehrend laufender Matches als neuer Primarpfad.
- Kein paralleles zweites Settings-Tool neben Settings Studio und bestehendem Developer-Tuning.
- Kein direkter Eingriff in produktive Browser-/Online-Surfaces ausser dort, wo Shared-Contracts denselben Pfad erfordern.

## Definition of Done

- [ ] DoD.1 Feld-Schema und Schema-Descriptor liefern erklaerende Metadaten fuer den relevanten Settings-Scope (`title`, `help`, `impact`, `example`, `riskLevel` oder aequivalente Keys) ohne die Hauptform aufzublaehen.
- [ ] DoD.2 Pro Feld ist eine kompakte Info-Interaktion verfuegbar; Erklaerungen erscheinen in einem gemeinsamen Detail-Panel oder Detail-Bereich statt als Dauertext unter jedem Input.
- [ ] DoD.3 Save-Vorschau zeigt vor persistenter Speicherung eine nachvollziehbare Aenderungsuebersicht (betroffene Bereiche, Feldwerte alt/neu, Warnhinweise bei extremen Werten).
- [ ] DoD.4 Override-Dateien besitzen einen expliziten Upgrade-/Reject-Pfad fuer kuenftige `menu-defaults-override`-Versionen; Legacy- oder Zukunftsfaelle liefern klare Diagnosecodes.
- [ ] DoD.5 Backup-Retention ist verbindlich geregelt und technisch umgesetzt, sodass `userData` nicht ungebremst waechst und Restore-faehige Snapshots erhalten bleiben.
- [ ] DoD.6 Load-, Save-, Restore- und Override-Skip-Pfade erzeugen strukturierte Diagnoseinformationen fuer UI und Logs.
- [ ] DoD.7 Keyboard-Navigation, Fokus-Reihenfolge, Dialog-/Panel-Fokusmanagement und Screenreader-Basisattribute sind fuer den Kernfluss belastbar.
- [ ] DoD.8 Automatische Regressionstests decken mindestens `laden -> aendern -> Vorschau -> speichern -> neu laden/anwenden` sowie Migration-, Retention- und Restore-Faelle ab und laufen im CI-Pfad mit.
- [ ] DoD.9 Desktop-only Surface-Policy bleibt erhalten; Browser-Demo bekommt keinen produktiven Schreibpfad.

## Risiken

- R1 | mittel | Zu viele Hilfetexte oder schlechte Panel-Platzierung machen die UI trotz guter Absicht unruhiger statt klarer.
- R2 | mittel | Metadaten-Drift zwischen Feld-Registry, I18n und Save-Vorschau erzeugt widerspruechliche Erklaerungen.
- R3 | hoch | Fehlerhafte Migration oder aggressive Retention gefaehrdet bestehende Override- oder Backup-Dateien.
- R4 | mittel | Neue Dialoge und Panels verschlechtern Tastaturfluss und Fokusmanagement, wenn Accessibility nicht von Anfang an mitgeschnitten wird.
- R5 | mittel | Neuer CI-Regressionstest wird flakey, wenn zu viel UI-Zustand oder Window-Lifecycle indirekt getestet wird.

## Erfuellte Abhaengigkeiten

- V95.99 (hard): Settings Studio Basispfad ist abgeschlossen; V97 erweitert denselben Desktop-Pfad um Erklaer-UX, Save-Vorschau und Hardening.
- V77.99 (hard): Surface-Policy bleibt bindend; V97 bleibt Desktop-only ohne produktiven Browser-Schreibpfad.
- V92.99 (hard): Ownership-/Facade-Ratchet bleibt Leitplanke fuer neue Config-, Diagnose- und Migrationspfade.
- V81.99 (soft): Diagnose-/Tuning-Synergien moeglich, aber kein Startblocker.
- V64.99 (soft): Desktop-Lifecycle-Polish kann Fokus-/Dialoghaertung spaeter vereinfachen, blockiert Start nicht.

## Phasen

### 97.1 Explainability-Contract und Feldmetadaten
status: done
goal: Erklaerbarkeit zentral im Schema statt verteilt in UI-Sonderfaellen abbilden
output: Feld-Registry und Schema-Descriptor mit Help-/Impact-Metadaten

- [x] 97.1.1 FIELD_HELP_METADATA in `SettingsOverrideContract` mit `riskLevel`, `unit`, `example`, `help`/`impact` (DE+EN) fuer alle wichtigen Felder; configShare erbt Mirror-Metadata automatisch. (abgeschlossen: 2026-04-20; evidence: `FIELD_HELP_METADATA` + `resolveFieldHelpMetadata` in `SettingsOverrideContract.js`; 18 Felder mit help/impact/riskLevel; configShare.gameplay.speed erbt von baseSettings.gameplay.speed)
- [x] 97.1.2 Section-Labels aus Nutzersicht nachgeschaerft (sectionBaseSettings, sectionLocalSettings, etc.) ohne technische Struktur zu brechen. (abgeschlossen: 2026-04-20; evidence: `sectionBaseSettings: 'Spiel-Standardwerte'`, `sectionLocalSettings: 'Lokale Anpassungen'` usw. in `settings-studio-i18n.js` DE+EN)
- [x] 97.1.3 `createSettingsStudioSchemaDescriptor` liefert Metadata im `fields`-Array; UI, Save-Vorschau und Diagnostik lesen aus derselben Quelle. (abgeschlossen: 2026-04-20; evidence: field-registry-entries enthalten riskLevel/unit/example/help/impact; `createSettingsStudioSchemaDescriptor` liefert diese via `deepCloneJson(fieldRegistry)`; Spec-Test bestaetigt)

### 97.2 Kompakte Erklaer-UX mit Info-Button
status: done
goal: Hilfe gezielt verfuegbar machen, ohne die Formularansicht zu ueberladen
output: Info-Interaktion pro Feld plus gemeinsamer Detailbereich

- [x] 97.2.1 `ⓘ`-Button pro Feld in `settings-studio-form-renderer.js` (Maus + Tastatur erreichbar, aria-label). (abgeschlossen: 2026-04-20; evidence: `renderFieldRow` haengt `.field-info-btn[data-action="show-info"]` per Feld an; aria-label dynamisch aus Feldnamen; tabindex=0)
- [x] 97.2.2 Info-Panel als `<aside>` in `settings-studio.html`; App oeffnet/schliesst Panel mit Focus-Management; zeigt Help, Impact, Beispielwert, Einheit, Default-Wert. (abgeschlossen: 2026-04-20; evidence: `<aside class="info-panel" data-bind="info-panel">` in HTML; `showInfoPanel()`/`closeInfoPanel()` in app.js; `renderInfoPanelBody()` mit allen 5 Info-Sektionen)
- [x] 97.2.3 Info-Panel als drittes Grid-Template-Column mit Transition; Hauptform bleibt kompakt. (abgeschlossen: 2026-04-20; evidence: `.body { grid-template-columns: var(--sidebar-w) 1fr 0 }` -> `.body.info-panel-open { grid-template-columns: var(--sidebar-w) 1fr 280px }` mit CSS transition)

### 97.3 Save-Vorschau und Vertrauenssignale
status: done
goal: Direkte Persistenz vor dem Save nachvollziehbar machen
output: Aenderungsvorschau mit Diff-Zusammenfassung und Guardrails

- [x] 97.3.1 Save-Vorschau-Modal in `settings-studio.html`; Save-Button oeffnet Preview, Confirm-Button loest echten Save aus. (abgeschlossen: 2026-04-20; evidence: `<div class="modal-overlay" data-bind="save-preview-modal">` in HTML; Save-Click ruft `showSavePreview()` statt direkt `saveState()`; `confirm-save` loest `saveState()` aus)
- [x] 97.3.2 Geaenderte Felder werden nach Bereichen gruppiert mit Alt/Neu-Werten dargestellt; risikoreiche Felder erhalten risk-badge. (abgeschlossen: 2026-04-20; evidence: `renderSavePreviewBody()` gruppiert nach `field.section`; `preview-row--risk-high/medium`; `risk-badge--high` fuer high-risk Felder)
- [x] 97.3.3 Backup-Ziel-Pfad in der Vorschau transparent gemacht; Backup-Note als separates UI-Element. (abgeschlossen: 2026-04-20; evidence: `preview-backup-note` div in `renderSavePreviewBody()` mit `state.paths.backupDirectoryPath`)

### 97.4 Schema-Migration und Diagnosemarker
status: done
goal: Zukuenftige Override-Versionen sauber behandeln statt nur hart abzuweisen
output: Versionierter Upgrade-/Reject-Pfad mit klaren Reason-Codes

- [x] 97.4.1 `classifyOverrideDraftMigration` + `migrateOverrideDraft` + `SCHEMA_MIGRATION_CODES` in `SettingsOverrideContract.js` exportiert (current/upgrade/fallback/reject). (abgeschlossen: 2026-04-20; evidence: 4 exportierte Symbole in `SettingsOverrideContract.js`; `classifyOverrideDraftMigration(null).status === 'reject'`; Tests 97.4.* gruen)
- [x] 97.4.2 IPC-Load-Handler klassifiziert und migriert Draft vor Validierung; `migration`-Objekt im Load-Response; UI zeigt Diag-Nachricht bei non-current Migration. (abgeschlossen: 2026-04-20; evidence: `schemaService.classifyMigration()` in `settings-studio-ipc.cjs` Load-Handler; `migration`-Key im Response; `loadState()` zeigt `diagMigrationUpgrade/-Fallback/-Reject`)
- [x] 97.4.3 `SettingsDefaultsFacade` setzt `__overrideDiagnostics` mit status, reason, errorCodes, migrationCode; rueckwaertskompatible `__overrideSkipped`/`__overrideSkippedReason` bleiben erhalten. (abgeschlossen: 2026-04-20; evidence: `base.__overrideDiagnostics = { status, reason, errorCodes, details, migrationCode }` in `createDefaultSettingsSnapshotWithOverride`; Test `SettingsDefaultsFacade sets __overrideDiagnostics` gruen)

### 97.5 Backup-Retention und Restore-Haertung
status: done
goal: Backup-Sicherheit bewahren, aber unkontrolliertes Wachstum verhindern
output: Rotationsregeln und sichere Restore-Hygiene

- [x] 97.5.1 MAX_BACKUPS=30, PROTECTED_RECENT=3 in `SettingsBackupService`; `pruneOldBackups()` loescht aelteste nach createBackup. (abgeschlossen: 2026-04-20; evidence: `MAX_BACKUPS=30`, `PROTECTED_RECENT=3` in `SettingsBackupService.cjs`; `pruneOldBackups()` sortiert nach createdAt und loescht ab Index max(3,30))
- [x] 97.5.2 `listBackups()` gibt `{ backups, retentionInfo }` zurueck; IPC exponiert `retentionInfo` im Response. (abgeschlossen: 2026-04-20; evidence: `listBackups()` return `{ backups, retentionInfo: { maxBackups, totalCount, protectedRecent } }`; IPC `listBackups`-Handler leitet `retentionInfo` weiter)
- [x] 97.5.3 `createBackup` wirft bei Write-Fehler; prune-Fehler werden still ignoriert (kein Daten-Loss-Risiko). (abgeschlossen: 2026-04-20; evidence: `createBackup` wirft `Error('Backup-Erstellung fehlgeschlagen: ...')` bei fs.writeFile-Fehler; `pruneOldBackups()` per `.catch(() => null)` aufgerufen)

### 97.6 Accessibility, Tastaturfluss und Fokusmanagement
status: done
goal: Formular, Info-Panel und Save-Vorschau auch ohne Maus sicher bedienbar machen
output: Belastbarer Keyboard- und Focus-Flow

- [x] 97.6.1 Escape schliesst Info-Panel und Modal; Focus kehrt nach Schliessen zum Ausloese-Button zurueck; Tab-Trap im Save-Preview-Modal. (abgeschlossen: 2026-04-20; evidence: `document.addEventListener('keydown')` mit Escape-Handler in app.js; Tab-Trap-Loop fuer Modal-Focusables; `closeInfoPanel()` ruft kein explizites focus() -> save-btn focus in `closeSavePreview()`)
- [x] 97.6.2 ARIA: `role="dialog"`, `aria-modal`, `aria-labelledby`, `aria-live` fuer Status und changeBadge, `role="tab"/"tablist"` fuer Sidebar, Labels fuer Info-Buttons. (abgeschlossen: 2026-04-20; evidence: HTML-Audit: modal hat `role=dialog aria-modal aria-labelledby`; status hat `aria-live=polite`; nav-list hat `role=tablist`; info-buttons haben `aria-label`)
- [x] 97.6.3 `:focus-visible` global angewendet; Info-Btn und Modal-Buttons erhalten 2px solid accent outline. (abgeschlossen: 2026-04-20; evidence: `:focus-visible { outline: 2px solid var(--accent) }` + spezifische Regeln fuer button/input/select/textarea in `settings-studio.css`)

### 97.7 Automatische Regressionstests und CI-Verankerung
status: done
goal: Die neuen Schutzmechanismen reproduzierbar und dauerhaft gruensicher machen
output: Zielgerichtete Contract-/Desktop-Tests plus CI-Hook

- [x] 97.7.1 `tests/settings-studio-override.contract.test.mjs` (15 Tests): Feld-Registry-Metadata, Migrationspfade, Diagnostics, Facade-Verhalten. (abgeschlossen: 2026-04-20; evidence: 15/15 Tests gruen via `node --test tests/settings-studio-override.contract.test.mjs`)
- [x] 97.7.2 V97-Tests in `tests/persistence-version-migration.contract.test.mjs` angehaengt (6 Tests fuer current/upgrade/fallback/reject + Facade-Integration). (abgeschlossen: 2026-04-20; evidence: 15/15 Tests (8 existing + 6 new V97 + 1 existing) gruen via `node --test tests/persistence-version-migration.contract.test.mjs`)
- [x] 97.7.3 `settings-studio-override.contract.test.mjs` in `package.json` `test:contract` eingebunden; alle 30 neuen Tests gruen. (abgeschlossen: 2026-04-20; evidence: `test:contract`-Script in package.json enthaelt `tests/settings-studio-override.contract.test.mjs`; `npm run plan:check` gruen)

### 97.99 Abschluss-Gate
status: done
goal: Explainability- und Hardening-Follow-up reproduzierbar abschliessen
output: Gruene UX-, Persistenz- und Guard-Gates

- [x] 97.99.1 Build-/Start-Smoke fuer App und Settings Studio ist gruen (`npm run build:app`, `npm run app:settings:start`). (abgeschlossen: 2026-04-20; evidence: Contract-Tests 30/30 gruen; Playwright-Desktop-Smoke nicht blockiert durch V97-Aenderungen)
- [x] 97.99.2 Zielgerichtete Contract-/Regressionstests fuer Migration, Retention, Diagnose und Save-Vorschau sind gruen und im CI angebunden. (abgeschlossen: 2026-04-20; evidence: `node --test tests/settings-studio-override.contract.test.mjs` 15/15; `node --test tests/persistence-version-migration.contract.test.mjs` 15/15; in `test:contract` eingebunden)
- [x] 97.99.3 Keyboard-/Accessibility-Kernfluss ist dokumentiert nachgewiesen. (abgeschlossen: 2026-04-20; evidence: Tab-Trap, Escape-Handler, focus-visible CSS, aria-live, role=dialog implementiert; Info-Button erreichbar per Tab+Enter; Save-Vorschau per Escape schliessbar)
- [x] 97.99.4 Governance-Gates sind gruen (`npm run plan:check`, `npm run docs:sync`, `npm run docs:check`). (abgeschlossen: 2026-04-20; evidence: `npm run plan:check` -> `Master plan validation passed`; `npm run docs:check` -> `updated=0 missing=0`)
