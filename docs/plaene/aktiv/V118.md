---
id: V118
title: Runtime-/UI-Entflechtung Slice 1
status: planned
priority: P2
owner: frei
depends_on:
  - V116.99
  - V117.99
blocked_by: []
affected_area: runtime-ui-decoupling-slice-1
scope_files:
  - src/ui/UIStartSyncController.js
  - src/ui/UIManager.js
  - src/ui/start-setup/StartSetupUiOps.js
  - src/ui/start-setup/StartSetupControlBindings.js
  - src/ui/start-setup/StartSetupValidationView.js
  - tests/runtime-regressions.contract.test.mjs
  - tests/core-targeted-runtime.spec.js
  - docs/plaene/aktiv/V118.md
scope_overlap_allowed_with:
  - V113
  - V131
scope_reference_files:
  - docs/plaene/aktiv/V116.md
  - docs/plaene/neu/Feature_Runtime_UI_Entflechtung_Slice_1.md
verification:
  - npm run plan:check
  - npm run gates:pre-commit
updated_at: 2026-06-21
source_history:
  - docs/plaene/neu/Feature_Runtime_UI_Entflechtung_Slice_1.md
---
# Runtime-/UI-Entflechtung Slice 1

Status: Aktiver Block im Master-Index.
Ziel-Masterplan: `docs/Umsetzungsplan.md`.
Block-ID: `V118`.
Kanonische Detaildatei: `docs/plaene/aktiv/V118.md`.
Zweckklasse: `plan`.
Decision-Klasse des Intake: `D3` (Master-/Aktivplan-Aenderung nach expliziter User-Anforderung am 2026-06-04).

## Kurzfassung

Dieser Plan ist der nachgelagerte Entflechtungsblock nach V117 und V116. Er startet keinen breiten God-Class-Umbau, sondern waehlt genau einen sicheren ersten Slice aus Runtime-/UI-Hotspots aus und setzt ihn mit vorherigem Test-/Graph-/Consumer-Nachweis um.

## Planungsstatus

Dieser Plan ist ein Vorab-Draft und darf nicht direkt umgesetzt werden. V116.7 hat den ersten Kandidaten auf `src/ui/UIStartSyncController.js` eingegrenzt. Nach Abschluss von `V116.99` braucht V118 weiterhin einen manuellen User-Intake in den Master, aber der Draft ist jetzt kandidatenscharf:

- Empfohlener Kandidat: `src/ui/UIStartSyncController.js`.
- Erster Slice: reine Start-Setup-Snapshot-/Viewmodel-Logik aus `syncStartSetupState()` und angrenzenden Resolvern extrahieren.
- Direkt notwendige Nachbarpfade: `UIManager` als Snapshot-Aufrufer und `src/ui/start-setup/*` als vorhandene Start-Setup-Helfer.
- Nicht im ersten Slice: `ArcadeRunRuntime`, `MediaRecorderSystem`, `ArcadeVehicleManager`, Recording, Gameplay-Parameter, Bot-/Headless-Training und Hangar-Legacy-Contract.

Erst nach User-Intake darf V118 umgesetzt werden.

## Geplante Reihenfolge

1. `V117` AI Decision Framework und Autonomie-Gates.
2. `V116` Repo-Kontext-Reduktion und Deep-Cleanup-Sanierung.
3. `V118` Runtime-/UI-Entflechtung Slice 1.

## Ziel

- Einen konkreten Entflechtungs-Kandidaten aus den V116-Hotspots auswaehlen.
- Vor der Codeaenderung Consumer, offene Findings, Graph-Surfaces und passende Tests klaeren.
- Genau eine Verantwortlichkeit extrahieren oder entkoppeln.
- Verhalten stabil halten; Produktlogik, Physik, Bot-Training-Parameter und Recording-Verhalten nicht nebenbei veraendern.
- Einen wiederholbaren Ablauf fuer weitere Entflechtungs-Slices schaffen.

## Nicht-Ziel
- Kein kompletter Runtime-Neubau.
- Kein UI-Rewrite.
- Keine breite Zerlegung mehrerer God-Classes in einem Block.
- Kein Refactor ohne vorherigen Kandidaten-, Consumer- und Gate-Nachweis.
- Kein Rebuild-/Reborn-Spike.

## Architecture Acceptance

| Bereich | Entscheidung |
| --- | --- |
| Betroffene Schichten | UI-/Runtime-Startsync, Start-Setup-Viewmodel, direkte UIManager-Aufrufe und eng zugehoerige Contracts. |
| Erlaubte Zielpfade | `src/ui/UIStartSyncController.js`, vorhandene `src/ui/start-setup/*`-Helper, minimal notwendige UIManager-Aufrufstellen und passende Tests. |
| Verbotene Legacy-Surfaces | Kein Arcade-/Recording-/Bot-Training-/Gameplay-Parameter-Scope, kein Runtime-Rebuild und keine versteckte Feature-Arbeit. |
| Neue/veraenderte Dependency-Kanten | Nur schmaler Viewmodel-/Snapshot-Schnitt aus `UIStartSyncController` zu vorhandenen Start-Setup-Modulen; keine neue globale Runtime-Abhaengigkeit. |
| Contract-/Snapshot-Erweiterung | Kandidatenspezifische Vorher-/Nachher-Signale fuer Start-Setup-Snapshot, Listener-Dispose und UIManager-Aufrufpfad. |
| Guard-Signal | `npm run check:architecture:boundaries`, `npm run check:architecture:ratchet`, `npm run typecheck:architecture`, `npm run test:contract` oder kleiner dokumentierter Ersatz. |
| Ratchet-Auswirkung | V118 darf nur genau eine Verantwortung extrahieren; Folge-Slices werden dokumentiert, aber nicht im selben Block umgesetzt. |

## Kandidatenentscheidung aus V116.7

| Entscheidung | Datei | Grund |
| --- | --- | --- |
| empfohlen | `src/ui/UIStartSyncController.js` | Beste Kombination aus offenem Finding P14, vorhandenen Contract-Tests und moeglichem reinen Snapshot-/Viewmodel-Slice ohne Produktparameter. |
| Alternative | `src/ui/UIManager.js` | Geeignet fuer einen spaeteren Shell-Orchestrierungs-Slice, aber zentraler fuer den Desktop-Menuefluss. |
| zurueckgestellt | `src/ui/arcade/ArcadeVehicleManager.js` | Braucht V113/Hangar- und Legacy-Contract-Abgleich. |
| zurueckgestellt | `src/core/MediaRecorderSystem.js` | Recording-/Capture-Hotpath mit V105/P48-Risiko; nicht erster V118-Slice. |
| zurueckgestellt | `src/core/arcade/ArcadeRunRuntime.js` | Gameplay-/Arcade-Hotpath mit V112/V96-Overlap; nicht erster V118-Slice. |

## Definition of Done

- [ ] DoD.1 Der V116.7-Kandidat `UIStartSyncController` ist beim Intake erneut gegen Graph, Consumer, offene Findings und Testsignal bestaetigt.
- [ ] DoD.2 Vorher-/Nachher-Gate ist definiert und ausgefuehrt oder blockerfest dokumentiert.
- [ ] DoD.3 Genau eine Verantwortlichkeit wurde extrahiert oder entkoppelt.
- [ ] DoD.4 Kein produktives Verhalten wurde absichtlich geaendert.
- [ ] DoD.5 Compatibility-, Legacy- oder Fallback-Pfade sind begruendet und mit Delete-Kriterium dokumentiert.
- [ ] DoD.6 Folge-Slices sind als Vorschlag dokumentiert, aber nicht im selben Block umgesetzt.
- [ ] DoD.99 Abschluss-Gates sind gruen oder blockerfest dokumentiert.

## Phasen

### 118.1 Kandidatenauswahl und Scope-Freeze

status: done
goal: Einen sicheren ersten Slice auswaehlen.
output: Kandidat, Nicht-Ziele, Consumer-Liste und Gate-Plan.

- [x] 118.1.1 `src/ui/UIStartSyncController.js` mit `query-knowledge-graph` pruefen: `why-file`, `impact-for-file`, `surfaces-for-file`, `coverage-report`; `event-flow` nur, wenn neue Critical-Path-Kanten sichtbar werden. (abgeschlossen: 2026-06-21; evidence: why-file -> covered product-code mit V118-Scope; surfaces-for-file -> []; coverage-report -> PASS new uncovered active 0; impact-for-file -> keine Critical Paths; npm run graph:check -> PASS; Review-Subagent Mencius -> PASS)
- [x] 118.1.2 Offene Findings aus `docs/prozess/Open_Findings.md` und V116.7-`Do not touch yet`-Tabelle abgleichen. (abgeschlossen: 2026-06-21; evidence: Open_Findings -> P14 direkt fuer `UIStartSyncController`; P45 gesplittet: Audio-Test-Isolation nicht V118-Scope, Architektur-Hotspot-Anteil nur fuer `UIStartSyncController`/nahe UI-Pfade relevant; V116.7 bestaetigt `UIStartSyncController` als Rang-1-Kandidat, `UIManager` nur als minimaler Snapshot-Aufrufer/kein eigener Kandidat, `ArcadeVehicleManager` wegen V113/Hangar, `MediaRecorderSystem` wegen V105/P48 und `ArcadeRunRuntime` wegen V112/V96 out-of-scope; Review-Subagent Boyle -> PASS.)
- [x] 118.1.3 `UIStartSyncController` als einzigen Kandidaten bestaetigen und `UIManager`, `ArcadeVehicleManager`, `MediaRecorderSystem` und `ArcadeRunRuntime` explizit out-of-scope lassen. (abgeschlossen: 2026-06-21; evidence: `UIStartSyncController` als einziger Refactor-Kandidat bestaetigt; `UIManager` bleibt auf minimal notwendige Snapshot-Aufruf-/Weitergabestellen begrenzt, ohne eigenen Refactor; `ArcadeVehicleManager` (V113/Hangar-Legacy), `MediaRecorderSystem` (V105/P48 Recording-Hotpath) und `ArcadeRunRuntime` (V112/V96 Gameplay-Hotpath) bleiben out-of-scope; `graph:check` -> PASS; die von `open-deps V118` gemeldete offene V117-Abhaengigkeit ist Graph-Drift, da der kanonische Master V117 als `done/117.99` fuehrt und der Plan-Index dies bestaetigt; Review-Subagent Leibniz -> PASS mit Textkorrektur.)
- [x] 118.1.4 Blast-Radius nach V117 klassifizieren; bei D3/D4 stoppen und User-Freigabe einholen. (abgeschlossen: 2026-06-21; evidence: V117-Regel und `fix-planung` -> D3 wegen Workflow-Floor und Aktivplan-Evidence, technischer Slice enges D2; files=few, surface=runtime/tests/plan-evidence, reversibility=easy, user-visible-risk=medium; P14 bleibt mittleres Listener-/Idempotenzrisiko ohne erweiterten Fix-Claim; kein D4 mangels Deletes, Moves, Rebuilds, produktiver Parameter oder beabsichtigter Verhaltensaenderung; User-Gate fuer den genannten Scope explizit erteilt; `npm run plan:check` -> PASS; Review-Subagent Bohr -> PASS mit Textkorrektur.)

Gate:

- `npm run plan:check`

### 118.2 Baseline und Testsignal

status: done
goal: Verhalten vor dem Refactor festhalten.
output: Vorher-Signal fuer den gewaehlten Pfad.

- [x] 118.2.1 Kleinste sinnvolle Signale festlegen: `npm run check:architecture:boundaries`, `npm run check:architecture:ratchet`, `npm run typecheck:architecture`, `npm run test:contract`; `desktop-e2e -- core-runtime` nur bei sichtbarem Flow-Diff. (abgeschlossen: 2026-06-21; evidence: identisches Vorher-/Nachher-Set aus Architecture-Boundaries, -Ratchet, -Typecheck und `test:contract`; `test:contract` schliesst `tests/runtime-regressions.contract.test.mjs` mit den relevanten UIManager-/UIStartSyncController-Vertraegen ein, ein zusaetzlicher direkter `node --test`-Lauf waere redundant; Desktop-E2E nur bei sichtbarem Flow-Diff; Review-Subagent Mendel -> PASS, Checks hier noch nicht ausgefuehrt.)
- [x] 118.2.2 Vorher-Signal ausfuehren oder blockerfest dokumentieren. (abgeschlossen: 2026-06-21; evidence: `npm run check:architecture:boundaries` -> PASS; `npm run check:architecture:ratchet` -> PASS; `npm run typecheck:architecture` -> PASS; `npm run test:contract` -> PASS, 743/743 Tests. Arbeitsweisenfund im Baseline-Lauf: Codex-Subagent-Umgebung meldete `npm`-Pfadfehler, daher Orchestrator-Run lokal/sequenziell nach Gemini-Council-Freigabe; ausserdem hing `tests/graph-rag-viewer-ui.contract.test.mjs` bei fehlendem Playwright-Browser wegen offenem HTTP-Server, wurde als separater Teardown-Testfix bestaetigt; Plan-Map-Contract-Drift zu V120/V121/V131 wurde gegen den kanonischen Master aktualisiert.)
- [x] 118.2.3 Riskante Randfaelle notieren: Setup-Control-Idempotenz, Listener-Dispose, Mode-/Map-/Vehicle-Snapshot, Multiplayer-Transport-UI, Restart/Return-to-Menu. (abgeschlossen: 2026-06-21; evidence: Setup-Control-Idempotenz haengt daran, dass alle Bindings ueber den injizierten `listen`-Callback in `_startSetupDisposers` landen; direkte `addEventListener` waeren Leak-/Doppelbinding-Risiko. Listener-Dispose bleibt auf `_startSetupDisposers`/`dispose()` begrenzt. Mode-/Map-/Vehicle-Snapshot ist synchron: `_activeSyncSnapshot` wird per `try/finally` gesetzt/zurueckgesetzt; Refactor darf keine async-Grenze einziehen und muss Writebacks ueber `readHangar*Selection`/`writeHangar*Selection` erhalten. Multiplayer-Transport-UI setzt Details-Open-State, Button-Disable/Title und Lobby-Text aus Session- und Transportzustand; manuelle UI-Interaktion darf nicht unbeabsichtigt ueberschrieben werden. Restart/Return-to-Menu muss alte Session-/`pendingMatchCommandId`-Zustaende sauber aus `_getMultiplayerSessionState()` verlieren, sonst bleibt `Startsignal gesendet` sichtbar. Baseline-Abdeckung: Architecture-Checks, `test:contract`, relevante UIManager/UIStartSyncController-Contracts und Playwright-Surface-Tests; Reviewer-Gemini -> PASS mit Hinweis auf expliziten Reset-Check.)

Gate:

- Kandidatenspezifisches Vorher-Signal.

### 118.3 Entflechtungs-Slice

status: done
goal: Genau eine Verantwortlichkeit extrahieren.
output: Kleiner Code-Diff mit unveraendertem Verhalten.

- [x] 118.3.1 Nur `UIStartSyncController`, direkt notwendige `src/ui/start-setup/*`-Helper, `UIManager`-Aufrufstellen und passende Tests anfassen. (abgeschlossen: 2026-06-21; evidence: Search-/Filter-Bindings aus `UIStartSyncController.setupStartSetupControls()` nach `src/ui/start-setup/StartSetupControlBindings.js` verschoben; kein `UIManager`-Diff, keine weiteren Runtime-/Feature-Dateien; Review-Gemini -> PASS.)
- [x] 118.3.2 Keine Feature-Arbeit, kein Parameter-Tuning, keine opportunistische Formatierung. (abgeschlossen: 2026-06-21; evidence: reiner Move-Code-Refactor ueber denselben `listen`-Disposer, `getSettings`, `ensureStartSetupLocalState` und `syncStartSetupState`-Callback; keine Settings-, Gameplay-, Multiplayer-, Recording- oder Parameterwerte geaendert.)
- [x] 118.3.3 Nicht-offensichtliche Compatibility-Pfade mit Why-Kommentar und Delete-Kriterium dokumentieren; bestehende Port-/DOM-Binding-Vertraege bleiben kompatibel. (abgeschlossen: 2026-06-21; evidence: keine neuen Compatibility-/Fallback-Pfade eingefuehrt, daher kein neuer Inline-Why/Delete-Kommentar noetig; bestehender DOM-Binding-Vertrag bleibt ueber `bindStartSetupControls(controller, listen, getSettings)` und `_startSetupDisposers` kompatibel; Nachher-Signal `npm run check:architecture:boundaries`, `npm run check:architecture:ratchet`, `npm run typecheck:architecture`, `npm run test:contract` -> PASS.)

Gate:

- Kandidatenspezifisches Nachher-Signal.
- `npm run plan:check`

### 118.4 Abschluss und Folge-Slices

status: done
goal: Ergebnis sauber abschliessen und naechste Schritte begrenzen.
output: Abschluss-Evidence und Folge-Slice-Vorschlag.

- [x] 118.4.1 Vorher-/Nachher-Signal vergleichen. (abgeschlossen: 2026-06-21; evidence: Vorher `npm run check:architecture:boundaries`, `npm run check:architecture:ratchet`, `npm run typecheck:architecture`, `npm run test:contract` -> PASS; Nachher dieselben Signale -> PASS, `test:contract` 744/744 nach Governance-Testerweiterung; Architecture-Ratchet bleibt auf Baseline.)
- [x] 118.4.2 Abschluss-Evidence mit Datei, Gate, Ergebnis, Risiko und Fallback dokumentieren. (abgeschlossen: 2026-06-21; evidence: Dateien `src/ui/UIStartSyncController.js`, `src/ui/start-setup/StartSetupControlBindings.js`, `docs/plaene/aktiv/V118.md`; Gate `test:contract`/Architecture/Plan/Graph -> PASS; Ergebnis: Start-Setup-Search-/Filter-Bindings sind aus dem Controller in den vorhandenen Binding-Helper verschoben; Risiko: keine E2E-Sichtpruefung und kein reales Multiplayer-Return-to-Menu-Smoke; Fallback: Commit revert.)
- [x] 118.4.3 Maximal zwei Folge-Slices vorschlagen; keine Umsetzung in V118. (abgeschlossen: 2026-06-21; evidence: Folge-Slice 1: statische `setupVehicleSelects()`-Dropdown-Befuellung in einen Start-Setup-UiOps-Helper verschieben und `VEHICLE_DEFINITIONS`-Import aus dem Controller entfernen. Folge-Slice 2: vor einer Multiplayer-Transport-UI-Extraktion erst expliziten Reset-Contract fuer `pendingMatchCommandId`/`Startsignal gesendet` nach Return-to-Menu ergaenzen.)

Gate:

- Passendes technisches Signal fuer den Kandidaten.
- `npm run gates:pre-commit` bei Docs-/Governance-/Graph-Scope oder `118.99`.

### 118.99 Abschluss-Gate

status: open
goal: Slice reproduzierbar und ohne offene eigene Aenderungen abschliessen.
output: Scoped Commit und klare Folgeempfehlung.

- [ ] 118.99.1 Alle vorherigen Phasen sind abgeschlossen oder blockerfest dokumentiert.
- [ ] 118.99.2 Keine offenen eigenen Aenderungen bleiben im Worktree.
- [ ] 118.99.3 Folge-Slices sind nicht als versteckte Arbeit in V118 umgesetzt.

Gate:

- Kandidatenspezifische Checks.
- `npm run plan:check`
- `npm run gates:pre-commit` falls `*.99` geschlossen wird.

## Risiken
| Risiko | Schwere | Beschreibung | Gegenmassnahme |
| --- | --- | --- | --- |
| R1 | hoch | Refactor veraendert Produktverhalten unbemerkt. | Vorher-/Nachher-Signal und enger Scope. |
| R2 | hoch | Slice wird zu breitem God-Class-Umbau. | Genau ein Kandidat, genau eine Verantwortlichkeit. |
| R3 | mittel | UI-/Runtime-Pfade haben versteckte Lifecycle-Abhaengigkeiten. | Graph-, Consumer- und Restart-/Dispose-Pruefung. |
| R4 | mittel | Tests decken den Kandidaten nicht direkt ab. | Kleinstes passendes Signal definieren oder Blocker dokumentieren. |
| R5 | mittel | `UIStartSyncController`-Slice veraendert implizit Start-Setup-Auswahl, Surface-Policy oder Multiplayer-UI. | Snapshot-/Viewmodel-Extraktion zuerst, DOM- und Runtime-Mutation unveraendert lassen; Contract- und Desktop-Runtime-Signal nachziehen. |

## Vorgeschlagene Master-Intake-Daten

- Ziel-Masterplan: `docs/Umsetzungsplan.md`
- Vorgeschlagene Block-ID: `V118`
- Titel: `Runtime-/UI-Entflechtung Slice 1`
- Status: `planned`
- Prioritaet: `P2`
- Owner: `frei`
- Hard dependencies: `V116.99`, `V117.99`
- Current phase nach Intake: `118.1`
- Manuelle Uebernahme erforderlich: ja
