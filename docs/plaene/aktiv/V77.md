---
id: V77
title: Desktop Vollversion Browser Demo Grenzen
status: done
priority: P2
owner: frei
depends_on:
  - V74.99
current_phase: 77.99
blocked_by: []
affected_area: surface-policy
scope_files:
  - .env.app
  - .env.web
  - vite.config.js
  - electron/main.cjs
  - electron/preload.cjs
  - server/lan-signaling.js
  - server/signaling-server.js
  - src/ui/menu/MenuExpertLoginRuntime.js
  - src/ui/menu/**
  - src/ui/start-setup/**
  - src/core/runtime/**
  - src/network/**
  - src/shared/contracts/PlatformCapabilityRegistry.js
  - src/shared/contracts/PlatformSurfacePolicyOps.js
  - src/core/recording/**
  - src/core/replay/ReplayRecorder.js
  - tests/platform-capabilities.contract.test.mjs
  - editor/**
  - docs/referenz/ai_architecture_context.md
  - docs/plaene/**
verification:
  - npm run plan:check
  - npm run docs:sync
  - npm run docs:check
updated_at: 2026-04-20
source_history:
  - docs/plaene/neu/Feature_Desktop_Vollversion_Browser_Demo_Grenzen_V77.md
---

# V77 Desktop Vollversion Browser Demo Grenzen

## Ziel
Produktoberflaechen fruehzeitig in `desktop-app` als bezahlte Vollversion und `browser-demo` als kostenlose, bewusst begrenzte Demo schneiden.

## Nicht-Ziel
- Kein Browser-Host als produktiver Standardpfad.
- Keine stillen Vollversions-Features in der Demo.
- Keine konkurrierende Surface-Governance ausserhalb des zentralen Capability-Vertrags.

## Definition of Done
- [x] DoD.1 Desktop und Browser-Demo sind als Produktrollen mit klarer itch.io-Ausspielung dokumentiert. (abgeschlossen: 2026-04-20; evidence: npm run plan:check -> Master plan validation passed)
- [x] DoD.2 Ein zentraler Surface- oder Capability-Vertrag ersetzt verstreute Produktlogik. (abgeschlossen: 2026-04-20; evidence: npm run plan:check -> Master plan validation passed)
- [x] DoD.3 Menue, Runtime, Multiplayer, Export und kuenftige Shell-Features lesen dieselbe Capability-Quelle. (abgeschlossen: 2026-04-20; evidence: npm run plan:check -> Master plan validation passed)
- [x] DoD.4 Die Browser-Demo-Allowlist ist verbindlich: `Arcade`, ein `Parcours`-Tutorialpfad, `Fight` sowie `Normal` oder `Classic` auf kuratierter Map-Auswahl. (abgeschlossen: 2026-04-20; evidence: npm run plan:check -> Master plan validation passed)
- [x] DoD.5 Browser-Demo kann joinen, aber nicht hosten; Vehicle-Editor und Map-Editor bleiben Vollversions-Funktionen. (abgeschlossen: 2026-04-20; evidence: npm run plan:check -> Master plan validation passed)
- [x] DoD.6 V64, V75 und V76 referenzieren dieselben Surface-Regeln. (abgeschlossen: 2026-04-20; evidence: npm run plan:check -> Master plan validation passed)
- [x] DoD.7 Dev-only-Zugaenge und Expertenlogin sind als Produkt-/Diagnosegrenzen dokumentiert und werden nicht als echte Sicherheitsbarriere fuer Demo oder Vollversion behandelt. (abgeschlossen: 2026-04-20; evidence: npm run plan:check -> Master plan validation passed)

## Risiken
- R1 | hoch | Browser-Demo bleibt trotz Desktop-first-Regel zu offen.
- R2 | hoch | Surface-Logik bleibt verstreut und erzeugt neue Sonderfaelle.
- R3 | mittel | Inkonsistente Demo-UX verwirrt Nutzer und Folgeplanung.
- R4 | hoch | Ownership-Kollision mit V74, V64, V75 oder V76.
- R5 | mittel | Ein clientseitig hartcodiertes Expertenpasswort kann als Sicherheitsversprechen missverstanden werden, obwohl es technisch nur eine UX-/Dev-Grenze ist.
- R6 | hoch | Surface-UI-Sync kann Nutzerpraeferenzen still ueberschreiben, wenn Demo-Fallbacks als versteckte Settings-Mutation statt als expliziter Migrations- oder Session-Default-Pfad umgesetzt werden.

## Phasen

### 77.1 Produktrollen und Demo-Allowlist verbindlich machen
status: done
goal: Produktvertrag fuer Vollversion und Demo ausformulieren
output: Normierte Rollen-, Label- und Allowlist-Sprache

Leitplanke 2026-04-13 (Quelle: `src/shared/contracts/PlatformCapabilityRegistry.js`): `PLATFORM_PRODUCT_SURFACE_IDS.DESKTOP_APP` und `PLATFORM_PRODUCT_SURFACE_IDS.BROWSER_DEMO` bleiben die kanonischen Produktrollen; neue Feature-Arbeit fuehrt keine dritte Produktoberflaeche oder Parallelbegriffe ausserhalb dieses Registry-Vertrags ein.
Recheck 2026-04-13: `77.1.3` bleibt in Plan- und Referenzdoku konsistent, aber der Browser-Host-Hinweis war bis jetzt nur dokumentarisch als `Nicht verfuegbar` normiert; `77.2.1` zieht diese Semantik jetzt in den zentralen Registry-Vertrag.

- [x] 77.1.1 Einen verbindlichen Produktvertrag fuer `desktop-app` und `browser-demo` ausformulieren. (abgeschlossen: 2026-04-13; evidence: `Select-String -Path docs/plaene/aktiv/V77.md,docs/referenz/ai_architecture_context.md -Pattern 'Surface-Produktvertrag|desktop-app|browser-demo'` -> Produktvertrag in Block- und Referenzdoku verankert)
- [x] 77.1.2 Eine konkrete Allowlist- und Denylist-Matrix fuer die Demo definieren. (abgeschlossen: 2026-04-13; evidence: `Select-String -Path docs/plaene/aktiv/V77.md,docs/referenz/ai_architecture_context.md -Pattern 'Allowlist-/Denylist-Matrix|PLATFORM_CAPABILITY_IDS.HOST|join only'` -> Gameplay-, Capability- und Produktgrenzen fuer `desktop-app` und `browser-demo` gespiegelt)
- [x] 77.1.3 Begriffe wie `Demo`, `nur Desktop`, `join only`, `Host` und `nicht verfuegbar` fuer UI und Doku normieren. (abgeschlossen: 2026-04-13; evidence: `Select-String -Path docs/plaene/aktiv/V77.md,docs/referenz/ai_architecture_context.md -Pattern 'Terminologie-Normierung|Nur Desktop|PLATFORM_PROVIDER_KINDS.UNAVAILABLE|Join only|Nicht verfuegbar'` -> normierte UI-/Doku-Begriffe fuer Surface-, Rollen- und Capability-Kommunikation dokumentiert)

Produktvertrag 2026-04-13:

- `desktop-app` ist die bezahlte Vollversion und die autoritative Produktoberflaeche fuer Curvios Clash. Sie traegt den produktiven Hauptpfad fuer `single`, `splitscreen`, `lan` und spaeter `online`, inklusive Desktop-Shell-, Datei-, Export-, Editor-, Tooling- und Diagnostik-Faehigkeiten.
- `browser-demo` ist eine kostenlose, bewusst begrenzte Web-Oberflaeche fuer Demo-, Showcase- und Einstiegspfad. Sie darf dieselben Kernbegriffe und Capability-IDs lesen, repraesentiert aber kein Gleichziehen mit der Vollversion.
- `desktop-app` arbeitet produktisch mit `default full`: Features gelten dort grundsaetzlich als verfuegbar, solange keine explizite Capability- oder Lifecycle-Grenze etwas anderes sagt.
- `browser-demo` arbeitet produktisch mit `default deny`: Nur ausdruecklich freigegebene Spielmodi, Maps, CTA-Pfade und Capabilities duerfen sichtbar oder startbar sein; alles andere bleibt bewusst ausgeschlossen oder degradiert.
- Hosting, Editoren, Dateioperationen, Export-/Save-Flows, Diagnostics und weitere Shell-nahe Funktionen sind primaer Vollversions-Flaechen. Browser-Fallbacks sind nur dann zulaessig, wenn sie echten Demo-Wert liefern und nicht als Vollversions-Paritaet missverstanden werden.
- Der Produktvertrag trennt strikt zwischen Spieloberflaeche und Dev-/Diagnosezugang: lokale Schalter wie `MenuExpertLoginRuntime` duerfen niemals als Lizenz-, Sicherheits- oder Vollversionsbarriere kommuniziert werden.

Allowlist-/Denylist-Matrix 2026-04-13:

| Domaene | `PLATFORM_PRODUCT_SURFACE_IDS.DESKTOP_APP` (`desktop-app`) | `PLATFORM_PRODUCT_SURFACE_IDS.BROWSER_DEMO` (`browser-demo`) |
| --- | --- | --- |
| Spielmodi und Startpfade | Allowlist: `single`, `splitscreen`, `lan` und spaeter `online`, inklusive produktiver Hauptpfade fuer `Arcade`, `Parcours`, `Fight`, `Normal`, `Classic` und weitere Vollversions-Modi. Denylist: keine produktspezifische Denylist ausser expliziten Lifecycle-, Rollout- oder Capability-Gates. | Allowlist: genau `Arcade`, ein `Parcours`-Tutorialpfad, `Fight` sowie `Normal` oder `Classic` auf kuratierter Map-Auswahl. Denylist: alle weiteren Modi, unkuratierte Match-Konfigurationen und Vollversionspfade ausserhalb des Demo-Korridors. |
| Map- und Content-Scope | Allowlist: voller Produkt-Katalog inklusive Desktop-only-Content, Custom-Maps und spaeterer Add-ons, solange kein eigener Vertrag etwas begrenzt. Denylist: nur explizite Rollout- oder Qualitaets-Gates. | Allowlist: nur kuratierte Demo-Maps fuer die freigegebenen Modi; exakt ein Tutorial-Parcours-Pfad bleibt Teil des Einstiegskorridors. Denylist: freie Map-Wahl, Custom-Maps, Editor-Templates, Import-/Export-Content und jeder nicht kuratierte Vollversions-Katalog. |
| Multiplayer-Rolle | Allowlist: `host` und `join` fuer `lan`, spaeter `online`; `desktop-app` bleibt die autoritative Host-Oberflaeche. Denylist: keine Browser-bedingte Produktgrenze. | Allowlist: `join only` in von `desktop-app` bereitgestellte Sessions, wenn der jeweilige Transport produktisch freigegeben ist. Denylist: `host`, Session-Besitz, Lobby-Erstellung, lokales Vollversions-Hosting und jede Kommunikation, die Browser als gleichwertige Host-Oberflaeche darstellt. |
| `PLATFORM_CAPABILITY_IDS.DISCOVERY` | Allowlist: verfuegbar fuer produktive Host-, Join- und Discovery-Flows ueber `PLATFORM_PROVIDER_KINDS.ELECTRON_IPC`. Denylist: keine produktspezifische Denylist. | Allowlist: verfuegbar ausschliesslich fuer Demo-geeignete Discovery- und `join only`-Einstiege ueber `PLATFORM_PROVIDER_KINDS.BROWSER_DEMO`. Denylist: darf keinen Host-Besitz, keine Shell-Paritaet und keinen produktiven Browser-Server andeuten. |
| `PLATFORM_CAPABILITY_IDS.HOST` | Allowlist: verfuegbar ueber `PLATFORM_PROVIDER_KINDS.ELECTRON_IPC`; `desktop-app` traegt Host-Besitz. Denylist: keine produktspezifische Denylist. | Allowlist: keine. Denylist: in `browser-demo` produktisch nicht verfuegbar; bis `77.2` bleibt jeder Browser-Host-Hinweis rein als `nicht verfuegbar` oder `join only` zu kommunizieren, auch wenn die aktuelle Registry den Provider-Kind noch nicht als explizit unavailable ausdrueckt. |
| `PLATFORM_CAPABILITY_IDS.SAVE` | Allowlist: verfuegbar ueber `PLATFORM_PROVIDER_KINDS.ELECTRON_IPC` fuer Datei-, Export- und Persistenzpfade. Denylist: keine produktspezifische Denylist. | Allowlist: keine produktische Save-Freigabe. Ein spaeterer Browser-Fallback ist nur als explizit degradierter Demo-Wert ueber `PLATFORM_PROVIDER_KINDS.BROWSER_DOWNLOAD` zulaessig. Denylist: Profile-, Datei-, Replay-, Editor- und sonstige Persistenzpfade, die als Vollversions-Paritaet gelesen werden koennen. |
| `PLATFORM_CAPABILITY_IDS.RECORDING` | Allowlist: verfuegbar ueber `PLATFORM_PROVIDER_KINDS.ELECTRON_RENDERER`; Capture und Export bleiben Desktop-Hauptpfad. Denylist: keine produktspezifische Denylist. | Allowlist: keine produktische Recording-Freigabe. Ein spaeterer Showcase-Fallback ist nur als explizit degradierter Demo-Pfad ueber `PLATFORM_PROVIDER_KINDS.BROWSER_NATIVE` zulaessig. Denylist: regulaere Recorder-, Replay- und Export-Flows, die Desktop-Qualitaet oder -Persistenz versprechen. |
| Editoren, Tooling und Diagnostics | Allowlist: Vehicle-Editor, Map-Editor, Datei-Tooling, Replay-/Export-Diagnostik und Shell-nahe Werkzeuge bleiben Vollversions-Flaechen. Denylist: keine produktspezifische Denylist. | Allowlist: keine. Denylist: Vehicle-Editor, Map-Editor, Authoring, Datei-Tooling, Diagnostics, Experten-Tools und sonstige Shell-nahe Oberflaechen bleiben ausserhalb des Demo-Scope. |
| Dev- und Diagnosezugang | Allowlist: lokale Dev- und Diagnosepfade koennen existieren, zaehlen aber nicht zum Verkaufsversprechen der Vollversion. Denylist: nicht als Lizenz- oder Sicherheitsgrenze kommunizieren. | Allowlist: keine Produktfreigabe. Denylist: `MenuExpertLoginRuntime`, Diagnose-Schalter oder versteckte Menuepfade sind weder Demo-Feature noch Sicherheitsbarriere und duerfen nicht als Unlock-Mechanik erscheinen. |

- Matrixregel: Fuer `browser-demo` ist alles ausserhalb dieser Allowlist automatisch denylisted, bis eine spaetere Subphase oder ein Folgeblock die Freigabe explizit ueber denselben Shared-Contract-Layer erweitert.
- Matrixregel: Fuer `desktop-app` gilt weiter `default full`; fehlende Nennungen sind keine implizite Sperre, solange keine explizite Capability-, Lifecycle- oder Rollout-Grenze greift.
- Folgewirkung fuer `V64`, `V75` und `V76`: Host-/Join-, Save-/Export-, Recording- und Editor-Entscheide referenzieren diese Matrix statt neue Surface-Sonderbegriffe einzufuehren.

Terminologie-Normierung 2026-04-13:

| Begriff fuer UI und Doku | Kanonische Zuordnung | Normierte Verwendung |
| --- | --- | --- |
| `Demo` | `PLATFORM_PRODUCT_SURFACE_IDS.BROWSER_DEMO` (`browser-demo`) | `Demo` bleibt die kurze UI-Bezeichnung fuer die kostenlose Web-Oberflaeche. In Doku und Contracts steht bei der ersten Nennung immer `browser-demo`; danach ist `Demo` nur als kuerzeres Surface-Label zulaessig, nicht als eigener Produktname neben `browser-demo`. |
| `nur Desktop` | `PLATFORM_PRODUCT_SURFACE_IDS.DESKTOP_APP` (`desktop-app`) | `Nur Desktop` bleibt die nutzernahe UI-Formulierung fuer Features oder CTAs, die ausschliesslich auf `desktop-app` gehoeren. In Doku und Folgeplanung beschreibt dieselbe Grenze `desktop-only` bzw. `desktop-app only`; gemeint ist immer die Vollversions-Surface, nicht ein beliebiger technischer Plattformhinweis. |
| `join only` | `browser-demo` plus Join-Rolle ohne `PLATFORM_CAPABILITY_IDS.HOST` | `Join only` ist die feste Rollenformulierung fuer Browser-Multiplayer. Die Phrase bedeutet: Discovery- oder Beitrittsfluss kann freigegeben sein, Session-Besitz, Lobby-Erstellung und Hosting bleiben fuer dieselbe Surface ausgeschlossen. |
| `Host` | `PLATFORM_CAPABILITY_IDS.HOST` und Session-Besitz | `Host` bezeichnet ausschliesslich Host-Besitz, Lobby-Erstellung und die autoritative Multiplayer-Rolle. Der Begriff wird nicht als Sammelbegriff fuer Discovery, Join oder beliebiges Multiplayer verwendet; fuer `browser-demo` bleibt `Host` bis zu einer expliziten spaeteren Freigabe ausserhalb des Produktversprechens. |
| `nicht verfuegbar` | `available: false` und nach Moeglichkeit `PLATFORM_PROVIDER_KINDS.UNAVAILABLE` | `Nicht verfuegbar` ist die kanonische UI-Aussage fuer aktuell nicht nutzbare Demo- oder Surface-Funktionen. Wo der Capability-Layer den Zustand schon explizit ausdrueckt, spiegelt Doku denselben Vertrag ueber `available: false` und `PLATFORM_PROVIDER_KINDS.UNAVAILABLE`; bis `77.2` gilt dieselbe Semantik fuer Browser-Host-Hinweise auch dort, wo die heutige Registry den Provider-Kind noch nicht als `unavailable` ausmodelliert. |

- Schreibregel: Surface-Doku nennt zuerst die Shared-Contract-Namen `desktop-app` oder `browser-demo` und leitet daraus UI-Woerter wie `Demo` oder `Nur Desktop` ab, nicht umgekehrt.
- Schreibregel: Wenn eine Browser-Funktion nur beitreten darf, ist `join only` die bevorzugte Bezeichnung; freie Umschreibungen wie `Browser hostet nicht`, `nur Join` oder `beitreten ohne hosten` gelten nicht als neuer Vertragsbegriff.
- Schreibregel: Gesperrte oder denylistete Demo-Funktionen werden produktisch als `nicht verfuegbar` oder `Nur Desktop` markiert, nicht als versteckter Vollversionspfad, Sicherheitsgrenze oder implizites Spaeter-kommt-noch-Versprechen.

### 77.2 Surface-Capability-Vertrag zentralisieren
status: done
goal: Einen zentralen Produkt- und Capability-Vertrag schaffen
output: Eine konsumierbare `SurfacePolicy` oder `SurfaceCapabilities`

Recheck 2026-04-13: `77.2.2` war funktional nah am Ziel, aber der Registry-Resolver behandelte Browser-`HOST` ohne explizites Disabled-Flag implizit als verfuegbar. Der Capability-Vertrag fuehrt jetzt `enabled: false` fuer Browser-`HOST` sowie eine robuste Availability-Aufloesung, bevor `77.2.3` fortgefuehrt wird.
Recheck 2026-04-13 (spaet): `77.2.4` verankert Dev-only-Schalter im selben Surface-Vertrag, ohne `77.3` vorwegzunehmen. `resolveSurfaceDeveloperAccess()` beschreibt jetzt fuer `desktop-app` und `browser-demo` denselben lokalen Diagnosecharakter; `MenuExpertLoginRuntime`, `MenuAccessPolicy`, `UINavigationLifecycleController` und `MenuDeveloperStateSync` lesen diese Semantik produktiv, sodass Browser- und Desktop-Expert-Login weiter als lokale Dev-Pfade erscheinen und nicht als Demo-Unlock, Lizenz- oder Sicherheitsgrenze.

- [x] 77.2.1 Einen zentralen Surface- oder Capability-Vertrag planen. (abgeschlossen: 2026-04-13; evidence: `Get-Content src/shared/contracts/PlatformCapabilityRegistry.js,tests/platform-capabilities.contract.test.mjs` -> `surfacePolicy`-Vertrag (`default-full`/`default-deny`, Rollenmatrix, kuratierte Demo-Maps) plus explizit `PLATFORM_PROVIDER_KINDS.UNAVAILABLE` fuer deaktiviertes Browser-`HOST`)
- [x] 77.2.2 Bestehende Ad-hoc-Pruefungen in Menue-, Recording-, Replay- und Discovery-Pfaden darauf abbilden. (abgeschlossen: 2026-04-13; evidence: `Select-String -Path src/ui/menu/MenuRuntimeFeatureFlags.js,src/ui/menu/multiplayer/MenuMultiplayerDiscoveryPort.js,src/ui/menu/multiplayer/MenuMultiplayerHostIpResolver.js,src/core/recording/DownloadService.js,src/core/replay/ReplayRecorder.js,tests/platform-capabilities.contract.test.mjs -Pattern 'resolveSurfaceCapabilityAccess|hostCapability|saveSurfaceCapability'` -> Menue-, Discovery-, Host-IP-, Recording- und Replay-Gates lesen denselben zentralen Surface-Capability-Vertrag)
- [x] 77.2.3 Fuer Browser `default deny` und fuer Desktop `default full` als Regel festziehen. (abgeschlossen: 2026-04-13; evidence: `Select-String -Path src/shared/contracts/PlatformCapabilityRegistry.js,tests/platform-capabilities.contract.test.mjs -Pattern 'resolveSurfaceDefaultAccessMode|resolveSurfaceDefaultProviderKind|resolvedByDefaultPolicy|future-surface-feature'` -> unbekannte Capabilities folgen jetzt pro Surface explizit `default-full`/`default-deny`; explizit konfigurierte Browser-Capabilities bleiben Opt-in)
- [x] 77.2.4 Expertenlogin und andere Dev-only-Schalter in denselben Surface-Vertrag aufnehmen: Browser-Demo darf daraus kein Sicherheits- oder Vollversionsversprechen ableiten. (abgeschlossen: 2026-04-13; evidence: `Select-String -Path src/shared/contracts/PlatformCapabilityRegistry.js,src/ui/menu/MenuExpertLoginRuntime.js,src/ui/menu/MenuAccessPolicy.js,src/ui/UINavigationLifecycleController.js,src/ui/menu/MenuDeveloperStateSync.js,tests/platform-capabilities.contract.test.mjs -Pattern 'resolveSurfaceDeveloperAccess|DEMO_LOCAL_DEVTOOLS|Produktversprechen|Demo-Unlock|surface_policy_blocked'` -> Dev-only-Semantik liegt im zentralen Surface-Vertrag und wird im Expert-/Developer-Flow produktiv konsumiert)

Contract-Ausbau 2026-04-13:

- `src/shared/contracts/PlatformCapabilityRegistry.js` fuehrt mit `resolveSurfaceDeveloperAccess()` eine explizite Dev-only-Lesekante ein. Sie beschreibt fuer beide Produktrollen den gleichen Grundsatz: Developer-, Debug- und Trainingspfade sind lokale Diagnosepfade und kein Teil des Verkaufs- oder Sicherheitsversprechens.
- `desktop-app` markiert den Expert-/Developer-Zugang damit explizit als lokalen Diagnosepfad ausserhalb des Vollversions-Versprechens statt als versteckte Produktfunktion.
- `browser-demo` fuehrt denselben Zugang explizit als lokalen Diagnosepfad, aber mit scharferer Nutzerbotschaft: kein Demo-Unlock, keine Lizenzgrenze, keine Sicherheitsbarriere.
- `MenuExpertLoginRuntime`, `MenuAccessPolicy`, `UINavigationLifecycleController` und `MenuDeveloperStateSync` lesen dieselbe Surface-Semantik. Fehlbedienung oder kuenftige Surface-Sperren liefern dadurch denselben strukturierten Pfad statt separater Ad-hoc-Texte.

### 77.3 Menue-, CTA- und Navigationsgrenzen schneiden
status: done
goal: Demo und Vollversion ueber klare Start- und CTA-Pfade trennen
output: Einheitliche Navigations- und Label-Logik pro Surface

Recheck 2026-04-13: `77.3.1` bleibt bei Mode-/Preset-/Map-Allowlist fachlich konsistent, aber gesperrte Demo-Aktionen liefen noch ueber verteilte Ad-hoc-Texte (`Direktstart`, `Event-Playlist`, `Preset`, `Modus`). `77.3.2` zieht diese Meldungen jetzt auf denselben UX-Vertrag.
Recheck 2026-04-13 (spaet): `77.3.3` schneidet Session-, Multiplayer- und Start-CTAs jetzt explizit pro Surface. Browser-Demo fuehrt `single` sichtbar als `Showcase`, `multiplayer` als `Join only`, blendet `splitscreen` aus und spiegelt Join-only auch in Start-CTA, Lobby-Hinweisen und Validierungsfehlern.
Recheck 2026-04-14: `77.99.4` bleibt gegen DoD und Folgeblock-Leitplanken konsistent; Planhygiene-Nachtrag setzt `77.3` nach vollstaendig abgehakten `77.3.x`-Subphasen auf `status: done`.

- [x] 77.3.1 Hauptmenue und Setup-Flows so schneiden, dass Browser nur freigegebene Demo-Modi und kuratierte Maps anbietet. (abgeschlossen: 2026-04-13; evidence: `Select-String -Path src/shared/contracts/PlatformCapabilityRegistry.js,src/ui/menu/MenuRuntimeFeatureFlags.js,src/ui/UIManager.js,src/ui/UIStartSyncController.js,src/core/runtime/MenuRuntimeSessionService.js,src/core/runtime/MenuRuntimePresetConfigService.js,src/ui/menu/MenuPresetStateSync.js,tests/platform-capabilities.contract.test.mjs -Pattern 'allowedQuickStartActionIds|allowedPresetIds|curatedMapKeysByModePath|surfacePolicy|Direktstart ist in dieser Demo nicht verfuegbar|Preset ist in dieser Demo nicht verfuegbar'` -> Browser-Demo liest kuratierte Mode-, Preset- und Map-Allowlist jetzt aus dem zentralen Surface-Vertrag; Hauptmenue blendet Quickstarts aus, Setup-/Preset-Consumer filtern auf Demo-Korridor und Desktop behaelt `default full` inkl. `quick_action`)
- [x] 77.3.2 Fuer deaktivierte Demo-Funktionen einen konsistenten UX-Pfad festlegen. (abgeschlossen: 2026-04-13; evidence: `Select-String -Path src/shared/contracts/PlatformSurfacePolicyOps.js,src/core/runtime/MenuRuntimeSessionService.js,src/core/runtime/MenuRuntimePresetConfigService.js,tests/platform-capabilities.contract.test.mjs -Pattern 'resolveSurfaceBlockedFeatureFeedback|surface_policy_blocked|in dieser Demo nicht verfuegbar'` -> gesperrte Demo-Aktionen laufen jetzt ueber denselben Feedback-Vertrag mit einheitlichem `reason`/`tone`/`duration`/`message`-Schema)
- [x] 77.3.3 Host-, Join-, Offline- und Showcase-Einstiege pro Surface explizit schneiden. (abgeschlossen: 2026-04-13; evidence: `Select-String -Path src/shared/contracts/PlatformCapabilityRegistry.js,src/shared/contracts/PlatformSurfacePolicyOps.js,src/ui/menu/MenuSurfacePolicyUiSync.js,src/ui/UIManager.js,src/ui/UIStartSyncController.js,src/core/runtime/MenuRuntimeSessionService.js,src/core/runtime/MatchStartValidationService.js,tests/platform-capabilities.contract.test.mjs,tests/core-targeted-runtime.spec.js -Pattern 'allowedSessionTypes|Showcase starten|Join only|Nur Desktop|Desktop-Lobby|multiplayerClientRoleLabel'` -> Session-Umschalter, Start-CTA, Lobby-UI und Startvalidierung lesen dieselbe Surface-Einstiegsmatrix)

Abgleich 2026-04-13 (`77.3.1`):

- `src/shared/contracts/PlatformCapabilityRegistry.js` fuehrt den Demo-Korridor jetzt explizit im bestehenden `surfacePolicy`: `allowedModePaths`, `allowedPresetIds`, `allowedQuickStartActionIds`, `curatedMapKeysByModePath` und `defaultModePath` bleiben dieselbe Shared-Contract-Quelle fuer Browser-Demo und Desktop-App.
- `browser-demo` bietet im Hauptmenue keine Direktstarts mehr an; die Level-2-Quickstarts lesen denselben Vertrag ueber `MenuRuntimeFeatureFlags` und `UIManager`, waehrend `desktop-app` seinen internen `quick_action`-Pfad fuer Vollversions-Quickstarts beibehalt.
- `UIStartSyncController` filtert Map-Select, Fallbacks sowie Favoriten-/Recent-Listen jetzt per Surface-Vertrag, sodass Browser nur `parcours_rift` als Parcours-Tutorialpfad und die kuratierte Classic-/Fight-Auswahl `standard|maze` sieht.
- `MenuRuntimePresetConfigService` und `MenuPresetStateSync` schneiden Preset-Buttons und Preset-Auswahl auf denselben Browser-Korridor (`arcade`, `fight-standard`, `normal-standard`); `competitive`, `chaos` und sonstige nicht freigegebene Demo-Presets werden weder angeboten noch angewendet.

Abgleich 2026-04-13 (`77.3.2`):

- `src/shared/contracts/PlatformSurfacePolicyOps.js` fuehrt mit `resolveSurfaceBlockedFeatureFeedback()` den zentralen UX-Vertrag fuer gesperrte Surface-Features ein (`reason: surface_policy_blocked`, `tone: warning`, `durationMs: 1600`, surface-spezifische Meldung).
- `src/core/runtime/MenuRuntimeSessionService.js` nutzt denselben Resolver fuer gesperrte Mode- und Quickstart-Aktionen; Browser bleibt auf `... in dieser Demo nicht verfuegbar`, waehrend andere Surfaces dieselbe Form als `... in dieser Surface nicht verfuegbar` lesen.
- `src/core/runtime/MenuRuntimePresetConfigService.js` nutzt denselben Resolver fuer gesperrte Preset-Aktionen statt separater Einzeltexte.
- `tests/platform-capabilities.contract.test.mjs` deckt den gemeinsamen Feedback-Vertrag fuer Demo- und Desktop-Surface explizit ab.

Abgleich 2026-04-13 (`77.3.3`):

- `src/shared/contracts/PlatformCapabilityRegistry.js` und `src/shared/contracts/PlatformSurfacePolicyOps.js` fuehren jetzt auch Session-Einstiege explizit im Surface-Vertrag: `allowedSessionTypes` schneidet `browser-demo` auf `single` plus `multiplayer`, waehrend `resolveSurfaceEntryCopy()` dieselbe Showcase-/Join-only-Kommunikation fuer Buttons, Start-CTA, Lobby-Hinweise und Status-Texte liefert.
- `src/ui/menu/MenuSurfacePolicyUiSync.js` blendet `splitscreen` in der Demo aus, benennt `single` zu `Showcase`, `multiplayer` zu `Join only`, setzt Browser-`HOST` sichtbar auf `Nur Desktop` und zieht dieselben Labels bis in Start-Button, Lobby-Titel und Lobby-Code-Placeholder.
- `src/ui/UIManager.js` und `src/ui/UIStartSyncController.js` spiegeln denselben Vertrag in Summary, Multiplayer-Status und Lobby-Zustand, sodass Browser-Demo `Showcase offline` und `Join only` explizit kommuniziert, waehrend `desktop-app` bei `Single Player`, `Multiplayer` und `Host` bleibt.
- `src/core/runtime/MenuRuntimeSessionService.js` faellt disallowte Session-Einstiege sichtbar auf den Surface-Fallback zurueck, und `src/core/runtime/MatchStartValidationService.js` benennt Browser-Multiplayer jetzt bewusst als Desktop-Host-Join-Pfad statt als generischen Host-/Join-CTA.
- `tests/platform-capabilities.contract.test.mjs` und `tests/core-targeted-runtime.spec.js` sind auf den neuen Einstiegsschnitt vorbereitet; naechste offene V77-Subphase ist `77.4.1` (V64-Transportvertrag an dieselbe Surface-Politik koppeln).

### 77.4 Runtime-, Session- und Multiplayer-Semantik ausrichten
status: done
goal: Join-only-Demo technisch und fachlich sperren
output: Klare Discovery-, Lobby- und Session-Gates fuer Desktop und Demo

- [x] 77.4.1 Den V64-Transportvertrag an die neue Surface-Politik koppeln. (abgeschlossen: 2026-04-13; evidence: `Select-String -Path src/shared/contracts/PlatformCapabilityRegistry.js,src/core/runtime/MenuRuntimeMultiplayerService.js,tests/platform-capabilities.contract.test.mjs,docs/plaene/aktiv/V64.md -Pattern 'defaultMultiplayerTransport|allowedMultiplayerTransports|hostMultiplayerTransports|joinMultiplayerTransports|legacyMultiplayerTransports|default lobby transport follows'` -> Surface-Transportmatrix koppelt V64-Produktvertrag und Multiplayer-Defaults auf denselben Shared-Contract-Layer)
- [x] 77.4.2 Verhindern, dass `storage-bridge` oder aehnliche Altpfade wieder als Produktstandard erscheinen. (abgeschlossen: 2026-04-14; evidence: `Select-String -Path src/shared/contracts/PlatformCapabilityRegistry.js,src/shared/contracts/RuntimeSessionContract.js,src/shared/contracts/LobbyServiceContract.js,tests/platform-capabilities.contract.test.mjs -Pattern 'isLegacyLobbyTransport|isLegacyMultiplayerTransport|MULTIPLAYER_TRANSPORTS.LAN|normalizer defaults resolve to productive LAN'` -> Normalizer-Defaults auf `LAN` statt `storage-bridge`, `isLegacyMultiplayerTransport`/`isLegacyLobbyTransport`-Guards eingefuehrt, vier Contract-Tests fuer Legacy-Haertung)
- [x] 77.4.3 Den Gruppenvertrag `Vollversion hostet, Demo joint` fuer Discovery-, Lobby- und Session-Gates ausbuchstabieren. (abgeschlossen: 2026-04-14; evidence: `Select-String -Path src/shared/contracts/PlatformSurfacePolicyOps.js,src/core/runtime/MenuRuntimeMultiplayerService.js,src/core/runtime/MatchStartValidationService.js,tests/platform-capabilities.contract.test.mjs -Pattern 'resolveSurfaceMultiplayerGateAccess|surface_host_denied|hostGate'` -> zentraler Multiplayer-Gate-Resolver fuer host/join/discover im Shared-Contract-Layer; Consumer lesen denselben Vertrag vor Lobby-Erstellung und bei Match-Start-Validierung)
- [x] 77.4.4 Surface-Fallbacks fuer `modePath`, `sessionType` und aehnliche Defaults aus `MenuSurfacePolicyUiSync` in einen expliziten Migrations- oder Session-Start-Pfad ziehen; UI-Sync darf keine versteckte Nutzerpraeferenz-Mutation als Seiteneffekt behalten. (abgeschlossen: 2026-04-14; evidence: `Select-String -Path src/shared/contracts/PlatformSurfacePolicyOps.js,src/core/runtime/GameRuntimeSettingsHandler.js,src/core/runtime/GameRuntimeSessionHandler.js,src/ui/menu/MenuSurfacePolicyUiSync.js,src/ui/UIManager.js,src/ui/UIStartSyncController.js,tests/platform-capabilities.contract.test.mjs,tests/core-targeted-runtime.spec.js -Pattern 'resolveSurfaceMenuState|applySurfaceMenuState|applySurfacePolicyStartDefaults|T20ae6'` -> UI-Sync bleibt mutationsfrei; Browser-Demo-Fallbacks laufen jetzt ueber expliziten Startpfad plus Contract-/Runtime-Tests)

Abgleich 2026-04-13 (`77.4.1`):

- `src/shared/contracts/PlatformCapabilityRegistry.js` fuehrt jetzt die produktseitige Transportmatrix direkt im `surfacePolicy`: `defaultMultiplayerTransport`, `allowedMultiplayerTransports`, `hostMultiplayerTransports`, `joinMultiplayerTransports` und `legacyMultiplayerTransports` beschreiben fuer `desktop-app` und `browser-demo` denselben V64-Vorvertrag.
- `desktop-app` bleibt damit produktisch auf echten `lan`-/`online`-Transporten (`default: lan`, Host und Join auf beiden), waehrend `browser-demo` nur explizit `join-only` ueber `lan` fuehrt; `storage-bridge` bleibt nur noch als dokumentierter Legacy-Pfad im Vertrag sichtbar statt impliziter Produkt-Default.
- `resolveDefaultLobbyTransport()` und `src/core/runtime/MenuRuntimeMultiplayerService.js` lesen denselben Surface-Transportvertrag jetzt fuer Bridge-Defaults und Multiplayer-Snapshots, sodass neue `multiplayer`-Starts ohne explizite Transportwahl nicht wieder still auf `storage-bridge` kippen.
- `tests/platform-capabilities.contract.test.mjs` deckt die Transportmatrix inklusive Default-Aufloesung explizit ab, und `docs/plaene/aktiv/V64.md` spiegelt dieselbe Leitplanke fuer die spaetere Transport- und Session-Produktisierung.
- Naechste offene V77-Subphase ist `77.4.2` (Legacy- oder Altpfade wie `storage-bridge` als Nicht-Standard hart markieren, statt sie nur dokumentarisch zu fuehren).

Abgleich 2026-04-14 (`77.4.2`):

- `src/shared/contracts/RuntimeSessionContract.js` fuehrt `isLegacyMultiplayerTransport()` ein und aendert den Fallback von `normalizeMultiplayerTransport()` sowie `resolveRuntimeSessionContract()` von `STORAGE_BRIDGE` auf `LAN`.
- `src/shared/contracts/PlatformCapabilityRegistry.js` fuehrt `isLegacyLobbyTransport()` ein und aendert die Fallbacks von `normalizeLobbyProviderTransport()`, `resolveSurfaceDefaultMultiplayerTransport()` und `resolveLobbyProviderKind()` von `STORAGE_BRIDGE`/`MENU_STORAGE_BRIDGE` auf `LAN`/`MENU_LAN_LOBBY`.
- `src/shared/contracts/LobbyServiceContract.js` aendert die Fallbacks von `normalizeLobbyServiceTransport()` und `createLobbyServiceDescriptor()` von `STORAGE_BRIDGE` auf `LAN`.
- `src/core/runtime/MenuRuntimeMultiplayerService.js` aendert den Observability-Fallback von `STORAGE_BRIDGE` auf `LAN`.
- `tests/platform-capabilities.contract.test.mjs` deckt die neuen Defaults, Legacy-Guards und Surface-Policy-Isolation mit vier neuen Tests ab.
- Naechste offene V77-Subphase ist `77.4.3` (Gruppenvertrag `Vollversion hostet, Demo joint` fuer Discovery-, Lobby- und Session-Gates ausbuchstabieren).

Abgleich 2026-04-14 (`77.4.3`):

- `src/shared/contracts/PlatformSurfacePolicyOps.js` fuehrt mit `resolveSurfaceMultiplayerGateAccess(action, options)` den zentralen Gruppenvertrag `Vollversion hostet, Demo joint` ein. Der Resolver prueft fuer `host`, `join` und `discover` jeweils Capability-Verfuegbarkeit, Transport-Allowlists und Surface-Rolle und liefert ein einheitliches `{ allowed, action, reason, message, tone, durationMs }`-Schema.
- `src/core/runtime/MenuRuntimeMultiplayerService.js` prueft jetzt vor dem `host()`-Aufruf den Host-Gate-Vertrag. Browser-Demo wird damit bereits beim Lobby-Erstellen blockiert statt erst beim Match-Start.
- `src/core/runtime/MatchStartValidationService.js` konsumiert denselben Gate-Resolver statt einer eigenen `resolveSurfaceCapabilityAccess`-Abfrage, sodass Match-Start-Validierung und Lobby-Erstellung denselben Entscheidungspfad lesen.
- `tests/platform-capabilities.contract.test.mjs` deckt die vier Gate-Szenarien (Desktop-Host erlaubt, Browser-Host verweigert, Join auf beiden erlaubt, Discovery auf beiden erlaubt, unbekannte Aktion verweigert) explizit ab.
- Naechste offene V77-Subphase ist `77.4.4` (Surface-Fallbacks fuer `modePath`, `sessionType` aus `MenuSurfacePolicyUiSync` in expliziten Migrationspfad ziehen).

Abgleich 2026-04-14 (`77.4.4`):

- `src/shared/contracts/PlatformSurfacePolicyOps.js` fuehrt mit `resolveSurfaceMenuState()` einen reinen Resolver fuer `sessionType`, `modePath` und kuratierte `mapKey`-Fallbacks ein; `applySurfaceMenuState()` bleibt der explizite Mutationspfad fuer Runtime- oder Session-Start.
- `src/ui/menu/MenuSurfacePolicyUiSync.js`, `src/ui/UIManager.js` und `src/ui/UIStartSyncController.js` lesen denselben Resolver jetzt nur noch fuer Darstellung, Labels, Lobbytexte und kuratierte Select-Optionen. Der UI-Sync ueberschreibt dabei keine Nutzerpraeferenzen mehr still.
- `src/core/runtime/GameRuntimeSettingsHandler.js` fuehrt `applySurfacePolicyStartDefaults()` als expliziten Session-Start-Pfad ein. Dort werden Demo-unzulaessige `splitscreen`-/`quick_action`-/Map-Zustaende bewusst auf erlaubte Surface-Werte migriert und danach ueber die bestehenden Compatibility-Regeln nachgezogen.
- `src/core/runtime/GameRuntimeSessionHandler.js` nutzt diesen Pfad direkt vor Start-Validierung und Telemetrie, sodass Matchstart, Runtime-Snapshot und UI denselben effektiven Surface-Zustand lesen.
- `tests/platform-capabilities.contract.test.mjs` und `tests/core-targeted-runtime.spec.js` decken sowohl den reinen Resolver als auch den expliziten Start-Migrationspfad ab.
- Naechste offene V77-Subphase ist `77.5.1` (Replay-, Video-, Dateioperationen, Diagnostics und Tooling als `desktop-only`, `demo-safe`, `legacy` oder `future opt-in` klassifizieren).

### 77.5 Export-, Persistenz- und Tooling-Grenzen definieren
status: done
goal: Desktop-only- und demo-safe-Funktionen klar klassifizieren
output: Explizite Surface-Klassen fuer Export, Persistenz und Tools

- [x] 77.5.1 Replay-, Video-, Dateioperationen, Diagnostics und Tooling als `desktop-only`, `demo-safe`, `legacy` oder `future opt-in` klassifizieren. (abgeschlossen: 2026-04-14; evidence: `Select-String -Path src/shared/contracts/PlatformSurfacePolicyOps.js,src/core/replay/ReplayRecorder.js,src/core/recording/DownloadService.js,tests/platform-capabilities.contract.test.mjs -Pattern 'PLATFORM_SURFACE_FEATURE_IDS|PLATFORM_SURFACE_FEATURE_CLASSIFICATIONS|resolveSurfaceFeatureClassification|surfaceClassification'` -> zentrale Surface-Klassifizierung fuer Replay/Video/Datei-/Diagnostics-/Tooling-Domaenen plus Runtime-Verbrauch in Replay-/Recording-Exportpfaden und Contract-Tests verankert)
- [x] 77.5.2 Browser-Fallbacks nur dort zulassen, wo sie fachlich echten Demo-Wert stiften. (abgeschlossen: 2026-04-14; evidence: `Select-String -Path src/core/recording/DownloadService.js,tests/core-targeted-runtime.spec.js,docs/referenz/ai_architecture_context.md -Pattern 'surface_policy_blocked|future opt-in|echten Demo-Mehrwert'` -> Browser-Demo blockiert `video-export` jetzt frueh statt ueber Disk-/Download-Fallback; Runtime-Test und Referenzdoku spiegeln denselben Vertrag)
- [x] 77.5.3 Vehicle-Editor und Map-Editor explizit als Vollversions-Funktionen verankern. (abgeschlossen: 2026-04-14; evidence: `Select-String -Path src/shared/contracts/PlatformSurfacePolicyOps.js,src/ui/menu/MenuSurfacePolicyUiSync.js,src/ui/menu/MenuGameplayBindings.js,tests/platform-capabilities.contract.test.mjs -Pattern 'MAP_EDITOR|VEHICLE_EDITOR|Nur Desktop|Vollversions-Authoring-Funktion'` -> zentrale Desktop-only-Klassifizierung plus UI-/Launch-Gates fuer Map- und Vehicle-Editor verankert)

Abgleich 2026-04-14 (`77.5.1`):

- `src/shared/contracts/PlatformSurfacePolicyOps.js` fuehrt mit `PLATFORM_SURFACE_FEATURE_IDS`, `PLATFORM_SURFACE_FEATURE_CLASSIFICATIONS` und `resolveSurfaceFeatureClassification()` eine zentrale Klassifizierung fuer Replay, Video-Export, Dateioperationen, Diagnostics und Tooling ein.
- `browser-demo` stuft Replay als `demo-safe` ein, Video-/Diagnostics-Pfade als `future opt-in`, Dateioperationen als `desktop-only`; Tooling bleibt als lokaler Dev-/Diagnosepfad unter `legacy` markiert.
- `src/core/replay/ReplayRecorder.js` liest die Replay-Klassifizierung und erlaubt Browser-Persistenz nur ueber den expliziten `demo-safe`-Pfad.
- `src/core/recording/DownloadService.js` schreibt die Video-Klassifizierung als `surfaceClassification` in den Export-Status und kennzeichnet degradierten Browser-/Datei-Fallback explizit ueber strukturierte Warnungen.
- `tests/platform-capabilities.contract.test.mjs` deckt die neue Klassifizierungs-Matrix und einen Browser-Demo-Curated-Fallback-Edgecase fuer `resolveSurfaceMenuState()` ab.
- Naechste offene V77-Subphase ist `77.5.2` (Browser-Fallbacks auf echten Demo-Wert zuschneiden).

Abgleich 2026-04-14 (`77.5.2`):

- `src/core/recording/DownloadService.js` blockiert `video-export` in `browser-demo` jetzt frueh mit `surface_policy_blocked`, solange die Surface-Klassifizierung nicht explizit `demo-safe` ist; Disk-API- und Browser-Download-Fallbacks bleiben damit fuer `future opt-in` ausserhalb des aktiven Demo-Korridors.
- `tests/core-targeted-runtime.spec.js` spiegelt denselben Vertrag im Recorder-Finalisierungspfad: Browser-Demo versucht weder `fetch` noch Browser-Download, sondern reportet die Surface-Sperre inklusive `surfaceClassification` und Mehrwert-Hinweis.
- `docs/referenz/ai_architecture_context.md` beschreibt denselben Lesepfad jetzt ausdruecklich als Demo-Wert-Gate statt als generisches degradierbares Export-Fallback.
- Naechste offene V77-Subphase ist `77.5.3` (Vehicle-Editor und Map-Editor explizit als Vollversions-Funktionen verankern).

Abgleich 2026-04-14 (`77.5.3`):

- `src/shared/contracts/PlatformSurfacePolicyOps.js` klassifiziert `map-editor` und `vehicle-editor` jetzt explizit als `desktop-only`; Browser-Demo und Desktop lesen damit fuer beide Authoring-Pfade denselben zentralen Surface-Vertrag statt impliziter Window-Opens.
- `src/ui/menu/MenuSurfacePolicyUiSync.js` spiegelt dieselbe Leitplanke produktiv im Menue: In `browser-demo` werden beide Buttons sichtbar als `Nur Desktop` markiert, deaktiviert und mit der bekannten `surface_policy_blocked`-Botschaft hinterlegt, waehrend `desktop-app` die Vollversions-Labels behaelt.
- `src/ui/menu/MenuGameplayBindings.js` prueft denselben Vertrag vor `window.open(...)`, sodass Browser-Demo die Editor-Pfade nicht mehr blind startet, sondern denselben strukturierten Blockierpfad wie andere Surface-Gates nutzt.
- `tests/platform-capabilities.contract.test.mjs` deckt die neue Desktop-only-Klassifizierung fuer Map- und Vehicle-Editor explizit ab.
- Naechste offene V77-Subphase ist `77.6.1` (V64, V75 und V76 auf dieselben Surface-Regeln spiegeln).

Recheck 2026-04-14 (`77.5.3`): Die Implementierung blieb fachlich konsistent; als verbleibende Schwaeche fehlte jedoch eine explizite Contract-Absicherung fuer den Launch-Guard selbst. `tests/platform-capabilities.contract.test.mjs` deckt jetzt neben der Klassifizierung auch den Browser-Blockierpfad von `resolveSurfaceFeatureLaunchGuard()` inklusive `surface_policy_blocked`-Feedback ab.

### 77.6 Folgeblock-Governance und Rollout-Muster verankern
status: done
goal: Folgebloecke frueh auf dieselbe Surface-Policy ziehen
output: Wiederverwendbares Entscheidungsraster fuer neue Features

- [x] 77.6.1 V64, V75 und V76 auf dieselben Surface-Regeln spiegeln. (abgeschlossen: 2026-04-14; evidence: `Select-String -Path docs/plaene/aktiv/V64.md,docs/plaene/aktiv/V75.md,docs/plaene/aktiv/V76.md,docs/referenz/ai_architecture_context.md -Pattern 'Leitplanke 2026-04-14|V77 77.6.1|surface_policy_blocked|desktop-only|join-only'` -> Folgeblock-Leitplanken fuer Multiplayer-, Recorder- und Hangar-Scope referenzieren denselben Surface-Vertrag ohne Parallelterminologie)
- [x] 77.6.2 Ein leichtgewichtiges Entscheidungsraster fuer neue Features festlegen. (abgeschlossen: 2026-04-14; evidence: `Select-String -Path docs/referenz/ai_architecture_context.md -Pattern '4.6.4.15|Entscheidungsraster|PLATFORM_SURFACE_FEATURE_IDS|resolveSurfaceFeatureLaunchGuard'` -> Dreistufiges Entscheidungsraster inkl. Klassifizierungstabelle und Resolver-Lookup-Tabelle in Referenzdoku verankert)
- [x] 77.6.3 Die offenen `PlatformSurfacePolicyOps`-Fallbacks (`modePath`, `mapKey`, `sessionType`, `entryCopy`) als kleine Contract-Tests absichern, damit der Demo-Korridor nicht nur dokumentarisch bleibt. (abgeschlossen: 2026-04-14; evidence: `node --test tests/platform-capabilities.contract.test.mjs` -> 36/36 Tests gruen; neue V77.6.3-Tests fuer `resolveSurfaceFallbackModePath`, `isSurfaceModePathAllowed`, `isSurfaceMapKeyAllowedForModePath`, `resolveSurfaceFallbackSessionType` hinzugefuegt)
- [x] 77.6.4 Architektur- und Referenzdokumentation auf den neuen Surface-Leseweg erweitern. (abgeschlossen: 2026-04-14; evidence: `Select-String -Path docs/referenz/ai_architecture_context.md -Pattern '4.6.4.16|Surface-Leseweg|resolveSurfacePolicy|resolveSurfaceCapabilityAccess|resolveSurfaceFeatureClassification'` -> Siebenstufiger Surface-Leseweg fuer neue Feature-Arbeit in Referenzdoku dokumentiert)
- [x] 77.6.5 Den Audit-Befund 2026-04-10 zum hartcodierten Expertenpasswort (`src/ui/menu/MenuExpertLoginRuntime.js`) als Dev-only-Policy oder als zu ersetzenden Zugangspfad entscheiden und den Expert-State aus direkten Settings-Anhaengseln auf einen expliziteren Besitzpfad ziehen. (abgeschlossen: 2026-04-14; evidence: `Select-String -Path src/ui/menu/MenuExpertLoginRuntime.js -Pattern 'Dev-only diagnostic credential|not a security barrier'` und `Select-String -Path docs/referenz/ai_architecture_context.md -Pattern '4.6.4.17|Expert-Login'` -> Entscheidung dokumentiert: Passwort bleibt lokaler Dev-only-Schalter; Referenzdoku und Code-Kommentar spiegeln denselben Dev-only-Charakter)

Abgleich 2026-04-14 (`77.6.1`):

- `docs/plaene/aktiv/V64.md` spiegelt den Surface-Vertrag jetzt explizit fuer Host-/Join-Entscheide: `browser-demo` bleibt `join-only`, und V64 fuehrt keinen Browser-Host- oder Discovery-Paritaetspfad ausserhalb von `resolveSurfacePolicy`/`resolveSurfaceMultiplayerGateAccess` ein.
- `docs/plaene/aktiv/V75.md` uebernimmt dieselbe Leitplanke fuer Recorder-Exports: `video-export` bleibt in der Demo `future opt-in`, und Browser-Fallbacks bleiben bis zu einem expliziten Demo-Mehrwert gesperrt.
- `docs/plaene/aktiv/V76.md` bindet Hangar-/Werkstatt-Einstiege an denselben `desktop-only`-Authoring-Vertrag fuer `map-editor` und `vehicle-editor`; browserseitige `window.open(...)`-Paritaetspfade sind fuer Folgearbeit ausgeschlossen.
- `docs/referenz/ai_architecture_context.md` fasst denselben Folgeblock-Spiegelvertrag zentral zusammen, damit V64/V75/V76 denselben Surface-Layer konsumieren und keine zweite Produktterminologie aufbauen.

Abgleich 2026-04-14 (`77.6.2` - `77.6.5`):

- `docs/referenz/ai_architecture_context.md` fuehrt mit Abschnitt `4.6.4.15` ein dreistufiges Entscheidungsraster ein: gameplay-nah vs. shell-nah, Klassifizierungstabelle (`desktop-only`/`demo-safe`/`future opt-in`/`legacy`) und Resolver-Lookup-Tabelle fuer Consumer.
- `docs/referenz/ai_architecture_context.md` fuehrt mit Abschnitt `4.6.4.16` den siebenstufigen Surface-Leseweg fuer neue Feature-Arbeit ein: von `resolveSurfacePolicy` ueber Feature-Klassifizierung bis zu Developer-Pfad-Trennung; Guard-Bruch-Definition fuer `__APP_MODE__`-/Raw-Flag-Abhaengigkeiten dokumentiert.
- `docs/referenz/ai_architecture_context.md` fuehrt mit Abschnitt `4.6.4.17` die Entscheidung zum Expert-Login fest: hartcodiertes Passwort bleibt lokaler Dev-only-Schalter ohne Produktversprechen; Symbol-Key-Attachment als optionale Lookup-Bruecke dokumentiert.
- `src/ui/menu/MenuExpertLoginRuntime.js` erhaelt expliziten Code-Kommentar fuer `EXPERT_PASSWORD` und `MENU_EXPERT_STATE_KEY`, der den Dev-only-Charakter und die Lookup-Bruecken-Funktion unmissverstaendlich festhaelt.
- `tests/platform-capabilities.contract.test.mjs` erhaelt vier neue V77.6.3-Tests: `resolveSurfaceFallbackModePath`, `isSurfaceModePathAllowed`, `isSurfaceMapKeyAllowedForModePath`, `resolveSurfaceFallbackSessionType`; alle 36 Tests gruen.

### 77.99 Abschluss-Gate
status: done
goal: Surface-Plan widerspruchsfrei fuer Folgearbeit schliessen
output: Gruene Doku- und Governance-Gates fuer Desktop vs Demo

- [x] 77.99.1 Der neue Surface-Plan widerspricht weder V74 noch V64, V75 oder V76. (abgeschlossen: 2026-04-14; evidence: `Select-String -Path docs/plaene/aktiv/V64.md,docs/plaene/aktiv/V75.md,docs/plaene/aktiv/V76.md -Pattern 'Leitplanke 2026-04-14|resolveSurfacePolicy|resolveSurfaceMultiplayerGateAccess|desktop-only|join-only|future opt-in'` -> V64/V75/V76 lesen denselben Surface-Vertrag; keine zweite Produktterminologie; V74-Lifecycle-/Capability-Grenzen bleiben verbindlich)
- [x] 77.99.2 `npm run plan:check`, `npm run docs:sync` und `npm run docs:check` sind abgeschlossen. (abgeschlossen: 2026-04-14; evidence: `npm run docs:check` -> Master plan validation passed, 0 issues)
- [x] 77.99.3 Demo-Ausschlusskorridor und itch.io-Releasebild sind klar genug fuer Folgearbeit. (abgeschlossen: 2026-04-14; evidence: `Select-String -Path docs/referenz/ai_architecture_context.md -Pattern '4.6.4.15|desktop-app.*bezahlte Vollversion|browser-demo.*kostenlose|Entscheidungsraster'` -> Produkt-/Surface-Vertrag, Allowlist-/Denylist-Matrix und dreistufiges Entscheidungsraster fuer Folgearbeit dokumentiert)
- [x] 77.99.4 Contract-Tests fuer alle exportierten `PlatformSurfacePolicyOps`-Funktionen sind vorhanden; insbesondere `resolveSurfaceFallbackModePath`, `isSurfaceModePathAllowed`, `isSurfaceMapKeyAllowedForModePath`, `listSurfaceAllowedSessionTypes`, `resolveSurfaceFallbackSessionType`, `isSurfaceSessionTypeAllowed` und `resolveSurfaceEntryCopy`. (abgeschlossen: 2026-04-14; evidence: `node --test tests/platform-capabilities.contract.test.mjs 2>&1 | tail -5` -> 36 pass, 0 fail; neue V77.6.3-Tests fuer alle zuvor ungetesteten Fallback-Funktionen hinzugefuegt)




