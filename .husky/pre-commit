#!/bin/sh
if [ -f ".husky/.bypass" ]; then
  exit 0
fi

# Lock-Protokoll nur im Bot-Mode pruefen (mode:bot aktiv = kein .bypass)
if [ -n "$BOT_MODE" ]; then
  echo "Pruefe Lock-Protokoll (Bot-Mode)..."
  node scripts/verify-lock.mjs
fi
