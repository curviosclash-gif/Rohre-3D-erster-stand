#!/bin/sh
set -e

if [ -f ".husky/.bypass" ]; then
  exit 0
fi

npm run guard:main

echo "Running plan and docs gates..."
npm run plan:check
npm run docs:check

echo "Architektur-Lint pruefen..."
npm run lint:architecture

echo "Creating auto snapshot tag..."
npm run snapshot:tag
