if [ -f ".husky/.bypass" ]; then
  exit 0
fi

echo "Running docs:check (aus AGENTS.md Regeln)..."
npm run docs:check

echo "Führe Fast-Tests aus, um Code vor dem Pushen abzusichern..."
npm run test:fast
