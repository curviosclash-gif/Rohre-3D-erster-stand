---
trigger: always_on
description: Rule for minimal artifacts and planning
---

- Keep all generated artifacts (`implementation_plan.md`, `task.md`, etc.) extremely brief and straight to the point.
- Only create an `implementation_plan.md` if the task involves spreading changes across multiple files or complex logic.
- For simple or single-file edits, skip the formal implementation plan unless the user explicitly requests one.
