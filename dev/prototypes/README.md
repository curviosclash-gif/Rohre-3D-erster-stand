# Reserved Dev Prototypes Path

This directory is reserved for the future move of `prototypes/**` into `dev/prototypes/**`.
Until then, runtime resolution falls back to the legacy `prototypes/` path.
